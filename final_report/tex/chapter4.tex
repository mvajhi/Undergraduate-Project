% !TeX root=../main.tex
\chapter{پیاده‌سازی}

\section{مقدمه}
\label{sec:4-1}

در فصل پیش، روش پیشنهادی برای پیش‌بینی نرخ عدم‌دریافت غذا و نحوه‌ی پیاده‌سازی آن تشریح
شد. این فصل به اجرای همان روش می‌پردازد و به چهار پرسش پاسخ می‌دهد: آزمایش‌ها در چه
محیط و با چه ابزارهایی انجام شدند، نتایج با چه معیاری سنجیده شدند، چه نتایجی به دست
آمد، و این نتایج چه معنایی دارند.

بند \ref{sec:4-2} محیط سخت‌افزاری و نرم‌افزاری اجرا، کتابخانه‌های اصلی، و زیرساخت ثبت آزمایش را
معرفی می‌کند. در بند \ref{sec:4-3} معیارهای ارزیابی، پروتکل اعتبارسنجی زمانی، خط پایه‌های مقایسه
و آزمون‌های معناداری آماری تعریف می‌شوند. بند \ref{sec:4-4} نتایج اعتبارسنجی و ارزیابی نهایی روی
مجموعه‌ی آزمون قفل‌شده را گزارش می‌کند و بند \ref{sec:4-5} این نتایج را از منظر خطا، تفسیرپذیری و
اثر کسب‌وکاری تحلیل می‌کند. بند \ref{sec:4-6} به ویژگی‌ها و دستاوردهای روش پیشنهادی و نیز
محدودیت‌های بازتولید آن اختصاص دارد.

\section{محیط و ابزارهای اجرا}
\label{sec:4-2}

\subsection{محیط سخت‌افزاری و نرم‌افزاری}
\label{sec:4-2-1}

آزمایش‌ها، به‌جز مواردی که در ادامه می‌آید، روی یک رایانه‌ی شخصی با پردازنده‌ی
\lr{Intel Core i7-1255U} (ده هسته و دوازده رشته‌ی پردازشی)، ۱۶ گیگابایت حافظه و
\lr{Ubuntu 24.04 LTS}، بدون پردازنده‌ی گرافیکی مجزا، اجرا شدند؛ کد با پایتون
\lr{3.12.3} در محیط مجازی مستقل و فایل قفلی از ۲۷۲ بسته با نسخه‌ی دقیقاً تثبیت‌شده (نه
بازه‌ی مجاز) اجرا می‌شود، برای تکرارپذیری عددی نتایج (ردیف ۱ دفترچه‌ی تصمیمات)، و ساخت
محیط از طریق اهداف \lr{\texttt{Makefile}} انجام می‌شود. نبود پردازنده‌ی گرافیکی محلی
برای چهار خانواده‌ی مدل (شبکه‌ی عصبی در سطح سلول و رزرو فردی، فرآیند گاوسی، و مدل بیزی
سلسله‌مراتبی) محدودکننده بود؛ این‌ها روی \lr{T4} در \lr{Google Colab} (با \lr{Kaggle}
به‌عنوان جایگزین) اجرا شدند، با قیدهایی که در بند \ref{sec:4-2-3} می‌آید. 

بخش عمده‌ی
کدنویسی و مستندسازی پروژه هم با کمک عامل‌های هوش مصنوعی در \lr{Claude Code} (مدل‌های
\lr{Opus 5} و \lr{Sonnet 5}) انجام شد؛ تقسیم کار میان پژوهشگر و عامل در بند
\ref{sec:4-6-3} شرح داده می‌شود.

\subsection{کتابخانه‌های اصلی}
\label{sec:4-2-2}

جدول \ref{tab:libs} کتابخانه‌های اصلیِ مؤثر بر نتیجه را با نسخه‌ی نصب‌شده و نقش هرکدام
نشان می‌دهد؛ سه بسته‌ی پشتیبان (\lr{\texttt{holidays}}، \lr{\texttt{pyarrow}}،
\lr{\texttt{dvc}}) و کتابخانه‌های اختصاصی مسیر پردازنده‌ی گرافیکی
(\lr{\texttt{torch}}، \lr{\texttt{gpytorch}}) که فقط در محیط ابری نصب می‌شوند، در جدول
نیامده‌اند.

\begin{table}[!ht]
\centering
\singlespacing
\caption{کتابخانه‌های اصلی، نسخه، و نقش هر کدام در پروژه}
\label{tab:libs}
\vspace{2mm}
\begin{tabular}{|p{3.4cm}|p{1.6cm}|p{8cm}|}
\hline
\textbf{کتابخانه} & \textbf{نسخه} & \textbf{نقش} \\
\hline
\lr{\texttt{pandas}} & \lr{2.3.3} & بارگذاری، پاکسازی و تجمیع داده در همه‌ی سطوح \\
\hline
\lr{\texttt{numpy}} & \lr{2.5.2} & محاسبات عددی و آرایه‌ای \\
\hline
\lr{\texttt{scipy}} & \lr{1.16.3} & آزمون‌های آماری و بهینه‌سازی عددی \\
\hline
\lr{\texttt{scikit-learn}} & \lr{1.9.0} & مدل‌های خطی، پیش‌پردازش ویژگی و ابزار مشترک ارزیابی \\
\hline
\lr{\texttt{statsmodels}} & \lr{0.14.6} & رگرسیون کوانتایل کلاسیک و آزمون‌های سری زمانی \\
\hline
\lr{\texttt{lightgbm}} & \lr{4.7.0} & درخت گرادیان‌بوست با زیان \lr{Pinball} \\
\hline
\lr{\texttt{catboost}} & \lr{1.2.10} & درخت گرادیان‌بوست با پشتیبانی مستقیم متغیر دسته‌ای \\
\hline
\lr{\texttt{quantile-forest}} & \lr{1.4.2} & جنگل تصادفی کوانتایل \\
\hline
\lr{\texttt{optuna}} & \lr{4.9.0} & تنظیم هایپرپارامتر و نصف‌سازی متوالی \\
\hline
\lr{\texttt{mlflow}} & \lr{3.15.1} & ثبت آزمایش و مقایسه‌ی اجراها \\
\hline
\lr{\texttt{shap}} & \lr{0.52.0} & تفسیرپذیری و سهم هر ویژگی \\
\hline
\lr{\texttt{matplotlib}} & \lr{3.11.1} & تولید نمودارهای کاوش داده و گزارش \\
\hline
\end{tabular}
\end{table}

\subsection{زیرساخت ثبت آزمایش}
\label{sec:4-2-3}

ثبت آزمایش‌ها با \lr{MLflow} روی یک پایگاه فایلی محلی انجام شد و تا پایان فاز مدل‌سازی
۲٬۲۱۲ اجرا ثبت شده است. در این مقیاس هر اجرا باید مختصات خودش را حمل کند: نام هر اجرا
از قالبی ثابت ساخته می‌شود که خانواده‌ی مدل، سطح داده، متغیر هدف، مجموعه‌ی ویژگی، نقطه‌ی
$\tau$، مرحله، سید و مُهر زمانی را کنار هم می‌گذارد، و در کنارش چند فیلد اجباری ثبت
می‌شود که مهم‌ترینشان مسیر فایل و شماره‌ی خط تابع آموزش‌دهنده است و هنگام اجرا خودکار از
خودِ تابع استخراج می‌شود؛ همراه با شناسه‌ی \lr{commit} گیت، بازتولید هر نتیجه به
بازگرداندن همان \lr{commit} و مراجعه به همان خط خلاصه می‌شود. دو پارامتر دیگر نقش
دروازه دارند: هش مجموعه‌ی داده و هش فایل \lr{fold}های اعتبارسنجی. اجرایی که این دو را
منطبق با نسخه‌ی رسمی نداشته باشد از جدول‌های مقایسه کنار گذاشته می‌شود، و نوت‌بوک‌های
ابری هم پیش از هر محاسبه‌ای همین دو هش را بررسی می‌کنند و در صورت عدم تطابق متوقف
می‌شوند.

\section{معیار ارزیابی}
\label{sec:4-3}

\subsection{مجموعه‌ی معیارها}
\label{sec:4-3-1}

چون هزینه‌ی کمبود و هزینه‌ی مازاد در این مسئله برابر نیستند و خروجی مدل یک کوانتایل است
نه یک برآورد نقطه‌ای، معیار اصلی انتخاب مدل زیان \lr{Pinball} در نقطه‌ی $\tau$ است که در
بند \ref{sec:2-3-1} تعریف شد. همه‌ی نتایج این فصل، مگر جایی که خلافش گفته شود، در
$\tau = 0.20$ محاسبه شده‌اند؛ چراییِ این عدد و برآورد نسبت هزینه‌ای که به آن می‌رسد در
بند \ref{sec:3-2-6} آمده و رفتار مدل در سایر نقاط $\tau$ در بند \ref{sec:4-5-5}. معیارهای
متقارن مانند \lr{MAE} و \lr{RMSE} فقط نقش تشخیصی دارند، چون هر دو خطای کمبود و مازاد را
یکسان جریمه می‌کنند و با هدف عملیاتی پروژه در تضادند. جدول \ref{tab:metrics} مجموعه‌ی
معیارهایی را که برای هر اجرا محاسبه و ثبت می‌شوند، همراه با رابطه‌ی دقیق هر کدام، نشان
می‌دهد.

در رابطه‌های این جدول هر \textit{سلول} یک ترکیب (روز، وعده، سلف، نوع غذا) است و همه‌ی
میانگین‌ها روی همین سلول‌ها گرفته می‌شوند، نه روی تک‌تک پرس‌ها و نه روی افراد. $N$ تعداد
سلول‌های مجموعه‌ی ارزیابی است، $\mathrm{Res}$ تعداد رزرو، $\mathrm{Recv}$ تعداد
دریافت‌کننده‌ی واقعی، $\rho = 1 - \mathrm{Recv}/\mathrm{Res}$ نرخ واقعی عدم‌دریافت،
$\hat\rho_\tau$ پیش‌بینی مدل، و $Q = \lceil \mathrm{Res}\,(1-\hat\rho_\tau) \rceil$
مقدار پخت پیشنهادی.

\begin{table}[!ht]
\centering
\singlespacing
\caption{مجموعه‌ی معیارهای محاسبه‌شده برای هر اجرا، رابطه‌ی هر کدام و نحوه‌ی خواندنش}
\label{tab:metrics}
\vspace{2mm}
\begin{tabular}{|p{2.4cm}|p{5.3cm}|p{5.3cm}|}
\hline
\textbf{معیار} & \textbf{چگونه محاسبه می‌شود} & \textbf{چطور خوانده می‌شود} \\
\hline
زیان \lr{Pinball} در $\tau$ &
$\displaystyle\frac{1}{N}\sum \max\{\tau e,\ (\tau-1)e\}$ که در آن $e = \rho - \hat\rho_\tau$؛ یک بار
در فضای نرخ و یک بار ضرب‌شده در $\mathrm{Res}$، یعنی فضای پرس &
معیار اصلی انتخاب مدل؛ کوچک‌تر بهتر است. در $\tau=0.20$ هر واحد کمبود چهار برابر همان
مقدار مازاد جریمه می‌شود \\
\hline
پوشش تجربی &
نسبت سلول‌هایی با $\rho \le \hat\rho_\tau$ &
باید به $\tau$ نزدیک بماند؛ سنجش کالیبراسیون \\
\hline
نرخ کمبود &
نسبت سلول‌هایی با $Q < \mathrm{Recv}$ &
قید عملیاتی اصلی \\
\hline
درصد کاهش هدررفت &
$\displaystyle 1 - \frac{\sum \max(Q-\mathrm{Recv},\,0)}{\sum \max(\mathrm{Res}-\mathrm{Recv},\,0)}$ &
معیار کسب‌وکاری \\
\hline
\lr{MAE} و \lr{RMSE} &
میانگین $|\rho - \hat\rho_\tau|$ و ریشه‌ی میانگین مربع آن؛ نسخه‌ی پرس روی
$|Q - \mathrm{Recv}|$ &
فقط تشخیصی: خطاهای بزرگ را نشان می‌دهد \\
\hline
$R^2$ &
$\displaystyle 1 - \frac{\sum(\rho-\hat\rho_\tau)^2}{\sum(\rho-\bar\rho)^2}$ در فضای نرخ &
فقط هشدار نشت داده، نه معیار انتخاب مدل \\
\hline
\end{tabular}
\end{table}

ردیف دوم و سوم جدول در واقع یک رویداد واحدند که در دو فضا اندازه گرفته شده‌اند: چون
$Q$ با $\mathrm{Res}\,(1-\hat\rho_\tau)$ ساخته می‌شود، شرط $Q<\mathrm{Recv}$ معادل همان
$\rho<\hat\rho_\tau$ است و تنها اختلافشان از گردکردن به بالا می‌آید. پس هر دو باید حول
$\tau$ بنشینند، و انحرافشان از $\tau$ جهت ناکالیبرگی را می‌گوید: مدلی که نرخ کمبودش
کمتر از $\tau$ است محافظه‌کارتر از ادعای خودش عمل کرده و بیش از حد لازم غذا پخته. این
تحلیل در بند \ref{sec:4-4-3} روی نتایج واقعی انجام می‌شود.

دلیل اینکه هر دو دسته کنار هم گزارش می‌شوند این است که به دو سؤال متفاوت جواب می‌دهند و
هیچ‌کدام به‌تنهایی کافی نیست: زیان \lr{Pinball} می‌گوید «به‌طور متوسط، هزینه‌ی خطا با
احتساب اندازه و نامتقارنی چقدر است؟»، در حالی که نرخ کمبود و پوشش می‌گویند «مدیریت سلف
با چه فرکانسی با گلایه‌ی دانشجویان روبه‌رو می‌شود؟» — فارغ از اینکه هر بار چقدر شدید بوده.
مدلی که فقط یک روز و با اختلاف زیاد کم بیاورد و مدلی که ده روز و هر بار اندکی کم
بیاورد، در سنجه‌ی دوم بسیار متفاوت‌اند ولی در سنجه‌ی اول ممکن است نزدیک به هم دربیایند.

دو نکته‌ی دیگر در محاسبه رعایت شد. نخست اینکه زیان \lr{Pinball} و خطاهای نقطه‌ای هم در
فضای نرخ و هم در فضای تعداد پرس محاسبه می‌شوند، چون یک اختلاف نرخ ثابت در سلولی با
پانزده رزرو و سلولی با هشتصد رزرو دو چیز کاملاً متفاوت‌اند و گزارش‌کردن تنها یکی از این
دو فضا تصویر گمراه‌کننده‌ای می‌دهد. دوم اینکه $R^2$ عمداً از معیارهای انتخاب مدل بیرون
گذاشته شد و تنها نقش هشدار نشت را دارد، طبق قاعده‌ای که در بند \ref{sec:3-3-4} تشریح
شده است.

\subsection{پروتکل اعتبارسنجی}
\label{sec:4-3-2}

قاعده‌ی حاکم بر تقسیم داده آن است که مرز تقسیم بر اساس تاریخ تعیین می‌شود و نه بر اساس
ردیف؛ تمام رکوردهای یک روز در یک بخش می‌مانند. دلیلش در داده است: ضریب همبستگی
درون‌گروهی روز حدود ۰.۲۲ برآورد شد، یعنی دو رکورد از یک روز مشاهدات مستقلی نیستند و
تقسیم تصادفی ردیفی عملاً نشت اطلاعاتی ایجاد می‌کند. بر همین پایه، معیار اصلی انتخاب مدل
اعتبارسنجی با مبدأ غلتان\LTRfootnote{Rolling-origin} روی پنج بخش با پنجره‌ی گسترشی\LTRfootnote{Expanding window} است که مبانی‌اش در بند \ref{sec:2-4-1} و
رویه‌ی ساخت و انجماد فایل بخش‌ها در بند \ref{sec:3-4-3} آمده است.

لایه‌ی دوم ارزیابی، پنجره‌ی انتهایی قفل‌شده است، اما این پنجره نقش آزمون فشار دارد و
معیار انتخاب مدل نیست. علت این تنزل صریح است: بیست‌وپنج درصد انتهایی داده از ۲۸ فروردین
۱۴۰۳ آغاز می‌شود و بازگشت پس از شکاف بیست‌ونه‌روزه‌ی ماه رمضان و نیز سه روز پایانی
سوگواری ملیِ پس از شهادت رئیس‌جمهور در سانحه‌ی بالگرد را در بر می‌گیرد؛ ممیزی مرحله‌ی مهندسی ویژگی نشان داد اتکای انحصاری به این پنجره
رتبه‌بندی مجموعه‌های ویژگی را گمراه می‌کند. نکته‌ی آخر آنکه میان بخش آموزش و بخش آزمون
فاصله‌ی حذفی در نظر گرفته نشد، چون همه‌ی ویژگی‌های تاریخی از قاعده‌ی برش بند \ref{sec:3-2-2} عبور
می‌کنند و هر ویژگی تنها از گذشته‌ی همان ردیف ساخته می‌شود؛ این ادعا خودش موضوع یک آزمون
خودکار در مخزن است و به فرض شفاهی واگذار نشده.

% TODO ۴-۳-۳ خط پایه‌های B1-B6 و نقش B3 به‌عنوان رقیب واقعی.
% منبع: reports/baselines.md
\subsection{خط پایه‌ها}
\label{sec:4-3-3}

% TODO ۴-۳-۴ Diebold-Mariano؛ بوت‌استرپ بلوکی دوبعدی و n\_eff؛ تصحیح چندگانه (طبق
% بند ۲-۵). ⚠️ اینجا ذکر شود که هر دو در src/cv.py دستی پیاده شده‌اند (نه از کتابخانه‌ی
% arch)، چون وابستگی داده هم‌زمان بر روز و بر سلف است — این نکته از بند ۴-۲-۲ به اینجا
% منتقل شد چون به آزمون معناداری مربوط‌تر است تا فهرست کتابخانه‌ها.
\subsection{آزمون معناداری}
\label{sec:4-3-4}

\section{نتایج به‌دست‌آمده}
\label{sec:4-4}

% TODO ۴-۴-۱ جدول (۴-۲): مقایسه با CI بوت‌استرپ بلوکی دوبعدی (فقط ۸-۱۰ ردیف شاخص،
% جدول کامل به پیوست)؛ نتایج منفی و پرونده‌های بسته‌شده گزارش شوند (معماری دومرحله‌ای،
% خوشه‌بندی شهر).
% منبع: ردیف ۴۰، ۴۴-۴۸ doc/decision_log.md
\subsection{نتایج اعتبارسنجی و انتخاب نامزدها}
\label{sec:4-4-1}

% TODO ۴-۴-۲ ⚠️ مهم‌ترین صداقت گزارش: قهرمان اعتبارسنجی و قهرمان Test یکی نیستند، با
% CI و توضیح علت، نه پنهان‌کاری. تأکید بر «Test فقط یک بار باز شد».
% منبع: ردیف ۴۹ doc/decision_log.md؛ reports/phase8/
\subsection{ارزیابی نهایی روی مجموعه‌ی قفل‌شده}
\label{sec:4-4-2}

% TODO ۴-۴-۳ پوشش حاشیه‌ای در برابر شرطی؛ شکاف نرخ کمبود مشاهده‌شده تا tau اسمی.
% منبع: reports/phase8/
\subsection{کالیبراسیون و پوشش}
\label{sec:4-4-3}

\section{تحلیل نتایج}
\label{sec:4-5}

% TODO ۴-۵-۱ تحلیل باقیمانده؛ تجزیه‌ی خطا؛ الگوی بدترین موارد؛ پایداری/حساسیت؛
% Overfit/Underfit.
% منبع: reports/phase8/
\subsection{تحلیل خطا و بدترین موارد}
\label{sec:4-5-1}

% TODO ۴-۵-۲ اهمیت ویژگی و SHAP؛ اثرات نهایی (PDP/ALE)؛ مدل جانشین قابل‌فهم.
% منبع: reports/phase9/
\subsection{تفسیرپذیری}
\label{sec:4-5-2}

% TODO ۴-۵-۳ تبیین تناقض ظاهری log\_res (کوانتایل در برابر میانگین، طبق بند ۲-۳-۲).
% منبع: ردیف ۵۱ doc/decision_log.md
\subsection{اعتبارسنجی دامنه‌ای}
\label{sec:4-5-3}

% TODO ۴-۵-۴ نتیجه‌ی L1/L2 در برابر L5 با CI.
% منبع: ردیف ۵۰ doc/decision_log.md
\subsection{مقایسه‌ی سطح تجمیعی در برابر سطح فرد}
\label{sec:4-5-4}

% TODO ۴-۵-۵ منحنی trade-off کمبود/هدررفت؛ سناریوهای سیاستی؛ صرفه‌جویی سالانه با CI
% (نه عدد نقطه‌ای)؛ پروتکل پایلوت.
% منبع: reports/phase10/
\subsection{تحلیل کسب‌وکاری}
\label{sec:4-5-5}

\section{ویژگی‌ها و دستاوردهای روش پیشنهادی}
\label{sec:4-6}

% TODO ۴-۶-۱ مزیت‌های عملی روش: کارکرد در سطح عملیاتی (d,m,r,f)؛ کنترل‌پذیری صریح
% نقطه‌ی tau به‌عنوان اهرم سیاستی؛ صرفه‌جویی برآوردی با CI. ⚠️ فقط ادعاهایی که در بند
% ۴-۴ شاهد عددی دارند.
% منبع: reports/phase10/؛ ردیف ۳۶ doc/decision_log.md
\subsection{مزیت‌های عملی}
\label{sec:4-6-1}

% TODO ۴-۶-۲ چه چیزی قفل است (هش snapshot، فایل fold، سید، code_ref) و چه چیزی نیست
% (نبود مقصد راه دور DVC و گیت، نبود هدف واحد `make all`).
% منبع: README.md؛ doc/data_manifest.md؛ doc/progress/11-*.md بند ۱۱.۴
\subsection{بازتولیدپذیری}
\label{sec:4-6-2}

% TODO ۴-۶-۳ ⚠️ حداکثر نیم صفحه (تصمیم کاربر: فقط یک اشاره‌ی کوتاه). محتوا: تقسیم کار
% «انسان صاحب تصمیم و پذیرش، عامل مجری و مستندساز»؛ قرارداد قابل‌بارگذاری AGENTS.md؛
% چهار دفتر دائمی و ضمانت مکانیکی src/report_coverage.py با کد خروجی ناصفر؛ و نتیجه‌ی
% عملی: هرجا قرارداد به قید کد تبدیل شد رعایت شد، هرجا فقط سند ماند نقض شد. جدول چهار
% اشتباه فاز ۷ و باگ سیم‌کشی فیچرست به پیوست می‌روند، نه به بدنه.
% منبع: AGENTS.md؛ doc/phase7-execution-standard.md؛ ردیف ۳۷، ۴۷ doc/decision_log.md
\subsection{اجرای پروژه با کمک عامل‌های هوش مصنوعی}
\label{sec:4-6-3}

% TODO ۴-۷ خلاصه و جمع‌بندی فصل
\section{خلاصه و جمع‌بندی}
\label{sec:4-7}
