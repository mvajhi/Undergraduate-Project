% !TeX root=../main.tex
\chapter{پیاده‌سازی}

% TODO ۴-۱ مقدمه فصل
\section{مقدمه}

% TODO ۴-۲ قاعده‌ی notebooks/=روایت، src/=حقیقت؛ ساختار src/؛ venv+requirements.lock؛
% Makefile؛ DVC روی داده‌ی خام؛ منبع واحد سید تصادفی.
% منبع: AGENTS.md؛ doc/progress/00-*.md
\section{معماری مخزن و پشته‌ی فناوری}

% TODO ۴-۳ ۱۱ فاز WBS، ماتریس وابستگی و مسیر بحرانی، دروازه‌های کیفیت M1-M5. شکل (۴-۱)
% جریان فازها و دروازه‌ها.
% منبع: doc/WBS-food-demand-forecasting.md؛ doc/progress/*.md
\section{گام‌های اجرای پروژه}

% TODO ۴-۴ قرارداد run\_name؛ tag/param/metric اجباری MLflow؛ مسیر GPU کولب/کگل با
% assert هش snapshot/fold.
% منبع: AGENTS.md؛ doc/gpu-run-playbook.md
\section{زیرساخت ثبت آزمایش و اجرای محاسباتی}

\section{اجرای پروژه با عامل‌های هوش مصنوعی}

% TODO ۴-۵-۱ تقسیم کار انسان/عامل: انسان صاحب تصمیم و پذیرش، عامل مجری و مستندساز.
\subsection{انگیزه و مدل کار}

% TODO ۴-۵-۲ AGENTS.md/CLAUDE.md به‌عنوان منبع حقیقتِ قابل‌لود؛ استاندارد اجرایی فاز ۷
% و منطق «WBS می‌گوید چه کاری، استاندارد می‌گوید چگونه».
% منبع: AGENTS.md؛ doc/phase7-execution-standard.md
\subsection{قرارداد عامل}

% TODO ۴-۵-۳ محورهای مستقل‌پذیر (خانواده، سری، tau، seed)؛ subagent با ایزولاسیون
% worktree؛ قاعده‌ی «هیچ دو اجرای موازی یک فایل مشترک نمی‌نویسند».
% منبع: CLAUDE.md
\subsection{موازی‌سازی}

% TODO ۴-۵-۴ چهار دفتر (decision\_log، findings\_register، data\_facts\_register،
% report-source-map) و ضمانت مکانیکی src/report\_coverage.py که با exit code!=۰
% شکست می‌دهد.
% منبع: src/report_coverage.py؛ reports/phase11/coverage_gaps.md
\subsection{حاکمیت و ضدفراموشی}

% TODO ۴-۵-۵ صادقانه: چهار اشتباه مستند فاز ۷ (تنظیم tau جدا از تصمیم، پیمایش نااعلام
% فضای نُه‌محوره، اجرای مدل بدون معیار کیفیت، بودجه‌ی trial بی‌توجه به کاردینالیتی —
% ۸۶٪ اتلاف، ۸.۷۶ ساعت-هسته) و اینکه هر چهار واقعاً رخ دادند؛ باگ سیم‌کشی فیچرست
% (ردیف ۴۷) به‌عنوان نمونه‌ی خطای بی‌صدا.
% منبع: doc/phase7-execution-standard.md («چرا این سند وجود دارد»)؛ ردیف ۳۷، ۴۷
\subsection{درس‌آموخته‌ها و محدودیت‌ها}

% TODO ۴-۶ دستور بازتولید کامل؛ چه چیزی قفل است (snapshot، fold، seed) و چه چیزی نیست
% (محدودیت DVC remote).
% منبع: README.md؛ doc/data_manifest.md
\section{بازتولیدپذیری}

% TODO ۴-۷ خلاصه و جمع‌بندی فصل
\section{خلاصه و جمع‌بندی}
