# AGENTS.md

راهنمای عامل‌های کدنویسی هوش مصنوعی (Claude Code، Codex، Cursor و مشابه) هنگام کار با کد این مخزن.

## پروژه

پیش‌بینی نرخ عدم‌دریافت غذا در سلف‌های دانشگاه تهران (رگرسیون کوانتایل روی سری‌های زمانی چندگانه)، برای پیشنهاد مقدار بهینه‌ی پخت. تعریف کامل مسئله: `doc/project-definition.md`. نقشه‌ی کامل فازها و بسته‌های کاری: `doc/WBS-food-demand-forecasting.md`. **پروتکل تفصیلی فاز ۷ (مدل‌سازی): `doc/WBS-phase7-modeling.md`.** خروجی عملیاتی نهایی همیشه سطح $(d,m,r,f)$ = (روز، وعده، سلف، نوع غذا) است.

**⚠️ تفکیک «مدل A / مدل B» از فاز ۷ به بعد حذف شده.** جایش را **سطح داده** به‌عنوان یک محور آزمایش گرفته و هر مدل روی هر سطحی که ساختارش اجازه دهد اجرا می‌شود:

| سطح | واحد رکورد | حجم |
|---|---|---|
| **L1** سلول | $(d,m,r,f)$ | ۷٬۵۷۹ |
| **L2** سلف×وعده | $(d,m,r)$ | ~۴٬۱۰۰ |
| **L3** عامل روز | $(d,m)$ | ۲۵۶ |
| **L4** سری زمانی | سری $(m,r)$ | ۴۱ سری، میانه ۱۹۷ نقطه |
| **L5** رزرو فردی | $(p,d,m,r,f)$ | ۲٬۰۴۹٬۳۲۲ رزرو · ۲۶٬۷۶۸ فرد |

اصطلاح «مدل A/B» در فازهای ۱ تا ۶ برای سازگاری تاریخی حفظ شده: مدل A ≡ L1/L2، مدل B ≡ L5. سطح $(m,r,f)$ هرگز سطح **آموزش** نیست (میانه‌ی طول سری ۴ نقطه، F39) — فقط سطح **خروجی**.

## قاعده‌ی برش اطلاعاتی — مهم‌ترین قانون پروژه

لحظه‌ی تولید پیش‌بینی **به تفکیک وعده** است، نه یکسان برای کل روز: **ساعت ۱۵ روز $d-1$ برای ناهار روز $d$**، **ساعت ۲۳ روز $d-1$ برای شام روز $d$**. هیچ فیچر یا مدلی، در هیچ فازی، نباید این مرز را بشکند:

| مجاز ✅ | ممنوع ❌ |
|---|---|
| `Reservation` تا $d+2$ (رزرو ۷۲ ساعت زودتر بسته می‌شود) | `ReceiveWithCard`, `ReceiveWithCode`, `DontReceive` برای روز $d$ |
| `Recv`, `NoRecv`, نرخ $\rho$ برای هر وعده که **تا لحظه‌ی برش همان وعده‌ی هدف** سرو شده باشد | هر ستون خروجی که پس از پخت مشخص می‌شود، و هر وعده‌ای که در لحظه‌ی برش هنوز سرو نشده |
| منو و ویژگی‌های تقویمی هر روز | — |
| `ReserveStatus` تاریخچه‌ی رفتاری فرد $p$ تا لحظه‌ی برش (مدل B) | `ReserveStatus_{p,d}` برای روز هدف $d$ |
| فیچرهای جمعیتی ثابت فرد (دانشکده/رشته/مقطع/خوابگاه/جنسیت، مدل B) | — |

**⚠️ تله‌ی رایج:** «هر $d' \le d-1$ مجاز است» به‌تنهایی **کافی نیست** و می‌تواند نشتی ایجاد کند — شام روز $d-1$ در ساعت ۱۵ همان روز (لحظه‌ی برش ناهار روز $d$) هنوز سرو نشده، پس با اینکه تاریخش $d-1$ است، **ممنوع** است. جدول «آخرین وعده‌ی در دسترس»:

| وعده‌ی هدف | لحظه‌ی برش | آخرین outcome در دسترس | ممنوع (با اینکه تاریخش $\le d-1$ است) |
|---|---|---|---|
| ناهار روز $d$ | ۱۵:۰۰ روز $d-1$ | ناهار روز $d-1$ | شام روز $d-1$ |
| شام روز $d$ | ۲۳:۰۰ روز $d-1$ | شام روز $d-1$ | — |

فیچرهای «آخرین نرخ/وعده‌ی شناخته‌شده» (lag) باید آگاه به نوع وعده‌ی هدف ساخته شوند. جزئیات کامل: بند ۴-۲ `doc/project-definition.md`، ردیف ۹ `doc/decision_log.md`. هر فیچر جدید پیش از استفاده باید در برابر این جدول بررسی شود.

## دستورات

```bash
make setup       # venv + نصب از requirements.lock
make lock        # بازتولید requirements.lock بعد از نصب پکیج جدید
make data-pull   # dvc pull
make mlflow-ui   # UI محلی MLflow روی mlruns/
```

سیدهای تصادفی از یک منبع واحد می‌آیند — همیشه اول این را صدا بزن:
```python
from src.config import set_global_seed
set_global_seed()
```

## ساختار مخزن

- `notebooks/` = روایت، `src/` = حقیقت — هیچ منطق مهمی فقط داخل نوت‌بوک نماند.
- مستندات پروژه در `doc/` است (نه `docs/` — نام‌گذاری قدیمی مخزن حفظ شده).
- تحلیل عمیق پشتیبان یک ردیف `doc/decision_log.md` (وقتی خودِ ردیف برای توضیح کافی نیست) در `doc/decisions/` قرار می‌گیرد، با نام‌گذاری `{شماره‌ردیف decision_log}-{موضوع-کوتاه-kebab-case}.md` (مثلاً `08-target-variable.md`) — نه بر اساس شماره‌ی بند WBS، چون آن شماره‌ها با بازنویسی WBS تغییر می‌کنند ولی شماره‌ردیف decision_log فقط افزایشی است.
- `data/raw/` فقط‌خواندنی است. رویه‌ی افزودن فایل خام جدید و شناسه‌ی هر snapshot: `doc/data_manifest.md`.
- **پیگیری وضعیت فاز:** با شروع هر فاز (طبق نقشه‌ی کلان فازها در WBS)، یک فایل `doc/progress/{شماره‌ی دو‌رقمی فاز}-{عنوان-فاز}.md` (مثلاً `doc/progress/01-تثبیت-و-تکمیل-تعریف-مسئله.md`) ساخته می‌شود — یک چک‌لیست Markdown با یک آیتم به‌ازای هر بسته‌کاری (بند) آن فاز در WBS. با تکمیل هر بسته‌کاری، آیتم تیک می‌خورد و شاهد آن (commit، فایل خروجی، یا ردیف `doc/decision_log.md`) در همان خط ذکر می‌شود. برای فازهای هنوز شروع‌نشده فایلی ساخته نمی‌شود.
- هر تصمیم روش‌شناختی (انتخاب فیچر، مدل، معیار) باید در `doc/decision_log.md` ثبت شود — این سند مستقیماً فصل «روش‌شناسی» گزارش نهایی می‌شود.

## ثبت آزمایش (MLflow)

`run_name` منحصربه‌فرد با این قالب (بند 7.7.1 سند فاز ۷):

```
{family}_{model}_{level}_{target}_{featureset}_{tau}_{stage}_{seed}_{timestamp}
مثال: F02_lightgbm_L1_rho_FSlgbm_t010_S2_s42_20260815T1130
```

- **tag:** `git_commit` · `stage` (S0–S3) · `compute` (`local`/`colab`/`kaggle`) · `family`
- **param:** `data_snapshot_hash` (از `doc/data_manifest.md`) · **`cv_folds_hash`** · `feature_set_id` · `model_family` · `model_name` · `level` · `target_transform` · `hyperparams` · `n_trials` · `sampler` · `seed`
- **metric:** `pinball@τ` (هم در فضای نرخ و هم تعداد پرس) · `RMSE` · `MAE` · `shortage_rate` · `waste_reduction_pct` · `coverage@τ` · `fit_seconds`

⚠️ **دروازه‌ی انصاف:** `src/cv.py` یک‌بار `data/processed/cv_folds.json` می‌سازد و هشش در `doc/data_manifest.md` ثبت می‌شود. **هر run با `cv_folds_hash` نامنطبق از جدول مقایسه حذف می‌شود** — این تنها راه تضمین اینکه یک نوت‌بوک کولب واقعاً روی همان foldها اجرا شده است.

سرور **MLflow MCP** در `.mcp.json` تعریف شده و به `mlruns/` وصل است — برای جست‌وجو/مقایسه‌ی experiment ها و run ها از ابزارهای MCP استفاده کن، نه اسکریپت pandas دستی. مقایسه‌ی ~۱۶۹ پیکربندی مدل فاز ۷ بدون این غیرقابل‌دفاع می‌شود.

## اجرای GPU (کولب / کگل)

اجرای محلی اولویت اول است. مدل‌هایی که روی CPU زمان غیرقابل‌قبول دارند (خانواده‌ی شبکه‌ی عصبی روی L5، بیزی سنگین) از این مسیر می‌روند:

- نوت‌بوک از `notebooks/gpu/_template_gpu.ipynb` ساخته می‌شود؛ **کد اصلی در نوت‌بوک نوشته نمی‌شود** — نوت‌بوک فقط `src/` را import می‌کند (قاعده‌ی «`notebooks/` = روایت، `src/` = حقیقت» در کولب هم برقرار است).
- سلول سوم هر نوت‌بوک **assert** روی `data_snapshot_hash` و `cv_folds_hash` می‌زند و در صورت عدم تطابق باید خطا بدهد.
- پس از اجرا: نوت‌بوک اجراشده (با خروجی‌ها) به `notebooks/gpu/executed/{name}__{تاریخ}.ipynb` و `mlruns_gpu/` در `mlruns/` ادغام می‌شود. **نوت‌بوک اجراشده حتی در صورت شکست آزمایش هم برمی‌گردد** — شکست هم داده است.
- checkpoint هر ۱۰ trial در `optuna_studies/*.db` روی Drive تا اجرا پس از قطع session ادامه‌پذیر باشد.

تفصیل کامل: بند 7.8 `doc/WBS-phase7-modeling.md`.

## Milestone ها

M1 پایان فاز ۳ (داده قفل‌شده) → M2 پایان فاز ۴ (دفتر حقایق) → M3 پایان فاز ۶ (خط پایه + پروتکل) → M4 پایان فاز ۷ → M5 گزارش نهایی.

**قبل از M3 هیچ مدل پیچیده‌ای آموزش داده نشود.**

## همروندی و اجرای موازی

فاز ۴ (هر جنبه‌ی EDA) و فاز ۷ به‌طور طبیعی مستقل‌پذیرند. در فاز ۷ این محورها موازی‌پذیرند: هر **خانواده‌ی مدل** · هر **سری** در خانواده‌ی سری‌زمانی (۴۱ سری L4) · هر **τ** · هر **seed**. ⚠️ خانواده‌ی ترکیب (7.12) و کالیبراسیون (7.13) **وابسته**‌اند و باید پس از تکمیل قهرمانان اجرا شوند.

هر اجرای موازی `run_name`ی منحصربه‌فرد می‌سازد (بالا) و خروجی‌اش را با نام یکتا در `data/interim`, `data/processed`, یا `models/` می‌نویسد — هیچ دو اجرای موازی هم‌زمان یک فایل مشترک را نمی‌نویسند.
