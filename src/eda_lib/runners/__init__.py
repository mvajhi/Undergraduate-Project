"""اجراکننده‌های بندهای فاز ۴ (EDA).

هر ماژول `sNN_*.py` یک بند WBS فاز ۴ را کامل اجرا می‌کند و **همه‌ی آماره‌های لازم برای
نوشتن سند یافته‌ها را روی stdout چاپ می‌کند** (به‌علاوه‌ی ذخیره‌ی نمودارها در
`reports/figures/`). دلیل این طراحی: بند ۴ چرخه‌ای است (اکتشاف → پرسش → اکتشاف مجدد)،
پس کد تحلیل باید ده‌ها بار و با پارامتر متفاوت اجرا شود؛ اسکریپت اجرایی برای این کار از
نوت‌بوک مناسب‌تر است. نوت‌بوک‌های `notebooks/04_*` لایه‌ی روایت‌اند و همین توابع را صدا
می‌زنند — منطق تحلیلی هرگز فقط داخل نوت‌بوک نمی‌ماند (قاعده‌ی `AGENTS.md`).

اجرا: `python -m src.eda_lib.runners.s01_univariate`
"""
