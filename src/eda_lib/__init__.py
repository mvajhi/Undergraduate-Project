"""کمک‌کدهای قابل‌استفاده‌ی مجدد برای نوت‌بوک‌های فاز ۴ (EDA).

هر بند WBS که کد کمکی تکراری نیاز داشت، یک ماژول جدید اینجا اضافه می‌کند
(مثلاً ``univariate_helpers.py`` برای بند ۴.۱). فایل‌های ``src/`` اصلی
(``config.py``, ``viz_fa.py``, ...) دست‌نخورده می‌مانند.
"""
