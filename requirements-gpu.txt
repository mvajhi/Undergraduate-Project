# بند 7.8.2 سلول ۱ — وابستگی‌های اجرای GPU (کولب/کگل)
#
# ⚠️ این فایل عمداً از requirements.txt جداست و هرگز در محیط محلی نصب نمی‌شود:
# محیط محلی این پروژه CPU-فقط است (همان دلیلی که LightGBMLSS به‌خاطر وابستگی ~۹۰۰
# مگابایتی torch کنار گذاشته شد — یادداشت خ۹ در doc/progress/07-*.md). نصب این‌ها
# فقط داخل session کولب/کگل معنا دارد.
#
# torch و jax روی کولب از پیش نصب‌اند و نسخه‌شان با درایور CUDA همان ماشین هماهنگ
# است؛ پین‌کردنشان از این‌جا معمولاً باعث نصب دوباره‌ی چند گیگابایتی و گاهی ناسازگاری
# با درایور می‌شود. پس عمداً پین نشده‌اند و به‌جایش نسخه‌ی واقعی هر اجرا در سلول ۵
# (`device_report()`) چاپ و در MLflow ثبت می‌شود — بازتولیدپذیری از راه **ثبت**،
# نه از راه پین‌کردنِ چیزی که کنترلش دست ما نیست.

# --- خ۶ (GP روی GPU) ---
gpytorch>=1.11

# --- خ۸ (بیزی، NUTS روی GPU) ---
numpyro>=0.13
arviz>=0.17

# --- خ۷ عضو ۲۳ (NeuralProphet روی L4، بند 7.16.1 گروه ۷-ب) ---
# ⚠️ بیرون فهرست کوتاه رسمی اسپرینت C (doc/decisions/37-phase7-rescope.md) —
# افزودنش درخواست صریح کاربر بود، نه بازنگری تصمیم ۳۷. کد: f07_neural_l4.py
neuralprophet>=0.9

# --- مشترک با محیط محلی (روی کولب معمولاً موجودند، برای اطمینان تکرار شده‌اند) ---
mlflow>=2.14
optuna>=3.6
pyarrow
pandas
scikit-learn
statsmodels
