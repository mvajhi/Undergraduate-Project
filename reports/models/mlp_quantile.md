# کارت مدل — mlp_quantile

> ⚡ **اجرای GPU** (بند 7.8) — کولب، Tesla T4. نوت‌بوک: `notebooks/gpu/executed/GPU01_neural_L1__20260816.ipynb`
> · گزارش خانواده: `reports/gpu/F07a_neural_L1.md`.
>
> ⚠️ **قهرمان خانواده نشد** (`mlp_entity_embedding` برنده شد، کارت کامل آن‌جا). این کارت
> فقط از داده‌ی R0/R2 پر شده — بدون بازبرازش سه-seed/ACI/DM که فقط برای قهرمان انجام شد
> (بودجه‌ی GPU محدود، بند 7.8). گام‌های ۱۱/۱۳ به همین دلیل سبک‌تر از کارت قهرمان‌اند.

## 1. جایگاه نظری

عضو ۱ خانواده‌ی F07 (بند 7.16.1، گروه ۷-الف) — **MLP** خط پایه‌ی عصبی: بدون embedding،
همان ماتریس یک‌هات خانواده‌های دیگر. سؤالش: «آیا صرفِ شبکه‌ی عصبی (بدون بازنمایی
آموخته‌شده) چیزی به `lightgbm_quantile` اضافه می‌کند؟» `src/models/families/f07_neural.py`.

## 2. سطوح داده‌ی قابل‌اعمال

L1 (سلول). نسخه‌ی مقابل embedding-دار: `mlp_entity_embedding` (قهرمان).

## 3. نگاشت هدف

`rho`، τ=۰.۲۰. مسیر **Q1 بومی** — سر چند-کوانتایلی روی `TAU_GRID`، خروجی با
`base + cumsum(softplus)` یکنوا (تقاطع کوانتایل ساختاراً ناممکن، بند 7.23.1).

## 4. آزمون‌های پیش‌پرواز ⚠️

بند 7.9.2 آزمون آماری اجباری برای شبکه ندارد. سیم‌چین نشتی: R0 داد $R^2=-2.191$ —
بسیار منفی، بدون نشانه‌ی نشت (خیلی دورتر از سقف ۰.۹). این عدد شدیداً منفی خودش گویای
ضعف مطلق مدل با هایپرپارامتر پیش‌فرض است، نه علامت نشت.

## 5. فیچرست اختصاصی

`FS_full_A` (۶۲ ستون پایه) — همان فیچرست `mlp_entity_embedding`، برای مقایسه‌ی محور
معماری نه فیچرست (بند 7.1.2).

## 6. پیش‌پردازش

`f07_neural.TabularPrep` — میانه‌گذاری + `RobustScaler` دستی، یک‌هات کامل (بدون
embedding — تفاوت اصلی این عضو با `mlp_entity_embedding`).

## 7. فضای هایپرپارامتر

۱۳ هایپرپارامتر (`_space_mlp`، `register_space` نسخه ۱):

| پارامتر | دامنه | مقدار برنده |
|---|---|---|
| `learning_rate` | $10^{-5}$…$10^{-2}$ log | 0.004547 |
| `batch_size` | ۶۴…۱۰۲۴ log-int | 85 |
| `weight_decay` | $10^{-6}$…$10^{-1}$ log | 1.618e−05 |
| `dropout` | ۰…۰.۶ | 0.0927 |
| `optimizer` | adam/adamw/rmsprop | adamw |
| `scheduler` | none/cosine/plateau | cosine |
| `grad_clip` | ۰.۱…۵.۰ log | 0.6669 |
| `epochs` | ۴۰…۲۵۰ | 171 |
| `patience` | ۵…۳۰ | 20 |
| `n_layers` | ۱…۵ | 3 |
| `hidden_dim` | ۱۶…۵۱۲ log-int | 193 |
| `activation` | relu/gelu/selu | gelu |
| `batch_norm` | True/False | False |

## 8. راهبرد و بودجه‌ی جستجو

Optuna TPE، بودجه‌ی **زمانی** ۱۸ دقیقه (بند 7.8) → **۳۸ trial کامل، صفر شکست**، هر ۵
fold رسمی، τ=۰.۲۰. زمان کل: ۱٬۰۸۰ ثانیه (۰.۳۰ ساعت-هسته).

## 9. شاهد همگرایی (A6) ⚠️

بهبود ۲۵٪ پایانی <۱٪ ⇒ **همگرا: بله ✅**. ⚠️ **آزمون پایداری بند 7.6.3: فقط ۱ از ۵** —
پایین‌ترین بین سه معماری این نوت‌بوک. pinball هر fold:

| fold0 | fold1 | fold2 | fold3 | fold4 |
|---|---|---|---|---|
| 0.01418 | 0.01549 | **0.02483** | 0.00964 | 0.01024 |

## 10. تحلیل حساسیت هایپرپارامتر ⚠️

fANOVA روی ۳۸ trial (`optuna_studies/mlp_quantile.db`) در گزارش خانواده محاسبه نشد؛
از الگوی `mlp_entity_embedding` (همان فضای مشترک، همان خانواده) انتظار می‌رود
`weight_decay`/`learning_rate` غالب باشند — بدون بازبرازش اضافی روی این مدل خاص تأیید
نشد؛ صادقانه به‌عنوان محدودیت ثبت می‌شود.

## 11. تشخیص برازش

بدون بازبرازش کامل (قهرمان خانواده نشد)، منحنی اعتبارسنجی per-fold ذخیره نشده.
شاهد غیرمستقیم: R0 با هایپرپارامتر پیش‌فرض $R^2=-2.191$ (شکست مطلق پیش از تنظیم)؛
پس از تنظیم S2 به سطح رقابتی رسید (fold pinball تا ۰.۰۰۹۶۴) — تنظیم هایپرپارامتر
اثر بزرگی داشت، ولی بدون بازبرازش دقیق نمی‌شود بیش‌برازش/کم‌برازش را کمّی کرد.

## 12. استخراج کوانتایل

Q1 — سر چند-کوانتایلی، بدون آفست تجربی، یک برازش برای هر پنج τ.

## 13. کالیبراسیون و پوشش ⚠️

فقط پوشش **R0** (fold۰، هایپرپارامتر پیش‌فرض، نه تنظیم‌شده) در دسترس است: پوشش=۰.۶۷۸
(بسیار پوش‌بیش نسبت به τ=۰.۲۰ — ولی این با هایپرپارامتر پیش‌فرض بود، نه هایپرپارامتر
S2). بدون بازبرازش با هایپرپارامتر تنظیم‌شده روی ۵ fold + ACI، عدد کالیبراسیون
نهایی این مدل گزارش نشده — محدودیت شناخته‌شده‌ی این کارت (بودجه‌ی GPU فقط صرف قهرمان شد).

## 14. جمع‌بندی و حکم

بهترین pinball (fold-mean) = **۰.۰۱۴۸۷**، از B3 (۰.۰۱۵۹۰) بهتر ولی از LightGBM خ۲
(۰.۰۱۳۸۸) و از هم‌خانواده‌ی خودش `mlp_entity_embedding` (۰.۰۱۴۶۷) بدتر. پایداری
ضعیف‌ترین (۱/۵). **حکم: ❌ کاندید S3 نیست** — نه به این دلیل که ذاتاً بد است، بلکه چون
`mlp_entity_embedding` (همان خانواده، فقط با embedding) در هر معیار (pinball، پایداری)
از آن جلوتر بود؛ نتیجه‌ی مفید این مقایسه خودش یافته است: embedding برای `RestaurantName`
سود واقعی داد، نه فقط تزئین.
