# کارت مدل — ft_transformer

> ⚡ **اجرای GPU** (بند 7.8) — کولب، Tesla T4. نوت‌بوک: `notebooks/gpu/executed/GPU01_neural_L1__20260816.ipynb`
> · گزارش خانواده: `reports/gpu/F07a_neural_L1.md`.
>
> ⚠️ **قهرمان خانواده نشد** (`mlp_entity_embedding` برنده شد، کارت کامل آن‌جا). این کارت
> فقط از داده‌ی R0/R2 پر شده — بدون بازبرازش سه-seed/ACI/DM.

## 1. جایگاه نظری

عضو ۴ خانواده‌ی F07 (بند 7.16.1، گروه ۷-الف) — **FT-Transformer** سبک: هر فیچر یک توکن
+ یک توکن [CLS]، توجه بین فیچرها. گران‌ترین و ساختاریافته‌ترین عضو سه‌گانه‌ی این
نوت‌بوک؛ سؤالش «آیا توجه بین فیچرها روی ۷٬۵۷۹ رکورد چیزی می‌ارزد؟» `f07_neural.py`.

## 2. سطوح داده‌ی قابل‌اعمال

L1 (سلول).

## 3. نگاشت هدف

`rho`، τ=۰.۲۰. مسیر **Q1 بومی** — سر چند-کوانتایلی با `cumsum(softplus)` (ضدتقاطع
ساختاری، بند 7.23.1)، دقیقاً مثل بقیه‌ی این خانواده.

## 4. آزمون‌های پیش‌پرواز ⚠️

سیم‌چین نشتی: R0 داد $R^2=-0.016$ — نزدیک صفر (نه مثبت)، دور از سقف نشت ۰.۹. بدون
نشانه‌ی نشت.

## 5. فیچرست اختصاصی

`FS_full_A` (۶۲ ستون) — همه‌ی دسته‌ای‌ها (نه فقط ۵ ستون منتخب `mlp_entity_embedding`)
embedding می‌گیرند، چون معماری «هر فیچر یک توکن» یک‌هات‌کردن را بی‌معنا می‌کند
(بند بالای `f07_neural.py::fit_l4` مشابه، همان استدلال).

## 6. پیش‌پردازش

`TabularPrep` با `emb_cols` = همه‌ی ستون‌های دسته‌ای فیچرست (نه فهرست ثابت
`EMBEDDING_COLS`) — تفاوت اختصاصی این معماری.

## 7. فضای هایپرپارامتر

۱۴ هایپرپارامتر (`_space_ft`):

| پارامتر | دامنه | مقدار برنده |
|---|---|---|
| `d_model` | {۱۶,۳۲,۶۴,۱۲۸,۲۵۶} | 32 |
| `n_heads` | {۱,۲,۴,۸} (باید بر `d_model` بخش‌پذیر باشد) | 8 |
| `n_blocks` | ۱…۴ | 4 |
| `ff_dim` | ۳۲…۵۱۲ log-int | 227 |
| `learning_rate` | $10^{-5}$…$10^{-2}$ log | 0.001145 |
| `batch_size` | ۶۴…۱۰۲۴ log-int | 174 |
| `weight_decay` | $10^{-6}$…$10^{-1}$ log | 1.051e−06 |
| `dropout` | ۰…۰.۶ | 0.0996 |
| `optimizer` | adam/adamw/rmsprop | adamw |
| `scheduler` | none/cosine/plateau | none |
| `grad_clip` | ۰.۱…۵.۰ log | 0.8654 |
| `epochs` | ۴۰…۲۵۰ | 210 |
| `patience` | ۵…۳۰ | 15 |

## 8. راهبرد و بودجه‌ی جستجو

Optuna TPE، بودجه‌ی زمانی ۲۴ دقیقه → **۱۴ trial کامل، صفر شکست** (گران‌ترین سه‌گانه —
کمترین trial در همان بودجه). زمان کل: ۱٬۵۵۰ ثانیه (۰.۴۳ ساعت-هسته)، بیشترین ساعت-هسته
با کمترین trial بین سه معماری — هزینه‌ی توجه چندسر محسوس بود.

## 9. شاهد همگرایی (A6) ⚠️

بهبود ۲۵٪ پایانی <۱٪ ⇒ ثبت‌شده **همگرا: بله ✅** ولی با فقط ۱۴ trial این چک ضعیف
است. ⚠️ **آزمون پایداری بند 7.6.3: ۰ از ۵** — بدترین بین هر مدل GPU این اسپرینت.
pinball هر fold:

| fold0 | fold1 | fold2 | fold3 | fold4 |
|---|---|---|---|---|
| 0.01383 | 0.01494 | **0.02426** | 0.01126 | 0.01013 |

## 10. تحلیل حساسیت هایپرپارامتر ⚠️

با فقط ۱۴ trial، fANOVA قابل‌اتکا نیست — گزارش نشد (محدودیت صادقانه، نه رد پرسش).
مشاهده‌ی کیفی: بهترین trial با `d_model` کوچک (۳۲، نه ۲۵۶) و `n_heads=8` (بیشترین
گزینه) برنده شد — نشانه‌ای که با این حجم داده، مدل کوچک‌تر با توجه ریزدانه‌تر بهتر از
مدل بزرگ عمل کرد؛ سازگار با انتظار بند 7.16 (شبکه‌ی بزرگ روی L1 بیش‌برازش می‌کند).

## 11. تشخیص برازش

بدون بازبرازش کامل، منحنی per-fold ذخیره نشده. غیرمستقیم: R0 پیش از تنظیم
$R^2=-0.016$ (نزدیک به بی‌اثر مطلق، نه بیش‌برازش) — با تنظیم به سطح رقابتی رسید.

## 12. استخراج کوانتایل

Q1 — سر چند-کوانتایلی، بدون آفست، یک برازش برای هر پنج τ.

## 13. کالیبراسیون و پوشش ⚠️

فقط پوشش R0 (هایپرپارامتر پیش‌فرض، fold۰) در دسترس: پوشش=۰.۲۳۲ (نزدیک‌ترین به
τ=۰.۲۰ بین هر سه معماری در R0، جالب توجه چون دقتش هنوز پایین بود). بدون بازبرازش
تنظیم‌شده + ACI روی ۵ fold، عدد نهایی گزارش نشده.

## 14. جمع‌بندی و حکم

بهترین pinball (fold-mean) = **۰.۰۱۴۸۸** — تقریباً برابر `mlp_quantile` (۰.۰۱۴۸۷)،
از B3 بهتر ولی از `mlp_entity_embedding` (۰.۰۱۴۶۷) و LightGBM (۰.۰۱۳۸۸) بدتر.
گران‌ترین (۰.۴۳ ساعت-هسته برای ۱۴ trial) و ناپایدارترین (۰/۵). **حکم: ❌ کاندید S3
نیست** — پیچیدگی معماری توجیهی در برابر LightGBM یا حتی MLP ساده‌ی همین خانواده
پیدا نکرد؛ شاهد مستقیم دیگری بر انتظار بند 7.16 که پیچیدگی روی این حجم داده کمکی
نمی‌کند.
