# کارت مدل — bhm_beta_binomial_crossed

> ⚡ **اجرای GPU** (بند 7.8) — کگل، NumPyro/NUTS. نوت‌بوک: `notebooks/gpu/executed/GPU03_bayesian_L1__20260816.ipynb`
> · گزارش خانواده: `reports/gpu/F08_bayesian_L1.md`.
>
> ⚠️ **قهرمان خانواده نشد** (`bhm_varying_dispersion` برنده شد، کارت کامل:
> `reports/models/bhm_varying_dispersion.md`). این کارت از داده‌ی R0/R2 + چک‌لیست
> همگرایی/PPC/حساسیت پیشین (که برای هر سه مدل این نوت‌بوک مشترک اجرا شد) پر شده —
> بدون بازبرازش سه-seed/ACI/DM که فقط برای قهرمان انجام شد.

## 1. جایگاه نظری

عضو ⭐۲ خانواده‌ی F08 (بند 7.17.1) — **اثرات تصادفی متقاطع (روز + سلف)**:
«منطبق‌ترین با F10» طبق خودِ سند (ICC(روز)=۰.۲۲۵ ≈ ICC(سلف)=۰.۲۰۵، دو منبع همبستگی
مستقل، نه تودرتو). `src/models/families/f08_bayesian.py::fit_generic(use_day=True,
varying_dispersion=False)`.

## 2. سطوح داده‌ی قابل‌اعمال

L1. اثر تصادفی سلف + اثر تصادفی روز، هر دو با پارامترسازی غیرمرکزی.

## 3. نگاشت هدف

`rho` (از راه `NoRecv`/`Res` با Beta-Binomial). τ=۰.۲۰. مسیر **Q4** — کوانتایل
تجربی از نمونه‌های پسین شبیه‌سازی‌شده (`posterior_rho_samples`)، با اثر روزِ آزمون
قرعه‌کشیده از پیشین (نه پسین) چون روز آزمون در train دیده نشده — بند ۱۲.

## 4. آزمون‌های پیش‌پرواز ⚠️

بند 7.9.2 آزمون آماری اجباری برای بیزی سلسله‌مراتبی ندارد؛ **چک‌لیست همگرایی بند
7.17.3 جای آن را می‌گیرد** (بند ۹).

## 5. فیچرست اختصاصی

۲۰ ستون عددی/باینری منتخب (`BAYES_FEATURES`، بند بالای `f08_bayesian.py`) — عمداً
کوچک: NUTS با ۱۵۴ ستون یک‌هات خ۱ هم کند می‌شود هم قیف پسین بدشکل می‌سازد. اثر سلف/روز
از راه اثر تصادفی وارد می‌شود، نه دامی دسته‌ای.

## 6. پیش‌پردازش

`BayesPrep` — استاندارد‌سازی (میانگین/انحراف‌معیار) عددی‌ها + واژگان صحیح `RestaurantName`
(سلف دیده‌نشده ⇒ اندیس −۱ ⇒ قرعه از پیشین، نه صفر).

## 7. فضای هایپرپارامتر

۴ هایپرپارامتر مشترک همه‌ی خ۸ (`_space_common`، کاردینالیتی=۲۷ اعلام‌شده — فضای
عملاً گسسته‌ی کوچک، بند 7.6.2 بازنویسی‌شده):

| پارامتر | دامنه | مقدار برنده |
|---|---|---|
| `prior_preset` | tight/default/wide | tight |
| `target_accept` | ۰.۸…۰.۹۹ | 0.8696 |
| `num_warmup` | {۳۰۰,۵۰۰,۸۰۰} | 500 |
| `num_samples` | {۴۰۰,۶۰۰,۱۰۰۰} | 600 |

## 8. راهبرد و بودجه‌ی جستجو

Optuna TPE، بودجه‌ی زمانی (بند 7.8) → **۳ trial کامل، صفر شکست**، هر ۵ fold رسمی.
زمان کل: ۱٬۷۸۱ ثانیه (۰.۴۹ ساعت-هسته) — گران‌ترین trial بین هر سه مدل خ۸ (اثر
متقاطع دو گروه به‌جای یک، دو بردار z اضافی).

## 9. شاهد همگرایی — چک‌لیست بند 7.17.3 (جایگزین A6) ⚠️

| بند | مقدار | آستانه | نتیجه |
|---|---|---|---|
| max R̂ | 1.0488 | <1.01 | ❌ رد شد |
| min ESS | 64.4 | >400 | ❌ رد شد |
| n_divergences | 0 | =0 (یا <۰.۱٪) | ✅ پاس شد |

⚠️ **دو از سه بند چک‌لیست همگرایی رد شدند.** با بودجه‌ی `num_warmup=500`/
`num_samples=600` (کوچک برای اثرات متقاطع دوگانه)، زنجیره‌ها کاملاً به توزیع پایا
نرسیده‌اند. صفر-divergence دلگرم‌کننده است ولی به‌تنهایی کافی نیست — این مدل رسماً
**همگرا نشده** حساب می‌شود.

آزمون پایداری بند 7.6.3: **۲ از ۵**. pinball هر fold:

| fold0 | fold1 | fold2 | fold3 | fold4 |
|---|---|---|---|---|
| 0.01249 | 0.01540 | **0.02706** | 0.00941 | 0.00965 |

## 10. تحلیل حساسیت هایپرپارامتر — حساسیت پیشین (بند 7.17.2) ⚠️

بند 7.17.3 برای بیزی حساسیت **پیشین** را می‌خواهد، نه fANOVA معمولی (با ۳ trial
معنادار هم نبود). روی fold۰، هایپرپارامترهای دیگر ثابت (`num_warmup=300,
num_samples=300`)، فقط `prior_preset` عوض شد:

| پیشین | σ پیشین | β پیشین | pinball | پوشش | max R̂ | divergences |
|---|---|---|---|---|---|---|
| tight | 0.5 | 1.0 | 0.01251 | 0.1862 | 1.0153 | 0 |
| default | 1.0 | 2.5 | **0.01250** | 0.1920 | 1.0218 | 0 |
| wide | 2.0 | 5.0 | 0.01261 | 0.1931 | 1.0188 | 0 |

**حساسیت کم**: pinball بین سه پیشین تنها ۰.۰۰۰۱۱ فرق می‌کند — نتیجه به انتخاب
پیشین حساس نیست، شاهدی برای اطمینان از این بخش مدل (برخلاف همگرایی که ضعیف بود).
جالب که TPE با بودجه‌ی کامل هم `tight` را انتخاب کرد، هم‌راستا با این جدول.

## 11. تشخیص برازش — Posterior Predictive Check (بند 7.17.3 بند ششم) ⚠️

| آماره | شبیه‌سازی‌شده | واقعی | هدف (F02/F03) |
|---|---|---|---|
| چولگی | 1.786 | 3.057 | 4.06 |
| سهم صفر | 0.056 | 0.062 | 0.049 |
| میانگین | 0.098 | 0.107 | — |

میانگین و سهم صفر نزدیک‌اند؛ **چولگی به‌وضوح کم‌برآورد شده** (۱.۷۹ در برابر واقعی
۳.۰۶) — پسین دُم راست توزیع واقعی $\rho$ را کامل بازتولید نمی‌کند. سازگار با
همگرایی ناقص گام ۹: زنجیره‌های نرسیده به پایایی معمولاً واریانس پسین را دست‌کم
می‌گیرند.

## 12. استخراج کوانتایل

Q4 — نمونه‌ی پسین شبیه‌سازی‌شده (Beta-Binomial با $\mu,\phi$ ثابت روی همه‌ی رکوردها
— برخلاف `bhm_varying_dispersion` که $\phi=f(\log Res)$ دارد)، اثر روز آزمون از
پیشین قرعه‌کشیده.

## 13. کالیبراسیون و پوشش ⚠️

فقط پوشش R0 (fold۰، `num_warmup/samples=300`): پوشش=۰.۱۹۸ (شکاف از τ=۰.۲۰: −۰.۰۰۲
— در نگاه اول خوب). ولی با همگرایی ناقص گام ۹ و کم‌برآوردی چولگی گام ۱۱، این عدد
تصادفی نزدیک می‌تواند باشد، نه کالیبراسیون واقعی. بدون بازبرازش کامل ۵ fold + ACI،
نتیجه‌گیری قطعی درباره‌ی کالیبراسیون این مدل زودهنگام است.

## 14. جمع‌بندی و حکم

بهترین pinball (fold-mean) = **۰.۰۱۴۸۰** — نزدیک‌ترین رقیب `bhm_varying_dispersion`
(۰.۰۱۴۷۷) درون همین خانواده، هر دو از B3 (۰.۰۱۵۹۰) بهتر. **حکم: ❌ کاندید S3 نیست**
— نه چون عملکردش ضعیف بود (تقریباً مساوی قهرمان)، بلکه چون قهرمان‌سازی به یک مدل
محدود بود (بودجه‌ی GPU) و `bhm_varying_dispersion` علاوه‌بر pinball مشابه، پاسخ
مستقیم‌تری به F06/F07 (دیسپرسیون متغیر) هم داشت. ⚠️ **هشدار مهم‌تر از رتبه:** این
مدل چک‌لیست همگرایی بند 7.17.3 را پاس نکرد (max R̂=۱.۰۴۹) — اگر روزی دوباره اجرا
شود، اول باید `num_warmup`/`num_samples` را به‌مراتب افزایش داد، نه فقط دوباره
tune کرد با همان بودجه‌ی کوچک.
