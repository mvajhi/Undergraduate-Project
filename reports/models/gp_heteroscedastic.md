# کارت مدل — gp_heteroscedastic

> ⚡ **اجرای GPU** (بند 7.8) — کگل، ۲× Tesla T4. نوت‌بوک: `notebooks/gpu/executed/GPU04_gp_L1__20260821.ipynb`
> · گزارش خانواده: `reports/gpu/F06_gp_L1.md`.
>
> ⚠️ **قهرمان خانواده نشد** (`gp_quantile` برنده شد، کارت کامل: `reports/models/gp_quantile.md`).
> این کارت فقط از داده‌ی R0/R2 پر شده — بدون بازبرازش سه-seed/ACI/DM.

## 1. جایگاه نظری

عضو K7 خانواده‌ی F06 (بند 7.15.1/7.15.3) — GP با **نویز ناهم‌واریانس**: مرحله‌ی دوم
باقیمانده‌ی مرحله‌ی اول را روی $\log Res$ رگرس می‌کند تا $\sigma_n^2(x)$ نقطه‌به‌نقطه
بسازد (`FixedNoiseGaussianLikelihood`) — پاسخ مستقیم به F06 (ناهم‌واریانسی، نسبت std
چارک کوچک به بزرگ ۳.۰۳). `src/models/families/f06_kernel.py::fit_gp(heteroscedastic=True)`.

## 2. سطوح داده‌ی قابل‌اعمال

L1 — همان استدلال `gp_quantile` (بند 7.15.4 محدودیت مقیاس‌پذیری روی GPU برطرف است).

## 3. نگاشت هدف

`rho`، τ=۰.۲۰. مسیر **Q4** — کوانتایل مستقیم از پسین گاوسی
$\hat\rho_\tau = m(x) + z_\tau\sqrt{s^2(x)+\sigma_n^2(x)}$؛ تفاوت با `gp_quantile`
دقیقاً همین $\sigma_n^2(x)$ نقطه‌به‌نقطه به‌جای ثابت است.

## 4. آزمون‌های پیش‌پرواز ⚠️

سیم‌چین نشتی: R0 داد $R^2=-0.173$ — دور از سقف نشت. بدون آزمون آماری اجباری دیگر
(بند 7.9.2 برای کرنل چیزی نمی‌خواهد).

## 5. فیچرست اختصاصی

همان `gp_quantile`: ۲۳ ستون عددی + یک‌هات `RestaurantName` + `t_index` خام برای
کرنل دوره‌ای (۵۴ بُعد). بند 7.15.5.

## 6. پیش‌پردازش

`GPPrep` (مقیاس‌بندی مقاوم + یک‌هات + محور زمان خام) — یکسان با `gp_quantile`.
برازش دومرحله‌ای اختصاصی: مرحله‌ی ۱ (کرنل K5_full، نویز ثابت) → باقیمانده →
`polyfit(log_res, log(resid²))` → مرحله‌ی ۲ با `FixedNoiseGaussianLikelihood`.

## 7. فضای هایپرپارامتر

۲ هایپرپارامتر (`_space_gp_het`؛ کرنل ثابت K5_full، بدون گزینه — تفاوت با
`gp_quantile` که کرنل را هم تنظیم می‌کند):

| پارامتر | دامنه | مقدار برنده |
|---|---|---|
| `n_iters` | ۶۰…۴۰۰ log-int | 122 |
| `learning_rate` | ۰.۰۰۵…۰.۲ log | 0.1668 |

## 8. راهبرد و بودجه‌ی جستجو

Optuna TPE، بودجه‌ی زمانی ۲۰ دقیقه (تخمینی از گزارش خانواده) → **فقط ۱ trial کامل
شد** پیش از اتمام بودجه (`budget_exhausted=True`) — هر trial یک برازش **دومرحله‌ای**
کامل روی ۵ fold است (دو تجزیه‌ی چولسکی به‌جای یک). زمان کل: ۱٬۲۸۹ ثانیه (۰.۳۶
ساعت-هسته) برای همان یک trial.

## 9. شاهد همگرایی (A6) ⚠️

با فقط ۱ trial، معیار «بهبود ۲۵٪ پایانی» بی‌معناست — به‌طور مکانیکی
`converged=True` ثبت شده (تنها یک نقطه، بهبودی برای سنجیدن نیست). **این محدودیت
باید صریح خوانده شود: عملاً هیچ جست‌وجویی انجام نشد.**

**آزمون پایداری بند 7.6.3: ۵ از ۵** — با این‌که فقط یک پیکربندی آزموده شد،
همان‌یک پیکربندی در هر ۵ fold هم در ۱۰٪ برتر بود (تنها گزینه = خودکار برترین).
این عدد نباید به «پایداری واقعی» تعبیر شود.

pinball هر fold:

| fold0 | fold1 | fold2 | fold3 | fold4 |
|---|---|---|---|---|
| 0.0203 | 0.02499 | **0.03244** | 0.01465 | 0.01566 |

## 10. تحلیل حساسیت هایپرپارامتر ⚠️

با ۱ trial، fANOVA تعریف‌نشده است — مستقیماً به بودجه‌ی ناکافی (بند 7.6.2؛ هر trial
این مدل ~۲۱ دقیقه گرفت) نسبت داده می‌شود، نه به مدل. اگر این خانواده دوباره اجرا شود،
اولویت اول افزایش بودجه‌ی زمانی این مدل خاص است، نه `gp_quantile`.

## 11. تشخیص برازش

بدون بازبرازش کامل، منحنی per-fold ندارد. $R^2$ خارج‌نمونه‌ی R0 منفی (بند ۴) —
طبیعی برای پیش‌بینی‌کننده‌ی کوانتایل.

## 12. استخراج کوانتایل

Q4 — مستقیم از پسین، با $\sigma_n^2(x)$ نقطه‌به‌نقطه (نه ثابت).

## 13. کالیبراسیون و پوشش ⚠️

فقط پوشش R0 در دسترس: پوشش=۰.۲۴۴ (شکاف از τ=۰.۲۰: +۰.۰۴۴) — نزدیک‌تر به هدف از
`gp_quantile` در همان R0 (۰.۱۸۶)، جالب چون خودِ ایده (نویز ناهم‌واریانس) دقیقاً برای
بهبود کالیبراسیون طراحی شده بود؛ ولی این فقط یک نقطه‌ی fold۰ با هایپرپارامتر
پیش‌فرض است، نه نتیجه‌ی تنظیم‌شده‌ی ۵ fold + ACI.

## 14. جمع‌بندی و حکم

بهترین pinball (fold-mean) = **۰.۰۲۱۶۱** — بدترین بین هر دو عضو GP، **از B3
(۰.۰۱۵۹۰) هم بدتر**. **حکم: ❌ کاندید S3 نیست، و شاهد کافی برای قضاوت واقعی هم
نداریم** — با فقط ۱ trial (بودجه‌ی زمانی هر trial ~۲۱ دقیقه‌ای این مدل دومرحله‌ای
را خورد)، این نتیجه بیشتر گواهی بر **ناکافی‌بودن بودجه** است تا ضعف ذاتی ایده‌ی
نویز ناهم‌واریانس. اگر در آینده اجرا شود، باید بودجه‌ی زمانی اختصاصی و بزرگ‌تری
بگیرد، نه اینکه با همان بودجه‌ی مدل تک‌مرحله‌ای مقایسه شود.
