# CLAUDE.md

@AGENTS.md

راهنمای مشترک پروژه (تعریف مسئله، قاعده‌ی برش اطلاعاتی، دستورات، ساختار مخزن، ثبت آزمایش، Milestone ها) در `AGENTS.md` است و با import بالا به‌طور خودکار در کانتکست Claude Code لود می‌شود. این فایل فقط شامل نکات مخصوص Claude Code است.

## اجرای موازی با subagent (مخصوص Claude Code)

وقتی چند subagent قرار است هم‌زمان کد داخل `src/` را ویرایش کنند (نه فقط اجرا)، از `isolation: "worktree"` ابزار Agent استفاده کن. برای اجراهای فقط‌خواندنی یا فقط‌نویسنده‌ی فایل جدید (EDA، آموزش مدل) نیازی به worktree نیست.
