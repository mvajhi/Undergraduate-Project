# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## پروژه

پیش‌بینی نرخ عدم‌دریافت غذا در سلف‌های دانشگاه تهران (رگرسیون کوانتایل روی سری‌های زمانی چندگانه)، برای پیشنهاد مقدار بهینه‌ی پخت. تعریف کامل مسئله: `doc/project-definition.md`. نقشه‌ی کامل فازها و بسته‌های کاری: `doc/WBS-food-demand-forecasting.md`. واحد تحلیل: $(d, m, r, f)$ = (روز، وعده، سلف، نوع غذا) برای مدل A (تجمیعی)؛ $(p, d, m, r, f)$ برای مدل B (طبقه‌بندی سطح فرد، بند ۱-۶ WBS) — خروجی عملیاتی نهایی هر دو مسیر همچنان سطح $(d,m,r,f)$ است.

## قاعده‌ی برش اطلاعاتی — مهم‌ترین قانون پروژه

پیش‌بینی هر روز $d$ در **پایان روز $d-1$** تولید می‌شود. هیچ فیچر یا مدلی، در هیچ فازی، نباید این مرز را بشکند:

| مجاز ✅ | ممنوع ❌ |
|---|---|
| `Reservation` تا $d+2$ (رزرو ۷۲ ساعت زودتر بسته می‌شود) | `ReceiveWithCard`, `ReceiveWithCode`, `DontReceive` برای روز $d$ |
| `Recv`, `NoRecv`, نرخ $\rho$ برای هر $d' \le d-1$ | هر ستون خروجی که پس از پخت مشخص می‌شود |
| منو و ویژگی‌های تقویمی هر روز | — |
| `ReserveStatus` تاریخچه‌ی رفتاری فرد $p$ برای هر $d' \le d-1$ (مدل B) | `ReserveStatus_{p,d}` برای روز هدف $d$ |
| فیچرهای جمعیتی ثابت فرد (دانشکده/رشته/مقطع/خوابگاه/جنسیت، مدل B) | — |

جزئیات کامل: بند ۴-۲ `doc/project-definition.md`. هر فیچر جدید پیش از استفاده باید در برابر این جدول بررسی شود. داده‌ی سطح فردی (`PersonId`) پیش از هر پردازش باید طبق پروتکل ناشناس‌سازی بند ۰-۷ WBS به `PersonHash` تبدیل شود — `PersonId` خام در هیچ فایل پردازش‌شده یا commit ظاهر نمی‌شود.

## دستورات

```bash
make setup       # venv + نصب از requirements.lock
make lock        # بازتولید requirements.lock بعد از نصب پکیج جدید
make data-pull   # dvc pull
make mlflow-ui   # UI محلی MLflow روی mlruns/
```

سیدهای تصادفی از یک منبع واحد می‌آیند — همیشه اول این را صدا بزن:
```python
from src.config import set_global_seed
set_global_seed()
```

## ساختار مخزن

- `notebooks/` = روایت، `src/` = حقیقت — هیچ منطق مهمی فقط داخل نوت‌بوک نماند.
- مستندات پروژه در `doc/` است (نه `docs/` — نام‌گذاری قدیمی مخزن حفظ شده).
- تحلیل عمیق پشتیبان یک ردیف `doc/decision_log.md` (وقتی خودِ ردیف برای توضیح کافی نیست) در `doc/decisions/` قرار می‌گیرد، با نام‌گذاری `{شماره‌ردیف decision_log}-{موضوع-کوتاه-kebab-case}.md` (مثلاً `08-target-variable.md`) — نه بر اساس شماره‌ی بند WBS، چون آن شماره‌ها با بازنویسی WBS تغییر می‌کنند ولی شماره‌ردیف decision_log فقط افزایشی است.
- `data/raw/` فقط‌خواندنی است. رویه‌ی افزودن فایل خام جدید و شناسه‌ی هر snapshot: `doc/data_manifest.md`.
- هر تصمیم روش‌شناختی (انتخاب فیچر، مدل، معیار) باید در `doc/decision_log.md` ثبت شود — این سند مستقیماً فصل «روش‌شناسی» گزارش نهایی می‌شود.

## ثبت آزمایش (MLflow)

هر run باید این‌ها را ثبت کند: `run_name` منحصربه‌فرد (`{model_family}_{feature_set_id}_{timestamp}`)، tag `git_commit`، params برای `data_snapshot_hash` (از `doc/data_manifest.md`)، `feature_set_id`، `model_family`، `hyperparams`، `cv_scheme`، و متریک‌های `pinball@τ`, `RMSE`, `MAE`, `shortage_rate`.

سرور **MLflow MCP** در `.mcp.json` تعریف شده و به `mlruns/` وصل است — برای جست‌وجو/مقایسه‌ی experiment ها و run ها از ابزارهای MCP استفاده کن، نه اسکریپت pandas دستی. مقایسه‌ی ۴۰-۵۰ آزمایش فاز ۷ بدون این غیرقابل‌دفاع می‌شود.

## Milestone ها

M1 پایان فاز ۳ (داده قفل‌شده) → M2 پایان فاز ۴ (دفتر حقایق) → M3 پایان فاز ۶ (خط پایه + پروتکل) → M4 پایان فاز ۷ → M5 گزارش نهایی.

**قبل از M3 هیچ مدل پیچیده‌ای آموزش داده نشود.**

## همروندی و اجرای موازی

فاز ۴ (هر جنبه‌ی EDA) و فاز ۷ (هر خانواده‌ی مدل × هر τ) به‌طور طبیعی مستقل‌پذیرند و کاندید اجرای موازی با ابزار `Agent` هستند.

- هر subagent موازی `run_name`ی منحصربه‌فرد می‌سازد (بالا) و خروجی‌اش را با نام یکتا در `data/interim`, `data/processed`, یا `models/` می‌نویسد — هیچ دو subagent هم‌زمان یک فایل مشترک را نمی‌نویسند.
- وقتی چند subagent قرار است هم‌زمان کد داخل `src/` را ویرایش کنند (نه فقط اجرا)، از `isolation: "worktree"` ابزار Agent استفاده کن. برای اجراهای فقط‌خواندنی یا فقط‌نویسنده‌ی فایل جدید (EDA، آموزش مدل) نیازی به worktree نیست.
