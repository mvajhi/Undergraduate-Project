# دفترچه تصمیمات (Decision Log)

هر تصمیم روش‌شناختی پروژه، از فاز ۰ تا فاز ۱۱، در این جدول ثبت می‌شود. این سند مستقیماً به فصل «روش‌شناسی» گزارش نهایی تبدیل می‌شود (بند 0.5 WBS) — پس هر ردیف باید به‌تنهایی برای خواننده‌ی گزارش قابل‌فهم باشد. برای تصمیم‌هایی که تحلیل پشتیبانشان طولانی‌تر از یک ردیف است، فایل جداگانه در `doc/decisions/{شماره‌ردیف}-{موضوع}.md` نوشته و از ستون «اثر بر نتیجه» به آن ارجاع داده می‌شود.

| # | تصمیم | تاریخ | گزینه‌های بررسی‌شده | دلیل انتخاب | اثر بر نتیجه |
|---|---|---|---|---|---|
| 1 | مدیریت محیط اجرا و وابستگی‌ها با `venv` + `pip freeze` | 2026-08-11 | `uv` + `pyproject.toml`/`uv.lock`، `conda`، Docker | سادگی و آشنایی بیشتر برای پروژه‌ی کارشناسی؛ نیازی به قابلیت‌های اضافه‌ی `uv` (workspace، سرعت resolve) در این مقیاس نیست | `requirements.txt` (وابستگی‌های سطح‌بالا) و `requirements.lock` (خروجی `pip freeze`) هر دو در ریشه‌ی پروژه نگه‌داری می‌شوند؛ بازتولید محیط با `python3 -m venv .venv && pip install -r requirements.lock` |
| 2 | سامانه‌ی ثبت آزمایش: MLflow (نه `experiments.csv` ساخت‌یافته) | 2026-08-11 | MLflow، Weights & Biases، `experiments.csv` دستی | MLflow از قبل روی سیستم نصب بود؛ UI و query API خودکار دارد که مقایسه‌ی ۴۰-۵۰ آزمایش فاز ۷ را بدون اسکریپت‌نویسی دستی ممکن می‌کند؛ W&B نیاز به حساب/سرویس بیرونی دارد که برای این پروژه ضروری نیست | tracking URI محلی `mlruns/` (بدون سرور جداگانه)؛ فیلدهای اجباری هر run طبق بند 0.4 WBS در `CLAUDE.md` مستند شده‌اند |
| 3 | نسخه‌بندی داده‌ی خام با DVC (نه فقط ثبت هش SHA-256) | 2026-08-11 | ثبت دستی هش SHA-256 در `docs/data_manifest.md`، DVC | DVC هم pointer قابل‌بازیابی می‌سازد و هم مسیر رشد پروژه (چند snapshot، remote storage در آینده) را باز می‌گذارد؛ هش SHA-256 هم به‌صورت مکمل (نه جایگزین) در `doc/data_manifest.md` ثبت می‌شود چون بند 0.3 صراحتاً آن را می‌خواهد و مستقل از الگوریتم هش داخلی DVC (MD5) است | فایل `data/raw/*.csv.dvc` و ردیف متناظر در `doc/data_manifest.md`؛ **remote هنوز تنظیم نشده** — cache فقط local است، این محدودیت را قبل از انتقال پروژه به ماشین دیگر باید رفع کرد |
| 4 | مقداردهی اولیه‌ی Git از همین فاز | 2026-08-11 | تعویق تا فاز ۱، انجام در فاز ۰ | بند 0.4 (فیلد `git_commit` در هر رکورد آزمایش) و بند 0.3 (نسخه‌بندی کد) بدون حداقل یک commit بی‌معنی‌اند | یک مخزن Git محلی (شاخه‌ی `main`، بدون remote) با commit اولیه‌ی شامل کل اسکلت فاز ۰ |
| 5 | افزودن MLflow MCP server (`mlflow-mcp`, PyPI) به زیرساخت | 2026-08-11 | بدون MCP (فقط CLI/UI دستی MLflow)، نوشتن ابزار داخلی سفارشی، استفاده از `mlflow-mcp` موجود | بسته‌ی آماده و تست‌شده‌ای است که ابزارهای جست‌وجو/مقایسه‌ی experiment و run را مستقیماً به‌صورت MCP tool در اختیار عامل (Claude) می‌گذارد؛ نوشتن ابزار داخلی معادل، کار تکراری و بدون ارزش افزوده بود | `.mcp.json` در ریشه‌ی پروژه سرور را با `MLFLOW_TRACKING_URI=mlruns` راه‌اندازی می‌کند؛ در فازهای بعدی (به‌خصوص فاز ۷) مقایسه‌ی run ها باید از طریق این ابزارها انجام شود، نه اسکریپت pandas دستی |
| 6 | منبع تقویم آموزشی: PDF مصوب معاونت آموزشی دانشگاه تهران (نه استخراج HTML/کراول سایت آموزش) | 2026-08-11 | کراول صفحه‌ی سایت آموزش دانشگاه (شکننده در برابر تغییر قالب سایت، بدون سند مرجع رسمی)، PDF مصوب دانلود‌شده از سایت معاونت آموزشی | PDF نسخه‌ی رسمی و امضاءشده‌ی تقویم است — قابل‌استناد در گزارش و پایدار در برابر تغییر بعدی سایت؛ استخراج تاریخ‌ها از آن دستی/نیمه‌دستی و یک‌باره است، پس نیازی به کراولر پایا نیست | فایل `data/raw/UT_calender_1402_1043.pdf` (کلیه مقاطع) منبع استخراج فیچرهای بند ۲-۲-۴ WBS (شروع/پایان ترم، میان‌ترم، حذف‌واضافه، امتحانات) خواهد بود؛ استخراج جدول تاریخ‌ها از PDF باید در `src/` و قابل‌بازتولید باشد، نه فقط دستی در نوت‌بوک. **اصلاحیه (۲۰۲۶-۰۸-۱۳، ردیف ۱۵):** بررسی کامل‌تر PDF نشان داد این توضیح ناقص بود — فایل فقط نیمسال اول نیست، **هر دو نیمسال ۱۴۰۲-۱۴۰۳ را دارد** (تا امتحانات تیر ۱۴۰۳)؛ نیازی به منبع دومی برای نیمسال دوم نبود |
| 7 | پذیرش داده‌ی خام سطح رزرو فردی (`PersonId`) و افزودن مسیر مدل‌سازی موازی «مدل B» (طبقه‌بندی سطح فرد) در کنار مدل A تجمیعی | 2026-08-12 | (الف) نادیده‌گرفتن داده‌ی سطح فردی و ادامه‌ی صرف با مدل A، (ب) جایگزینی کامل مدل A با مدل B، (ج) ساخت مدل B به‌عنوان مسیر مکمل و موازی مدل A | داده‌ی سطح فردی دقیق‌تر از تجمیعی است (تاریخچه‌ی رفتاری فرد، فیچرهای جمعیتی) و امکان تفسیرپذیری و برش تحلیلی جدیدی (مثلاً cold-start) می‌دهد، اما فرمول‌بندی رگرسیون کوانتایل مستقیم مدل A دفاع‌پذیرتر و بدون نیاز به فرض استقلال در تجمیع است؛ حذف کامل مدل A ریسک از دست‌دادن مسیر اثبات‌شده را داشت، پس هر دو موازی ساخته و در بند ۸-۱۰ WBS با آزمون Diebold-Mariano مقایسه می‌شوند | `doc/project-definition.md` (بند ۳-۱-ب، ۴-۱-الف، ۴-۲) و `doc/WBS-food-demand-forecasting.md` (بند ۱-۶ و بندهای مدل B در فازهای ۰، ۲–۹، ۱۱) به‌روزرسانی شدند؛ داده‌ی خام مرتبط با `dvc add data/raw/per_person_raw_data` نسخه‌بندی و در `doc/data_manifest.md` ثبت شد؛ نیازمند پروتکل ناشناس‌سازی مستقل (بند ۰-۷ WBS) پیش از هر پردازش به‌دلیل قابلیت شناسایی بالقوه‌ی دانشجویان |
| 8 | تثبیت متغیر هدف مدل A روی نرخ $\rho=NoRecv/Res$ با وزن‌دهی $\sqrt{Res}$ در pinball loss، انقباض تجربی برای سلول‌های $Res$ کوچک، و لایه‌ی کالیبراسیون Conformalized Quantile Regression (CQR) | 2026-08-12 | گزینه‌ی B (تعداد مطلق $NoRecv$ با وزن‌دهی)، گزینه‌ی C (مدل دوجمله‌ای/Beta-Binomial)، گزینه‌ی D (هدف مستقیم $Recv$)، به‌علاوه ۵ خانواده‌ی تکمیلی از ادبیات (Fractional Response Regression، GAMLSS/NGBoost، CQR/EnCQR، چارچوب‌های Newsvendor در پژوهش عملیات، Zero-Inflated/Negative Binomial) | نرخ $\rho$ تنها گزینه‌ای است که با ابزار موجود (LightGBM quantile) مستقیم سازگار است، سابقه‌ی عملی موفق در ادبیات نیوزوندور دارد، و هزینه‌ی پیاده‌سازی‌اش با محدودیت زمانی فاز ۱ (۵٪ تلاش کل) و قاعده‌ی انضباطی «قبل از M3 مدل پیچیده آموزش داده نشود» سازگار است؛ چون تبدیل $\rho\leftrightarrow Recv$ یکنواخت است، تفاوت واقعی گزینه‌ها نه در درستی نظری بلکه در رفتار عملی مدل روی داده‌ی محدود است — وزن‌دهی $\sqrt{Res}$ لازم است تا اهمیت مالی سلف‌های پرحجم دست‌کم گرفته نشود؛ گزینه‌ی C (Binomial) با فرض استقلال رزروکنندگان که محتمل نقض شود رد شد اما به‌عنوان رقیب جدی فاز ۷ (در صورت ضعف کالیبراسیون A) نگه داشته شد | مسیر اصلی فاز ۷ برای مدل A مشخص شد؛ Beta-Binomial و GAMLSS به‌عنوان رقیب جدی، Zero-Inflated/Hurdle مشروط به یافته‌ی EDA فاز ۴؛ تحلیل کامل در `doc/decisions/08-target-variable.md`؛ این تصمیم فقط مدل A را می‌پوشاند — متغیر هدف مدل B (طبقه‌بندی $ReserveStatus$) جداگانه در بند ۴-۱-الف سند مسئله تثبیت شده |
| 9 | تثبیت زمان‌بندی دقیق قرارداد خروجی (۱۵:۰۰ روز $d-1$ برای ناهار، ۲۳:۰۰ روز $d-1$ برای شام) و پالایش قاعده‌ی برش اطلاعاتی به تفکیک وعده | 2026-08-12 | (الف) نگه‌داشتن قاعده‌ی ساده‌ی «هر $d'\le d-1$ مجاز» بدون تفکیک وعده، (ب) لحظه‌ی برش یکسان («پایان روز $d-1$») برای هر دو وعده، (ج) لحظه‌ی برش مجزا برای ناهار (۱۵:۰۰) و شام (۲۳:۰۰) با جدول دسترسی مجزا | پاسخ صریح ذی‌نفع (بند ۱-۳ WBS) نشان داد شام روز $d-1$ در ساعت ۱۵ همان روز هنوز سرو نشده، پس گزینه‌ی (الف) یک نشتی واقعی ایجاد می‌کرد (اجازه‌ی استفاده از $Recv/NoRecv/\rho$ شام $d-1$ برای پیش‌بینی ناهار $d$، صرفاً چون تاریخش $\le d-1$ است)؛ گزینه‌ی (ج) تنها گزینه‌ای است که با واقعیت عملیاتی (دو لحظه‌ی تصمیم متفاوت در همان روز) سازگار است | `doc/project-definition.md` بند ۴-۲ با جدول «آخرین وعده‌ی در دسترس» بازنویسی شد؛ `doc/WBS-food-demand-forecasting.md` بند ۱-۳ تکمیل شد؛ فیچرهای lag در فاز ۵ باید آگاه به نوع وعده‌ی هدف باشند (برای هدف ناهار، شام $d-1$ را رد کرده و به ناهار $d-1$ برسند)؛ سؤال باز جدید: آیا نرخ در سطح سلف×وعده $\rho_{d,m,r}$ برآورد و سپس بین دو غذا تجزیه شود، به‌جای برآورد مستقل هر غذا — تصمیم در فاز ۶ |
| 10 | پاسخ‌های پرسش‌نامه‌ی ذی‌نفعان (بند ۱-۴ WBS) — رد پخت مرحله‌ای، رد لغو رزرو، رد تغییر سیاست جریمه، رد قید ظرفیت آشپزخانه | 2026-08-12 | برای هر پرسش، دو حالت ممکن (تأیید/رد) بررسی شد؛ پاسخ‌دهنده کاربر/صاحب پروژه بود (دسترسی مستقیم به معاونت دانشجویی برای این نسخه فراهم نشد) | پاسخ‌ها مستقیماً فضای طراحی فازهای بعدی را محدود و ساده می‌کنند: مسئله تک‌تصمیمی می‌ماند (نه دومرحله‌ای)، نیازی به فیچر/مدل لغو رزرو نیست، منبع محتمل Concept Drift (تغییر سیاست جریمه) منتفی شد، و قید ظرفیت/بسته‌ی گسسته از خروجی مدل حذف شد؛ سرنوشت غذای مازاد را کاربر جداگانه در $C_o$ لحاظ می‌کند و از محدوده‌ی این پروژه خارج ماند | `doc/project-definition.md` بندهای ۵-۲ (فرضیات) و ۵-۳ (ریسک Concept Drift، پایین‌آورده‌شد) به‌روزرسانی شدند؛ `doc/WBS-food-demand-forecasting.md` بند ۱-۴ با پاسخ‌ها تکمیل شد |
| 11 | فرآیند تعیین $\tau$ نهایی (بند ۱-۲ WBS): تثبیت چارچوب سناریو در فاز ۱، محک تجربی پس از EDA، توصیه با اولویت نادر ماندن کمبود، تصویب نهایی توسط مدیریت سلف | 2026-08-12 | (الف) تثبیت یک عدد قطعی $\tau$ همین حالا بدون شواهد تجربی، (ب) صرفاً جدول حساسیت بدون فرآیند تصمیم مشخص (نسخه‌ی قبلی بند ۱-۲)، (ج) فرآیند ۴مرحله‌ای: تثبیت سناریوها اکنون → محک تجربی پس از EDA در سند مجزا → توصیه‌ی مبتنی‌بر «کمبود نادر بماند حتی به قیمت صرفه‌جویی کمتر» → تصویب نهایی توسط مدیریت سلف | گزینه‌ی (الف) رد شد چون در فاز ۱ هنوز هیچ شواهد تجربی‌ای از رفتار واقعی داده روی سناریوهای مختلف نیست؛ گزینه‌ی (ج) تصمیم سیاستی (انتخاب نسبت هزینه‌ی کمبود به مازاد) را به‌درستی نزد مدیریت سلف نگه می‌دارد و آن را از یک فرض دلبخواهی تیم پروژه به یک تصمیم مستندشده و قابل‌دفاع تبدیل می‌کند؛ اولویت‌دادن به نادر ماندن کمبود مستقیماً از عدم‌تقارن $C_u \gg C_o$ که در بند ۲-۲ سند مسئله مطرح شده منتج می‌شود | بند ۱-۲ WBS و بند ۲-۲ سند مسئله با فرآیند ۴مرحله‌ای به‌روزرسانی شدند؛ $\tau=0.10$ همچنان مقدار پیش‌فرض موقت است تا `reports/tau_sensitivity.md` (پس از فاز ۴، پیش از معیار پایان M3 در فاز ۶) تهیه و به تأیید مدیریت سلف برسد؛ این خروجی ورودی مستقیم بند ۶-۴ (Pinball Loss @ τ) و بند ۶-۵ (baselines) است |
| 12 | جمع‌آوری داده‌های جوی و کیفیت هوای تهران از APIهای Open-Meteo و راه‌حل منع نشت داده با فیچرهای وقفه (Lag-1/Lag-2) | 2026-08-12 | (الف) NASA POWER، (ب) کراول سایت کنترل کیفیت هوای تهران (`airnow.tehran.ir`)، (ج) APIهای رسمی و رایگان Historical Weather و Air Quality سرویس Open-Meteo | گزینه (ج) دقیق‌ترین تفکیک مکانی (مختصات $35.705^\circ N, 51.396^\circ E$ پردیس مرکزی دانشگاه تهران)، استخراج ساعتی و تجمیع روزانه استاندارد را بدون نیاز به کلید API فراهم می‌کند؛ برای منع تله نشت داده (بند ۲.۳.۳ WBS)، مقادیر واقعی روز $d$ فقط در EDA فاز ۴ نگه داشته شده و در فیچرسازی مدل‌ها فقط متغیرهای وقفه Lag-1 و Lag-2 وارد می‌شوند | ماژول `src/data/weather.py` پیاده‌سازی و داده‌های پاکسازی‌شده روزانه در `data/external/weather_aqi_tehran.csv` ذخیره شدند؛ هش SHA-256 و شناسنامه در `doc/data_manifest.md` ثبت شد؛ از نشت داده‌های جوی روز هدف در فاز ۵ صریحاً جلوگیری می‌شود |
| 13 | تکمیل بند ۲.۱ WBS (دریافت/دیکشنری/بازرسی داده‌ی اصلی): محدوده‌ی این بند فقط **تأیید و مستندسازی کمّی** ناهنجاری‌های کشف‌شده است، نه حل آن‌ها؛ تست T6 به‌صورت دوسطحی (دقیق با نام سلف / درشت بدون نام سلف) گزارش شد تا علت عدم‌تطابق (نرمال‌سازی متن، نه خطای ساختاری) مشخص شود | 2026-08-12 | (الف) پاکسازی/حل فوری هر ناهنجاری کشف‌شده همین‌جا در فاز ۲ (مثلاً نگاشت نام سلف یا حذف ردیف‌های تکراری)، (ب) عدم‌ذکر ناهنجاری‌هایی که فوراً قابل‌حل نیستند، (ج) مستندسازی کامل و کمّی هر ناهنجاری بدون حل آن، با موکول صریح به بسته‌کاری آینده‌ی WBS | گزینه‌ی (الف) مرز فازها را نقض می‌کرد (بند ۲.۱ = دریافت/تأیید، نه پاکسازی — پاکسازی نام سلف بند ۳.۳ است) و ریسک تصمیم‌های موقت مستندنشده داشت؛ گزینه‌ی (ب) با هدف «دفتر حقایق داده» (فاز ۴) در تضاد بود؛ گزینه‌ی (ج) امکان پیگیری بدون از دست رفتن یافته‌ها را فراهم کرد. تست T6 دوسطحی طراحی شد چون تطابق تک‌سطحی (با نام سلف) هیچ اطلاعاتی درباره‌ی *چرایی* شکست نمی‌داد | `src/data/inspect_raw.py` (اسکریپت بازتولیدپذیر پروفایل + T1-T7 + T6)؛ `doc/data_manifest.md` (بخش «بازرسی یکپارچگی»، ۲٬۸۸۳ ردیف تکراری کشف‌شد)؛ `doc/data_dictionary.md` و `doc/data_dictionary_individual.md` (دیکشنری کامل هر دو منبع)؛ `doc/leakage_audit.md` (بخش مقدماتی — T6 با نرخ تطابق ۰٪ دقیق/۱٫۵۸٪ درشت، علت: عدم‌نرمال‌سازی متن + نگاشت نام سلف حل‌نشده؛ T7 با ۴۵۴٬۳۲۰ نقض سراسری، کلید واقعی = `Reserveid`+فایل/ماه)؛ `doc/data_requests.md` (نامه‌ی رسمی آماده‌ی ارسال + جدول پیگیری). **یافته‌ی حیاتی برای فاز ۳:** مقادیر `ReserveStatus` بین ۷ فایل ماهانه ناپایدار است (۳ تا ۴ مقدار متفاوت بسته به ماه؛ فروردین حتی مقدار پایه‌ی «دریافت نشده» را ندارد) — نگاشت به متغیر هدف باید صریح طراحی شود، نه فرض ساده‌ی باینری. |
| 14 | تکمیل بند ۲.۴ WBS (داده رویدادی و بافتاری): آرشیو خبری دستی با ۹ رویداد کاملاً تأییدشده (منبع + تطبیق روز هفته) به‌جای فهرست جامع اما نامطمئن، و تثبیت صریح نقش این داده به «توضیح پرت در فاز ۴» نه فیچر مدل | 2026-08-13 | (الف) فهرست جامع هر رویداد قابل‌تصورِ بازه (تعطیلی‌های آلودگی، اعتصاب‌ها، رویدادهای دانشگاهی محلی) بدون تأیید مستقل هرکدام، (ب) نوشتن اسکریپت خزنده/scraper برای آرشیو خبری فارسی، (ج) پژوهش هدفمند وب برای رویدادهای با احتمال بالای صحت (ورزش ملی، انتخابات) + تأیید متقاطع هر رویداد نامطمئن با تطبیق روز هفته‌ی محاسبه‌شده (`jdatetime`) در برابر روز هفته‌ی ذکرشده در منبع، و حذف هر رویداد که تأیید نشد | گزینه‌ی (ب) با دستور صریح WBS («یک فایل دستی») و ماهیت یک‌باره‌ی این داده در تضاد بود؛ گزینه‌ی (الف) در عمل پرخطر بود — جست‌وجوی رویدادهای تکرارشونده‌ی سالانه (تعطیلی مدارس تهران به‌دلیل آلودگی هوا) به‌شدت مستعد اختلاط سال است، زیرا موتور جست‌وجو نتایج سال‌های ۱۴۰۳/۱۴۰۴ را با نتایج ۱۴۰۲ درهم می‌آمیزد؛ **این اختلاط عملاً کشف و در ۴ مورد رد شد** (مثلاً یک خبر «تعطیلی ۱۸ و ۱۹ آذر» با فچ مستقیم صفحه ثابت شد متعلق به آذر ۱۴۰۳ است، نه ۱۴۰۲، و کنار گذاشته شد؛ یک خبر اعتصاب اتوبوسرانی که خلاصه‌ی اولیه‌ی جست‌وجو آن را «۱۶ بهمن» گزارش کرده بود، در منبع مستقل (VOA فارسی) «چهارشنبه ۱۶ اسفند» بود که با محاسبه‌ی `jdatetime` روی روز هفته تطبیق داشت، پس تاریخ اصلاح‌شده پذیرفته شد). به همین دلیل قاعده‌ی کار این شد: هر رویداد فقط در صورتی وارد فایل شود که (۱) منبع مستقل و قابل‌استناد (source_url) داشته باشد و (۲) در صورت ذکر روز هفته در منبع، با محاسبه‌ی مستقل `jdatetime` روی تاریخ میلادی تطبیق داده شود؛ رویدادهای بدون تأیید (مثل جشن فارغ‌التحصیلی/همایش‌های خاص دانشگاه تهران در این بازه) حذف و به‌عنوان کار باز مستند شدند، نه حدس زده شدند | خروجی: `data/external/events.csv` (۹ ردیف، هش SHA-256 در `doc/data_manifest.md`) شامل ۶ بازی تیم‌ملی ایران در جام ملت‌های آسیا ۲۰۲۳ قطر (با ساعت شروع تبدیل‌شده به وقت تهران و ستون `affected_meal` برای مشخص‌کردن هم‌پوشانی با ناهار/شام)، انتخابات مجلس/خبرگان ۱۱ اسفند ۱۴۰۲، یک تعطیلی اضطراری آلودگی هوا (۱۳ دی ۱۴۰۲، فقط مدارس K-12 تأیید شد نه لزوماً دانشگاه) و یک اعتصاب رانندگان اتوبوسرانی تهران (۱۶ اسفند ۱۴۰۲)؛ **⚠️ طبق تصریح بند ۲-۴ WBS این فایل هرگز در فاز ۵ به فیچر مدل تبدیل نمی‌شود** (هر رویداد یکتا فقط ۱ مشاهده در ۱۴۲ روز داده دارد ⇒ overfit قطعی) — نقشش صرفاً توضیح داده‌های پرت هنگام «دفتر حقایق داده» فاز ۴ است؛ **کار باز:** رویدادهای خاص دانشگاه تهران (فارغ‌التحصیلی/همایش) و فهرست کامل‌تر تعطیلی‌های آلودگی هوا در بازه یافت نشد — روش پیشنهادی تکمیل در فاز ۳-۱۰: تطبیق روزهای `is_served=False` کشف‌شده در شبکه‌ی کامل با این آرشیو خبری، به‌جای کاوش کور در اخبار |
| 15 | تکمیل بند ۲.۲ WBS (داده تقویمی): منبع تعطیلات پکیج `holidays` (ایران) + تقویم آموزشی استخراج‌شده از PDF موجود، با اعتبارسنجی تجربی کامل در برابر شکاف‌های سرو فایل تجمیعی؛ تعطیلی اضطراری آلودگی هوا آگاهانه `is_holiday` علامت زده نشد | 2026-08-13 | (الف) اسکرپ دستی `time.ir` برای فهرست تعطیلات، (ب) پکیج `python-holidays` (پشتیبانی رسمی Iran)، (ج) هاردکدکردن دستی فهرست تعطیلات از یک منبع واحد بدون کتابخانه؛ برای طبقه‌بندی بلوک نوروز/تعطیلات بین‌ترمی: (الف) فرض تعطیلات نوروز ثابت ۴ روزه‌ی پکیج، (ب) استخراج بازه‌ی واقعی از تقویم آموزشی دانشگاه؛ برای تعطیلی اضطراری: (الف) هر خبر تأییدشده‌ی آلودگی را `is_holiday` بزن، (ب) فقط در صورت هم‌پوشانی با شکاف واقعی سرو بزن | گزینه‌ی (ب) در هر دو مورد اول انتخاب شد: پکیج `holidays` تست شد (۵۵ تعطیلی ۲۰۲۳-۲۰۲۴، منطبق با تاریخ‌های شناخته‌شده) و از اسکرپ شکننده‌ی دستی بی‌نیاز کرد؛ `data/raw/UT_calender_1402_1043.pdf` (که برخلاف توضیح تصمیم #۶ فقط نیمسال اول نیست — **هر دو نیمسال ۱۴۰۲-۱۴۰۳ را دارد**، تا امتحانات تیر ۱۴۰۳) با `pdftotext -layout` کاملاً قابل‌استخراج بود و نشان داد بلوک نوروز دانشگاه (اسفند ۲۶ تا فروردین ۱۰) به‌مراتب طولانی‌تر از ۴ روز رسمی پکیج است. برای تعطیلی اضطراری: تحلیل شکاف تجربی (۴۰ روز غایب از ۱۸۲ روز تقویمی) نشان داد **هیچ‌کدام از تاریخ‌های آلودگی هوای تأییدشده (بند ۲-۴، ردیف ۱۴) با شکاف واقعی سرو هم‌پوشانی ندارند** — یعنی سلف دانشگاه در آن روزها سرویس‌دهی داشته؛ گزینه‌ی (الف) برای تعطیلی اضطراری واقعیت داده را نقض می‌کرد | ماژول `src/data/calendar.py` (هم‌الگو با `weather.py`) و `data/external/calendar_tehran.csv` (۲۴۳ ردیف، ۲۰۲۳-۱۱-۰۱ تا ۲۰۲۴-۰۶-۳۰؛ هش در `doc/data_manifest.md`). **اعتبارسنجی کامل:** نرخ تطابق `day_of_week` مستقل با ستون موجود `DayOfWeek` فایل تجمیعی = ۱٫۰۰؛ از ۴۰ روز غایب فایل تجمیعی، ۳۹ روز دقیقاً با `is_holiday_any` (جمعه + تعطیلات ملی/مذهبی + بلوک نوروز + تعطیلات بین‌ترمی کشف‌شده‌ی بهمن ۱۵-۲۰) توضیح داده شد و ۱ روز باقی‌مانده (فروردین ۱۱، دقیقاً بین بلوک نوروز و دو تعطیلی ملی متوالی) به‌عنوان `is_bridge_day` — صفر شکاف توضیح‌نیافته. **اصلاحات نسبت به فرض‌های قبلی:** توضیح تصمیم #۶ («نیمسال اول») باید «هر دو نیمسال» خوانده شود؛ ادعای بند ۲-۲-۴ WBS («خرداد ۱۴۰۳ ... بازه امتحانات پایان‌ترم») نادقیق است — طبق تقویم استخراج‌شده کلاس‌های نیمسال دوم تا خرداد ۲۴ ادامه دارند و امتحانات پایان‌ترم اصلاً از تیر ۱۴۰۳ شروع می‌شود، یعنی بازه‌ی قفل‌شده‌ی داده (تا خرداد ۱) به امتحانات پایان‌ترم نیمسال دوم نمی‌رسد؛ نوع ششم تعطیلی («تعطیلات بین‌ترمی») به فهرست ۵-تایی بند ۲-۲-۲ WBS اضافه شد. **بازبینی و اصلاح ۲۰۲۶-۰۸-۱۳ (به درخواست کاربر):** با رندر PDF در وضوح ۳۰۰dpi (`pdftoppm`) و مقایسه‌ی مستقل با استخراج یک مدل دوم، ۳ خطای واقعی در تقویم آموزشی نیمسال‌های اول/دوم کشف و اصلاح شد — استخراج اولیه‌ی مبتنی‌بر `pdftotext` (بدون رندر تصویری) برای ردیف‌هایی که چند برچسب رنگی در یک هفته پشت‌سرهم می‌آمدند به‌اشتباه به سلول مجاور نسبت داده می‌شد: (۱) حذف‌واضافه‌ی نیمسال اول واقعاً مهر ۱۵-۱۷ بود نه ۱۷-۱۹؛ (۲) پایان کلاس‌های نیمسال اول واقعاً دی ۲۰ بود نه ۲۷ (هفته‌ی دی ۲۱-۲۷ در واقع «هفته‌ی برگزاری کلاس‌های جبرانی» است، نه ادامه‌ی کلاس عادی)؛ (۳) حذف‌واضافه‌ی نیمسال دوم واقعاً اسفند ۵-۸ بود، نه بهمن ۲۸ تا اسفند ۵ (هفته‌ی کاملاً اشتباه). مقادیر «امتحانات میان‌ترم» هر دو نیمسال، «شروع/پایان کلاس‌ها»ی نیمسال دوم، و «شروع امتحانات پایان‌ترم» هر دو نیمسال با رندر تصویری و با خروجی مدل دوم مستقل تطبیق داده شد و **بدون خطا** بودند. تابع `semester_of` هم اصلاح شد تا بازه‌ی «تعلق به نیمسال» را تا پایان امتحانات (نه فقط پایان کلاس) بگسترد، در حالی‌که `week_of_semester` هم‌چنان فقط در بازه‌ی کلاس عادی تعریف می‌شود. **علاوه بر آلودگی هوا، تعطیلی‌های ناشی از یخبندان/برف هم به‌طور مجزا جست‌وجو و بررسی شد** — تاریخ‌های تأییدشده (دی ۲۳، بهمن ۷-۸، ۱۴، ۲۱ ۱۴۰۲) نیز **هیچ‌کدام با شکاف واقعی سرو هم‌پوشانی ندارند**، دقیقاً همان الگوی آلودگی هوا؛ نتیجه‌گیری اصلی بند تغییر نکرد. اعتبارسنجی نهایی پس از اصلاح دوباره اجرا شد: نرخ تطابق `day_of_week`=۱٫۰۰ و صفر شکاف توضیح‌نیافته، بدون تغییر (چون سه خطای یافته‌شده در فیچرهای غیر-تعطیلاتی/تقویم‌آموزشی بودند، نه در منطق `is_holiday_any`). `data/external/calendar_tehran.csv` بازتولید و هش آن در `doc/data_manifest.md` به‌روزرسانی شد. `events.csv` (بند ۲.۴) هم مستقلاً بازبینی شد — تطابق روز هفته‌ی هر ۹ رویداد با محاسبه‌ی مستقل `jdatetime` صد-در-صد تأیید شد، بدون خطا |
| 16 | نرمال‌سازی متن فارسی (بند ۳.۲) بدون وابستگی سنگین (`hazm`/`parsivar`) + نگاشت فازی نام سلف/خوابگاه (بند ۳.۳) و تجزیه‌ی نام غذا (بند ۳.۴) با `rapidfuzz` | 2026-08-13 | برای نرمال‌سازی: (الف) `hazm`، (ب) `parsivar`، (ج) تابع regex اختصاصی؛ برای نگاشت نام سلف: (الف) پذیرش خودکار خروجی فازی بالای یک آستانه‌ی امتیاز، (ب) بازبینی دستی کامل و اِعمال خودکار — نه ذخیره‌ی «امتیاز پایین = نامعتبر» — روی هر ۷۳ ردیف | گزینه (ج) برای نرمال‌سازی: تنها تبدیل موردنیاز یکسان‌سازی حروف عربی/فارسی، ارقام، نیم‌فاصله و کاراکتر نامرئی بود؛ کتابخانه‌ی NLP کامل برای این کار غیرضروری است. برای نگاشت سلف: بازبینی دستی کامل ۷۳ ردیف (نه صرفاً پذیرش خودکار امتیاز بالا) **دو خطای واقعی با امتیاز بالا (۹۰)** آشکار کرد — `دانشکده-سلف علوم اجتماعی`→`علوم` (باید `علوم اجتماعی`) و `دانشکده-سلف ژئوفیزیک`→`فیزیک` (باید `ژئوفیزیک`) — چون WRatio به رشته‌ی زیرمجموعه امتیاز بالا می‌دهد حتی با تفاوت معنایی؛ **در مقابل** ۹ ردیف با امتیاز پایین (۶۰-۸۵٫۵) پس از بازبینی **صحیح** تشخیص داده شدند (فقط رشته‌ی خام طولانی‌تر با پسوند «سلف مهمان» امتیاز را پایین آورده بود). این نشان داد آستانه‌ی امتیاز به‌تنهایی نه کافی است نه لازم — فقط بازبینی دستی کامل قابل‌اعتماد است، دقیقاً طبق روش تجویزشده‌ی بند ۳.۳ WBS | `src/data/text_normalize.py` (نرمال‌سازی، کاهش غذای یکتای فایل تجمیعی از ۹۱ به ۷۸ پس از یکسان‌سازی رسم‌الخط)؛ `src/data/mapping.py` + `data/external/restaurant_mapping.csv` (۷۳ ردیف: ۶۱ فازی مستقیم، ۹ فازی امتیاز-پایین-ولی-صحیح، ۲ اصلاح دستی، ۱ حذف‌شده `تست اپ`)، `data/external/dorm_mapping.csv` (۷۱ ردیف — بررسی خودهمبستگی فازی نشان داد پس از نرمال‌سازی متن هیچ تکرار واقعی باقی نمانده، نگاشت نهایی همانی است)، `data/external/food_mapping.csv` (۶۸۲ ردیف، تجزیه با جداکننده‌های `+`/`،`/پرانتز، ۶۵۲ مورد با امتیاز≥۹۰ به یکی از ۷۸ غذای کانونیک نگاشته شد، ۳۰ مورد بدون نگاشت اجباری ماندند چون واقعاً معادلی در فایل تجمیعی ندارند — صبحانه، نسخه‌ی سویا، پیتزا، خریدهای بوفه‌ای). **اثر مستقیم روی T6:** نرخ تطابق کلید (d,m,r,f) بین دو فایل از ۰٪/۱٫۵۸٪ فاز ۲ به **۹۸٫۵٪** رسید (بررسی‌شده در بند ۳.۱۲) |
| 17 | قفل snapshot پردازش‌شده‌ی فایل تجمیعی — مدل A (بند ۳.۵ تا ۳.۱۱ WBS، معیار پایان M1) | 2026-08-13 | برای dedup: (الف) نگه‌داشتن هر دو نسخه با فلگ، (ب) حذف کامل طبق بند ۳.۸؛ برای شبکه‌ی کامل (بند ۳.۱۰): (الف) توقف در سطح (تاریخ×سلف×وعده) و کنار گذاشتن بعد غذا به‌خاطر نبود منوی رسمی، (ب) گسترش کامل تا سطح (تاریخ×سلف×وعده×غذا) با تعریف عملیاتی «غذا در منو نبوده» = آن (سلف,وعده) همان روز فعال بوده ولی این غذای خاص سرو نشده | (ب) برای dedup: ۲٬۸۸۳ ردیف کاملاً تکراری (فاز ۲ کشف کرد) طبق قاعده‌ی صریح بند ۳.۸ «تکرار کامل → حذف» بدون ابهام حذف شدند. (ب) برای شبکه‌ی کامل: چون تنها منبع «منوی واقعی هر روز» خودِ داده‌ی رزرو است (منوی رسمی هنوز دریافت نشده، بند ۲.۱.۲ ردیف 🟡)، گسترش کامل ممکن و منطقی بود — غیاب یک غذای خاص در روزی که آن سلف/وعده فعال بوده دقیقاً یعنی «آن روز جزو منو نبود»، بدون ابهام؛ توقف زودهنگام (گزینه الف) اطلاعات لازم فاز ۵ (فیچرهای «چند روز از آخرین سرو این غذا گذشته») را از بین می‌برد | `src/data/clean.py` (`parse_dates`, `drop_exact_duplicates`, `aggregate_over_gender`, `build_full_grid`) + `src/validate.py` (T1-T7 قابل‌استفاده مجدد) + `src/data/make_dataset.py`. نتیجه: `data/processed/dataset_v1.csv` (۷٬۵۹۱ ردیف در سطح (d,m,r,f)، T1-T5 صفر نقض پس از dedup)؛ `data/interim/coverage_grid_v1.csv` (۹۰۸٬۵۴۴ ردیف شبکه‌ی کامل با ستون `absence_reason` ∈ {served, holiday, restaurant_meal_not_offered, restaurant_closed_that_day, food_not_on_menu_that_day}) — **M1 (پایان فاز ۳، داده قفل‌شده) برای مسیر مدل A محقق شد** |
| 18 | فایل‌های خام تکراری/غیرقابل‌استفاده کشف‌شده در فاز ۳: کنار گذاشتن `ReserveDay1402-part2.xlsx` (تکراری بایت‌به‌بایت با part1)، تثبیت حذف `DailySellBahman1402(1).xlsx` (تکراری تأییدشده‌ی فاز ۲)، و کنار گذاشتن کامل پوشه‌ی `dailysell/` از هر دو پایپ‌لاین فاز ۳ | 2026-08-13 | برای part1/part2: (الف) فرض اولیه‌ی WBS/data_manifest («دو نیمه‌ی دی‌ماه») بدون بررسی، (ب) بررسی مستقیم (`DataFrame.equals`) پیش از بارگذاری در پایپ‌لاین؛ برای `dailysell/`: (الف) تلاش برای یکپارچه‌سازی آن با `Reserve*` به‌عنوان منبع تکمیلی، (ب) کنار گذاشتن کامل از snapshot این فاز با مستندسازی دلیل | گزینه (ب) در هر دو مورد: **بررسی مستقیم نشان داد `ReserveDay1402-part1.xlsx` و `ReserveDay1402-part2.xlsx` کاملاً یکسانند** (۴۳۷٬۴۷۰ ردیف، همان بازه‌ی `Reserveid`، همان بازه‌ی تاریخ، `p1.equals(p2) == True`) — نام‌گذاری «part1/part2» گمراه‌کننده بود؛ بارگذاری هر دو دی‌ماه را دوبار می‌شمرد (تأیید غیرمستقیم: حذف part2 نرخ تطابق T6 مجموع را از حدود ۱۲۰٪ به سطح واقع‌بینانه رساند). `dailysell/` فاقد `PersonId`/`Reserveid` است (ستون‌های `studentActive`/`personActive` را دارد که ساختار متفاوتی است) — نمی‌تواند به `person_dim`/`person_reservation_fact` (بند ۳.۱۲) بپیوندد و در هیچ‌کدام از خروجی‌های الزامی بند ۳.۱۱/۳.۱۲ ذکر نشده؛ ادغام آن با منابع دیگر بدون کلید فردی بیش از ارزش تحلیلی‌اش پیچیدگی/ریسک نشت اضافه می‌کرد | `src/data/clean_individual.py` (`EXCLUDED_INDIVIDUAL_FILES = {"ReserveDay1402-part2.xlsx"}`)؛ `doc/data_manifest.md` به‌روزرسانی شد (یادداشت هشدار روی هر دو ردیف part1/part2 و ردیف‌های `dailysell/`)؛ `dailysell/` و `DailySellBahman1402(1).xlsx` به‌عنوان «دریافت‌شده ولی خارج از محدوده‌ی snapshot پردازش‌شده» مستند شدند، نه حذف فیزیکی از `data/raw/` (طبق قاعده‌ی فقط‌خواندنی بودن `data/raw/`) |
| 19 | نگاشت آماری `ReserveStatus`→`dont_receive` (بند ۳.۱۲) + قفل snapshot سطح فردی — مدل B (`person_dim_v1.csv`, `person_reservation_fact_v1.csv`) → **تکمیل کامل M1** | 2026-08-13 | برای نگاشت `ReserveStatus`: (الف) نگاشت لفظی صرف بر پایه‌ی معنای فارسی کلمات («دریافت شده»=دریافت، بقیه=عدم‌دریافت) بدون شاهد آماری، (ب) نگاشت آماری با اتکا به پیوستن رکوردهای فایل فردی به `dataset_v1.csv` (پس از نگاشت‌های بند ۱۶) و مقایسه‌ی Σ Count هر مقدار با Recv/NoRecv واقعی؛ برای کلید یکتای T7: (الف) پذیرش (Reserveid, source_file) طبق فرض قبلی فاز ۲، (ب) بررسی ۷ نقض باقیمانده پیش از قطعی‌کردن کلید | (ب) در هر دو مورد: WBS صریحاً «حدس‌زدن ممنوع» را برای `ReserveStatus` تصریح کرده بود، پس نگاشت صرفاً لفظی (گزینه الف) کافی نبود، هرچند نتیجه‌ی نهایی با آن هم‌راستا درآمد. روی ۷٬۵۳۸ گروه (d,m,r,f) مشترک (نرخ تطابق کلید فردی↔تجمیعی = ۹۸٫۵٪، ناشی از بند ۱۶)، فرضیه‌ی «دریافت‌شده=Recv؛ ارسال‌نشده+دریافت‌نشده+منقضی‌شده=NoRecv» نرخ تطابق دقیق ۹۰٫۸٪ (NoRecv, MAE=۰٫۳۶) و ۸۵٫۵٪ (Recv, MAE=۳٫۴) داد؛ فرضیه‌ی جایگزین (کنار گذاشتن «ارسال نشده» از NoRecv) نرخ تطابق را به ۶۳٫۹٪ افت داد — شواهد آماری قاطعانه رد کرد که «ارسال نشده» رفتاری شبیه دریافت داشته باشد. برای T7: بررسی ۷ نقض (۱۴ ردیف) نشان داد **هیچ‌کدام تکرار/خطای داده نیستند** — هر Reserveid یک تراکنش را روی دو ردیف غذای متفاوت شکسته (غذای اصلی + قلم همراه، `Count=1` روی یکی و `Count=0` روی دیگری، `Price` فقط روی یکی) — پس هر دو ردیف واقعی‌اند و باید نگه داشته شوند؛ افزودن `FoodName` به کلید ترکیبی نقض را بدون حذف هیچ ردیفی به صفر رساند | `src/data/clean_individual.py` (`map_reserve_status`, کلید T7 = `Reserveid`+`source_file`+`FoodName`) + `src/data/make_dataset_individual.py`. نتیجه: `data/processed/person_dim_v1.csv` (۲۶٬۷۷۲ ردیف، یک به ازای هر `PersonId`، آخرین مقدار شناخته‌شده‌ی ویژگی‌های جمعیتی) و `data/processed/person_reservation_fact_v1.csv` (۲٬۱۲۷٬۴۴۹ ردیف، T2/T7 صفر نقض، نرخ کلی `dont_receive`=۸٫۲٪ — سازگار با میانگین $\rho$ مشاهده‌شده در `dataset_v1.csv`)؛ `food_matched`=۹۶٫۸٪ در کل داده (بالاتر از نمونه‌ی ۹۵٫۷٪ چون فایل تکراری part2 دیگر دوبار شمرده نمی‌شود). **M1 (پایان فاز ۳، داده قفل‌شده) اکنون برای هر دو مسیر مدل A و مدل B محقق شد** |
| 20 | بازسازی کامل آرشیو رویدادی (بند ۲.۴): تولید `data/external/events.csv` با اسکریپت بازتولیدپذیر `src/data/events.py` به‌جای فایل دستی ۹ردیفی؛ تفکیک «فرضیه» از «واقعیت» با دو ستون `affected_meal` و `service_status`؛ استخراج دوره‌های آلودگی/برف از داده‌ی خودمان به‌جای آرشیو خبری | 2026-08-13 | (الف) نگه‌داشتن فایل دستی و افزودن چند ردیف خبری جدید، (ب) تلاش برای استخراج کامل تعطیلی‌های آلودگی ۱۴۰۲ از خبرگزاری‌ها، (ج) اسکریپت بازتولیدپذیر با سه دسته منبع (خبری تأییدشده، مشتق از `weather_aqi_tehran.csv`، تقویم قمری/رسمی) به‌همراه برچسب‌گذاری خودکار اثر مشاهده‌شده در داده | گزینه‌ی (الف) رد شد چون فایل قبلی فقط بازه‌ی ۱۳ دی تا ۱۶ اسفند را پوشش می‌داد و **۶ هفته‌ی ابتدایی و ۱۱ هفته‌ی انتهایی داده کاملاً بدون رویداد بود** — از جمله بزرگ‌ترین رویداد ساختاری کل بازه (ماه رمضان) و پرالتهاب‌ترین روزهای پایانی (سقوط بالگرد رئیس‌جمهور). گزینه‌ی (ب) رد شد چون جست‌وجوی خبری برای تعطیلی‌های آلودگی ۱۴۰۲ به‌طور سیستماتیک نتایج سال‌های ۱۴۰۳/۱۴۰۴ برمی‌گرداند؛ نمونه‌ی عینی: مقاله‌ی fararu/808074 که در ظاهر «تعطیلی ۲۱ و ۲۲ آذر» را تأیید می‌کرد، پس از fetch مستقیم تاریخ انتشار ۲۰ آذر **۱۴۰۳** داشت — ثبت تاریخ اشتباه در آرشیوی که قرار است داده‌های پرت را توضیح دهد، بدتر از نبودِ ردیف است. گزینه‌ی (ج) این ریسک را حذف می‌کند: دوره‌های آلودگی (AQI حداکثر ≥۱۵۵ برای ≥۲ روز پیوسته) و برف (≥۱ سانتی‌متر) با آستانه‌ی صریح از داده‌ی Open-Meteo خودمان مشتق می‌شوند، پس تاریخ‌هایشان قطعی و بازتولیدپذیر است. تفکیک `affected_meal` (فرضیه) از `service_status` (مشتق از داده) پس از کشف چند برچسب غلط در نسخه‌ی دستی ضروری شد — قاعده‌ی «جمعه سرو ندارد» برای ۵ جمعه‌ی بازه غلط است (۲۹ دی، ۶ و ۱۳ بهمن در ایام امتحانات؛ ۲۵ اسفند و ۱۷ فروردین در رمضان سرو داشتند) | `src/data/events.py` + `data/external/events.csv`: ۳۹ رویداد (از ۹)، پوشش هر ۶ ماه شمسی بازه. **مهم‌ترین کشف: ماه رمضان (۲۲ اسفند ۱۴۰۲ تا ۲۱ فروردین ۱۴۰۳) سرو ناهار را به‌مدت ۲۹ روز کاملاً متوقف کرده و عید فطر سرو شام را ۲ روز** — این شکاف‌ها ساختاری‌اند نه داده‌ی گمشده، و بند ۵.۲/۵.۳ (lag و rolling) باید صریحاً از عبور پنجره از روی آن‌ها جلوگیری کند؛ افزودن `is_ramadan` به `src/data/calendar.py` طبق بند ۵.۱ WBS باز می‌ماند. ستون `observed_in_data` هر ادعای اثر را کنار $\rho$ واقعی و انحرافش از میانه‌ی همان روزِ هفته می‌گذارد؛ همین ستون نشان می‌دهد اثر شروع رمضان روی شام (z تا +۴٫۹) و سه روز پایانی سوگواری رئیس‌جمهور (z=−۰٫۲→+۰٫۵→+۱٫۲) واقعی است، ولی بیشتر دوره‌های آلودگی اثر معنادار ندارند. ⚠️ سه روز پایانی داده تحت تأثیر سوگواری ملی‌اند و چون انتهای سری‌اند کاملاً داخل مجموعه‌ی آزمون فاز ۶ می‌افتند — باید در طراحی backtest لحاظ شود |
| 21 | **پنج سلف خارج از تهران‌اند و داده‌ی هواشناسی/AQI باید به تفکیک شهر باشد** (اصلاحیه‌ی دامنه‌ای، ورودی ذی‌نفع در شروع فاز ۴): «کشاورزی»→کرج، «فارابی»→قم، «ابوریحان»→پاکدشت، «کاسپین»→رضوانشهر (گیلان)، «فومن»→فومن (گیلان). ستون‌های `city`/`province`/`is_tehran` به هر دو دیتاست افزوده شد و `weather_aqi_by_city.csv` (۶ شهر) جایگزین الحاق سراسری `weather_aqi_tehran.csv` شد | 2026-08-13 | (الف) نادیده‌گرفتن تفاوت شهر و ادامه‌ی الحاق داده‌ی تهران به همه (وضع قبلی)، (ب) حذف کامل پنج پردیس غیرتهرانی از داده، (ج) نگه‌داشتن آن‌ها + ستون شهر + بازگشت به فاز ۲ برای گرفتن هواشناسی/AQI هر شهر از همان API (Open-Meteo) | گزینه‌ی (الف) رد شد چون داده نشان داد خطا **بزرگ و سیستماتیک** است، نه نویز جزئی: میانگین AQI تهران ۱۱۶.۲ در برابر فومن ۴۶.۱ و رضوانشهر ۴۹.۳ (کمتر از نصف)، و بارش فومن ۳.۶ برابر تهران — یعنی کل فرضیه‌های H8/H9 (اثر AQI و دما) برای ~۲۵٪ داده روی متغیر بی‌ربط آزمون می‌شد. گزینه‌ی (ب) رد شد چون این پنج پردیس ۲۵.۲٪ رکوردها (۱۹۱۳ از ۷۵۷۹) و ۲۰.۵٪ کل رزروها (۴۱۴٬۰۶۳ از ۲٬۰۱۸٬۴۴۹) را می‌سازند و حذفشان اتلاف داده‌ی سالم است؛ ضمناً خودِ تفاوت شهر یک یافته است (ρ وزنی‌شان ۳.۱٪–۷.۰٪ در برابر ۸.۷٪ تهران؛ میانگین ساده ۳.۹٪–۷.۳٪ در برابر ۱۱.۰٪). گزینه‌ی (ج) با هزینه‌ی ناچیز (۵ فراخوانی اضافه‌ی API، همان تابع پارامتری موجود) هر دو مشکل را حل می‌کند و مسیر بازگشتی `4 → 2` که WBS صریحاً مجاز کرده را استفاده می‌کند | `data/external/campus_geo.csv` (۳۲ سلف → ۶ شهر با مختصات)، `data/external/weather_aqi_by_city.csv` (۱۴۵۸ ردیف)، `src/data/campus_geo.py`، `process_daily_weather_aqi_by_city()` در `src/data/weather.py`. **هر تحلیل متغیر خارجی (بند ۴.۱۰) و هر فیچر هواشناسی فاز ۵ باید با کلید `(city, date)` الحاق شود، نه فقط `date`.** ⚠️ `is_tehran` هم‌خطی شدیدی با هویت سلف دارد (هر سلف دقیقاً یک شهر دارد) — پس نباید به‌عنوان فیچر مستقل کنار `restaurant_id` وارد مدل شود؛ نقشش تفکیک تحلیلی و درست‌کردن الحاق هواشناسی است |
| 22 | **حذف کامل «دوره حوزه علوم انسانی» (هر دو نسخه) از داده** به‌عنوان داده‌ی پرت | 2026-08-13 | (الف) نگه‌داشتن با پرچم عدم‌قطعیت، (ب) ادغام با نزدیک‌ترین سلف، (ج) حذف کامل از هر دو منبع تجمیعی و فردی | گزینه‌ی (ج) طبق تصمیم صریح ذی‌نفع پروژه انتخاب شد و شواهد داده‌ای هم آن را تأیید می‌کند: این دو ردیف در بند ۴.۱ (یافته‌ی F1.12) پیش‌تر به‌عنوان «نمونه‌ی به‌شدت ناکافی» پرچم شده بودند — ۵ و ۷ رکورد در برابر میانه‌ی ۱۹۷.۵ رکورد سایر سلف‌ها (کمتر از ۴٪)، با میانگین Res≈۱۶ (کوچک‌ترین سلف، ۴۸ برابر کوچک‌تر از بزرگ‌ترین) و ρ تا ۰.۵۰ در نسخه‌ی «(مهر)». با این حجم، نه سری زمانی معناداری دارد، نه می‌تواند از مدل global سهم ببرد، و ρ نویزی‌اش (بند ۴.۸: واریانس ∝ ۱/Res) صرفاً دُم بالای توزیع هدف را آلوده می‌کند. گزینه‌ی (ب) رد شد چون سلف مشابه/هم‌مکانی برای ادغام وجود ندارد | فهرست صریح در `data/external/excluded_restaurants.csv` (پایپ‌لاین hardcode نمی‌کند). اثر: داده‌ی تجمیعی ۷۵۹۱→۷۵۷۹ ردیف (۱۲ حذف، ۰.۱۶٪)، داده‌ی فردی ۲٬۱۲۷٬۴۴۹→۲٬۱۲۷٬۲۴۰ (۲۰۹ حذف، ۰.۰۱٪)، تعداد سلف‌ها ۳۲→۳۰. **همه‌ی نتایج بند ۴.۱ که روی `dataset_v1` اجرا شده بودند با این تغییر باطل و روی `dataset_v2` بازاجرا شدند** |
| 23 | افزودن `is_dorm_resident` (ساکن خوابگاه / غیرساکن) در سطح **فرد** به `person_dim`، جدا از `RestaurantType` موجود در سطح **سلف** | 2026-08-13 | (الف) اکتفا به `RestaurantType` تجمیعی (خوابگاهی/دانشگاهی)، (ب) استخراج پرچم فرد از `GroupName`، (ج) تعریف رفتاری (هرکس دست‌کم یک‌بار در سلف خوابگاهی رزرو کرده = ساکن) | ذی‌نفع پروژه خواست تفکیک خوابگاه/غیرخوابگاه صریحاً در تحلیل بیاید. گزینه‌ی (الف) کافی نیست چون `RestaurantType` می‌گوید *غذا کجا سرو شده*، نه *چه کسی آن را رزرو کرده* — گزارش تحلیل قبلی (`report.pdf`) دقیقاً همین را با `RestaurantType` آزمود و به p=۰.۰۶۱ («تفاوتی وجود ندارد») رسید، در حالی که سؤال واقعی سطح فرد است. گزینه‌ی (ب) انتخاب شد چون `GroupName` سامانه‌ی مبدأ برای ساکنان نام خوابگاه و برای غیرساکنان نام دانشکده/پردیس را ثبت می‌کند، پس یک صفت *اعلامی* و بدون نشتی است. گزینه‌ی (ج) به‌عنوان **بررسی متقاطع** در بند ۴.۱۳ استفاده می‌شود، نه تعریف اصلی، چون تعریف رفتاری از خودِ داده‌ی رزرو مشتق می‌شود و در فیچرسازی ریسک دور-باطل دارد | قاعده: وجود «خوابگاه» در `dorm_canonical` یا پسوند «متاهلین» (واحد مسکونی متأهلین پردیس کشاورزی، با سرو شام و صبحانه در برابر «(وعده ناهار)» روزانه). تابع `derive_dorm_residency()` در `src/data/clean_individual.py`؛ ستون در `person_dim_v2.csv` |
| 24 | **معماری مدل: Global با فیچر سلف/خوشه** — نه per-cluster، نه per-restaurant (تصمیم الزامی بند ۴.۶.۴ WBS) | 2026-08-13 | (الف) یک مدل به‌ازای هر سلف، (ب) یک مدل به‌ازای هر خوشه (۲ خوشه‌ی کشف‌شده)، (ج) یک مدل global با `restaurant_id` و برچسب خوشه به‌عنوان فیچر | گزینه‌ی (الف) با داده‌ی موجود **غیرممکن** است: سری‌های سطح واحد تحلیل $(d,m,r,f)$ میانه‌ی طول **۴ نقطه** دارند و **۱۰۰٪ آن‌ها زیر ۳۰ نقطه‌اند** (یافته‌ی F7.1)؛ حتی در سطح درشت‌تر (سلف×وعده) که ۴۱ سری با میانه‌ی ۱۹۷ نقطه دارد، برازش ۴۱ مدل کوانتایل جداگانه روی ~۲۰۰ نقطه، در دُم توزیع (که تصمیم عملیاتی به آن نیاز دارد) نمونه‌ی کافی ندارد. گزینه‌ی (ب) وسوسه‌انگیز بود چون خوشه‌بندی برخلاف هشدار WBS **پایدار** درآمد (توافق زوجی bootstrap ۰.۹۴۲±۰.۰۵۹) و مرز خوشه‌ها دقیقاً روی «خوابگاهی/غیرتهرانی در برابر دانشکده‌ای تهران» افتاد (F6.2)؛ اما همین که خوشه‌ها با دو **متغیر رسته‌ای موجود** (`RestaurantType` و `city`) تقریباً منطبق‌اند یعنی همان اطلاعات را می‌توان به‌صورت فیچر به مدل global داد، بدون آنکه داده به دو نیم تقسیم و حجم آموزش هر مدل نصف شود. گزینه‌ی (ج) ضمناً تنها راهی است که «یادگیری تقاطعی بین سلف‌های با مقیاس متفاوت» (هدف صریح بند ۴.۱ سند مسئله) را ممکن می‌کند | مدل global روی همه‌ی ۷۵۷۹ رکورد آموزش می‌بیند، با فیچرهای `restaurant_id`، `RestaurantType`، `city`، برچسب `cluster2`، و **چهار فیچر تعاملی صریح** که در بند ۴.۹ معنادار شدند: `dow×restaurant_type` (ΔAIC=۵۶.۲)، `meal×restaurant_type` (۵۲.۷)، `dow×city` (۲۰.۷)، `city×meal` (۱۹.۴). تعامل `meal×food_type` ساخته **نمی‌شود** (ΔAIC=−۱.۲، p=۰.۳۸). ⚠️ ارزیابی فاز ۸ باید خطا را **به تفکیک خوشه** هم گزارش کند تا اگر مدل global به خوشه‌ی پرحجم‌تر اریب شد، دیده شود |
| 25 | **مدیریت ناهم‌واریانسی: وزن‌دهی $w=Res$ + خطای استاندارد مقاوم، و رد Binomial GLM ساده** | 2026-08-13 | (الف) وزن‌دهی نمونه با $w_i=Res_i$، (ب) مدل دوجمله‌ای/Beta-Binomial، (ج) کوچک‌سازی بیزی تجربی روی متغیر هدف | ناهم‌واریانسی قاطعانه تأیید شد (Breusch-Pagan p=۶.۴e−۴۰، White p=۱.۴e−۵۳، Levene p=۵.۷e−۱۴۸؛ نسبت std چارک کوچک به بزرگ ۳.۰۳). اما **کلید تصمیم، شکل بیش‌پراکندگی بود**: نسبت واریانس مشاهده‌شده به واریانس نظری دوجمله‌ای از ۳.۷ برای رزروهای کوچک تا **۱۵.۶** برای رزروهای بزرگ **صعود** می‌کند (F8.2). اگر منبع واریانس فقط نمونه‌گیری دوجمله‌ای بود، این نسبت باید ~۱ و ثابت می‌ماند؛ صعودی بودنش یعنی جزء غالب واریانس، ناهمگنی **واقعی بین رکوردها**ست که با بزرگ‌شدن $Res$ محو نمی‌شود — پس گزینه‌ی (ب) در شکل ساده‌اش (Binomial GLM) **رد** می‌شود چون واریانس را به‌شدت کم‌برآورد می‌کند و فاصله‌های اطمینان/کوانتایل‌های خوش‌بینانه می‌دهد. گزینه‌ی (ج) به‌عنوان **تبدیل هدف** رد شد (متغیر هدف باید همان چیزی بماند که تصمیم عملیاتی به آن نیاز دارد) ولی به‌عنوان **ابزار تحلیلی** مفید است | وزن‌دهی $w=Res$ در تابع زیان Pinball فاز ۷، به‌همراه گزارش خطا به تفکیک دسته‌ی $Res$ در فاز ۸. کوچک‌سازی بیزی تجربی با α=۰.۹۷۶ و β=۱۱.۱۵ (معادل پیشین ~۱۲ رزرو) فقط برای **نمایش و تحلیل اکتشافی** استفاده می‌شود، نه به‌عنوان هدف آموزش — جابه‌جایی‌اش خودتنظیم است (میانگین ۰.۰۶۵۵ برای Res<۲۰ در برابر ۰.۰۰۰۳ برای Res>۸۰۰). Beta-Binomial به‌عنوان یکی از خانواده‌های کاندید فاز ۷ باقی می‌ماند (نه به‌عنوان تنها راه‌حل) |
| 26 | **AQI از فهرست فیچرهای کاندید حذف می‌شود؛ دما در حد نگه‌داشتن مشروط** | 2026-08-13 | (الف) افزودن AQI و دما به فیچرست طبق فرضیه‌های H8/H9، (ب) حذف هر دو، (ج) حذف AQI و نگه‌داشتن مشروط دما | همبستگی خام AQI با نرخ (+۰.۲۷۷، p=۶e−۱۳۴) کاملاً **کاذب** است و سه شاهد مستقل آن را رد می‌کنند: (۱) پس از کنترل سلف×وعده×روزهفته به −۰.۰۱۳ (p=۰.۲۵) می‌رسد؛ (۲) **فقط داخل تهران** — جایی که AQI واقعاً روزبه‌روز تغییر می‌کند — همبستگی خام +۰.۰۰۶ (p=۰.۶۵) و همبستگی جزئی با کنترل دما **−۰.۱۲۰** یعنی علامت معکوس؛ (۳) AQI و دمای کمینه در زمستان تهران به‌شدت هم‌خطی‌اند (ρ=−۰.۴۹۹). سازوکار همبستگی کاذب روشن است: تهران هم‌زمان بالاترین AQI و بالاترین نرخ را دارد، پس AQI صرفاً نماینده‌ی پنهانِ «شهر» بود. **این کشف تنها پس از اصلاحیه‌ی ردیف ۲۱ ممکن شد** — با داده‌ی قبلی (AQI تهران برای همه‌ی سلف‌ها) تفاوت بین‌شهری اصلاً وجود نداشت و پروژه به‌احتمال زیاد یک «اثر آلودگی» جعلی گزارش می‌کرد. برای دما شواهد مرزی است: اثر کنترل‌شده r=−۰.۰۶۱ (معنادار ولی ناچیز) و تحلیل چارک‌بندی داخل تهران **هیچ الگوی یکنوا یا U-شکلی** نشان نداد (F9.3) | `aqi_us_max`, `pm2_5_mean`, `pm10_*` وارد فیچرست فاز ۵ نمی‌شوند. متغیرهای دما فقط اگر در فاز ۷ بهبود معنادار روی مجموعه‌ی اعتبارسنجی نشان دهند نگه داشته می‌شوند، و در آن صورت **یک** نماینده (`temp_min`) به‌دلیل VIF بالای خانواده‌ی دما (temp_mean=۱۸۲.۹). ⚠️ بارش تنها متغیر جوی با شاهد باقی‌مانده است (CCF تأخیر ۰، r=+۰.۲۰۲) ولی مقدار واقعی روز $d$ در لحظه‌ی برش در دسترس نیست — فقط *پیش‌بینی* بارش مجاز است |
| 27 | **کشف و رفع دو نقص داده‌ی سطح فردی در دور ۲ فاز ۴ → نسخه‌ی `person_*_v3`** (مسیر بازگشتی `4 → 3` که WBS پیش‌بینی کرده) | 2026-08-13 | (الف) نادیده‌گرفتن، (ب) اصلاح در محل نسخه‌ی v2، (ج) رفع در پایپ‌لاین + نسخه‌ی جدید v3 با ثبت کامل | **نقص ۱ — تکرار بین‌فایلی:** روز ۱۴۰۳-۰۲-۰۱ (۲۰۲۴-۰۴-۲۰)، که روز مرزی دو ماه است، به‌طور کامل هم در `ReserveFarvardin-1403.xlsx` و هم در `ReserveOrdibehesht-1403.xlsx` آمده بود و ۱۶٬۸۴۳ رزرو دقیقاً دو بار شمرده می‌شد. **چرا دروازه‌ی فاز ۳ نگرفت:** تست T7 یکتایی را روی کلید `(Reserveid, source_file, FoodName)` می‌سنجید و `source_file` **داخل** کلید بود — انتخابی که عمداً برای حفظ موردِ قانونیِ «یک Reserveid روی دو ردیف غذای مجزا» (ردیف ۱۹) انجام شده بود، ولی تکرار بین‌فایلی را نامرئی می‌کرد. کلید درست `(Reserveid, FoodName)` است. **نقص ۲ — وعده‌های خارج از دامنه:** فایل فردی علاوه بر ناهار و شام، `breakfast` (۳۸٬۰۸۶ ردیف) و `sahar` (۲۲٬۹۸۷ ردیف، سحری رمضان) هم دارد که در فایل تجمیعی **اصلاً وجود ندارند** و بخشی از مسئله‌ی «مقدار پخت ناهار/شام» نیستند. هر دو نقص از طریق سؤال Q8 دور ۲ («منشأ اختلاف ۲.۳٪ بین دو منبع چیست؟») کشف شدند — یعنی خودِ آزمون سازگاری بند ۴.۱۳.۴ کارش را کرد. گزینه‌ی (ب) رد شد چون تغییر بی‌صدای محتوای یک artifact نسخه‌دار دقیقاً همان چیزی است که انضباط `data_manifest` برای جلوگیری از آن وجود دارد | `drop_cross_file_duplicates()` و ستون `is_main_meal` در `src/data/clean_individual.py`. اثر: ۲٬۱۲۷٬۲۴۰ → ۲٬۱۱۰٬۳۹۷ ردیف؛ با فیلتر ناهار/شام ۲٬۰۴۹٬۳۲۲ ردیف. **بهبود چشمگیر در آزمون سازگاری دو منبع:** همبستگی Res از ۰.۹۹۰۷ به **۰.۹۹۸۵**، گروه‌های فقط-فردی از ۳۸۸ به **۱۵** (۰.۰۲٪ حجم)، اختلاف کل از +۲.۳۳٪ به +۱.۵۱٪، نرخ کل فردی از ۰.۰۸۲۰ به **۰.۰۸۰۶** (در برابر ۰.۰۸۰۵ تجمیعی). ⚠️ **همچنین برآورد بیش‌پراکندگی را تصحیح کرد: ۲۱.۸ برابر → ۱۴.۴ برابر** — عدد قبلی با روز دوبارشمرده متورم شده بود. `dataset_v2` (تجمیعی) بدون تغییر است چون منبعش فایل دیگری است |
| 28 | **بازنویسی فاز ۵ WBS به نسخه ۲.۱ پیش از اجرا** — حذف ۴ خانواده فیچر رد‌شده، تغییر ۴ تعریف، و افزودن سه بند جدید (۵.۱۷ عامل روز، ۵.۱۸ برهم‌کنش ضرب‌شونده، ۵.۱۹ پل A↔B) + گسترش بند مدل B از ۵ به ۹ زیربند | 2026-08-14 | (الف) اجرای WBS نسخه ۲.۰ همان‌طور که هست و ثبت انحراف‌ها در گزارش، (ب) بازنویسی بند ۵ پیش از اجرا بر اساس شواهد فاز ۴ | نسخه ۲.۰ پیش از دیدن داده نوشته شده بود و فاز ۴ چند پیشنهاد آن را صریحاً رد کرد (`days_since_same_food` مصنوع تقویمی بود، `aqi` همبستگی کاذب، `has_extras`/`Count` ستون منحط) و سه ساختار مهم کشف کرد که اصلاً جایی در آن نداشتند (۸۳٪ واریانس = شوک روزانه، ساختار ضرب‌شونده، تفاوت شام/ناهار در ترکیب). اجرای بی‌تغییر یعنی ساختن آگاهانه‌ی فیچرهایی که می‌دانیم بی‌اثرند و نساختن مهم‌ترین ساختار کشف‌شده. ذی‌نفع پروژه هم صریحاً خواست مدل B انرژی بیشتری بگیرد | بند ۵ WBS از ۱۳۶ به ۲۶۰ خط؛ جدول کامل تغییرات در بند ۵.۰. خروجی‌ها: `src/features/` (۷ ماژول)، `features_A_v1.parquet` (۷٬۵۷۹×۱۰۰)، `person_features_v1.parquet` (۲٬۰۴۹٬۳۲۲×۲۴)، ۶ فیچرست، `doc/feature_registry.md`، `doc/leakage_audit.md` |
| 29 | **قاعده‌ی برش به یک نامساوی واحد فرو کاسته شد** (`meal_seq_obs ≤ meal_seq_target − 2`) و همه‌ی فیچرهای تاریخی از یک ماژول مشترک `src/features/cutoff.py` عبور می‌کنند | 2026-08-14 | (الف) پیاده‌سازی جداگانه‌ی قاعده در هر خانواده‌ی فیچر با شرط `if meal == 'lunch'`، (ب) شماره‌گذاری سراسری وعده‌ها و یک نامساوی واحد | با شماره‌گذاری ناهار روز $t$ → $2t$ و شام روز $t$ → $2t+1$، هر دو حالتِ جدول «آخرین وعده‌ی در دسترس» به یک عدد ثابت (۲) می‌رسند. این یعنی به‌جای شرط‌های پراکنده‌ی if/else در هر فیچر — که WBS آن را «رایج‌ترین منبع باگ نشتی پروژه» خوانده — یک تابع و **یک آزمون خودکار** کل پروژه را پوشش می‌دهد | `src/features/cutoff.py`. آزمون ساختاری روی ۲۶٬۲۰۲ ردیف از ۳۰۰ فرد: صفر ناسازگاری. ⚠️ همین فرمول‌بندی نشان داد تحلیل اکتشافی دور ۳ (که از `groupby().shift()` استفاده می‌کرد) برای شام، ناهار همان روز را هم می‌شمرد؛ اندازه‌ی خوش‌بینی AUC ۰.۷۲۰۱ در برابر ۰.۷۱۷۹ — کوچک، ولی نسخه‌ی درست مبنا شد |
| 30 | **حذف شمارنده‌های تجمعی (`cell_n_prior_days`, `person_n_prior_reservations` خام) از فیچرست‌ها** و جایگزینی با نرخ ایستا در زمان | 2026-08-14 | (الف) نگه‌داشتن شمارنده‌ی خام، (ب) تبدیل اشباع‌شونده $n/(n+k)$، (ج) حذف شمارنده‌ی سلول + تبدیل شمارنده‌ی فرد به نرخ «رزرو در هفته» | ممیزی بند ۵.۱۳ کشف کرد `cell_n_prior_days` به‌تنهایی $R^2$ خارج‌نمونه را از **+۰.۰۸ به −۱.۰۹** می‌برد (میانگین پیش‌بینی ۰.۱۴۴ در برابر واقعی ۰.۰۸۴). سازوکار: این فیچر عملاً یک **شاخص تقویم** است (Spearman ۰.۹۵ با روز تقویم)؛ در تقسیم زمانی، ۴۰٪ ردیف‌های آزمون مقداری بیرون از دامنه‌ی آموزش دارند و مدل درختی که برون‌یابی نمی‌کند همه را در آخرین سطلِ آموزش می‌گذارد — سطلی متناظر با اسفند (پرنرخ‌ترین ماه). گزینه (ب) **آزموده و رد شد**: تبدیل اشباع‌شونده یکنواخت است و مدل درختی نسبت به تبدیل یکنواخت ناوردا، پس نتیجه بیت‌به‌بیت یکسان ماند — درسی که ارزش ثبت دارد چون شهود اولیه خلافش را می‌گفت | `cutoff.usage_rate()`. پس از تصحیح، همه‌ی فیچرست‌ها $R^2$ مثبت دارند (FS_day بهترین: +۰.۱۳۹). ⚠️ **قاعده‌ی عمومی برای فازهای بعد:** هیچ فیچری که Spearman آن با تقویم بالای ~۰.۷ باشد نباید مستقیم وارد مدل درختی شود |
| 31 | **آستانه‌ی فاصله‌ی lag به‌ازای هر lag تعریف شد** ($k$ + ۶ روز)، نه یک آستانه‌ی سراسری | 2026-08-14 | (الف) آستانه‌ی سراسری ۳۵ روز (کافی برای بزرگ‌ترین lag)، (ب) آستانه‌ی مخصوص هر lag | آستانه‌ی سراسری خودش یک منبع نشت مفهومی بود: شکاف ۲۹ روزه‌ی رمضان (F38) کمتر از ۳۵ است، پس `lag_1` از رویش می‌پرید و مقدار «۳۴ روز پیش» را با برچسب «دیروز» تحویل مدل می‌داد. بازرسی ۷۶ ردیف با این وضعیت پیدا کرد (تا فاصله‌ی ۳۴ روز) | `same_meal_lag(gap_buffer_days=6)` و همان قاعده در `build_day_factor`. پوشش `rho_cell_lag1` از ۹۸.۹٪ به مقدار واقع‌بینانه‌تر رسید |
| 32 | **پروتکل اعتبارسنجی: walk-forward ۵ fold به‌عنوان معیار اصلی؛ پنجره‌ی انتهایی فقط «آزمون فشار»** | 2026-08-14 | (الف) تقسیم منفرد ۶۰/۲۰/۲۰ طبق WBS ۲.۰، (ب) walk-forward به‌عنوان معیار اصلی + پنجره‌ی انتهایی قفل‌شده به‌عنوان آزمون ثانویه، (ج) فقط walk-forward بدون پنجره‌ی قفل‌شده | گزینه‌ی (الف) رد شد چون ممیزی فاز ۵ نشان داد ۲۵٪ انتهایی این داده (۱۴۰۳-۰۱-۲۸ تا ۱۴۰۳-۰۳-۰۱) شامل بازگشت پس از شکاف ۲۹ روزه‌ی رمضان و سه روز سوگواری ملی است — یعنی همه‌ی رژیم‌های غیرعادی دقیقاً در مجموعه‌ی آزمون جمع می‌شوند و هیچ نمونه‌ی مشابهی در آموزش نیست. شاهد عینی: با همان تقسیم، $R^2$ خارج‌نمونه‌ی فیچرست‌های غنی‌تر **منفی** شد (FS_lag = −۱.۱۴) در حالی که فیچرست ساده مثبت بود — رتبه‌بندی کاملاً وارونه. مجموعه‌ی آزمون باید نماینده‌ی شرایط استقرار باشد و یک پنجره‌ی ۲۵ روزه‌ی تماماً غیرعادی این شرط را ندارد. گزینه‌ی (ج) رد شد چون آزمون فشار ارزش گزارشی دارد | `src/cv.py` با ۵ fold گسترشی (حداقل ۶۰ روز آموزش). سه تست واحد پاس: عدم هم‌پوشانی تاریخ، ترتیب زمانی، و **استقلال فیچر انبساطی از مرز fold** (که ادعای «purge gap لازم نیست» را می‌آزماید). ⚠️ fold۲ فقط ۱۸۵ ردیف آزمون دارد چون روی شکاف رمضان می‌افتد — در تفسیر نتایج باید لحاظ شود |
| 33 | **بوت‌استرپ بلوکی دوبعدی و گزارش اجباری اندازه‌ی نمونه‌ی مؤثر** | 2026-08-14 | (الف) بوت‌استرپ معمولی (i.i.d.)، (ب) بلوکی روی روز، (ج) بلوکی دوبعدی روی روز **و** سلف | F10 نشان داد ICC(روز)=۰.۲۲۵ و ICC(سلف)=۰.۲۰۵ تقریباً برابرند، پس نادیده‌گرفتن هرکدام CI را به‌طور غیرقابل‌قبولی باریک می‌کند. محاسبه‌ی اندازه‌ی مؤثر عمق مشکل را نشان داد: با ۵۳ رکورد در روز و ICC=۰.۲۲۵، **n مؤثر فقط ۵۹۳ است در برابر ۷٬۵۷۹ خام — ضریب تورم واریانس ۱۲.۸ برابر**. یعنی هر CI که مشاهدات را مستقل فرض کند ۳.۶ برابر باریک‌تر از واقعیت است | `block_bootstrap_2d()` و `effective_sample_size()` در `src/cv.py`. **هر ادعای برتری مدل در فاز ۷ باید با این ابزار سنجیده شود، نه با CI معمولی** |
| 34 | **$\tau = 0.20$ به‌عنوان توصیه‌ی پروژه** (معادل $C_u = 4C_o$) — تعهد بند ۲-۲ سند مسئله | 2026-08-14 | شبکه‌ی هشت‌تایی τ ∈ {۰.۰۲ … ۰.۳۳}، کشیده‌شده تا $C_u = 2C_o$ به‌درخواست ذی‌نفع تا کل دامنه‌ی محتمل کسب‌وکاری پوشش داده شود | ذی‌نفع پروژه نسبت هزینه‌ی کمبود به مازاد را حدود **۴ برابر** برآورد کرد که طبق رابطه‌ی نیوزوندور به $\tau=0.20$ می‌افتد. **تحلیل حاشیه‌ای مستقلاً همین نقطه را تأیید می‌کند:** گام ۰.۱۵→۰.۲۰ نرخ مبادله‌ی ۴.۸× دارد (>۴ ⇒ صرفه دارد) و گام ۰.۲۰→۰.۲۵ نرخ ۳.۶× (<۴ ⇒ صرفه ندارد) — یعنی τ=۰.۲۰ دقیقاً نقطه‌ای است که مبادله از صرفه می‌افتد. دو مسیر مستقل (فرمول و تحلیل حاشیه‌ای) بدون هماهنگی روی یک عدد توافق دارند. گزینه‌ی محافظه‌کارانه‌ی $\tau=0.05$ هم گزارش شد چون سند اولویت را «نادر ماندن کمبود» گذاشته؛ تفاوت: ۱۵.۴ واحد درصد کاهش هدررفت کمتر در برابر ۷.۲ واحد درصد کمبود کمتر | `reports/tau_sensitivity.md` + `report_16_tau_tradeoff.png` (با محور بالای نسبت هزینه). τ=۰.۲۰ ⇒ کاهش هدررفت **۶۰.۱٪** با نرخ کمبود واقعی **۹.۶٪**. ⚠️ **نرخ کمبود مشاهده‌شده به مدیریت اعلام شود، نه خودِ τ** — مدل محافظه‌کارتر از ادعای نظری‌اش است (در τ=۰.۲۰ کمبود ۹.۶٪ است نه ۲۰٪). جدول تحلیل حاشیه‌ای اجازه می‌دهد با هر $C_u/C_o$ دیگری عدد بهینه مستقیماً خوانده شود |
| 35 | **مدل فاز ۷ باید کوانتایل را مستقیم و شرطی هدف بگیرد، نه میانگین + آفست سراسری** | 2026-08-14 | کشف حین اجرای هشت خط پایه: مقایسه‌ی سه روش رسیدن به کوانتایل | سه خط پایه که فقط در جای برآورد آفست کوانتایل فرق دارند: میانگین گروه + آفست **سراسری** (pinball ۰.۰۰۹۴۰) · میانگین گروه + آفست **گروهی** (۰.۰۰۸۱۰) · کوانتایل **مستقیم** گروه (۰.۰۰۷۹۸). فاصله‌ی ۰.۰۰۹۴۰ تا ۰.۰۰۸۱۰ (**۱۴٪**) دقیقاً هزینه‌ی فرض سراسری‌بودن کوانتایل است. دلیل ناهم‌واریانسی تأییدشده است (F06: نسبت std چارک کوچک به بزرگ ۳.۰۳؛ F07: بیش‌پراکندگی صعودی ۳.۷×→۱۵.۶×): آفست سراسری برای سلف پرحجم بیش‌ازحد محافظه‌کار و برای سلف کم‌حجم ناکافی است. شاهد مکمل: B6 بهترین **RMSE** را دارد (۰.۱۰۶۰) ولی در pinball ششم است — بهترین پیش‌بینی‌کننده‌ی میانگین، لزوماً بهترین کوانتایل نیست | **رگرسیون کوانتایل (LightGBM با objective=quantile یا معادلش) مسیر پیش‌فرض فاز ۷ است**، نه رگرسیون میانگین + پس‌پردازش. بهترین خط پایه B3 با pinball ۰.۰۰۷۹۸ (تجمیع‌شده) سقفی است که هر مدل باید با CI عدم‌پوشش صفر بشکند |
| 36 | **تصحیح خطای فرمول نیوزوندور در سند تعریف مسئله (بند ۲-۲)** | 2026-08-14 | (الف) نگه‌داشتن فرمول $\tau^*=C_u/(C_u+C_o)$ و تغییر تعریف τ به کوانتایل تقاضا، (ب) تصحیح فرمول به $\tau^*_\rho = C_o/(C_u+C_o)$ و حفظ تعریف τ به‌عنوان کوانتایل $\rho$ | سند فرمول کلاسیک $C_u/(C_u+C_o)$ را نوشته بود ولی τ را همه‌جا (بند ۴-۱، فرمول $\widehat{CookQty}$) کوانتایل **نرخ عدم‌دریافت $\rho$** تعریف کرده بود. این دو ناسازگارند: با فرمول سند، پیش‌فرض $\tau=0.10$ به معنای $C_u = C_o/9$ می‌شد، یعنی کمبود غذا **ارزان‌تر** از مازاد — عکس کل انگیزه‌ی پروژه. ریشه: فرکتایل کلاسیک در فضای **تقاضا** تعریف می‌شود، ولی $\rho$ متمم تقاضاست ($D = Res(1-\rho)$)، پس $P(D \le Q) = P(\rho \ge \hat\rho_\tau) = 1-\tau$ و در نتیجه $\tau^*_\rho = C_o/(C_u+C_o)$. گزینه‌ی (ب) انتخاب شد چون کل پیاده‌سازی و همه‌ی خروجی‌های عملیاتی بر مبنای کوانتایل $\rho$ ساخته شده‌اند و عددِ پیش‌فرض ۰.۱۰ هم در همان فضا درست بود — فقط فرمولِ کنارش غلط بود | بند ۲-۲ `doc/project-definition.md` با جدول تبدیل ($C_u/C_o$ ← فرکتایل تقاضا ← $\tau_\rho$) تصحیح شد؛ همان توضیح در `reports/tau_sensitivity.md` هم آمد تا خواننده به ابهام نخورد. **تأیید عددی:** شبیه‌سازی مستقیم نیوزوندور با $C_u=4C_o$ روی ۴۰۰ هزار نمونه، کمینه‌ی هزینه را دقیقاً در $\tau_\rho=0.20$ می‌دهد و در همان نقطه $P(\text{تقاضا} \le \text{پخت})=0.80$ — هر دو فرمول یک تصمیم واحد، در دو فضای مختلف |
