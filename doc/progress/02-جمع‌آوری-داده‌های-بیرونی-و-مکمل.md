# TODO — فاز ۲: جمع‌آوری داده‌های بیرونی و مکمل

> پیگیری وضعیت بسته‌های کاری فاز ۲ (`doc/WBS-food-demand-forecasting.md`). *چرا*ی هر تصمیم در `doc/decision_log.md` است؛ این فایل فقط *وضعیت* را نشان می‌دهد.

- [x] 2.1 داده اصلی (سامانه تغذیه) — 2.1.1 (بازرسی یکپارچگی، `data_manifest.md`)، 2.1.3 (`doc/data_dictionary.md`)، 2.1.4 (`doc/data_dictionary_individual.md` + بخش مقدماتی `doc/leakage_audit.md`) کامل؛ 2.1.2 نامه در ۲۰۲۶-۰۸-۱۲ ارسال شد (شاهد: decision_log #7, #13؛ `doc/data_requests.md`). دریافت و ثبت پاسخ نامه در جدول پیگیری همان سند باز می‌ماند (به‌روزرسانی سبک، مسدودکننده‌ی فاز بعد نیست). نگاشت نام سلف/خوابگاه (`restaurant_mapping.csv`, `dorm_mapping.csv`) بخشی از این آیتم نبود — به بند ۳.۳ WBS تعلق دارد.
- [x] 2.2 داده تقویمی و تعطیلات — تقویم پایه (شمسی/میلادی)، تعطیلات ملی/مذهبی (پکیج `holidays`)، بلوک نوروز و تقویم آموزشی هر دو نیمسال (استخراج از `data/raw/UT_calender_1402_1043.pdf`)، فیچرهای مشتق (`is_bridge_day`, `days_to_next_holiday`, `week_of_semester`, ...)؛ اعتبارسنجی کامل در برابر فایل تجمیعی: تطابق ۱٫۰۰ `day_of_week`، صفر شکاف توضیح‌نیافته از ۴۰ روز غایب (شاهد: decision_log #15، `src/data/calendar.py`، `data/external/calendar_tehran.csv`)
- [x] 2.3 داده آب‌وهوا و کیفیت هوا — استخراج داده‌های روزانه دما، بارش، برف، باد، PM2.5, PM10 و AQI از APIهای Open-Meteo برای پردیس مرکزی دانشگاه تهران (شاهد: decision_log #12، `src/data/weather.py`، `data/external/weather_aqi_tehran.csv`)
- [x] 2.4 داده رویدادی و بافتاری — آرشیو خبری دستی با ۹ رویداد تأییدشده (۶ بازی ایران در جام ملت‌های آسیا ۲۰۲۳، انتخابات ۱۱ اسفند ۱۴۰۲، ۱ تعطیلی اضطراری آلودگی هوا، ۱ اعتصاب اتوبوسرانی) برای تحلیل داده‌های پرت فاز ۴ (شاهد: decision_log #14، `data/external/events.csv`). رویدادهای خاص دانشگاه تهران (فارغ‌التحصیلی/همایش) و فهرست کامل‌تر تعطیلی‌های آلودگی هوا کار باز ماند — روش تکمیل در بند ۳-۱۰ (تطبیق روزهای `is_served=False`).

**معیار پایان فاز:** هر ۴ بسته‌کاری تیک بخورد. ✅ هر ۴ بسته‌کاری (۲.۱ تا ۲.۴) تکمیل شدند — فاز ۲ به پایان رسید. تنها آیتم باز باقی‌مانده (غیرمسدودکننده): دریافت و ثبت پاسخ نامه‌ی ۲.۱.۲ در `doc/data_requests.md`.
