# TODO — فاز ۹: تفسیرپذیری

> پیگیری وضعیت بندهای فاز ۹ (`doc/WBS-food-demand-forecasting.md` بند 9.1–9.6).
> کانون: قهرمان رسمی پروژه (`lightgbm_quantile`)، برازش‌شده روی مرز holdout بند ۸.۱
> (همان train/test فاز ۸، برای هم‌راستایی با کل ارزیابی نهایی — نه بازبرازش تازه‌ی
> بی‌ربط). بند ۹.۶ (مدل B/L5) به‌طور جداگانه مقیاس‌بندی می‌شود چون L5 توصیه‌ی نهایی
> پروژه نیست (بند ۸.۱۰) و تفسیرش نیازمند SHAP DeepExplainer + torch/GPU است.
> هر آیتم با تیک‌خوردن باید شاهدش (فایل/commit) کنار خودش ذکر شود — طبق `AGENTS.md`.

## 9.1 اهمیت ویژگی

- [x] اهمیت درختی (gain/split) + Permutation Importance (Δpinball، ۱۰ تکرار) روی Test —
      `src/models/run_phase9_feature_importance.py` → `reports/phase9/9.1_feature_importance.md`
      + `reports/figures/phase9/9.1_feature_importance.png`. **هر دو روش روی `RestaurantName`
      و `dow_x_city` به‌عنوان دو فیچر برتر توافق دارند** (سازگار با F10/F12: هویت سلف و
      برهم‌کنش روز×شهر قوی‌ترین سیگنال‌های شناخته‌شده‌ی پروژه)
- [x] مقایسه‌ی رتبه‌بندی: همبستگی Spearman متوسط (ρ=۰.۳۳۵) بین gain و permutation —
      سازگار با هشدار WBS (gain به کاردینالیتی بالا سوگیری دارد)؛ همپوشانی ۱۰ از ۱۵
      فیچر برتر بین دو روش

## 9.2 SHAP

- [x] Global (beeswarm + bar) — `src/models/run_phase9_shap.py` →
      `reports/phase9/9.2_shap_analysis.md`. رتبه‌بندی SHAP با Permutation بند ۹.۱
      تقریباً یکی است (`RestaurantName` → `dow_x_city` → `log_res`)
- [x] Dependence plots ۵ فیچر برتر — `reports/figures/phase9/9.2_dependence_*.png`
- [x] Local waterfall ۳ مورد (بحرانی‌ترین پیش‌بینی، روز معمولی، یک روز قبل‌تعطیل) —
      `reports/figures/phase9/9.2_waterfall_*.png`
- [x] Interaction values — 🔎 **دو یافته:** (۱) `city`/`Meal`/`RestaurantType` خام اهمیت
      SHAP **دقیقاً صفر** دارند (مدل هرگز رویشان split نمی‌زند) چون `RestaurantName` +
      ستون‌های تعاملی مهندسی‌شده همان اطلاعات را واضح‌تر می‌دهند — تأیید مستقیم اینکه
      تصمیم فاز ۵ (ساخت ستون تعاملی صریح) کار کرد. (۲) ⚠️ **رتبه‌بندی ΔAIC فاز ۴ با
      اهمیت واقعی در مدل نهایی همخوان نیست**: `dow_x_type` بالاترین ΔAIC (۵۶.۲) را داشت
      ولی اهمیتش یک‌دهم `dow_x_city` (ΔAIC=۲۰.۷) است — معناداری آماری در مدل خطی
      سطح-جمعیت، سودمندی پیش‌بینی در مدل درختی را پیش‌بینی نمی‌کند

## 9.3 اثرات نهایی

- [x] PDP + ICE + ALE برای ۵ فیچر عددی برتر (توافق بند ۹.۱/۹.۲) + اثر حاشیه‌ای سطوح
      برای ۲ فیچر دسته‌ای برتر — `src/models/run_phase9_effects.py` →
      `reports/phase9/9.3_effects.md` + ۷ نمودار. ⚠️ **دستی پیاده شد نه با
      `sklearn.inspection`**: مسیر داخلی sklearn هنگام جایگزینی شبکه، dtype ستون‌های
      `category` را می‌شکند (همان باگی که در بند ۹.۱ هم خورده بود)
- [x] ICE/ناهمگنی — نسبت ناهمگنی (ICE p90 ÷ دامنه‌ی PDP) بین ۱.۵۱ تا ۲.۱۱ برای هر ۵
      فیچر: ناهمگنی **متوسط و یکنواخت**، نه رفتارهای متضاد — PDP اینجا نماینده‌ی
      قابل‌قبولی است ولی نه کامل. بیشترین ناهمگنی: `res_vs_history` (۲.۱۱)
- [x] ALE (مرجع اصلی طبق بند ۹.۳ WBS) — **ALE و PDP نزدیک به هم درآمدند** (مثلاً
      `log_res`: دامنه ۰.۰۶۰۴ در برابر ۰.۰۵۷۷، هر دو صعودی) یعنی خوشه‌ی هم‌خط یافته‌ی ۹
      فاز ۷ روی این ۵ فیچر خاص، PDP را به‌طور جدی تحریف نکرده — نتیجه‌ی مثبت و
      قابل‌گزارش، نه چیزی که پنهان بماند

## 9.4 اعتبارسنجی دامنه‌ای

- [ ] هر یافته‌ی بندهای بالا در برابر منطق دامنه سنجیده شود؛ روابط بی‌توجیه به‌عنوان هشدار (نه یافته) گزارش شوند

## 9.5 مدل جانشین قابل‌فهم

- [x] درخت عمق-۳ (۸ برگ) روی **پیش‌بینی‌های قهرمان** + ۸ قاعده‌ی عملیاتی به زبان
      «چند درصد رزرو بپزید» — `src/models/run_phase9_surrogate.py` →
      `reports/phase9/9.5_surrogate.md` + `9.5_rules.csv` + نمودار/متن درخت.
      وفاداری به قهرمان R²=۰.۶۶۲؛ pinball خودِ درخت روی واقعیت ۰.۰۱۲۲۳ (بدتر از قهرمان
      ۰.۰۱۰۰۹ و از B3 ۰.۰۱۰۴۹) — **دو ستون صریحاً تفکیک شدند** تا «وفاداری» با «کیفیت
      پیش‌بینی» قاطی نشود
- [x] پیام قابل انتقال: پرتکرارترین حالت (۶۸۳ از ۱٬۵۲۶ ردیف) = «۹۳٪ رزرو بپزید»؛
      کل دامنه‌ی تصمیم فقط ۱۰ واحد درصد (۹۰٪ تا ۱۰۰٪)
- [x] 🔎 **یافته:** درخت جانشین روی `RestaurantName`/`dow_x_city` (دو فیچر برتر قهرمان
      در بند ۹.۱/۹.۲) اصلاً split نزد — فقط روی فیچرهای نرخ-تاریخی پیوسته. چون آن دو
      دسته‌ای پرسطح‌اند (۳۰/۴۲ سطح) و درخت عمق-۳ با ۷ split نمی‌تواند مفید تقسیمشان کند،
      ولی نرخ تاریخی هر سلول خودش هویت سلف را حمل می‌کند. **پیامد عملیاتی: قاعده‌ی ساده
      لازم نیست نام سلف را بداند** — کافی است نرخ تاریخی همان سلف/روز را بداند

## 9.6 تفسیرپذیری مدل B (L5)

- [ ] بررسی زیرساخت/شدنی‌بودن محلی؛ در صورت نیاز به GPU، محدودیت صریح ثبت شود (مثل بند ۸.۱۰)
