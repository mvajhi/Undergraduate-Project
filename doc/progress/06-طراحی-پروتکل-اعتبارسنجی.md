# TODO — فاز ۶: طراحی پروتکل اعتبارسنجی

> پیگیری وضعیت بسته‌های کاری فاز ۶ (`doc/WBS-food-demand-forecasting.md` نسخه ۲.۱).
> *چرا*ی هر تصمیم در `doc/decision_log.md` (ردیف‌های ۳۲–۳۵) است؛ این فایل فقط *وضعیت* را نشان می‌دهد.
> کد: `src/cv.py` · `src/baselines.py` · `src/run_protocol.py`

## ⚠️ بازنویسی WBS پیش از اجرا

WBS فاز ۶ به نسخه‌ی **۲.۱** بازنویسی شد (بند ۶.۰ شامل جدول کامل تغییرات). هشت تغییر، همه مبتنی بر شواهد فاز ۴ و ۵:

- 🔄 walk-forward معیار **اصلی** شد؛ پنجره‌ی انتهایی به «آزمون فشار» تنزل یافت
- ➕ بوت‌استرپ بلوکی **دوبعدی** (روز و سلف)، نه تک‌بعدی
- ➕ گزارش اجباری **اندازه‌ی نمونه‌ی مؤثر**
- 🔄 B2/B3 با کوچک‌سازی بیزی به‌جای میانه‌ی خام
- ➕ B6 (عامل روز) و B7 (کوانتایل باقیمانده‌ی گروهی) افزوده شدند
- ➕ بند ۶.۱۰ (تحلیل حساسیت τ) و ۶.۱۱ (سیاست پرت در CV)
- ⚠️ هشدار صریح: جمع ساده‌ی احتمالات مدل B عدم‌قطعیت را ۳.۸ برابر کم‌برآورد می‌کند

## 6.1 تقسیم زمانی
- [x] تقسیم بر اساس **تاریخ** نه ردیف (چون ICC روز=۰.۲۲۵) — `src/cv.py`
- [x] طراحی دولایه: walk-forward (اصلی) + پنجره‌ی انتهایی (آزمون فشار) — ردیف ۳۲
- [x] پرچم‌گذاری سه روز پایانی (سوگواری ملی) برای گزارش جداگانه‌ی فاز ۸

## 6.2 اعتبارسنجی متقاطع زمانی
- [x] پنجره‌ی گسترشی (گزینه A)، ۵ fold، حداقل ۶۰ روز آموزش اولیه
- [x] اثبات اینکه **purge gap لازم نیست** — با تست واحد، نه با ادعا

## 6.3 افق پیش‌بینی
- [x] $h=1$ وعده تثبیت شد؛ کاوش $h=2,3$ به فاز ۷ موکول (رزرو ۷۲ ساعت زودتر بسته می‌شود)

## 6.4 مجموعه معیارها
- [x] pinball، نرخ کمبود، ٪ کاهش هدررفت، MAE/RMSE — همه در **هر دو** فضای نرخ و پرس — `src/baselines.py`
- [x] sMAPE صریحاً کنار گذاشته شد (با ρ نزدیک صفر منفجر می‌شود)

## 6.5 خط پایه‌ها
- [x] هر **هشت** خط پایه (B0–B7) اجرا و ثبت شد — `reports/baselines.md`
- [x] تصحیح کوانتایل برای خط پایه‌های میانگین‌محور (مقایسه‌ی منصفانه با B3)

## 6.6 آزمون معناداری
- [x] Diebold-Mariano — `diebold_mariano()`
- [x] بوت‌استرپ بلوکی دوبعدی — `block_bootstrap_2d()` — ردیف ۳۳
- [x] اندازه‌ی نمونه‌ی مؤثر: **۵۹۳ در برابر ۷٬۵۷۹ خام** (تورم واریانس ۱۲.۸×)

## 6.7 تطبیق سلسله‌مراتبی
- [x] چهار رویکرد تعریف شد (شامل مدل دومرحله‌ای فاز ۵)؛ **مقایسه‌شان در فاز ۷** است

## 6.8 پیاده‌سازی و آزمون پروتکل
- [x] `src/cv.py` با سه تست واحد — همه PASS
- [x] تست عدم هم‌پوشانی تاریخ ✅ · ترتیب زمانی ✅ · استقلال فیچر انبساطی از مرز fold ✅

## 6.9 اعتبارسنجی مدل B
- [x] تثبیت شد که از **همان مرزهای تاریخی** استفاده شود
- [x] هشدار حیاتی تجمیع (F08) در WBS ثبت شد
- [ ] اجرای واقعی مدل B ← فاز ۷ (اینجا فقط پروتکلش قفل شد)

## 6.10 تحلیل حساسیت τ
- [x] هر پنج سناریو روی walk-forward — `reports/tau_sensitivity.md` + `report_16_tau_tradeoff.png`
- [x] تحلیل حاشیه‌ای (نرخ مبادله در هر گام)
- [x] **توصیه: τ=۰.۰۵** (کاهش هدررفت ۴۴.۷٪، نرخ کمبود ۲.۴٪) — ردیف ۳۴

## 6.11 سیاست پرت در CV
- [x] تثبیت شد: هیچ پرتی از **آزمون** حذف نمی‌شود؛ در این داده هیچ پرت «خطای ثبت» وجود ندارد (F05) ⇒ عملاً هیچ ردیفی حذف نمی‌شود

---

## 🔑 یافته‌های خودِ فاز ۶

| # | یافته | شاهد |
|---|---|---|
| ۱ | **کوانتایل باید شرطی باشد، نه سراسری** | آفست سراسری ۰.۰۰۹۴۰ · آفست گروهی ۰.۰۰۸۱۰ · کوانتایل مستقیم ۰.۰۰۷۹۸ — فاصله‌ی ۱۴٪ |
| ۲ | **بهترین پیش‌بینی‌کننده‌ی میانگین ≠ بهترین کوانتایل** | B6 بهترین RMSE (۰.۱۰۶۰) ولی ششم در pinball |
| ۳ | **B3 هنوز شکست‌ناپذیر است** | هیچ خط پایه‌ای آن را با p<۰.۰۵ نبرد؛ B7 نزدیک‌ترین (p=۰.۱۴) |
| ۴ | **اندازه‌ی نمونه‌ی مؤثر ۱۲.۸ برابر کوچک‌تر از خام است** | ۵۹۳ در برابر ۷٬۵۷۹ |
| ۵ | **مدل محافظه‌کارتر از ادعایش است** | نرخ کمبود مشاهده‌شده همیشه کمتر از τ؛ شکاف تا −۱۰.۴ واحد درصد در τ=۰.۲۰ |
| ۶ | **تحلیل تجربی، فرمول نیوزوندور را تأیید کرد** | نرخ مبادله ۱۴.۵× در گام ۰.۰۵→۰.۱۰ و ۸.۳× در ۰.۱۰→۰.۱۵ ⇒ بهینه دقیقاً τ≈۰.۱۰ با فرض $C_u=9C_o$ |

### ⚠️ تصحیحی که حین کار انجام شد
یک‌بار ادعا شد B7 بهترین خط پایه است — آن مقایسه نادرست بود (pinball تجمیع‌شده‌ی یکی با میانگین‌گیری‌شده‌ی دیگری مقایسه شده بود). با سنجش یکسان، B3 با هر دو روش تجمیع برنده است. هر دو عدد اکنون در `reports/baselines.md` کنار هم گزارش می‌شوند.

---

## ✅ دروازه‌ی M3

| شرط (بند ۶.۸ WBS) | وضعیت |
|---|---|
| پروتکل CV پیاده‌سازی و **تست** شده | ✅ `src/cv.py` + ۳ تست واحد PASS |
| همه‌ی baseline ها اجرا و در جدول ثبت شده | ✅ **۸ خط پایه** در `reports/baselines.md` |
| تحلیل حساسیت τ (تعهد بند ۲-۲ سند مسئله) | ✅ `reports/tau_sensitivity.md` |
| پروتکل مدل B با همان مرزهای تاریخی | ✅ تثبیت‌شده در WBS ۶.۹ |

**M3 (خط پایه + پروتکل) اعلام می‌شود.** گام بعد: فاز ۷ (مدل‌سازی) — با رگرسیون کوانتایل به‌عنوان مسیر پیش‌فرض (ردیف ۳۵) و سقف B3 = pinball ۰.۰۰۷۹۸.
