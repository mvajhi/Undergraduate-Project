# TODO — فاز ۳: پاکسازی و یکپارچه‌سازی

> پیگیری وضعیت بسته‌های کاری فاز ۳ (`doc/WBS-food-demand-forecasting.md`). *چرا*ی هر تصمیم در `doc/decision_log.md` است؛ این فایل فقط *وضعیت* را نشان می‌دهد.

- [x] 3.1 بازرسی اولیه — عمدتاً در فاز ۲ (`src/data/inspect_raw.py`) انجام شد؛ یافته‌های تکمیلی فاز ۳ (فایل تکراری part1/part2، ۷ نقض T7 واقعی) حین ۳.۶/۳.۱۲ کشف شد (شاهد: decision_log #۱۸، #۱۹)
- [x] 3.2 نرمال‌سازی متن فارسی — `src/data/text_normalize.py` (regex اختصاصی، بدون `hazm`/`parsivar`)؛ کاهش غذای یکتای فایل تجمیعی از ۹۱ به ۷۸ (شاهد: decision_log #۱۶)
- [x] 3.3 یکسان‌سازی نام سلف‌ها (و خوابگاه‌ها) — `src/data/mapping.py` + `data/external/restaurant_mapping.csv` (۷۳ ردیف، بازبینی دستی کامل) + `data/external/dorm_mapping.csv` (۷۱ ردیف) (شاهد: decision_log #۱۶)
- [x] 3.4 تجزیه نام غذا از مخلفات — `src/data/mapping.py::parse_food_name` + `data/external/food_mapping.csv` (۶۸۲ ردیف، ۹۵٫۶٪ نگاشت‌شده) (شاهد: decision_log #۱۶)
- [x] 3.5 مدیریت زمان — `src/data/clean.py::parse_dates` (مدل A)، `src/data/clean_individual.py::parse_individual_dates` (مدل B، فرمت `1402/9/1`→`1402-09-01`) (شاهد: decision_log #۱۷, #۱۹)
- [x] 3.6 بررسی سازگاری حسابداری (T1-T7) — `src/validate.py` (توابع `check_t1`..`check_t7`)؛ T1-T5 صفر نقض روی `dataset_v1.csv`؛ T7 با کلید مرکب `(Reserveid, source_file, FoodName)` صفر نقض روی داده‌ی فردی پس از بررسی دستی ۷ نقض اولیه (شاهد: decision_log #۱۷, #۱۹)
- [x] 3.7 داده گمشده — فایل تجمیعی ۰٪ گمشده (تأییدشده فاز ۲)؛ تفکیک «سرو نشده» از «گم‌شده» در بند ۳.۱۰ (`build_full_grid`) عملیاتی شد، نه ستون گمشدگی جداگانه چون نیازی نبود (شاهد: decision_log #۱۷)
- [x] 3.8 داده تکراری — ۲٬۸۸۳ ردیف کاملاً تکراری فایل تجمیعی حذف شد (`drop_exact_duplicates`)؛ در سطح فردی، `ReserveDay1402-part2.xlsx` (تکراری کامل فایل) و `DailySellBahman1402(1).xlsx` کنار گذاشته شدند (شاهد: decision_log #۱۷, #۱۸)
- [x] 3.9 تجمیع روی جنسیت — `src/data/clean.py::aggregate_over_gender` (فرمول دقیق بند ۳.۹: Res/Recv/NoRecv/gender_ratio/card_ratio) (شاهد: decision_log #۱۷)
- [x] 3.10 ساخت شبکه کامل و تشخیص عدم سرو — `src/data/clean.py::build_full_grid`، ۹۰۸٬۵۴۴ ردیف در `data/interim/coverage_grid_v1.csv`، ستون `is_served`/`absence_reason` با ۴ دسته‌ی WBS (شاهد: decision_log #۱۷)
- [x] 3.11 قفل کردن snapshot پردازش‌شده (`make_dataset.py` → M1، مدل A) — `data/processed/dataset_v1.csv` (۷٬۵۹۱ ردیف، T1-T5 صفر نقض)؛ هش در `doc/data_manifest.md` (شاهد: decision_log #۱۷)
- [x] 3.12 پاکسازی اختصاصی داده سطح فردی (`make_dataset_individual.py`، مدل B) — `data/processed/person_dim_v1.csv` (۲۶٬۷۷۲ ردیف) + `person_reservation_fact_v1.csv` (۲٬۱۲۷٬۴۴۹ ردیف)؛ نگاشت آماری `ReserveStatus`→`dont_receive` (نه حدسی)؛ نرخ تطابق T6 با فایل تجمیعی از ۰٪/۱٫۵۸٪ به ۹۸٫۵٪ رسید (شاهد: decision_log #۱۹)

**معیار پایان فاز:** هر ۱۲ بسته‌کاری تیک بخورد و M1 (داده قفل‌شده) اعلام شود. ✅ **M1 محقق شد** — هر دو snapshot (مدل A: `dataset_v1.csv`؛ مدل B: `person_dim_v1.csv` + `person_reservation_fact_v1.csv`) قفل و در `doc/data_manifest.md` ثبت شدند؛ فاز ۳ به پایان رسید، فاز ۴ (EDA) آغاز می‌شود.
