# فاز ۱۱ — مستندسازی و ارائه

نقشه‌ی فاز: بند ۱۱ `doc/WBS-food-demand-forecasting.md`.

**کد منبع گزارش نهایی در `final_report/` است** (قالب لاتک `tehran-thesis`، حالت `bsc`،
۶ فصل — عنوان‌ها عیناً فهرست مطالب `BScThesis_template_new.docx`: مقدمه و بیان مسئله،
مفاهیم اولیه و پیش‌زمینه، مدل‌سازی، پیاده‌سازی، جمع‌بندی/نتیجه‌گیری/پیشنهادها، مراجع +
پیوست‌ها). قرارداد نگارش برای عامل‌ها: `final_report/AGENTS.md`. محدودیت حجم بدنه:
**۳۰–۴۰ صفحه A4**. ثبت تصمیم پیوستن به این قالب: ردیف ۵۳ `doc/decision_log.md`؛ اصلاح
عنوان/ترتیب فصل‌ها تا عیناً منطبق docx شود (نه اسکلت پیش‌فرض خودِ قالب لاتک): ردیف ۵۴.

نقشه‌ی کامل محتوا: `doc/report-outline.md` (فعال است، نه منسوخ — نقشه‌اش همان چیزی است
که `final_report/tex/chapter*.tex` پیاده کرده‌اند).

فهرست دقیق TODO هر بند **داخل خودِ فایل‌ها** است (کامنت `% TODO` زیر هر
`\section`/`\subsection` در `final_report/tex/chapter*.tex`، هرکدام با ارجاع به سند/ردیف
مشخص در `doc/`) — اینجا تکرار نمی‌شود؛ چک‌لیست پایین فقط سطح فصل/بسته‌کاری است.

---

## ۱۱.۱ گزارش فنی

- [x] پاکسازی قالب: حذف محتوای نمونه/آموزشی خودِ کلاس (راهنمای لاتک در فصل ۱، مثال دامنه‌ی نامرتبط در فصل ۳، سه پیوست آموزشی) و اسکلت واقعی پروژه با TODO در هر فصل — ردیف ۵۳ `decision_log`
- [x] رفع باگ کامپایل قالب (`\latindepartment` + `\\`) — ردیف ۵۳ `decision_log`
- [x] اصلاح عنوان/ترتیب هر ۶ فصل تا عیناً فهرست مطالب docx باشد؛ افزودن فصل ۶ (مراجع) و رفع دو باگ `\thebibliography` (`\refname`→`\bibname`، `\chapter*`→`\chapter` شماره‌دار) — ردیف ۵۴ `decision_log`
- [x] صفحه‌ی عنوان: مشخصات دانشجو، استاد راهنما، داور، عنوان، تاریخ پر شد — `final_report/tex/faTitle.tex`, `enTitle.tex`
- [ ] نگارش محتوای هر فصل طبق TODOهای درون‌فایلی؛ ترتیب پیشنهادی (ریسک‌محور):
  1. ~~فصل ۴ پیاده‌سازی، بند «اجرای پروژه با عامل‌های هوش مصنوعی» (بیشترین محتوای تازه)~~ ✅ **تمام** — هر ۷ بند نوشته شد (شامل کل بند ۴-۵ با پنج زیربندش)، شکل (۴-۱) با TikZ، جدول‌های (۴-۱) تا (۴-۳)؛ `\SepMark{-}` فعال شد تا شماره‌گذاری «شکل (۴-۱)» شود. کامپایل `build.sh` بدون خطای فاتال. شاهد: ردیف ۵۶ `decision_log`، `final_report/tex/chapter4.tex`
     ⚠️ **حجم ۸ ص در برابر بودجه‌ی ‹۵ ص›.** بودجه‌ی `doc/report-outline.md` بدون احتساب `\doublespacing` قالب و بدون احتساب جدول‌ها برآورد شده بود. اگر همین نسبت (~۱.۶×) برای بقیه برقرار باشد، بدنه به‌جای ۳۰–۴۰ ص حدود ۶۰ ص می‌شود — **بازتنظیم بودجه پیش از نگارش فصل ۳ (‹۱۶ ص›) تصمیم باز با کاربر است**
     ↳ پس از بازچینش مرز فصل ۳/۴ (ردیف ۵۷)، بند **۴-۳-۳ خط پایه‌ها** نوشته شد: جدول (۴-۳) با هشت خط پایه‌ی B0–B7 روی τ=۰.۲۰.
        شاهد: ردیف ۵۸ `decision_log`، `reports/baselines_tau020.md`. ⚠️ ضمن نگارش، یک **باگ رندر دوسویه** کشف شد —
        عدد اعشاری با رقم فارسی و نقطه وارونه چاپ می‌شود (۰.۲۲ ← ۲۲.۰)؛ تنها شکل درست `$...$` با رقم اسکی است.
        یک نمونه‌ی رندرشده در بند ۴-۳-۲ هم داشت که رفع شد. **هر عدد اعشاری تازه باید `$...$` نوشته شود.**
     ↳ بند **۴-۳-۴ آزمون معناداری** نوشته شد، عمداً **یک پاراگراف (~¼ صفحه) و بدون مکانیک آزمون‌ها**
        (تصمیم کاربر: «برای آزمون‌های آماری انقدر وارد جزئیات نشو»، «از سطح من بالاتر است»). نسخه‌ی اول ~۱.۵ صفحه
        بود؛ رابطه‌ی $n_{\text{eff}}$، فرمول آماره‌ی DM، رویه‌ی نمونه‌گیری بوت‌استرپ و اعداد سه پنجره حذف شدند.
        در دور دوم بازخورد، **BH-FDR کلاً حذف شد** (کاربر: «اصلا این بخش رو حذف کن») و همراهش بند «وارونه‌شدن
        حکم `knn_quantile` میان دو آزمون» و آمار «۵ از ۲۵ مدل». آنچه ماند: هر آزمون چه ویژگی‌ای دارد و چرا اینجا
        به کار رفت (DM با هر تابع زیانی از جمله Pinball کار می‌کند؛ بوت‌استرپ بلوکی دوبعدی وابستگی روز و سلف را
        هم‌زمان می‌بیند و اندازه‌ی نمونه‌ی مؤثر را کمتر از تعداد خام می‌کند)، به‌علاوه‌ی قاعده‌ی «CI شامل صفر ⇒
        برتری ادعا نمی‌شود».
        ⚠️ در نتیجه، **«تصحیح چندگانه» که `doc/report-outline.md` برای این بند تعهد کرده بود در گزارش نیامد** —
        اگر لازم شد، جایش بند ۲-۵ (ابزارهای آماری) است نه اینجا. عدد «۵ از ۲۵ مدل» هم باید در بند ۴-۴-۱ بیاید.
        منابع: `reports/phase7/model_comparison.md`, `reports/baselines_tau020.md`، ردیف‌های ۳۳/۴۰ `decision_log`.
        به‌علاوه پانویس «Intraclass correlation coefficient» در نخستین کاربردش (بند ۴-۳-۲) اضافه شد.
        کامپایل `build.sh` بدون خطای فاتال، ۴۲ صفحه.
     ↳ بند **۴-۴-۱ نتایج اعتبارسنجی و انتخاب نامزدها** نوشته شد: جدول (۴-۴) با ۱۰ ردیف شاخص از ۲۵ مدل
        (زیان Pinball، Δ نسبت به B3 با CI، ستون «برد معنادار»؛ نشان † برای ۴ مدل GPU که با DM سنجیده شدند،
        نه بوت‌استرپ). سه پرونده‌ی منفی صریحاً گزارش شد: معماری دومرحله‌ای (معنادار بدتر از B3)، خوشه‌بندی
        شهر (۵ از ۱۰، بدون برتری نظام‌مند)، و اینکه مقایسه‌ی مستقیم L5 با L1 انجام نشد. ۱۶ برش اجباری و
        فهرست نامزدهای فاز ۸ هم آمدند.
        ⚠️ **ردیف ۴۷ (باگ سیم‌کشی فیچرست خ۱) — چطور رعایت شد:** ردیف ۴۷ منع کرده بود که یافته‌ی ۱۳/ردیف ۴۰
        («هیچ مدل خطی معنادار نبرد») بدون هشدار نقل شود. نسخه‌ی اول بند دقیقاً همان جمله را داشت؛ حذف شد.
        سپس پاراگرافی توضیحی درباره‌ی خود باگ اضافه شده بود که **به‌درخواست کاربر حذف شد** («این چرت و پرت
        است») — یک باگ داخلی پروژه وسط نتایج، با عددی (۰.۰۱۳۰۶) که با جدول بالای خودش نمی‌خواند.
        **تعهد ردیف ۴۷ همچنان برقرار است، چون بدنه هیچ‌جا ادعا نمی‌کند خانواده‌ی خطی نمی‌تواند B3 را ببرد**؛
        جدول (۴-۴) فقط عدد منجمد فاز ۷ آن مدل را گزارش می‌کند. اگر روزی آن ادعا به بدنه برگشت، هشدار هم
        باید برگردد. جای طبیعی خودِ باگ، بند ۴-۶-۳ یا پیوست است، نه بند نتایج.
        منابع: `reports/phase7/{model_comparison,mandatory_cuts,three_explicit_questions,f01_s2_feature_bug_check}.md`،
        ردیف‌های ۴۰/۴۴/۴۵/۴۶/۴۷/۴۸ `decision_log`. کامپایل `build.sh` بدون خطای فاتال، ۴۲ صفحه.
     ↳ بند **۴-۴-۲ ارزیابی نهایی روی مجموعه‌ی قفل‌شده** نوشته شد (ردیف ۴۹): جدول (۴-۵) با ۳ نامزد + B2/B3.
        صداقت اصلی بند: قهرمان اعتبارسنجی (`lightgbm_quantile`) روی این پنجره برتری معنادار **ندارد**
        (CI شامل صفر) و تنها `bhm_varying_dispersion` دارد. بند صریح می‌گوید چرا این قهرمان رسمی را عوض
        نمی‌کند (آزمون فشار بودنِ از پیش‌ثبت‌شده‌ی پنجره + n_eff=۱۰۵ از ۱٬۵۲۶) و در عین حال نتیجه را
        بی‌معنا نمی‌خواند (مدل بیزی زیر گسست ساختاری پایدارتر، سازگار با طراحی خانواده‌اش)، با دو قید:
        بودجه‌ی تنظیم کوچک و شکست چک‌لیست همگرایی، و ~سه‌ربع ساعت برازش در برابر <۱ ثانیه.
        ⚠️ **خطای واقعی در بند ۴-۳-۲ کشف و رفع شد:** نوشته بود «بیست‌وپنج **درصد** انتهایی داده از
        ۲۸ فروردین ۱۴۰۳ آغاز می‌شود» — هیچ منبعی در مخزن این را نمی‌گفت. `src/cv.py::holdout_split`
        آخرین **۲۵ روزِ** سرویس را می‌گیرد: ۴ اردیبهشت تا ۱ خرداد ۱۴۰۳ (2024-04-23 تا 2024-05-21).
        اصلاح شد.
        پاراگراف «انتخاب سه نامزد» از انتهای ۴-۴-۱ حذف شد (تصمیم کاربر) چون ۴-۴-۲ همان را می‌گوید.
        منبع: `reports/phase8/8.1_final_holdout_evaluation.md`. کامپایل بدون خطای فاتال، ۴۴ صفحه.
     ↳ بند **۴-۴-۳ کالیبراسیون و پوشش** نوشته شد (~نیم صفحه، بدون جدول). دو پاراگراف: (الف) پوشش حاشیه‌ای
        روی پنجره‌ی قفل‌شده در τ=۰.۲۰ برابر ۳۳.۶٪ یعنی +۱۳.۶ واحد بیش‌پوشش، بیشینه +۱۸ در τ=۰.۳۰، وارونه
        در کوانتایل‌های بالا (−۵ در τ=۰.۹۵)؛ ACI شکاف را به +۰.۱ رساند و pinball را هم بهبود داد
        (۰.۰۱۰۰۸→۰.۰۰۹۷۵) یعنی معاوضه‌ای با دقت نبود. (ب) **پوشش حاشیه‌ای در برابر شرطی** — نرخ کمبود کل
        ۲۰.۲٪ تقریباً روی τ می‌نشیند ولی از ۹.۴٪ (چارک رزرو کوچک) تا ۳۲.۷٪ (چارک بزرگ) و پوشش سلف‌ها از
        ۱۷٪ تا ۵۰٪ نوسان دارد ⇒ ریسک کمبود در پرحجم‌ترین سلول‌ها متمرکز است، پس کالیبراسیون گروهی لازم است.
        تعهد بند ۴-۳-۱ («این تحلیل در بند ۴-۴-۳ روی نتایج واقعی انجام می‌شود») ادا شد.
        منابع: `reports/phase8/{8.8_calibration,8.3_error_decomposition}.md`, `8.3_by_res_quartile.csv`,
        `8.3_by_RestaurantName.csv`. **بند ۴-۴ کامل شد.** کامپایل بدون خطای فاتال، ۴۴ صفحه.
     ↳ بند **۴-۵ تحلیل نتایج** (هر پنج زیربند) نوشته شد، هرکدام ~نیم صفحه:
        · **۴-۵-۱ خطا/بدترین موارد:** دُم راست سنگین (چولگی +۴.۲، کشیدگی +۲۸.۵) ولی باقیمانده‌ی روزانه سفید
          (Ljung-Box p=۰.۶۹) ⇒ شوک موردی نه الگوی زمانی جامانده. محور تفسیری: **دو نوع شکست در دو سر طیف
          اندازه** — بدترین pinball در سلول‌های کم‌حجم (هدررفت)، بدترین کمبود در سلف‌های ۵۰۰–۱۸۰۰ رزرو؛ همان
          عدم‌تقارن بند ۴-۴-۳ — که با **شکل (۴-۱)** (باقیمانده در برابر حجم رزرو، از `reports/figures/phase8/`)
          و **جدول (۴-۶)** (۴ نمونه‌ی «بیشترین زیان» + ۴ نمونه‌ی «بیشترین کمبود») نشان داده شد، به‌درخواست
          کاربر که خواست از نمودار/جدول استفاده شود و نثر در عوض چکیده شود.
          ⚠️ **پاراگراف پایداری/حساسیت به‌درخواست کاربر حذف شد** (حساسیت بذر ۰.۰۰۹۸۳–۰.۰۱۱۱۲، آزمون تنش حذف
          فیچرهای هوا p=۰.۱۶، نسبت test/train=۱.۱۲). یعنی تعهد «پایداری/حساسیت؛ Overfit/Underfit» که
          `doc/report-outline.md` برای بند ۴-۵-۱ گذاشته بود **در گزارش پوشش داده نمی‌شود**؛ اگر لازم شد،
          جای طبیعی‌اش یک جمله در همین بند یا پیوست است. منبع اعداد: `reports/phase8/8.6_*`, `8.9_*`.
        · **۴-۵-۲ تفسیرپذیری:** permutation importance مرجع؛ سلف/روزهفته×شهر/حجم غالب‌اند و لگ‌های تاریخی
          ناچیز ⇒ توضیح می‌دهد چرا B3 این‌قدر سرسخت بود. درخت جانشین ۸ برگی = ابزار توضیح، نه جایگزین.
        · **۴-۵-۳ اعتبارسنجی دامنه‌ای** (ردیف ۵۱): تناقض ظاهری log_res با تفکیک کوانتایل/میانگین حل شد
          (میانگین نزولی ۰.۱۲۲→۰.۰۷۹، کوانتایل صعودی ۰.۰۳۱→۰.۰۴۷؛ سهم ρ=۰ از ۱۵.۹٪ به ۰.۲٪).
        · **۴-۵-۴ L1/L2 در برابر L5** (ردیف ۵۰): Δ=۰.۰۰۵۵۱ (~۴۶٪ بدتر)، با قید صریح که دو طرف روی یک
          پنجره ارزیابی نشده‌اند.
        · **۴-۵-۵ کسب‌وکاری:** جدول (۴-۶) سه سناریو. محور تفسیری: **بازده نزولی ریسک** — ۰.۰۲→۰.۱۰ حدود
          ۶ میلیارد اضافه می‌کند ولی ۰.۱۰→۰.۲۰ فقط ~۲ میلیارد با دوبرابرشدن نرخ کمبود. پنج فرض بند ۱۰.۴
          صریح نقل شدند.
        کامپایل بدون خطای فاتال، ۴۶ صفحه. **فصل ۴ فقط بند ۴-۶ و ۴-۷ باقی مانده.**
     ↳ **دو شکل دیگر به فصل ۴ اضافه شد** (قاعده‌ی کاربر: «هرجا می‌شود با نمودار تصویری حرف زد، توضیحات را
        مختصر کن و بگذار نمودارها صحبت کنند»): **شکل (۴-۱)** نمودار قابلیت اطمینان در ۴-۴-۳ و **شکل (۴-۳)**
        اهمیت جای‌گشتی در ۴-۵-۲. نثر هر دو بند در عوض کوتاه شد (شمردن یک‌به‌یک نقطه‌های τ و رتبه‌های اهمیت حذف).
        ⚠️ **نمودارهای `reports/figures/` مستقیم قابل‌استفاده نبودند و بازتولید شدند** (اسکریپت یک‌بارمصرف):
        (الف) حروف `τ` و `Δ` در برچسب‌ها با Vazirmatn مربع خالی چاپ می‌شوند — در متن شکل نباید به کار روند
        (`$\tau$` mathtext هم نه: با patch بیدی `viz_fa` واژه‌ی فارسیِ کنارش وارونه می‌شود؛ راه‌حل، نوشتن
        معادل فارسی مثل «کوانتایل اسمی» است)؛ (ب) عنوان داخل شکل با کپشن تکراری است و واژه‌ی «قهرمان» دارد
        که از بدنه حذف شده؛ (ج) `9.1_feature_importance.png` دوپنلی است و پنل چپش همان اهمیت درختیِ سوگیرداری
        است که متن ردش می‌کند.
        ⚠️ **برای ۴-۵-۵ عمداً شکل اضافه نشد:** هر دو منحنی معاوضه‌ی موجود پایه‌ی متفاوتی با جدول (۴-۷) دارند —
        `8.5_tradeoff_curve.png` روی پنجره‌ی Test (۲۵ روزه) و `report_16_tau_tradeoff.png` از تحلیل حساسیت
        فاز ۶ (نرخ کمبود ۹.۷٪ در τ=۰.۲۰ در برابر ۱۵.۴٪ جدول). گذاشتنشان تناقض بصری می‌ساخت. منحنی هم‌پایه
        نیازمند بازاجرای قهرمان روی τهای بیشتر است، یک بسته‌کار جدا.
        کامپایل بدون خطای فاتال، ۴۸ صفحه؛ هر سه شکل فصل ۴ در فهرست تصاویر آمدند.
     ↳ **شکل (۴-۴): درخت جانشین عمق ۳** به ۴-۵-۲ اضافه شد (درخواست کاربر). `9.5_surrogate_tree.png` موجود
        قابل‌استفاده نبود: عنوان انگلیسی با واژه‌ی champion، و نسبت ۲.۱:۱ که در عرض متن اندازه‌ی قلم گره‌ها را
        به ~۳pt می‌رساند. بازرسم شد با برچسب‌های کوتاه فارسی به‌جای نام خام فیچرها، و برگ‌ها به‌جای ρ̂ مقدار
        عملیاتی («درصدی از رزرو که باید پخته شود») نشان می‌دهند. ساختار از `9.5_surrogate_tree.txt`.
        **اسکریپت‌های تولید شکل‌ها در `tools/report_figures/` ماندگار شدند** (+README با سه دلیل اینکه چرا
        نمودارهای `reports/figures/` مستقیم به گزارش نمی‌روند) تا دفعه‌ی بعد از نو کشف نشود.
        فصل ۴ اکنون ۴ شکل و ۷ جدول دارد. کامپایل بدون خطای فاتال، ۵۰ صفحه.
     ↳ **شکل (۴-۵): منحنی معاوضه‌ی کمبود/هدررفت مدل نهایی** به ۴-۵-۵ اضافه شد (درخواست کاربر: «دقیقاً شکل
        report_16 ولی برای مدل نهایی»). چون هیچ منحنی هم‌پایه‌ای وجود نداشت، **یک بند محاسباتی تازه ساخته شد**:
        `src/models/run_phase10_tau_curve.py` → `reports/phase10/10.7_tau_curve.{csv,md}`. قهرمان روی شبکه‌ی
        ۷ نقطه‌ای τ (۰.۰۲ تا ۰.۳۰) روی همان ۵ fold رسمی بازاجرا شد، با همان `oof_predictions` بند ۱۰.۳/۱۰.۴.
        ✅ **دو چک سازگاری پاس شد:** نرخ کمبود در τ=۰.۰۲/۰.۱۰/۰.۲۰ دقیقاً ۱.۴٪/۷.۹٪/۱۵.۴٪ درآمد (برابر
        `10.3_policy_scenarios.csv`) و کاهش هدررفت در τ=۰.۲۰ برابر ۷۱.۱٪ (برابر `model_comparison.md`).
        نمودار یک یافته‌ی تازه هم نشان داد که به متن اضافه شد: **نرخ کمبود واقعی در سراسر دامنه زیر خط انتظار
        می‌ماند** — همان محافظه‌کاری بند ۴-۴-۳، این‌بار در فضای ریسک عملیاتی.
     ↳ **بندهای ۴-۶ و ۴-۷ نوشته شدند — فصل ۴ کامل شد.** ۴-۶-۱ (مزیت‌های عملی): سه مزیت با شاهد عددی
        بند ۴-۴/۴-۵ — خروجی مستقیم در سطح $(d,m,r,f)$ بدون لایه‌ی تجمیع/تفکیک (بند ۱۰.۱: هیچ قید
        عملیاتی فعال نیست)، τ به‌عنوان اهرم با معنای اقتصادی (τ=۰.۲۰ ⇔ $C_u/C_o$=۴، بند ۱۰.۲) که
        تغییرش فقط یک برازش تازه می‌خواهد، و هر دو کفه‌ی ترازو با هم: ۱۹.۸ [۱۴.۱, ۲۶.۰] میلیارد در
        برابر ۱۵.۴٪ نرخ کمبود که وزن‌شده تنها ۰.۲۸٪ تقاضای برآورده‌نشده است (بدترین روز ۲۰۷ پرس).
        بند با تصریح متواضع‌بودن برتری فنی بسته شد. ۴-۶-۲ (بازتولیدپذیری): قفل‌شده‌ها (هش SHA-256 هر
        فایل، `cv_folds.json` منجمد، بذر واحد، فایل قفل کتابخانه‌ها، `code_ref`+`git_commit` روی هر
        ۲٬۲۱۲ اجرا) در برابر قفل‌نشده‌ها (`.dvc/config` خالی ⇒ بدون مقصد راه دور، نبود هدف `make all`،
        وابستگی اجرای ابری به فضای شخصی). ۴-۷: جمع‌بندی دوپاراگرافی با پل به فصل ۵.
        ❌ **۴-۶-۳ «اجرای پروژه با کمک عامل‌های هوش مصنوعی» به تصمیم کاربر حذف شد** — نوشته نشد و
        `\subsection` هم از `chapter4.tex` برداشته شد؛ ردیف مربوطه در `doc/report-outline.md` خط‌خورده
        علامت خورد. تنها اشاره‌ی باقی‌مانده، یک جمله در ۴-۲-۱ است. کامپایل بدون خطای فاتال، ۵۲ صفحه؛
        بندهای ۴-۶ و ۴-۷ روی صفحات ۲۵ تا ۲۷ گزارش (~۲.۳ ص در برابر برآورد ۰.۷۵ ص نقشه برای ۴-۶).
     ↳ **فصل ۲ آغاز شد: بند ۲-۱ (مقدمه) و کل بخش اول (۲-۲) نوشته شدند.** مقدمه با قاب «تصمیم پیش از
        مسئله‌ی یادگیری» + نقشه‌ی راه پنج بخش. ۲-۲-۱: صورت‌بندی کلاسیک، فرض ساختاری «تک‌دوره‌ای» (نه افق
        کوتاه)، و تفاوت این مسئله با نیوزوندور کلاسیک — $Res$ در لحظه‌ی تصمیم معلوم است پس عدم‌قطعیت در
        نرخ کران‌دار $\rho$ متمرکز می‌شود (توجیه اینکه چرا هدف مدل نرخ است نه تعداد پرس). ۲-۲-۲: $C_o$
        ≈۱۲۰ هزار تومان/پرس در برابر $C_u$ی که پارامتر سیاستی است؛ محور تفسیری = **معیار متقارن خنثی
        نیست، در سکوت $C_u=C_o$ ادعا می‌کند**. ۲-۲-۳: رابطه (۱) $\tau^*_\rho = C_o/(C_u+C_o)$ +
        معکوسش $(1-\tau)/\tau$ به‌عنوان صورت‌بندی خواندنی برای تصمیم‌گیرنده، سه نسبت ۲×/۴×/۹×، تفسیر
        «$\tau$ = نرخ کمبود مورد انتظار در حالت کالیبره»، و تصحیح ردیف ۳۶ **به‌عنوان دستاورد** (چرا خطا
        پنهان می‌ماند: هر دو فرمول در فضای خودشان درست‌اند و هر دو عددی در $[0,1]$ می‌دهند) + تأیید عددی
        ۴۰۰ هزار نمونه. سه مرجع تازه به `MyReferences.bib`: Arrow-Harris-Marschak 1951 (مبدأ کلاسیک،
        خارج از فهرست `Literature_Review.md`)، Ban & Rudin 2019، Huber et al. 2019. فرمول فرکتایل
        کلاسیک از inline به display منتقل شد چون در RTL بین دو خط شکسته و ناخوانا می‌شد. لینتر فارسی:
        «می‌گردد» رفع شد. کامپایل `build.sh` بدون خطای فاتال، ۵۴ صفحه؛ بخش اول روی صفحات ۴ تا ۶ فصل.
        شاهد: `final_report/tex/chapter2.tex`
     ↳ ⚠️ **بازنویسی نثر بند ۲-۱ و ۲-۲ پس از بازخورد کاربر** (چهار نقل‌قول مستقیم از PDF). سه الگو رد شد:
        وارونه‌سازی بلاغی («پرسشی که این پژوهش… پیش از آنکه X باشد، Y است»، «نکته‌ی رهایی‌بخش این است که»،
        «صرفاً یک ظرافت نمادین نیست»)، استعاره‌ی انتزاعی به‌جای واقعیت ملموس («تقاضا قفل شود» ⟵ «روز پیش از
        سرو تعیین شود، در حالی که تعداد دریافت‌کنندگان تا پایان وعده معلوم نمی‌شود»؛ «قلمرو»، «دستگاه
        مختصات»، «حلقه‌های زنجیره»)، و جمله‌ای که فقط مرز رشته‌ها را اعلام می‌کند («همین ادغام مسئله را از
        پژوهش در عملیات به قلمرو یادگیری ماشین منتقل می‌کند» — حذف شد و جایش یک جمله‌ی وصل به داده‌ی پروژه
        نشست: چولگی + تراکم در صفر ⇒ چرا رویکرد بدون فرض توزیعی اینجا مزیت دارد). واژه‌های ناآشنا هم عوض
        شدند: «خط پژوهشی»⟵«پژوهش‌های دو دهه‌ی اخیر»، «استدلال حاشیه‌ای»⟵«منطقش ساده است»، «زیر
        عدم‌قطعیت»⟵«در شرایط عدم‌قطعیت»، «بده‌بستان»⟵«معاوضه» (هم‌راستا با فصل ۴). قاعده در حافظه‌ی
        دائمی ثبت شد.
     ↳ ⚠️ **پاراگراف اول بند ۲-۱ دوباره بازنویسی شد** — کاربر گفت شروع فصل با «مقدار پخت هر وعده باید روز
        پیش از سرو تعیین شود…» بد است: مقدمه‌ی فصل نباید مستقیم وسط جزئیات عملیاتی مسئله بیفتد و آنچه فصل ۱
        گفته را تکرار کند. الگوی درست همان چیزی است که مقدمه‌ی فصل ۴ دارد: **اول وصل به فصل قبل، بعد اینکه
        این فصل چه می‌کند، بعد نقشه‌ی بندها.** نسخه‌ی نهایی با «در فصل پیش… تشریح شد» شروع می‌شود و سه
        محدودیت مسئله (تصمیم پیش از مشاهده‌ی تقاضا، نابرابری هزینه‌ی دو جهت خطا، ترتیب زمانی داده) را
        به‌عنوان قاب فصل معرفی می‌کند. تکرار «اعتبارسنجی متعارف اعتبار ندارد» در نقشه‌ی راه هم رفع شد.
     ↳ ⚠️ **دو پاراگراف پایانی بند ۲-۲-۳ به تصمیم کاربر حذف شدند**: روایت تاریخی تصحیح (فرکتایل کلاسیک
        کنار τ فضای نرخ، پیامد $C_u=C_o/9$، چرا خطا پنهان می‌ماند) و تأیید عددی شبیه‌سازی ۴۰۰ هزار نمونه.
        یعنی خواسته‌ی «ردیف ۳۶ را به‌عنوان دستاورد بنویسید» در `doc/report-outline.md` **در گزارش پوشش
        داده نمی‌شود**؛ خودِ تصحیح (گذار از فضای تقاضا به فضای نرخ و رابطه (۱)) در پاراگراف دوم همان بند
        باقی است، پس عنوان بند («کوانتایل بهینه و تصحیح فرمول») همچنان درست است. بند از ~۱.۳ ص به ~۰.۶ ص
        رسید. برچسب `eq:tau-star` دیگر ارجاعی ندارد ولی بی‌ضرر است.
     ↳ **بخش سوم فصل ۲ (بند ۲-۴، اعتبارسنجی و نشت اطلاعاتی) نوشته شد** — پس از بخش دوم (۲-۳) که
        پیش‌تر و بدون ثبت در همین فایل نوشته شده بود. **عنوان ۲-۴-۱ به درخواست کاربر عوض شد:**
        «چرا K-fold تصادفی معتبر نیست؛ اعتبارسنجی rolling-origin» ⟵ «اعتبارسنجی متقاطع در داده‌ی
        وابسته به زمان» (لحن پرسشی/محاوره‌ای مناسب تیتر پایان‌نامه نبود)؛ ردیف متناظر در
        `doc/report-outline.md` هم به‌روز شد. محتوای ۲-۴-۱: دو نقض فرض جای‌گشت‌پذیری (آموزش از آینده؛
        شباهت مشاهده‌های مجاور با شاهد Ljung-Box=۲۷۳.۱ و ACF شام lag1=۰.۷۵۸ از F37/F33) + اثر
        **پنهان‌کننده**ی تقسیم تصادفی (رانش تقویمی؛ شاهد ردیف ۳۰: $R^2$ از +۰.۰۸ به −۱.۰۹) + تعریف
        مبدأ غلتان و دو صورت گسترشی/لغزان. ۲-۴-۲: سه مسیر نشت (هدف، زمانی، کدگذاری هدف) با شاهد کمّی
        نشت زمانی از `leakage_audit.md` (AUC ۰.۷۲۰۱ در برابر ۰.۷۱۷۹) و محور تفسیری **کوچکی اثر معیار
        پذیرش نقض تعریفی نیست**. تفکیک با فصل‌های دیگر رعایت شد: ICC و پیکربندی ۵ fold و آزمون فشار در
        ۴-۳-۲ ماند، ممیزی در ۳-۳-۴ و صورت‌بندی مرز در ۳-۲-۲. چهار پانویس تکراری
        (Walk-forward/Rolling-origin/Expanding window) از ۴-۳-۲ برداشته شد چون اکنون نخستین کاربردشان
        در فصل ۲ است. کامپایل بدون خطای فاتال، ۶۱ صفحه؛ بند ۲-۴ روی صفحات ۱۰ تا ۱۲ فصل (~۱.۲ ص در
        برابر برآورد ۱ ص نقشه). شاهد: `final_report/tex/chapter2.tex`
     ↳ ⚠️ **۲-۴-۲ به درخواست کاربر ~۴۵٪ کوتاه شد** (از ۵ پاراگراف به ۳، و بند ۲-۴ به ~۱ ص یعنی
        بودجه‌ی نقشه رسید). حذف‌شده‌ها: توضیح اینکه ستون‌های خروجی کنار متغیر هدف در همان جدول‌اند،
        و پرانتز «کوچک‌سازی برای رسته‌های کم‌مشاهده = مسئله‌ی نویز نه نشت». ترتیب سه صورت هم عوض شد
        (هدف ← کدگذاری هدف ← زمانی) تا صورت زمانی، که مفصل‌ترین است، مستقیم به جمله‌ی پایانیِ
        «لحظه‌ی برش» وصل شود.
     ↳ **بخش چهارم فصل ۲ (بند ۲-۵، ابزارهای آماری و استنباطی) نوشته شد.** ⚠️ **نسخه‌ی اول جدولی
        بود و کاربر ردش کرد** («هم سخته هم غیر قابل فهم و هم ساختار جدولی خیلی برای این موضوع
        مناسب نیست») — جدول (۲-۱) با دوازده ردیف و ستون‌های «چه می‌سنجد / بر چه فرضی استوار است /
        چرا لازم بود» کامل حذف و با **نثر گروه‌بندی‌شده** جایگزین شد. الگوی نسخه‌ی دوم: هر آزمون با
        **سؤالی که به آن جواب می‌دهد** معرفی می‌شود («X می‌پرسد آیا …»)، نه با تعریف رسمی و نه با
        ذکر فرض‌هایش؛ و ابزارها در پنج گروه کاربردی آمده‌اند نه در فهرست: (۱) رفتار سری در طول زمان
        (ADF/KPSS، Ljung-Box، ARCH-LM) · (۲) مقایسه‌ی گروه‌ها (Kruskal-Wallis، Mann-Whitney،
        Wilcoxon، Levene، آزمون نسبت واریانس) · (۳) رابطه‌ی دو متغیر و اندازه‌ی اثر (Spearman،
        Kendall، Mann-Kendall، Cliff's δ) · (۴) مقایسه‌ی مدل‌ها (Diebold-Mariano، بوت‌استرپ بلوکی
        دوبعدی، n_eff) · (۵) تصحیح چندگانه (Benjamini-Hochberg). پاراگراف اول توضیح می‌دهد چرا
        مسیر ناپارامتری انتخاب شد (نرمال‌نبودن + وابستگی رکوردها). **بدون عدد** — تعریف اینجاست و
        نتیجه در بند مصرف‌کننده (۲-۳-۵، ۲-۴-۱، ۳-۳-۳، ۴-۳-۴). با این بند **تعهد معلقِ «تصحیح
        چندگانه»** که در بازخورد دور دوم از بند ۴-۳-۴ حذف شده بود (بالای همین فایل) در جای درستش
        ادا شد. سه مرجع تازه به `MyReferences.bib`: Diebold-Mariano 1995، Benjamini-Hochberg 1995،
        Cliff 1993. دو پانویس تکراری (Two-dimensional block bootstrap / Effective sample size) از
        ۴-۳-۴ برداشته شد چون نخستین کاربردشان اکنون در فصل ۲ است. حجم: ~۱.۵ ص (صفحات ۱۲ تا ۱۴
        بدنه)، برابر برآورد نقشه. کامپایل `build.sh` بدون خطای فاتال.
        شاهد: `final_report/tex/chapter2.tex`، `final_report/tex/MyReferences.bib`
     ↳ ⚠️ **بند ۴-۳-۲ ناقص بود و کامل شد** (بازخورد کاربر روی جمله‌ی پایانی ۲-۴-۱ که «دلیلش در بند
        ۴-۳-۲ آمده است»): آن بند فقط تقسیم بر اساس تاریخ و تنزل پنجره‌ی انتهایی را توجیه می‌کرد و
        **انتخاب پنجره‌ی گسترشی در برابر لغزان اصلاً در گزارش نبود** (فقط در WBS ۶.۲). پاراگراف تازه:
        ۱۴۲ روز سرو، رشد آموزش از ۶۰ به ۱۲۴ روز در پنج fold (محاسبه‌شده از `cv_folds.json`)، پس پنجره‌ی
        لغزان با طول ثابت در fold آخر بیش از نیمی از تاریخچه را کنار می‌گذاشت؛ و اینکه دورریختن داده
        تنها با تغییر رژیم می‌ارزد، که در ارزیابی نهایی نشانه‌ای از آن دیده نشد (ارجاع به ۴-۵-۱،
        باقیمانده‌های سفید p=۰.۶۹). کامپایل بدون خطای فاتال، ۶۱ صفحه.
     ↳ **بخش پنجم فصل ۲ (بند ۲-۶، مروری بر پیشینه‌ی تحقیق) نوشته شد** — پنج پاراگراف نثر +
        **جدول پیشینه** (`tab:lit`) با پانزده منبع، گروه‌بندی‌شده در چهار محور با `\multicolumn` («منبع / روش و
        داده / یافته‌ی کلیدی»): نظری روزنامه‌فروش داده‌محور (Ban & Rudin، Huber، Buttler، Risks) ·
        عدم‌حضور در صنایع دارای رزرو (Amberger & Schreyer، Lawrence، Chin، Popp، Carreras-García) ·
        کترینگ نهادی (Rodrigues، OPUS، Malefors) · جعبه‌ابزار فنی (Grinsztajn، Makridakis، Lainder
        & Wolfinger). **ساختار بر پایه‌ی سنتز نوشته شد نه فهرست‌کردن منابع** — هر محور با یک ادعای
        تفسیری وصل‌شده به نتیجه‌ی واقعی همین پروژه بسته می‌شود: هشدار کالیبراسیونِ جدا از دقت نقطه‌ای
        ⟶ پوشش یک‌سویه‌ی ۴-۴-۲؛ نرخ وزنی ۸.۰۵٪ در بازه‌ی هواپیمایی ولی **شکست انتقال روشِ سطح-فرد**
        (۴۶٪ بدتر، ۴-۵-۴) برخلاف Lawrence/Carreras-García؛ و **عدم‌تکرار یافته‌ی OPUS** درباره‌ی
        صدرنشینی وقفه‌های کوتاه‌مدت (اینجا هویت سلف/روزهفته×شهر/حجم رزرو صدرند، ۴-۵-۲). بند پایانی
        خلأ را در سه سطح می‌گوید: نبود پیشینه‌ی ایرانی، تفاوت معیار (متقارن روی تعداد در برابر
        نامتقارن روی نرخ)، و **تفاوت مبنای مقایسه** — برتری ~۹٪ این پروژه بر یک جدول جست‌وجوی
        تنظیم‌شده با کاهش ۳ تا ۱۶٪ آن پژوهش‌ها بر میانگین تاریخی هم‌جنس نیست.
        دوازده مرجع تازه به `MyReferences.bib`. ⚠️ **دو ورودی مشکوک عمداً ناقص ثبت شدند** با کامنت
        هشدار بالای هرکدام: `Risks26coherent` (بدون نویسنده در `Literature_Review.md`، و جلد ۱۹ با
        سال ۲۰۲۶ نشریه‌ی Risks جور درنمی‌آید ⟶ جلد/شماره/صفحه وارد نشد) و `Chin23noshow` (فهرست
        نویسندگان ناقص) — طبق قاعده‌ی «حدس‌زدن ارجاع از خالی‌گذاشتنش بدتر است». ⚠️ **اعشار در بدنه
        باید در حالت ریاضی بیاید** (`$8.05$` نه `۸.۰۵`): این نخستین بند بود که عدد اعشاری وارد متن
        چاپ‌شده کرد و xepersian خودش رقم‌ها را فارسی و جداکننده را «٫» می‌کند؛ همان قرارداد `$7.5$
        درصد` فصل ۴. عدد صحیح در نثر همچنان فارسی می‌ماند (۱۴۲ روز). حجم: ~۱.۲ ص نثر + ~۰.۸ ص جدول
        (صفحات ۱۵ تا ۱۷ بدنه) در برابر برآورد ۱.۲۵ ص نقشه. ⚠️ **شماره‌ی این جدول (۲-۱) شد نه
        (۲-۲)**، چون بازنویسی بعدی بند ۲-۵ جدولش را حذف کرد؛ متن با `\ref{tab:lit}` نوشته شده
        پس خودبه‌خود درست ماند، ولی `report-outline.md` اصلاح شد. کامپایل `build.sh` بدون خطای فاتال،
        ۶۶ صفحه. شاهد: `final_report/tex/chapter2.tex`، `final_report/tex/MyReferences.bib`
     ↳ ⚠️ **بند ۲-۲-۱ به درخواست کاربر بسط یافت** (بازخورد پس از نگارش ۲-۶: «مسئله‌ی
        روزنامه‌فروش داده‌محور باید بیشتر باز شود، چون ۲-۶ به آن تکیه می‌کند»). پاراگراف
        پایانی به سه پاراگراف شد: (۱) **چرا ترتیب دومرحله‌ای اشتباه است** — برازش توزیع خطا را
        روی کل دامنه پخش می‌کند ولی تصمیم فقط به یک نقطه نیاز دارد، پس خانواده‌ای که تنه را خوب
        می‌گیرد می‌تواند در نقطه‌ی تصمیم بد باشد و این خطا در معیار برازش دیده نمی‌شود؛
        (۲) **افزوده‌ی شرطی‌شدن** — صورت کلاسیک یک عدد برای کل مسئله می‌دهد و صورت داده‌محور
        برای هر $(d,m,r,f)$ عدد خودش را، که با F15 (بزرگ‌ترین $\eta^2$ دفترچه؛ میانه‌ی نرخ
        ۰.۰۲۰ تا ۰.۱۵۲ یعنی بیش از هفت برابر) توجیه شد؛ (۳) **رد تجربی فرض توزیعی** با F03
        (۴.۹٪ صفر دقیق) و F04 (Beta از Gamma بدتر، برخلاف انتظار اولیه)، به‌علاوه یک جمله درباره‌ی
        بهای رویکرد (وابستگی به حجم داده). بند از ~۰.۶ ص به ~۱ ص رسید؛ کل فصل ۲ یک صفحه جابه‌جا
        شد ولی حجم کل گزارش تغییر نکرد (۶۶ صفحه).
     ↳ 🐞 **باگ رندر کشف و رفع شد: عدد اعشاری فارسی در بدنه وارونه چاپ می‌شود.** `۴.۹ درصد`
        در PDF «۹.۴ درصد» درآمد، چون نقطه در متن راست‌به‌چپ نویسه‌ی خنثی است و عدد را به دو تکه
        می‌شکند. کامپایل بدون خطا و هشدار رد می‌شود، پس فقط با خواندن خودِ PDF پیدا شد. همه‌ی
        اعشارهای بندهای ۲-۲-۱ و ۲-۶ به حالت ریاضی منتقل شدند (`$4.9$`، `$8.05$`، `$90.4$`،
        `$67.6$`، `$4.7$`) — همان قرارداد `$7.5$ درصد` فصل ۴؛ عدد صحیح در نثر فارسی ماند.
        اکنون صفر عدد اعشاری با «.» در کل PDF باقی است. قاعده + دستور چک یک‌خطی در
        `final_report/AGENTS.md` ثبت شد. شاهد: `final_report/AGENTS.md`، `final_report/tex/chapter2.tex`
     ↳ **بند ۳-۱ (مقدمه‌ی فصل ۳) و کل بند ۳-۲ (روش پیشنهادی، شش زیربند) نوشته شد** — جدول‌های
        (۳-۱) پنج سطح داده، (۳-۲) آخرین وعده‌ی در دسترس در لحظه‌ی برش، (۳-۳) هشت دسته‌ی ویژگی،
        (۳-۴) ماتریس کاربردپذیری ۱۳ خانواده × ۵ سطح، و رابطه‌ی (۴) عدد پخت. دو مرجع تازه به
        `MyReferences.bib`: `JamiesonTalwalkar16halving` (نصف‌سازی متوالی) و `CawleyTalbot10selection`
        (بیش‌برازش انتخاب مدل — پشتوانه‌ی حاشیه‌ی هم‌ارزی $\delta$). حجم: ~۸ صفحه (صفحات ۲۱ تا ۲۸
        بدنه)، کل PDF ۷۶ صفحه. بند ۳-۱ ترتیب خوانش را توضیح می‌دهد (روش پیش از داده، با ارجاع رو
        به جلو به ۳-۳-۳) طبق هشدار `report-outline.md`.
        ⚠️ **جدول پوشش شرطی عمداً در ۳-۲-۵ نیامد** و به‌جایش ارجاع به ۴-۴-۳ گذاشته شد: اعداد
        پوشش نتیجه‌اند نه روش، و فصل ۴ همان ارقام (۱۷٪ تا ۵۰٪ به تفکیک سلف، ۹.۴٪ تا ۳۲.۷٪ به
        تفکیک چارک رزرو) را دارد؛ تکرارشان بودجه‌ی صفحه را بی‌فایده خرج می‌کرد.
        🐞 **یک باگ جهت‌دهی رفع شد:** `$\Delta$\lr{AIC}` در جدول (۳-۳) به‌صورت «AICΔ» چاپ می‌شد
        چون Δ خارج از بلوک لاتین بود؛ درستش `\lr{$\Delta$AIC}` است. نماد ماتریس (۳-۴) عمداً
        `$\bullet$`/`$\circ$`/`$\times$` (حالت ریاضی) است نه ✅/⚠️/❌، چون قلم‌های فارسی قالب آن
        نویسه‌ها را ندارند. شاهد: `final_report/tex/chapter3.tex`
     ↳ **بازبینی کاربر روی همین بندها (هشت مورد) اعمال شد:** «دست روش را می‌بندند» حذف شد؛
        بند ۳-۲-۲ از سه پاراگراف به دو پاراگراف کوتاه رسید (جدول ۳-۲ خودش گویاست) و جمله‌ی
        «قاعده‌ی برش تنها یک قید حفاظتی نیست…» حذف شد، ولی شاهد عددی $0.159$/$0.050$/نسبت
        خطر $3.18$ عمداً ماند؛ «قاعده‌ی حاکم بر ساخت ویژگی‌ها آن بود که…» به «برای هر ویژگی
        پیش از ساختش یک شاهد مشخص خواسته شد» بازنویسی شد (قاعده‌ی لحن حاکمیتی در
        `final_report/AGENTS.md`)؛ «جوی» ← «جَوّی»؛ تعریف **شوک روز** با رابطه‌ی (۴) به ۳-۲-۱
        افزوده شد (پیش‌تر در فصل ۳ و ۴ بدون تعریف به کار می‌رفت)؛ تعریف **رمزگذاری هدف**
        بسط یافت؛ و چگالی «—» از ۱۱ به ۵ رسید تا با فصل ۲ و ۴ هم‌تراز شود.
     ↳ ⭐ **شکل (۳-۱) ساخته شد** — `tools/report_figures/make_featureset_fig.py` →
        `reports/figures/5.12_feature_sets_fa.png` → symlink `final_report/img/feature_sets.png`.
        نمودار میله‌ای $R^2$ خارج‌نمونه‌ی شش فیچرست در برابر تعداد ویژگی، که ادعای «افزودن
        ستون بیشتر لزوماً کمک نمی‌کند» را جای شمردن شش عدد در نثر می‌نشاند. اعداد از
        `doc/leakage_audit.md` (لایه ۳) و `data/processed/feature_sets_v1.json` **خوانده**
        می‌شوند، نه محاسبه. ⚠️ متن سؤال کاربر «$R^2$ روی چه مدلی؟» در بدنه پاسخ گرفت:
        درخت تقویت‌شده‌ی گرادیانی پیش‌فرض روی تقسیم زمانی $75/25$، اجراشده برای **ممیزی نشت**
        نه انتخاب مدل — پس فقط مقایسه‌ی نسبی معتبر است. 🐞 برچسب محور $y$ اول
        `$R^2$ خارج‌نمونه` بود و patch بیدی `src/viz_fa` واژه‌ی فارسیِ کنار بخش ریاضی را
        وارونه کرد («هنومن‌جراخ») — همان تله‌ی مستند در `tools/report_figures/README.md`؛
        با «ضریب تعیین خارج‌نمونه» (بدون حالت ریاضی) رفع شد.
        ⚠️ **شکل خط لوله‌ی نرم‌افزاری بند ۳-۴-۱ حالا (۳-۲) است، نه (۳-۱)** — `report-outline.md`
        و کامنت TODO فصل ۱ و ۳ اصلاح شدند.
     ↳ **پاراگراف معرفی پنج مدل برنده به ۳-۲-۴ افزوده شد** (پرسش کاربر: این مدل‌ها کجا
        توضیح داده شوند؟). فصل ۴ فقط نامشان را در جدول (۴-۴) دارد و هیچ‌جا نمی‌گفت
        `qrf` یا `bhm_varying_dispersion` چیست؛ طبق مرز فصل ۳/۴ (ردیف ۵۷) «مدل چیست» در
        فصل ۳ می‌نشیند و «چه نمره‌ای گرفت» در فصل ۴. منبع توضیح‌ها: `reports/models/*.md`
        گام ۱ هر کارت. حجم فصل ۳ به ~۱۰ صفحه رسید، کل PDF ۷۸ صفحه.
     ↳ **دور دوم بازخورد کاربر (۱۹ مورد) — بازچینش ساختاری، نه فقط ویرایش لفظی:**
        ⭐ **بند تازه‌ی ۲-۵ «خانواده‌های مدل و راهبرد جست‌وجو» به فصل ۲ افزوده شد** و
        بندهای ۲-۵ تا ۲-۷ قبلی به ۲-۶ تا ۲-۸ شیفت کردند (فقط ۳ برچسب، همه با `\ref`).
        محتوا: سه راه گرفتن کوانتایل شرطی؛ **جدول (۲-۱)** پنج خانواده در برابر «جدول
        جست‌وجو» به‌عنوان خط پایه، با ستون «چه چیزی بیش از جدول جست‌وجو دارد»؛ و تعریف
        **نصف‌سازی متوالی** + **بیش‌برازش انتخاب مدل** + **حاشیه‌ی هم‌ارزی**. دلیل: کاربر
        خواست معرفی مدل‌ها از بدنه‌ی فصل ۳ به پیش‌زمینه برود و ساده‌تر گفته شود. جدول
        پیشینه از (۲-۱) به (۲-۲) شیفت شد (خودکار، `\ref{tab:lit}`).
        ⭐ **بند ۳-۲-۴ کامل بازنویسی شد**: پاراگراف‌های متراکم جای خود را به **جدول (۳-۵)
        چهار مرحله‌ی قیف** دادند (مرحله · پرسش · بودجه · معیار عبور)، و معرفی پنج مدل
        برنده به فصل ۲ رفت. اصطلاحات: «قهرمانان» ← «مدل‌های منتخب»؛ «رهبر فعلی» ←
        «بهترین مدل تا آن لحظه»؛ «انحراف معیار بین‌بخشی» ← «زیان یک مدل از بخشی به بخش
        دیگرِ اعتبارسنجی»؛ «فضای دومقداری» ← «مدلی که تنها دو حالت ممکن داشت»؛ «تکرار
        بیت‌به‌بیت» حذف شد. «قیف غربالگری» پانویس `Screening funnel` گرفت.
        ⭐ **پاراگراف «چرا این پنج مدل بردند» به ۴-۴-۱ افزوده شد** (شکاف واقعی: فصل ۴
        فقط نتیجه را می‌داد، نه تفسیرش). ادعا: تفاوت در توانایی گرفتن **برهم‌کنش** است،
        نه پیچیدگی محاسباتی — هر ۱۵ عضو خانواده‌ی خطی نزدیک خط پایه ماندند در حالی که
        هر پنج برنده اثر غیرجمع‌شونده می‌گیرند؛ شاهد ارزان‌بودنش `knn_quantile` است.
        منبع: `reports/phase7/model_comparison.md`.
        🐞 **تصادم شماره‌ی پانویس در جدول (۳-۴).** ۱۳ پانویس با `\footnotemark[N]` دستی
        (۱ تا ۱۳) با شمارنده‌ی خودکار برخورد کرد و «Screening funnel» هم شماره‌ی ۴ گرفت.
        رفع با یک شمارنده‌ی مستقل (`fnfamily` + ماکروهای `\fnfam`/`\fnfamtext`) که از
        `\value{footnote}` شروع می‌کند و در پایان شمارنده‌ی اصلی را جلو می‌برد؛ حالا
        ۴ تا ۱۷ پیوسته‌اند. **الگوی قابل‌استفاده برای هر جدول چندپانویسی دیگر.**
        اصلاحات لفظی: ستون چهارم جدول (۳-۲) حذف و محتوایش به کپشن رفت · «کوهورت» ←
        «ترکیب رزروکنندگان» (+ پانویس `Cohort features`) · «جوی» با اعراب · «فرو
        می‌ریخت» ← «اثری باقی نمی‌ماند» · «درخت تقویت‌شده‌ی گرادیانی» ← `Gradient Boosting`
        · «ضریب تعیین خارج‌نمونه» ← `$R^2$ خارج‌نمونه` (در شکل هم: `Out-of-sample $R^2$`،
        که چون تماماً لاتین است از تله‌ی وارونگی بیدی هم در امان است) · «شبکه‌ی
        پیش‌خوری»/«یک‌هات‌کردن» حذف شدند · «خواسته شد» ← «آورده شد».
        ⚠️ یک **خطای واقعی** هم رفع شد: نسخه‌ی قبلی نوشته بود مقدار پخت شام «حوالی ساعت
        ۲۳ همان روز» اعلام می‌شود، در حالی که لحظه‌ی برش ۲۳ روز $d-1$ است.
        کامپایل بدون خطای فاتال؛ ۸۰ صفحه؛ چک عدد اعشاری وارونه خالی.
     ↳ **بند ۳-۲-۲ به یک پاراگراف کوتاه رسید** (بازخورد سوم کاربر: «بخش ساده‌ای است،
        در حد نصف پاراگراف با جدول کافی است»). دو پاراگراف به یکی شد و از ~۱۳ خط به ۶
        خط رسید. آنچه حذف شد چیزی است که جای دیگری گفته شده: *چرا* نشت خطای اعتبارسنجی
        را مصنوعی پایین می‌آورد در بند ۲-۴-۲ آمده، و بندِ «شام روز $d-1$ با اینکه
        تاریخش پیش‌تر است مجاز نیست» در کپشن جدول (۳-۲) نشسته. آنچه ماند: تفاوت لحظه‌ی
        برش دو وعده، قید ۷۲ساعته‌ی رزرو، آگاه‌بودن lag به نوع وعده، شاهد عددی
        $0.159$/$0.050$/$3.18$ (کاربر صریحاً خواسته بود بماند)، و دو ارجاع به ۳-۳-۴ و
        ۳-۴-۲. کل PDF همچنان ۸۰ صفحه.
     ↳ **دور چهارم بازخورد (۷ مورد، همه دربارهٔ سادگی نگارش):** «آزمون زوجی» با تعریف
        درون‌متنی («سنجش اختلاف دو مدل روی همان سلول‌ها، نه مقایسهٔ میانگین کلی‌شان») و
        پانویس `Paired comparison` آمد · «حاشیهٔ هم‌ارزی» یادآوری یک‌جمله‌ای گرفت با
        ارجاع به ۲-۵ · **چراییِ «۶۸ مشاهدهٔ مستقل» توضیح داده شد** (نرخ همهٔ سلف‌ها در
        یک روز با هم بالا و پایین می‌رود، پس هر روز بیشتر به یک مشاهده می‌مانَد) ·
        «به‌اندازهٔ ویژگی‌های ساخته‌شده…» ← «سه نامزد دیگر هم بررسی و رد شدند» · «دو
        تصمیم کمّی، این قیف را از یک رتبه‌بندی ساده جدا می‌کنند» حذف و مستقیم وارد
        محتوا شد · پاراگراف بودجه از روایت «اول چه می‌کردیم، بعد چه کردیم» به بیان
        مستقیم قاعدهٔ نهایی تبدیل شد (۸.۷۶ ساعت-هسته، ۸۶٪ اتلاف و ۳۱۶ اجرا حذف شدند؛
        توجیه در یک بند ماند: «وگرنه مدلی با فضای دوحالته هم ده‌ها بار همان دو حالت را
        تکرار می‌کند») · پاراگراف سه‌ابزارِ ۳-۲-۵ حدود یک‌سوم کوتاه‌تر شد.
        ⚠️ **چیزی که عمداً حذف شد و ممکن است بعداً لازم شود:** شاهد کمّی ۸۶٪ اتلاف و
        «۳۱۶ اجرا روی یک نقطه از فضای نُه‌محوره» دیگر در بدنه نیست؛ اگر داور دلیل
        بازطراحی فاز ۷ را پرسید، جایش `doc/decisions/37-phase7-rescope.md` است.
     ↳ **دور پنجم بازخورد (۳ مورد، همه در ۳-۲-۶):** «در گفت‌وگو با بهره‌بردار کنار
        گذاشته شدند» حذف شد — قید نبودن ظرفیت و بسته‌ی گسسته حالا به‌صورت **حقیقت**
        نوشته شده، نه گزارشِ یک گفت‌وگو · «این عدد از مسیری مستقل هم تأیید شد» حذف شد ·
        ⭐ **شکل (۳-۲) نرخ مبادله‌ی حاشیه‌ای ساخته شد**
        (`tools/report_figures/make_tau_marginal_fig.py` → `6.10_tau_marginal_fa.png`):
        هفت گام τ به‌صورت میله، با خط‌چین افقی روی نسبت هزینه‌ی ۴؛ ستون‌های بالای خط
        به‌صرفه‌اند و آخرین‌شان دقیقاً همان است که به τ=۰.۲۰ می‌رسد. اعداد از جدول
        «تحلیل حاشیه‌ای» `reports/tau_sensitivity.md` **خوانده** می‌شوند (همان اعدادی
        که ردیف ۳۴ `decision_log` استناد می‌کند)، نه محاسبه.
        ⚠️ **عمداً از `10.2_net_value_by_tau.csv` استفاده نشد** با اینکه CSV است: آن
        فایل روی پنجره و مبنای دیگری حساب شده (۳.۸× و ۲.۸× به‌جای ۴.۸× و ۳.۶×) و
        مخلوط‌کردن دو مبنا در یک ادعا نادرست بود.
        🐞 دو تله‌ی تکراری شکل‌سازی باز هم خوردند و رفع شدند: برچسب «گام $\tau$» با
        patch بیدی وارونه شد (← «گام کوانتایل»)، و محور لگاریتمی برچسب‌های فرعی
        (`6×10¹`) را روی برچسب‌های دستی چاپ می‌کرد (← `minorticks_off()`).
        حذف تکرارها: جمله‌ی «τ اهرم سیاستی است و تغییرش فقط یک برازش تازه می‌خواهد»
        (عیناً در ۴-۶-۱ هست) و جمله‌ی تجمیع روی نوع غذا (عیناً در ۳-۲-۱ هست).
        شکل خط لوله‌ی ۳-۴-۱ حالا **(۳-۳)** است. کل PDF ۸۰ صفحه.
     ↳ **دور ششم: جدول (۲-۱) از «خانواده» به «مدل» تغییر کرد.** بازخورد کاربر: «من
        می‌دانم درخت تصادفی چیست ولی نمی‌دانم `lightgbm` و `catboost` چه هستند». ردیف‌ها
        حالا همان پنج مدلی‌اند که در فصل ۴ نامشان می‌آید (`lightgbm_quantile`،
        `catboost_quantile`، `qrf`، `bhm_varying_dispersion`، `mlp_entity_embedding`)
        به‌علاوه‌ی ردیف خط پایه، و ستون «ایده‌ی اصلی» **تمایز خودِ کتابخانه** را می‌گوید
        نه فقط خانواده‌اش: `LightGBM` سطل‌بندی مقادیر پیوسته + شکافتن پربارترین برگ ·
        `CatBoost` رمزگذاری دسته‌ای با میانگین ردیف‌های پیشین (نه کل داده) + درخت
        متقارن · `QRF` نگه‌داشتن مقادیر برگ به‌جای میانگین · مدل بیزی توزیع مشترک
        اثرها + پراکندگی تابع حجم رزرو · `MLP` بردار بازنمایی آموخته‌شده به‌جای one-hot.
        منبع: گام ۱ کارت‌های `reports/models/*.md` + مشخصات منتشرشده‌ی دو کتابخانه.
        ستون سوم («چه چیزی بیش از جدول جست‌وجو دارد») دست‌نخورده ماند چون کاربر گفت
        همان قالب خوب است. پاراگراف تفاوت‌ها هم از «خانواده» به «همین پنج مدل» بازنویسی
        و کوتاه شد، و ردیف $k$-نزدیک‌ترین همسایه حذف شد (جزو پنج مدل معنادار نبود).
        ارجاع بند ۳-۲-۴ هم به‌روز شد.
     ↳ **شکل نرخ مبادله‌ی حاشیه‌ای τ (شکل ۳-۲ سابق) به‌درخواست کاربر حذف شد** («نمودار
        خوب نبود»). استدلال به نثر برگشت ولی کوتاه ماند: دو جمله، با همان دو عدد
        $4.8$ و $3.6$. `make_tau_marginal_fig.py`، `6.10_tau_marginal_fa.png` و
        symlinkش پاک شدند؛ اگر لازم شد از کامیت `d79878d` برمی‌گردند.
        ⚠️ **شماره‌ی شکل خط لوله‌ی ۳-۴-۱ دیگر در هیچ کامنتی هاردکد نمی‌شود.** این عدد در
        این نشست سه بار جابه‌جا شد (۳-۱ → ۳-۲ → ۳-۳ → ۳-۲) و هر بار سه فایل را
        می‌خواست؛ حالا کامنت فصل ۱ و `report-outline.md` فقط به بندش ارجاع می‌دهند و
        شماره از `\ref` می‌آید.
     ↳ ⚠️ **نشست موازی روی همین مخزن.** حین این دورها یک نشست دیگر در حال نگارش بند ۳-۳
        بود و `chapter3.tex`، `tools/report_figures/README.md` و دو شکل EDA را
        هم‌زمان تغییر می‌داد. یک بار `git add` کلی کارِ آن نشست را داخل کامیت این نشست
        برد که با `reset --soft` جدا شد. **قاعده‌ی عملی: در این مخزن فقط مسیر صریح
        stage شود، و برای فایل مشترک فقط hunk خودی با `git apply --cached`.**
     ↳ **بند ۳-۴ (معماری و پیاده‌سازی سامانه) و بند ۳-۵ (خلاصه و جمع‌بندی) نوشته شدند — فصل ۳ کامل شد.**
        ۳-۴-۱: قاعده‌ی «نوت‌بوک روایت، `src/` حقیقت» + **جدول (۳-۶)** هشت لایه‌ی کد + **شکل خط لوله**
        (TikZ، از فایل خام تا عدد پخت، با برچسب هش روی دو خروجی میانی) + نسخه‌بندی دولایه و منبع واحد بذر.
        ۳-۴-۲: فروپاشی قاعده‌ی برش به یک نامساوی با شماره‌گذاری سراسری وعده (**رابطه‌ی تازه**، `src/features/cutoff.py`)
        + رجیستری خودکار + ممیزی به‌عنوان دروازه‌ی اجرایی. ۳-۴-۳: انجماد `cv_folds.json` و سه آزمون خودکار پروتکل؛
        عمداً ICC/گسترشی-لغزان/آزمون فشار تکرار نشد (در ۴-۳-۲ است) و فقط سازوکار پیاده‌سازی آمد.
        ۳-۴-۴: امضای مشترک `fit_predict`، `RunConfig` اجباری، **الگوریتم (۳-۱)** حلقه‌ی تنظیم/ارزیابی، و درس
        «هارنسی که فقط زمان اجرا را ثبت می‌کرد دو باگ را پنهان کرد». ۳-۴-۵: `cook_qty` همان تابعی است که هر
        ارزیابی صدا می‌زند (پس معیار گزارش‌شده و رفتار مستقرشده نمی‌توانند واگرا شوند)، محدودکردن به $[0,1]$
        تنها پس‌پردازش، و تذکر اینکه جمع عددهای پخت روی نوع غذا **کوانتایل جمع نیست** (وصل به ۲-۳-۳).
        منابع: `AGENTS.md`، `src/config.py`، `src/features/{cutoff,registry,audit}.py`، `src/cv.py`،
        `src/models/{axes,s2_runner}.py`، `src/baselines.py`، `doc/phase7-execution-standard.md`،
        `doc/leakage_audit.md`، `doc/data_manifest.md`، `reports/phase10/10.1_cook_quantity.md`.
        ⚠️ **باگ رندر دوسویه در گره‌های TikZ کشف و رفع شد (فصل ۳ و فصل ۱).** متن فارسی داخل `\node` با
        `align=center` چپ‌به‌راست چیده می‌شود و ترتیب واژه‌های هر خط وارونه چاپ می‌شود («فاز ۰» ← «۰ فاز»،
        «تعریف مسئله» ← «مسئله تعریف»). کامپایل بی‌خطا رد می‌شود، پس فقط با نگاه‌کردن به PDF دیده می‌شود.
        `execute at begin node=\setRTL` فقط گره‌های **بدون** `align` را درست می‌کند؛ راه‌حل مؤثر، بستن
        **هر خط** گره در `\rl{...}` است. **شکل (۱-۱) از روز اولش این باگ را داشت** و همین‌جا رفع شد.
        به همین ترتیب، فارسی داخل پرانتز ریاضی (`$g(\text{آموزش}, \text{آزمون})$`) هم وارونه می‌شود؛
        در الگوریتم (۳-۱) با بازنویسی به نثر رفع شد.
        عنوان ۳-۴-۳ از «فولدهای اعتبارسنجی» به «**بخش‌های** اعتبارسنجی» تغییر کرد (قاعده‌ی «آوانویسی ممنوع»
        `final_report/AGENTS.md` + یکدستی با «بخش» که فصل‌های ۳ و ۴ همه‌جا به کار می‌برند)؛ ردیف متناظر
        `doc/report-outline.md` هم به‌روز شد. `\newacronym{DVC}` به `acronyms.tex` افزوده شد.
        ⚠️ **حجم: ۶ صفحه در برابر برآورد ۳.۵ ص نقشه**؛ بدنه اکنون ۷۰ صفحه در برابر بودجه‌ی ۳۰–۴۰ ص.
        کامپایل `build.sh` بدون خطای فاتال، ۹۶ صفحه؛ چک اعشار وارونه خالی.
  2. فصل ۲، بخش اول تا چهارم (Newsvendor، رگرسیون کوانتایل، اعتبارسنجی زمانی، آزمون‌های آماری)
  3. فصل ۳، بندهای ۳-۵ نتایج به‌دست‌آمده و ۳-۶ تحلیل نتایج (مواد آماده در `reports/`؛ کار = انتخاب و فشرده‌سازی)
  4. فصل ۳، بقیه‌ی بندها (۳-۲ روش پیشنهادی، ۳-۳ داده، ۳-۴ معیار ارزیابی)
  5. ~~فصل ۲، بخش پنجم (مروری بر پیشینه‌ی تحقیق)~~ — ✅ انجام شد (بالا)
  6. ~~فصل ۱~~ ✅ **تمام** — هر ۷ بند نوشته شد (۱-۱ تا ۱-۷). عدد وزنی F01 (`$8.05\%$`) نقل شد، نه رقم خام ۸٪ سند مسئله؛
     میانگین ساده ۹.۵۹٪ هم به‌عنوان تقابل آمد. برآورد مالی به‌جای نقل مستقیم «۵۰ میلیارد»، اول عدد مستقیماً
     قابل‌محاسبه (۱۶۲ هزار پرس × ۱۲۰ هزار تومان ≈ ۱۹ میلیارد در پنج ماه) و بعد سالانه‌شده‌اش آمد، تا با
     بودجه‌ی ۳۲هفته‌ای بند ۴-۵-۵ در تناقض ظاهری نیفتد.
     ⚠️ **دو رفع در جدول (۱-۱) دروازه‌ها:** ردیف M3 عیناً قاعده‌ی حاکمیتی «پیش از M3 هیچ مدل پیچیده‌ای آموزش
     داده نشود» را نقل می‌کرد — دقیقاً همان نقض «لحن دستوری» که `final_report/AGENTS.md` منع کرده؛ با توجیه
     روش‌شناختی جایگزین شد («بدون مرجع مقایسه، برتری هر مدل تنها با معیاری سرخود قابل‌ادعا بود»). ردیف M5 هم
     از «محدودیت‌ها باید ... نوشته می‌شدند» به خبری تغییر کرد.
     **حجم: ۵ صفحه در برابر برآورد ۳.۵ ص نقشه** — پس از دو دور فشرده‌سازی (نثر از ~۱۴۰۰ به ~۱۱۷۰ واژه، و
     کوتاه‌سازی سلول‌های جدول). شکل (۱-۱) نیم صفحه و جدول (۱-۱) یک‌سوم صفحه می‌گیرند؛ کاهش بیشتر بدون
     حذف یکی از این دو ممکن نیست. بدنه اکنون ۷۶ صفحه در برابر بودجه‌ی ۳۰–۴۰ ص.
     کامپایل `build.sh` بدون خطای فاتال، ۹۴ صفحه؛ چک اعشار وارونه خالی؛ `fa_lint` روی نثر تمیز.
     شاهد: `final_report/tex/chapter1.tex`
  6b. فصل ۵ — آخر، تا با محتوای واقعی هم‌خوان باشد
  7. فصل ۶ مراجع (سبک IEEE)، چکیده‌ی فارسی/انگلیسی، واژه‌نامه (`words.tex`)، فهرست اختصارات (`acronyms.tex`)

## ۱۱.۲ کارت مدل (Model Card)

- [ ] **پیوست الف** — کاربرد مورد نظر، داده‌ی آموزش، معیارها، عملکرد به تفکیک زیرگروه، محدودیت‌ها، شرایطی که مدل نباید استفاده شود (تابستان، ترم جدید بدون بازآموزی، سلف تازه‌تأسیس)
  ⚠️ **بسته‌کاری اجرانشده**، نه صرفاً نگارش.

## ۱۱.۳ مجموعه نمودارهای کلیدی

- [ ] انتخاب حداکثر ۱۲ شکل از ۲۷ فایل `reports/figures/`
- [ ] عنوان **گزاره‌ای** فارسی برای هر شکل (نه توصیفی)
- [ ] بررسی فونت فارسی صحیح در خروجی نهایی

## ۱۱.۴ مخزن کد قابل بازتولید

- [ ] `README.md`: نصب، ساختار، دستور بازتولید کامل (`make all`)، جدول نتایج مرجع
- [ ] پیوست: رجیستری کامل ویژگی‌ها + دستور بازتولید

## ۱۱.۵ ارائه به ذی‌نفعان

- [ ] نسخه‌ی فنی (استاد/داور): روش‌شناسی و اعتبارسنجی محور
- [ ] نسخه‌ی مدیریتی (معاونت دانشجویی): منحنی trade-off بند ۸.۵، قواعد ساده بند ۹.۵، عدد صرفه‌جویی با CI

## ۱۱.۶ فصل محدودیت‌ها

- [ ] `final_report/tex/chapter5.tex`، بند ۵-۲-۲ «محدودیت‌ها» — **با همان دقت نتایج نوشته شود**، نه خوش‌بینانه‌تر

---

## ضمانت مکانیکی (سطح `doc/`، نه `final_report/`)

`python -m src.report_coverage` — خروجی در `reports/phase11/coverage_gaps.md`.
هر ردیف `decision_log`، هر حقیقت `F##` و هر گزارش `reports/*.md` باید در نقشه‌ی منبع یا
دفتر یافته‌ها ارجاع داشته باشد، وگرنه با exit code≠۰ شکست می‌دهد. این ضمانت محتوای
`doc/` را می‌سنجد؛ معیار اتمام خودِ گزارش لاتک، کامپایل بدون خطا با `xelatex` است
(`final_report/AGENTS.md`).

## پیوست‌های برنامه‌ریزی‌شده

کارت مدل · جدول کامل آزمون‌های آماری و مقایسه‌ها (شامل نتایج منفی) · فهرست تصمیمات ·
دفتر حقایق و یافته‌ها · رجیستری ویژگی و دستور بازتولید.
