# فاز ۱۱ — مستندسازی و ارائه

نقشه‌ی فاز: بند ۱۱ `doc/WBS-food-demand-forecasting.md`.
**فهرست نهایی و قفل‌شده‌ی گزارش: `doc/report-outline.md`** (ردیف ۵۲ `decision_log`).

قالب تحویل: `BScThesis_template_new.docx` — پایان‌نامه‌ی کارشناسی، دانشکده مهندسی برق و
کامپیوتر، پردیس دانشکده‌های فنی دانشگاه تهران. ساختار **عیناً همان قالب**: ۵ فصل محتوایی
+ فصل ۶ مراجع + پیوست‌ها. هدف حجم: **۳۰–۴۰ صفحه A4 بدنه** (برنامه: ۳۷.۵).

---

## ۱۱.۱ گزارش فنی

- [x] گنجاندن محتوای پروژه در بندهای موجود قالب، بدون افزودن یا حذف فصل — `doc/report-outline.md`، ردیف ۵۲ `decision_log`
- [x] بودجه‌ی صفحه به‌تفکیک بند (بدنه ۳۷.۵ ص) + ستون «فشرده‌پذیر تا» برای کنترل سقف ۴۰ — بند ۲ سند فهرست
- [x] استخراج قواعد قالب (پاورقی اصطلاح لاتین، «شکل (N-M)» زیر شکل، «جدول (N-M)» بالای جدول، «رابطه (N)»، مراجع IEEE، پیش‌متن و پس‌متن) — بند ۳ سند فهرست
- [x] نگاشت معکوس: تضمین اینکه هر ۱۱ فصل `doc/report-source-map.md` خانه دارد — بند ۶ سند فهرست
- [x] فهرست خلأها (چیزهایی که در هیچ سند مخزن نیست و باید تازه نوشته شود) — بند ۵ سند فهرست

### نگارش (به‌ترتیب ریسک، نه به‌ترتیب فصل — بند ۸ سند فهرست)

- [ ] **فصل ۴ — پیاده سازی** (اولویت ۱: بیشترین محتوای تازه، کمترین وابستگی)
  - [ ] ۴-۲ معماری مخزن و پشته‌ی فناوری
  - [ ] ۴-۳ گام‌های اجرای پروژه + شکل (۴-۱) جریان فازها و دروازه‌ها
  - [ ] ۴-۴ زیرساخت ثبت آزمایش و اجرای محاسباتی
  - [ ] ۴-۵ اجرای پروژه با عامل‌های هوش مصنوعی (۵ زیربند، شامل ۴-۵-۵ درس‌آموخته‌های صادقانه‌ی فاز ۷)
  - [ ] ۴-۶ بازتولیدپذیری
- [ ] **بندهای ۲-۲ تا ۲-۵** — متن نظری تازه در «بخش اول تا چهارم» فصل ۲:
  - [ ] ۲-۲ مسئله‌ی روزنامه‌فروش و تصمیم تحت عدم‌قطعیت (شامل تصحیح فرمول، ردیف ۳۶)
  - [ ] ۲-۳ پیش‌بینی احتمالاتی و رگرسیون کوانتایل
  - [ ] ۲-۴ اعتبارسنجی و نشت اطلاعاتی در داده‌ی زمانی
  - [ ] ۲-۵ ابزارهای آماری و استنباطی + جدول (۲-۱) دوازده آزمون
- [ ] **بندهای ۳-۵ و ۳-۶** — نتایج و تحلیل (مواد آماده؛ کار = انتخاب و فشرده‌سازی)
- [ ] **بندهای ۳-۲، ۳-۳، ۳-۴** — روش پیشنهادی، ابزار و داده، معیار ارزیابی
- [ ] **۲-۶** مروری بر پیشینه‌ی تحقیق (از `doc/Literature_Review.md`)
- [ ] **فصل ۱ و فصل ۵** — آخر نوشته شوند تا با محتوای واقعی هم‌خوان باشند
- [ ] **فصل ۶ — مراجع** به سبک IEEE
- [ ] اجزای قالب: چکیده + کلمات کلیدی، Abstract + Keywords، فهرست علائم اختصاری، «مقدمه» و «خلاصه و جمع‌بندی» هر فصل، بند ۱-۷ ساختار پایان‌نامه، جلد فارسی و لاتین، تعهدنامه

## ۱۱.۲ کارت مدل (Model Card)

- [ ] **پیوست الف** — کاربرد مورد نظر، داده‌ی آموزش، معیارها، عملکرد به تفکیک زیرگروه، محدودیت‌ها، شرایطی که مدل نباید استفاده شود (تابستان، ترم جدید بدون بازآموزی، سلف تازه‌تأسیس)
  ⚠️ **بسته‌کاری اجرانشده**، نه صرفاً نگارش — اولویت ۲.

## ۱۱.۳ مجموعه نمودارهای کلیدی

- [ ] انتخاب حداکثر ۱۲ شکل از ۲۷ فایل `reports/figures/` (تخصیص به‌تفکیک فصل در بند ۷ سند فهرست)
- [ ] عنوان **گزاره‌ای** فارسی برای هر شکل (نه توصیفی)
- [ ] بررسی فونت فارسی صحیح در خروجی نهایی

## ۱۱.۴ مخزن کد قابل بازتولید

- [ ] `README.md`: نصب، ساختار، دستور بازتولید کامل (`make all`)، جدول نتایج مرجع
- [ ] **پیوست ث** — رجیستری کامل ویژگی‌ها + دستور بازتولید

## ۱۱.۵ ارائه به ذی‌نفعان

- [ ] نسخه‌ی فنی (استاد/داور): روش‌شناسی و اعتبارسنجی محور
- [ ] نسخه‌ی مدیریتی (معاونت دانشجویی): منحنی trade-off بند ۸.۵، قواعد ساده بند ۹.۵، عدد صرفه‌جویی با CI

## ۱۱.۶ فصل محدودیت‌ها

- [ ] بند **۵-۲-۲** گزارش — **با همان دقت نتایج نوشته شود** (فهرست اقلام الزامی در بند ۴ سند فهرست، فصل ۵)

---

## دو پیامد پذیرفته‌شده‌ی وفاداری کامل به قالب

1. **فصل ۳ حدود ۱۶ صفحه می‌شود** — قالب همه‌ی روش + داده + معیار + نتایج + تحلیل را در
   یک فصل می‌گذارد. جبران با ۶ بند سطح‌دو که هر کدام مثل یک فصل کوچک خوانده شوند.
2. **ترتیب `3-2 روش پیشنهادی` پیش از `3-3 ابزارها و داده`** برخلاف ترتیب واقعی کار است
   (کاوش داده انتخاب روش را دیکته کرد). مدیریت با **ارجاع رو به جلو** در متن، نه با
   جابه‌جایی بند.

## ضمانت مکانیکی

`python -m src.report_coverage` — خروجی در `reports/phase11/coverage_gaps.md`.
هر ردیف `decision_log`، هر حقیقت `F##` و هر گزارش `reports/*.md` باید در نقشه‌ی منبع یا
دفتر یافته‌ها ارجاع داشته باشد، وگرنه با exit code≠۰ شکست می‌دهد.

## پیوست‌های برنامه‌ریزی‌شده

الف کارت مدل · ب جدول کامل آزمون‌های آماری و مقایسه‌ها (شامل نتایج منفی) · پ فهرست ۵۲
ردیف تصمیم · ت دفتر حقایق و یافته‌ها · ث رجیستری ویژگی و دستور بازتولید · Abstract و
جلد لاتین.
