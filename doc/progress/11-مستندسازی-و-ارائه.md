# فاز ۱۱ — مستندسازی و ارائه

نقشه‌ی فاز: بند ۱۱ `doc/WBS-food-demand-forecasting.md`.

**کد منبع گزارش نهایی در `final_report/` است** (قالب لاتک `tehran-thesis`، حالت `bsc`،
۵ فصل: مقدمه، مروری بر مطالعات انجام‌شده، روش تحقیق، نتایج، بحث و نتیجه‌گیری + پیوست‌ها).
قرارداد نگارش برای عامل‌ها: `final_report/AGENTS.md`. محدودیت حجم بدنه: **۳۰–۴۰ صفحه A4**.
ثبت تصمیم پیوستن به این قالب (به‌جای فهرست قبلی برای Word): ردیف ۵۳ `doc/decision_log.md`.

⚠️ `doc/report-outline.md` **منسوخ** است — برای قالب Word نوشته شده بود، دیگر استفاده نمی‌شود.

فهرست دقیق TODO هر بند **داخل خودِ فایل‌ها** است (کامنت `% TODO` زیر هر
`\section`/`\subsection` در `final_report/tex/chapter*.tex`، هرکدام با ارجاع به سند/ردیف
مشخص در `doc/`) — اینجا تکرار نمی‌شود؛ چک‌لیست پایین فقط سطح فصل/بسته‌کاری است.

---

## ۱۱.۱ گزارش فنی

- [x] پاکسازی قالب: حذف محتوای نمونه/آموزشی خودِ کلاس (راهنمای لاتک در فصل ۱، مثال دامنه‌ی نامرتبط در فصل ۳، سه پیوست آموزشی) و اسکلت واقعی پروژه با TODO در هر فصل — ردیف ۵۳ `decision_log`
- [x] رفع باگ کامپایل قالب (`\latindepartment` + `\\`) — ردیف ۵۳ `decision_log`
- [x] صفحه‌ی عنوان: مشخصات دانشجو، استاد راهنما، داور، عنوان، تاریخ پر شد — `final_report/tex/faTitle.tex`, `enTitle.tex`
- [ ] نگارش محتوای هر فصل طبق TODOهای درون‌فایلی؛ ترتیب پیشنهادی (ریسک‌محور):
  1. فصل ۳ روش تحقیق، بخش «پیاده‌سازی پروژه» (بیشترین محتوای تازه، شامل اجرای پروژه با عامل هوش مصنوعی)
  2. فصل ۲، «تعاریف، اصول و مبانی نظری» (Newsvendor، رگرسیون کوانتایل، آزمون‌های آماری)
  3. فصل ۴ نتایج (مواد آماده در `reports/`؛ کار = انتخاب و فشرده‌سازی)
  4. فصل ۳، بقیه‌ی بندها (داده، فیچر، پروتکل اعتبارسنجی)
  5. فصل ۲، مروری بر ادبیات (از `doc/Literature_Review.md`)
  6. فصل ۱ و فصل ۵ — آخر، تا با محتوای واقعی هم‌خوان باشند
  7. مراجع (سبک IEEE)، چکیده‌ی فارسی/انگلیسی، واژه‌نامه (`words.tex`)، فهرست اختصارات (`acronyms.tex`)

## ۱۱.۲ کارت مدل (Model Card)

- [ ] **پیوست الف** — کاربرد مورد نظر، داده‌ی آموزش، معیارها، عملکرد به تفکیک زیرگروه، محدودیت‌ها، شرایطی که مدل نباید استفاده شود (تابستان، ترم جدید بدون بازآموزی، سلف تازه‌تأسیس)
  ⚠️ **بسته‌کاری اجرانشده**، نه صرفاً نگارش.

## ۱۱.۳ مجموعه نمودارهای کلیدی

- [ ] انتخاب حداکثر ۱۲ شکل از ۲۷ فایل `reports/figures/`
- [ ] عنوان **گزاره‌ای** فارسی برای هر شکل (نه توصیفی)
- [ ] بررسی فونت فارسی صحیح در خروجی نهایی

## ۱۱.۴ مخزن کد قابل بازتولید

- [ ] `README.md`: نصب، ساختار، دستور بازتولید کامل (`make all`)، جدول نتایج مرجع
- [ ] پیوست: رجیستری کامل ویژگی‌ها + دستور بازتولید

## ۱۱.۵ ارائه به ذی‌نفعان

- [ ] نسخه‌ی فنی (استاد/داور): روش‌شناسی و اعتبارسنجی محور
- [ ] نسخه‌ی مدیریتی (معاونت دانشجویی): منحنی trade-off بند ۸.۵، قواعد ساده بند ۹.۵، عدد صرفه‌جویی با CI

## ۱۱.۶ فصل محدودیت‌ها

- [ ] `final_report/tex/chapter5.tex`، بند «محدودیت‌ها» — **با همان دقت نتایج نوشته شود**، نه خوش‌بینانه‌تر

---

## ضمانت مکانیکی (سطح `doc/`، نه `final_report/`)

`python -m src.report_coverage` — خروجی در `reports/phase11/coverage_gaps.md`.
هر ردیف `decision_log`، هر حقیقت `F##` و هر گزارش `reports/*.md` باید در نقشه‌ی منبع یا
دفتر یافته‌ها ارجاع داشته باشد، وگرنه با exit code≠۰ شکست می‌دهد. این ضمانت محتوای
`doc/` را می‌سنجد؛ معیار اتمام خودِ گزارش لاتک، کامپایل بدون خطا با `xelatex` است
(`final_report/AGENTS.md`).

## پیوست‌های برنامه‌ریزی‌شده

کارت مدل · جدول کامل آزمون‌های آماری و مقایسه‌ها (شامل نتایج منفی) · فهرست تصمیمات ·
دفتر حقایق و یافته‌ها · رجیستری ویژگی و دستور بازتولید.
