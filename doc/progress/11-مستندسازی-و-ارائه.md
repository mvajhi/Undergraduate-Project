# فاز ۱۱ — مستندسازی و ارائه

نقشه‌ی فاز: بند ۱۱ `doc/WBS-food-demand-forecasting.md`.
**فهرست نهایی و قفل‌شده‌ی گزارش: `doc/report-outline.md`** (ردیف ۵۲ `decision_log`).

قالب تحویل: `BScThesis_template_new.docx` — پایان‌نامه‌ی کارشناسی، دانشکده مهندسی برق و
کامپیوتر، پردیس دانشکده‌های فنی دانشگاه تهران. هدف حجم: **۳۰–۴۰ صفحه A4 بدنه**.

---

## ۱۱.۱ گزارش فنی

- [x] نگاشت ساختار موضوعی ۱۱فصلی به قالب رسمی دانشگاه؛ تصمیم ۷ فصل محتوایی + مراجع + پیوست‌ها — `doc/report-outline.md`، ردیف ۵۲ `decision_log`
- [x] بودجه‌ی صفحه به‌تفکیک بند (بدنه ۳۸ ص) + ستون «فشرده‌پذیر تا» برای کنترل سقف ۴۰ — بند ۲ سند فهرست
- [x] استخراج قواعد قالب (پاورقی اصطلاح لاتین، «شکل (N-M)» زیر شکل، «جدول (N-M)» بالای جدول، «رابطه (N)»، مراجع IEEE، پیش‌متن و پس‌متن) — بند ۳ سند فهرست
- [x] نگاشت معکوس: تضمین اینکه هر ۱۱ فصل `doc/report-source-map.md` خانه دارد — بند ۷ سند فهرست
- [x] فهرست خلأها (چیزهایی که در هیچ سند مخزن نیست و باید تازه نوشته شود) — بند ۵ سند فهرست
- [ ] **فصل ۵ — پیاده‌سازی** (اولویت ۱ نگارش: بیشترین محتوای تازه، کمترین وابستگی)
  - [ ] ۵-۲ معماری مخزن و پشته‌ی فناوری
  - [ ] ۵-۳ گام‌های اجرای پروژه + شکل (۵-۱) جریان فازها و دروازه‌ها
  - [ ] ۵-۴ زیرساخت آزمایش و اجرای GPU
  - [ ] ۵-۵ روش‌شناسی اجرای پروژه با عامل‌های هوش مصنوعی (۵ زیربند، شامل درس‌آموخته‌های صادقانه‌ی فاز ۷)
  - [ ] ۵-۶ بازتولیدپذیری
- [ ] **بندهای ۲-۲ تا ۲-۵** — متن نظری تازه (Newsvendor و زیان نامتقارن، رگرسیون کوانتایل و کالیبراسیون، اعتبارسنجی زمانی و انواع نشت، جدول (۲-۱) دوازده آزمون آماری)
- [ ] **فصل ۶ — نتایج و تحلیل** (مواد آماده؛ کار = انتخاب و فشرده‌سازی)
- [ ] **فصل ۳ و ۴** — داده/کاوش/فیچر و روش/پروتکل
- [ ] **فصل ۱ و ۷** — آخر نوشته شوند تا با محتوای واقعی هم‌خوان باشند
- [ ] **فصل ۸ — مراجع** به سبک IEEE
- [ ] اجزای قالب: چکیده‌ی فارسی + کلمات کلیدی، Abstract + Keywords، فهرست علائم اختصاری، مقدمه و «خلاصه و جمع‌بندی» هر فصل، بند ۱-۷ ساختار پایان‌نامه، جلد فارسی و لاتین، تعهدنامه

## ۱۱.۲ کارت مدل (Model Card)

- [ ] **پیوست الف** — کاربرد مورد نظر، داده‌ی آموزش، معیارها، عملکرد به تفکیک زیرگروه، محدودیت‌ها، شرایطی که مدل نباید استفاده شود (تابستان، ترم جدید بدون بازآموزی، سلف تازه‌تأسیس)
  ⚠️ این یک **بسته‌کاری اجرانشده** است، نه صرفاً نگارش — زودتر شروع شود.

## ۱۱.۳ مجموعه نمودارهای کلیدی

- [ ] انتخاب حداکثر ۱۲ شکل از ۲۷ فایل `reports/figures/` (تخصیص پیشنهادی در بند ۶ سند فهرست)
- [ ] عنوان **گزاره‌ای** فارسی برای هر شکل (نه توصیفی)
- [ ] بررسی فونت فارسی صحیح در خروجی نهایی

## ۱۱.۴ مخزن کد قابل بازتولید

- [ ] `README.md`: نصب، ساختار، دستور بازتولید کامل (`make all`)، جدول نتایج مرجع
- [ ] **پیوست ث** — رجیستری کامل ویژگی‌ها + دستور بازتولید

## ۱۱.۵ ارائه به ذی‌نفعان

- [ ] نسخه‌ی فنی (استاد/داور): روش‌شناسی و اعتبارسنجی محور
- [ ] نسخه‌ی مدیریتی (معاونت دانشجویی): منحنی trade-off بند ۸.۵، قواعد ساده بند ۹.۵، عدد صرفه‌جویی با CI

## ۱۱.۶ فصل محدودیت‌ها

- [ ] بند ۷-۲-۲ گزارش — **با همان دقت نتایج نوشته شود** (فهرست اقلام الزامی در بند ۴ سند فهرست، فصل ۷)

---

## ضمانت مکانیکی

`python -m src.report_coverage` — خروجی در `reports/phase11/coverage_gaps.md`.
هر ردیف `decision_log`، هر حقیقت `F##` و هر گزارش `reports/*.md` باید در نقشه‌ی منبع یا
دفتر یافته‌ها ارجاع داشته باشد، وگرنه اسکریپت با exit code≠۰ شکست می‌دهد.

## پیوست‌های برنامه‌ریزی‌شده

الف کارت مدل · ب جدول کامل آزمون‌های آماری (شامل نتایج منفی) · پ فهرست ۵۲ ردیف تصمیم ·
ت دفتر حقایق و یافته‌ها · ث رجیستری ویژگی و دستور بازتولید · Abstract و جلد لاتین.
