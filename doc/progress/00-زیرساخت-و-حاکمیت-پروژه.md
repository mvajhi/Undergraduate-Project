# TODO — فاز ۰: زیرساخت و حاکمیت پروژه

> پیگیری وضعیت بسته‌های کاری فاز ۰ (`doc/WBS-food-demand-forecasting.md`). *چرا*ی هر تصمیم در `doc/decision_log.md` است؛ این فایل فقط *وضعیت* را نشان می‌دهد.

- [x] 0.1 محیط اجرا و مدیریت وابستگی — `requirements.lock` + `requirements.txt` + `src/config.py` (شاهد: decision_log #1)
- [x] 0.2 ساختار مخزن — `data/`, `notebooks/`, `src/`, `models/`, `reports/figures/`, `doc/` برپا شده
- [x] 0.3 نسخه‌بندی کد و داده — Git (`main`) + DVC روی `data/raw/*` (شاهد: decision_log #3, #4، `doc/data_manifest.md`)؛ ⚠️ باقی‌مانده: DVC remote هنوز تنظیم نشده (فقط cache محلی)
- [ ] 0.4 سامانه ثبت آزمایش — MLflow + MLflow MCP انتخاب و پیکربندی شد (شاهد: decision_log #2, #5، `.mcp.json`، هدف `make mlflow-ui`)؛ باقی‌مانده: هنوز هیچ run واقعی ثبت نشده (`mlruns/` هنوز ساخته نشده)
- [x] 0.5 دفترچه تصمیمات — `doc/decision_log.md` فعال و در حال به‌روزرسانی مستمر
- [x] 0.6 برنامه زمانی و نقاط کنترل — Milestone های M1–M5 و قاعده‌ی «قبل از M3 مدل پیچیده نه» در `CLAUDE.md` تثبیت شد

**معیار پایان فاز:** هر ۶ بسته‌کاری تیک بخورد. در حال حاضر ۵ از ۶ کامل است؛ فقط 0.4 باز است.
