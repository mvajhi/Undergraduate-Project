# TODO — فاز ۸: ارزیابی و تحلیل خطا

> پیگیری وضعیت بندهای فاز ۸ (`doc/WBS-food-demand-forecasting.md` بند 8.1–8.10).
> ورودی این فاز: قهرمانان تأییدشده‌ی فاز ۷ (`reports/phase7/model_comparison.md`، ردیف‌های ۴۰/۴۶ decision_log).
> کد: `src/cv.py::holdout_split` (تا امروز هرگز فراخوانی نشده — پنجره‌ی انتهایی هنوز دست‌نخورده)
> ⚠️ **نقطه‌ی بی‌بازگشت:** طبق ماتریس وابستگی WBS، پس از باز کردن Test در بند ۸-۱، دیگر نمی‌توان مدل را تغییر داد.
> هر آیتم با تیک‌خوردن باید شاهدش (فایل/commit/ردیف decision_log) کنار خودش ذکر شود — طبق قرارداد `AGENTS.md`.

## 8.0 آماده‌سازی پیش از باز کردن Test

- [x] انتخاب «حداکثر ۳ مدل + baseline ها» طبق سقف بند 8.1 WBS — نامزدها بررسی و ثبت شدند
      در `doc/decision_log.md` ردیف ۴۸: `lightgbm_quantile` (قهرمان مطلق)، `bhm_varying_dispersion`
      (نزدیک‌ترین رقیب، با پرچم بدهی همگرایی)، `catboost_quantile` (سومین برنده‌ی CPU-محض،
      کامل با بوت‌استرپ). خط پایه‌های مقایسه: B2 و B3 (طبق بند 8.7 WBS)
- [x] تأیید صریح کاربر برای باز کردن Test — ۲۰۲۶-۰۸-۲۶، گفت‌وگوی همین نشست

## 8.1 ارزیابی نهایی روی Test قفل‌شده

- [x] `holdout_split` یک‌بار فراخوانی شد (train تا ۲۰۲۴-۰۴-۲۲ → test ۲۰۲۴-۰۴-۲۳..۲۰۲۴-۰۵-۲۱)؛
      هر ۳ مدل + B2 + B3 با بازبرازش واقعی ارزیابی شدند (تمام معیارهای بند 6.4 بجز MASE/CI ریالی —
      این دو در بند ۸.۶/۸.۷ تکمیل می‌شوند). **نتیجه‌ی غافلگیرکننده:** روی این پنجره‌ی خاص
      `bhm_varying_dispersion` (۰.۰۰۹۵۰) نه‌فقط قهرمان که تنها برد آماری‌معنادار در برابر
      B3 است — برخلاف رتبه‌بندی رسمی فاز ۷ (`lightgbm_quantile` قهرمان). طبق طبقه‌بندی
      خودِ `cv.py` این پنجره «آزمون فشار» است (رژیم غیرعادی: بازگشت از رمضان + سوگواری
      ملی)، **نه معیار انتخاب** — قهرمان رسمی تغییر نمی‌کند. ردیف ۴۹ decision_log ·
      `reports/phase8/8.1_final_holdout_evaluation.md/.csv`

## 8.2 تحلیل باقیمانده

- [x] هیستوگرام/Q-Q + باقیمانده در برابر پیش‌بینی/Res/تاریخ/سلف/روز هفته — روی قهرمان
      رسمی (`lightgbm_quantile`) و پیش‌بینی‌های ذخیره‌شده‌ی بند ۸.۱ (بدون بازبرازش تازه).
      میانگین باقیمانده +۰.۰۲۴۷۵ (بایاس کم‌برآورد خفیف)، چولگی +۴.۲۲، Shapiro-Wilk
      نرمالیتی را رد می‌کند (p≈۰) — سازگار با توزیع ذاتاً چوله‌ی ρ (F02 فاز ۴).
      `src/models/run_phase8_residuals.py` → `reports/phase8/8.2_residual_analysis.md`
      + ۶ نمودار در `reports/figures/phase8/`
- [x] ACF باقیمانده (سری روزانه، میانگین روی سلف×وعده) + Ljung-Box — **سفید**
      (p=۰.۶۹۱۰ روی ۲۵ روز/۱۰ وقفه؛ توان کم به‌دلیل طول کوتاه سری صریحاً ذکر شد) —
      هیچ الگوی زمانی سیستماتیک جامانده در باقیمانده‌ی روزانه‌ی پنجره‌ی Test نیست

## 8.3 تجزیه خطا (Error Decomposition)

- [x] جدول عملکرد به تفکیک سلف/وعده/روز هفته/چارک Res/نوع غذا/نوع روز/ماه‌به‌ماه —
      `src/models/run_phase8_error_decomposition.py` → `reports/phase8/8.3_error_decomposition.md`
      + ۷ CSV. **الگوی سراسری:** شکاف پوشش مثبت (+۲٪ تا +۳۰٪) در تقریباً همه‌ی برش‌ها —
      قهرمان روی این پنجره سیستماتیک بیش از τ=۰.۲۰ پوشش می‌دهد (سازگار با نبودِ ACI روی
      Test، ردیف ۴۹ decision_log؛ ورودی مستقیم برای بند ۸.۸). Q4 حجم‌بالا بهترین pinball
      و بیشترین نرخ کمبود هم‌زمان دارد (۳۲.۷٪) — نشانه‌ی هزینه‌ی واقعی کمبود در
      سلف‌های پرحجم بالاتر از عدد pinball خام نشان می‌دهد. drift ماه‌به‌ماه مثبت
      (Δ=+۰.۰۰۲۰۴، فقط ۲ ماه/۷۱ ردیف در ماه دوم — نمونه‌ی کوچک)
- [ ] (مدل B در فهرست نامزدهای ۸.۱ نیست؛ این زیربند به بند ۸.۱۰ موکول شد)

## 8.4 تحلیل بدترین موارد

- [x] ۲۰ پیش‌بینی با بیشترین pinball + ۲۰ بدترین کمبود جداگانه —
      `src/models/run_phase8_worst_cases.py` → `reports/phase8/8.4_worst_cases.md`.
      **دو یافته‌ی مهم:** (۱) بدترین ۲۰ pinball روی چهارشنبه‌ها متمرکزند (۴۵٪ در برابر
      پایه‌ی ۱۸٪) — الگوی روز هفته که در بند ۸.۳ هم دیده شد (چهارشنبه دومین pinball
      میانگین بدتر بعد از سه‌شنبه)، سرنخ برای فیچر/بررسی بیشتر؛ اکثر این موارد رزرو
      کم‌حجم با جهش ناگهانی نرخ عدم‌دریافت واقعی هستند (مدل عقب می‌ماند، نه جلو).
      (۲) **صفر همپوشانی** بین ۲۰ بدترین pinball و ۲۰ بدترین کمبود مطلق — بدترین
      کمبودهای مطلق همه در سلف‌های پرحجم (Res=۵۰۰-۱۸۰۰) با خطای نسبی **کوچک** رخ
      می‌دهند (چون واحد پرس با Res مقیاس می‌گیرد)، نه در سلف‌هایی که pinball بد دارند.
      پیامد عملیاتی: پایش کیفیت مدل صرفاً با pinball میانگین، بزرگ‌ترین ریسک کسب‌وکاری
      (کمبود مطلق در سلف‌های پرحجم) را نشان نمی‌دهد — باید هر دو معیار جداگانه پایش شوند

## 8.5 شبیه‌سازی عملیاتی (Backtest کسب‌وکاری)

- [ ] پرس/ریال صرفه‌جویی‌شده، تعداد روز-سلف-وعده با کمبود
- [ ] منحنی trade-off (نرخ کمبود × صرفه‌جویی، هر نقطه یک τ) — نمودار کلیدی مدیریتی

## 8.6 پایداری و حساسیت

- [ ] پایداری بین fold ها (std معیار اصلی)
- [ ] حساسیت به seed (۵ seed)
- [ ] آزمون تنش (حذف یک فیچر مهم)
- [ ] جدول کامل حساسیت به τ

## 8.7 مقایسه آماری با خط پایه

- [ ] DM-test + بوت‌استرپ CI در برابر B2 و B3 روی Test — گزاره‌ی نهایی با Δ و CI

## 8.8 کالیبراسیون کوانتایل

- [ ] نمودار قابلیت اطمینان (τ∈{0.05..0.95})؛ در صورت ناکالیبره‌بودن، بازکالیبراسیون (ACI طبق یافته‌ی ۲۲ فاز ۷)

## 8.9 بررسی Overfit / Underfit

- [ ] منحنی یادگیری + شکاف train-test

## 8.10 مقایسه نهایی سطح تجمیعی (L1/L2) در برابر سطح فرد (L5)

- [ ] بهترین پیکربندی L1/L2 در برابر بهترین L5 روی همان Test، DM + بوت‌استرپ دوبعدی، تفکیک ناهار/شام (برش اجباری)
- [ ] یکی از سه نتیجه‌ی ممکن (بند 8.10 WBS) با عدد و CI مستند شد

## دروازه M5

- [ ] Test فقط یک‌بار باز شده (بند 8.1)
- [ ] منحنی trade-off رسم شده (بند 8.5)
- [ ] محدودیت‌ها با همان دقت نتایج نوشته شده‌اند
- [ ] نتیجه‌ی مقایسه‌ی L1/L2 در برابر L5 (بند 8.10) با CI مستند شده
