# TODO — فاز ۱۰: لایه تصمیم و تحلیل کسب‌وکاری

> پیگیری وضعیت بندهای فاز ۱۰ (`doc/WBS-food-demand-forecasting.md` بند 10.1–10.6).
> ورودی: قهرمان رسمی (`lightgbm_quantile`) + ارزیابی Test بند ۸.۱/۸.۵ + معماری
> `output_aggregation="per_food"` (`src/models/axes.py`، پیش‌فرض همان سطحی که قهرمان
> رویش آموزش دید — بند ۶.۷ محور آزمایش، نه تصمیم جدید اینجا).
> هر آیتم با تیک‌خوردن باید شاهدش کنار خودش ذکر شود — طبق `AGENTS.md`.

## 10.1 تبدیل پیش‌بینی به عدد پخت

- [x] فرمول CookQty (`src/baselines.py::cook_qty`) + مثال کاربردی از Test واقعی —
      `reports/phase10/10.1_cook_quantity.md`. ⚠️ **قیود عملیاتی (کف/سقف/مضرب
      بسته/ظرفیت) هیچ‌کدام فعال نیستند** — قبلاً با ذی‌نفع روشن شده
      (`project-definition.md` خط ۲۲۲): «خروجی مدل عدد دقیق است». تجمیع روی f لازم
      نیست چون قهرمان مستقیماً روی سطح $(d,m,r,f)$ آموزش دیده (`output_aggregation=per_food`)

## 10.2 بهینه‌سازی Newsvendor

- [x] جدول τ*_ρ (نظری، ۱۱ نسبت Cu/Co از ۱ تا ۹۹) + تحلیل نقطه‌ی سربه‌سر تجربی روی
      Test — `src/models/run_phase10_newsvendor.py` → `reports/phase10/10.2_newsvendor.md`.
      **در برابر B0:** هیچ نقطه‌ی سربه‌سری در کل بازه‌ی آزموده‌شده (Cu/Co=۲ تا ۴۹) پیدا
      نشد — سود خالص همیشه مثبت. **در برابر B3:** نتیجه مخلوط و نزدیک صفر است (در
      τ=۰.۲۰ دقیقاً −۰.۰۰۳ میلیارد تومان)، سازگار با یافته‌ی بند ۸.۱ که روی همین پنجره
      برد قهرمان بر B3 از نظر آماری معنادار نبود — این عدد جدید همان یافته را در واحد
      کسب‌وکاری تکرار می‌کند، نه یک تناقض تازه

## 10.3 سناریوهای سیاستی

- [x] جدول محافظه‌کارانه(τ=۰.۰۲)/متعادل(τ=۰.۱۰)/تهاجمی(τ=۰.۲۰) با نرخ کمبود و صرفه‌جویی
      سالانه — `src/models/run_phase10_annual_savings.py` → `reports/phase10/10.3_policy_scenarios.md`.
      پایه: OOF قهرمان روی ۵ fold رسمی (نه Test غیرعادی). نرخ کمبود: ۱.۴٪/۷.۹٪/۱۵.۴٪؛
      صرفه‌جویی سالانه: ۱۱.۶/۱۷.۵/۱۹.۸ میلیارد تومان

## 10.4 برآورد صرفه‌جویی با عدم‌قطعیت

- [x] تعمیم سالانه با ۵ فرض صریح (پایه‌ی fold رسمی نه Test، ۳۲ هفته‌ی آموزشی نه ۵۲،
      حجم/قیمت ثابت، تکرار الگوی هفتگی) + بوت‌استرپ بلوکی دوبعدی (روز×سلف، ۱۰۰۰ تکرار) —
      `reports/phase10/10.4_annual_savings.md`. **نقطه‌ی عملیاتی τ=۰.۲۰: ۱۹.۸۲ میلیارد
      تومان [CI۹۵٪: ۱۴.۱۲, ۲۶.۰۴]**. عبارت صادقانه‌ی نهایی دقیقاً طبق الگوی WBS نوشته شد
      (نه یک عدد قطعی). ⚠️ آماره‌ی بوت‌استرپ **میانگین ردیفی** انتخاب شد نه نسبت
      «مجموع/روز-یکتا» — دومی زیر بوت‌استرپ بلوکی سوگیری رو به بالا نشان داد (نقطه‌ی
      واقعی زیر کل بازه‌ی CI می‌افتاد)، شاهد عددی در کد ثبت شد

## 10.5 پروتکل پیشنهادی پیاده‌سازی آزمایشی

- [x] طرح پایلوت با **دو جفت همتاشده** (پایلوت+کنترل از هر دو خوشه‌ی پایدار F41)،
      ۴ هفته، پروتکل روزانه و سنجه‌های موفقیت — `reports/phase10/10.5_pilot_protocol.md`.
      سلف‌ها روی داده‌ی واقعی انتخاب شدند (حجم نزدیک درون هر جفت): مدیریت/علوم (خوشه B،
      فقط ناهار) و کشاورزی/کوی (خوشه A، ناهار+شام). ⚠️ **هشدار توان آماری صریح ثبت شد**:
      ۷۲ مشاهده در هر بازو ⇒ این یک آزمایش شدنی‌بودن عملیاتی است، نه آزمایشی با قدرت
      کافی برای برآورد دقیق اندازه‌ی اثر؛ و طراحی شبه‌آزمایشی است نه RCT

## 10.6 بسط اختیاری: Newsvendor چندکالایی

- [x] یادداشت کار آینده — `reports/phase10/10.6_multi_item_newsvendor_note.md`.
      خارج از محدوده چون ظرفیت آشپزخانه طبق ذی‌نفع قید فعالی نیست (بند ۱۰.۱)، پس فرض
      استقلال کوانتایل دو نوع غذا بی‌ضرر است؛ شرط بازبینی آینده صریح نوشته شد

---

**فاز ۱۰ کامل شد** (بندهای ۱۰.۱ تا ۱۰.۶). با فازهای ۸ و ۹، هر چهار شرط **دروازه‌ی M5**
برآورده شده‌اند.
