# ساختار شکست کار (WBS)
## پروژه: بهینه‌سازی پیش‌بینی میزان پخت غذا در سلف‌های دانشگاه تهران

**نسخه:** ۲.۰ | **مبنا:** سند تعریف مسئله (نسخه به‌روزشده با داده سطح فرد) | **واحد تحلیل:** $(d, m, r, f)$ برای مدل A، $(p, d, m, r, f)$ برای مدل B

> **قرارداد شماره‌گذاری:** کدهای WBS با ارقام لاتین (`4.3.2`) نوشته شده‌اند تا در گزارش، commit message و برنامه زمانی قابل ارجاع مستقیم باشند.
> هر بسته‌کاری سه بخش دارد: **فضای کاوش** (چه گزینه‌هایی باید بررسی شود)، **معیار پایان** (کِی این بسته تمام است)، **خروجی** (چه فایل/سندی تولید می‌شود).

> **مسیر مدل B:** با دسترسی به داده خام سطح رزرو فردی (`PersonId`)، این WBS یک مسیر مدل‌سازی موازی («مدل B» — طبقه‌بندی سطح فرد) را در تمام فازهای مرتبط تعریف می‌کند (بند ۱.۶).

> **پیگیری وضعیت (`doc/progress/`):** با شروع هر فاز، یک فایل `doc/progress/{شماره‌ی دو‌رقمی فاز}-{عنوان-فاز}.md` ساخته می‌شود، شامل یک چک‌لیست با یک آیتم به‌ازای هر بسته‌کاری آن فاز (همان زیربندهای این سند، مثلاً 3.1 تا 3.12 برای فاز ۳). با تکمیل هر بسته‌کاری، تیک می‌خورد و شاهدش (commit، فایل خروجی، یا ردیف `doc/decision_log.md`) ذکر می‌شود. این جایگزین `decision_log.md` نیست — decision_log برای *چرا*ی تصمیم‌هاست، `doc/progress/` فقط برای *وضعیت پیشرفت* کارها.

---

## نقشه کلان فازها

| فاز | عنوان | ماهیت | وزن تلاش تقریبی |
|---|---|---|---|
| 0 | زیرساخت و حاکمیت پروژه | یک‌باره | ۵٪ |
| 1 | تثبیت و تکمیل تعریف مسئله | تحلیلی | ۵٪ |
| 2 | جمع‌آوری و غنی‌سازی داده | اکتشافی-عملیاتی | ۱۵٪ |
| 3 | پاکسازی و یکپارچه‌سازی | مهندسی | ۱۵٪ |
| 4 | کاوش اکتشافی داده (EDA) | تحلیلی-آماری | ۲۰٪ |
| 5 | مهندسی ویژگی | مهندسی-خلاقانه | ۱۰٪ |
| 6 | طراحی پروتکل اعتبارسنجی | متدولوژیک | ۵٪ |
| 7 | مدل‌سازی و تنظیم | آزمایشی | ۱۵٪ |
| 8 | ارزیابی و تحلیل خطا | تحلیلی | ۵٪ |
| 9 | تفسیرپذیری | تحلیلی | ۳٪ |
| 10 | لایه تصمیم و تحلیل کسب‌وکاری | کاربردی | ۲٪ |
| 11 | مستندسازی و ارائه | گزارشی | موازی |
| 12 | استقرار و پایش | خارج از محدوده | — |

> **یادداشت وزن‌دهی:** اضافه‌شدن مسیر مدل B به فازهای ۲، ۳، ۴، ۵، ۶، ۷، ۸ و ۹ حجم کار هرکدام را افزایش می‌دهد (تخمین: ۱۵-۲۵٪ کار اضافه در هر فاز لمس‌شده)، ولی ترتیب و وزن نسبی کلی فازها را تغییر نمی‌دهد چون مدل B مسیر **موازی**، نه جایگزین، مدل A است.

> **قاعده حاکم بر کل WBS:** فرآیند **حلقوی** است نه خطی. مسیرهای بازگشت مجاز و پیش‌بینی‌شده: `4 → 2` (نیاز به داده جدید)، `5 → 4` (فرضیه جدید)، `8 → 5` (خطای ساختاری در ویژگی‌ها)، `9 → 3` (کشف اشکال داده‌ای)، `4.13 → 2.1.2` (نیاز به داده فردی تکمیلی مثل زمان ثبت رزرو).

---

# فاز ۰ — زیرساخت و حاکمیت پروژه

هدف: هر عددی که در گزارش نهایی می‌نویسید باید ۶ ماه بعد با یک دستور بازتولید شود.

### 0.1 محیط اجرا و مدیریت وابستگی
- **فضای کاوش:** `venv` + `requirements.txt` / `conda` / `uv` + `pyproject.toml` / Docker.
- توصیه برای پروژه کارشناسی: `uv` یا `venv` + فایل قفل‌شده نسخه‌ها (`pip freeze`).
- تثبیت `random_seed` سراسری (numpy, python, sklearn, lightgbm, torch) در یک ماژول `config.py`.
- **خروجی:** `requirements.lock`, `src/config.py`

### 0.2 ساختار مخزن
```
project/
├── data/
│   ├── raw/         # فقط-خواندنی، هرگز تغییر نمی‌کند
│   ├── external/    # هوا، تقویم، AQI
│   ├── interim/     # میانی
│   └── processed/   # آماده مدل‌سازی (snapshot قفل‌شده)
├── notebooks/       # شماره‌گذاری‌شده: 01_eda_univariate.ipynb
├── src/             # کد قابل import (پاکسازی، فیچر، مدل، ارزیابی)
├── models/          # artifact های آموزش‌دیده
├── reports/figures/
└── docs/            # سند مسئله، لاگ تصمیم، دیکشنری داده
```
- **قاعده:** هیچ منطق مهمی فقط داخل نوت‌بوک نماند؛ نوت‌بوک = روایت، `src/` = حقیقت.

### 0.3 نسخه‌بندی کد و داده
- Git برای کد؛ برای داده: DVC یا (ساده‌تر) ثبت **هش SHA-256** هر فایل خام در `docs/data_manifest.md`.
- **معیار پایان:** هر snapshot داده یک شناسه دارد که در گزارش قابل ذکر است.

### 0.4 سامانه ثبت آزمایش
- **فضای کاوش:** MLflow / Weights & Biases / یک `experiments.csv` ساخت‌یافته.
- حداقل فیلدهای اجباری هر رکورد آزمایش: `exp_id, timestamp, git_commit, data_snapshot_hash, feature_set_id, model_family, hyperparams, cv_scheme, pinball@τ, RMSE, MAE, shortage_rate, notes`.
- **چرا حیاتی است:** بدون این، مقایسه ۴۰-۵۰ آزمایش در فاز ۷ غیرقابل‌دفاع می‌شود.

### 0.5 دفترچه تصمیمات (Decision Log)
- هر تصمیم روش‌شناختی با فرمت: `تصمیم | تاریخ | گزینه‌های بررسی‌شده | دلیل انتخاب | اثر بر نتیجه`.
- **خروجی:** `docs/decision_log.md` — این سند مستقیماً به فصل «روش‌شناسی» گزارش نهایی تبدیل می‌شود.

### 0.6 برنامه زمانی و نقاط کنترل
- Milestone های پیشنهادی: M1 پایان فاز ۳ (داده قفل‌شده)، M2 پایان فاز ۴ (دفتر حقایق)، M3 پایان فاز ۶ (خط پایه + پروتکل)، M4 پایان فاز ۷، M5 گزارش نهایی.
- **قاعده انضباطی:** قبل از M3 هیچ مدل پیچیده‌ای آموزش داده نشود.

---

# فاز ۱ — تثبیت و تکمیل تعریف مسئله

سند تعریف مسئله موجود است؛ این فاز آن را از «سند» به «قرارداد قابل‌اجرا» تبدیل می‌کند.

### 1.1 تثبیت متغیر هدف و بررسی جایگزین‌ها
- **گزینه A (فعلی):** نرخ $\rho = NoRecv/Res$ — مزیت: نرمال‌سازی مقیاس.
- **گزینه B:** تعداد مطلق $NoRecv$ با وزن‌دهی.
- **گزینه C (پیشنهاد جدی):** مدل‌سازی **دوجمله‌ای** — $NoRecv \sim \text{Binomial}(Res, p)$. این طبیعتاً ناهم‌واریانسی را حل می‌کند و مشکل «نرخ بی‌ثبات در رزروهای کوچک» (ریسک شماره ۳ سند شما) را از ریشه می‌زند، چون واریانس ذاتاً $Res \cdot p(1-p)$ مدل می‌شود.
- **گزینه D:** هدف مستقیم $Recv$ (تعداد دریافت‌شده) — نزدیک‌تر به تصمیم واقعی، اما رابطه بدیهی با $Res$ را به مدل تحمیل می‌کند.
- **کار:** هر چهار گزینه در فاز ۷ به‌صورت تجربی مقایسه شوند، ولی یکی به‌عنوان مسیر اصلی تثبیت شود.
- **خروجی:** بند تثبیت‌شده در `doc/decision_log.md` (ردیف ۸) — تحلیل کامل (۹ گزینه از WBS + ادبیات، جدول مقایسه، توصیه‌ی نهایی) در `doc/decisions/08-target-variable.md`. تصمیم: گزینه‌ی A (نرخ $\rho$) با وزن‌دهی $\sqrt{Res}$ و لایه‌ی CQR؛ گزینه‌ی C (Binomial/Beta-Binomial) رقیب جدی فاز ۷ باقی می‌ماند.

### 1.2 تحلیل اقتصادی $C_u$ و $C_o$ (تعیین $\tau$) — فرآیند تصمیم تثبیت‌شده ۲۰۲۶-۰۸-۱۲
- $C_o$ = هزینه پرس مازاد ≈ ۱۲۰ هزار تومان (هزینه تمام‌شده — باید تفکیک شود به مواد اولیه + دستمزد + سربار).
- $C_u$ = هزینه کمبود — **مؤلفه‌های آن را صریح فهرست کنید:** جبران فوری (غذای جایگزین/بن)، نارضایتی و شکایت دانشجویی، هزینه اعتباری مدیریت، ریسک عدم‌تکرار رزرو.
- **فرآیند تصمیم (۴ گام، به‌جای تعیین یک عدد قطعی در همین فاز):**
  1. **همین فاز (فاز ۱):** فقط چارچوب سناریوها تثبیت می‌شود — $\tau \in \{0.02, 0.05, 0.10, 0.15, 0.20\}$ (پخت به‌ترتیب ۲٪ تا ۲۰٪ کمتر از رزرو) به‌عنوان فضای کاوش رسمی ثبت می‌شود، نه یک عدد نهایی.
  2. **پس از EDA (فاز ۴)، پیش از قفل پروتکل اعتبارسنجی (فاز ۶):** سند مجزای `reports/tau_sensitivity.md` هر سناریو را روی داده‌ی واقعی ۵ ماهه محک می‌زند و برای هرکدام دقیقاً دو عدد گزارش می‌کند: (الف) ٪ صرفه‌جویی در غذای مازاد نسبت به baseline B0 «پخت = کل رزرو» (بند ۶-۵)، (ب) ٪ مواردی که پخت پیشنهادی کمتر از دریافت واقعی بود (رخداد کمبود).
  3. همان سند بر اساس این دو عدد یک نتیجه‌گیری صریح می‌دهد: کدام سناریو تعادل مناسبی بین «کاهش هدررفت» و «جلوگیری از کمبود» ایجاد می‌کند و به‌عنوان پیشنهاد اصلی معرفی می‌شود — با این اصل راهنما که **کمبود باید همیشه نادر بماند، حتی به قیمت صرفه‌جویی کمتر.**
  4. انتخاب نهایی از بین سناریوها یک **تصمیم سیاستی** است، نه صرفاً یک نتیجه‌ی آماری، و باید به تأیید مدیریت سلف برسد؛ تیم پروژه فقط تحلیل و توصیه را تهیه می‌کند.
- **ارتباط با فاز ۶:** این سند باید پیش از معیار پایان M3 (بند 6.8) آماده باشد، چون $\tau$ مستقیماً روی معیار اصلی (Pinball Loss @ τ) و جدول baselines (بند 6.5) اثر می‌گذارد؛ تا دریافت تأیید مدیریت سلف، $\tau=0.10$ (بند ۲-۲ سند مسئله) مقدار پیش‌فرض قابل‌تنظیم است.
- **خروجی:** `reports/tau_sensitivity.md` (گام ۲-۳)؛ تصمیم نهایی (یا وضعیت «در انتظار تأیید مدیریت») در `doc/decision_log.md` ثبت می‌شود.

### 1.3 قرارداد خروجی (Output Contract) — پاسخ‌داده‌شده ۲۰۲۶-۰۸-۱۲
- **کِی؟** ساعت ۱۵ روز $d-1$ برای ناهار روز $d$، و ساعت ۲۳ روز $d-1$ برای شام روز $d$. **⚠️ پیامد فنی مستقیم روی قاعده‌ی برش اطلاعاتی (بند ۴-۲ سند مسئله):** لحظه‌ی برش برای دو وعده یکسان نیست. در ساعت ۱۵ روز $d-1$ (لحظه‌ی تصمیم برای ناهار)، شام روز $d-1$ هنوز سرو نشده — یعنی $Recv/NoRecv/\rho$ شام $d-1$ **در دسترس نیست**، هرچند تاریخش $d-1 \le d-1$ است و طبق فرمول ساده‌ی «هر $d'\le d-1$» به‌اشتباه مجاز به نظر می‌رسد. برای پیش‌بینی شام $d$ در ساعت ۲۳ روز $d-1$، شام $d-1$ (که پیش‌تر همان روز سرو شده) در دسترس است. جزئیات و جدول اصلاح‌شده در بند ۴-۲ سند مسئله و ردیف ۹ `doc/decision_log.md`. این نکته باید در فاز ۵ (مهندسی ویژگی، هنگام ساخت فیچرهای lag) و فاز ۳-۶ (ممیزی نشت) صریحاً بررسی شود چون منبع رایج‌ترین باگ نشتی است.
- **چه چیزی؟** یک عدد صحیح به ازای $(d, m, r, f)$ + جمع آن روی $f$. عدد باید به تفکیک نوع غذا اعلام شود (این الزامی است). **سؤال باز برای فاز ۵/۶:** آیا مناسب‌تر است یک نرخ عدم‌دریافت کلی در سطح سلف×وعده $\rho_{d,m,r}$ برآورد و سپس بر اساس سهم هر غذا از رزرو به دو عدد $(d,m,r,f)$ تجزیه شود، به‌جای برآورد مستقل $\rho_{d,m,r,f}$ برای هر غذا؟ این گزینه‌ی سوم، مکمل دو راه‌حل تطبیق سلسله‌مراتبی زیر است و باید در فاز ۶ با آن‌ها مقایسه شود.
- **با چه قید عملیاتی؟** خیر — نه پخت در بسته‌ی گسسته انجام می‌شود و نه کف/سقف ظرفیت آشپزخانه محدودیتی است؛ عدد دقیق (بدون گرد کردن به مضرب خاص) اعلام می‌شود. این قید را از فضای طراحی حذف کنید.
- **⚠️ نکته فنی حیاتی:** سند شما می‌گوید خروجی «با جمع روی $f$» اعلام می‌شود. **جمع کوانتایل‌ها ≠ کوانتایل جمع.** اگر برای هر غذا کوانتایل ۹۰٪ بگیرید و جمع بزنید، جمع حاصل محافظه‌کارتر از کوانتایل ۹۰٪ مجموع است (چون خطاها همبستگی کامل ندارند). دو راه‌حل: (الف) مدل جداگانه در سطح تجمیع‌شده $(d,m,r)$، (ب) تطبیق سلسله‌مراتبی (Hierarchical Reconciliation: MinT / bottom-up). این باید در فاز ۶ تصمیم‌گیری و در فاز ۱۰ اعمال شود.

### 1.4 پرسش‌نامه ذی‌نفعان — پاسخ‌داده‌شده ۲۰۲۶-۰۸-۱۲ (پاسخ‌گو: کاربر/صاحب پروژه، نه معاونت دانشجویی)
سؤالاتی که پاسخشان مسیر پروژه را تغییر می‌دهد:
1. آیا امکان **پخت مرحله‌ای/تأخیری** وجود دارد (پخت ۸۰٪ + ۲۰٪ رزرو)؟ **خیر.** مسئله تک‌تصمیمی (single-shot newsvendor) می‌ماند، نه دومرحله‌ای — فرمول‌بندی بند ۴-۱ سند مسئله بدون تغییر تثبیت شد.
2. غذای مازاد چه سرنوشتی دارد؟ **خارج از محدوده‌ی این پروژه** — کاربر خودش $C_o$ واقعی را جداگانه لحاظ می‌کند؛ این پروژه همچنان از تقریب ثابت ~۱۲۰ هزار تومان (بند ۲-۲ سند مسئله) استفاده می‌کند و نیازی به تفکیک دورریز/فروش/اهدا نیست.
3. آیا دانشجو می‌تواند رزرو را لغو کند؟ **خیر.** نیازی به مدل‌سازی لغو یا فیچر مرتبط با آن نیست؛ $Reservation$ ثبت‌شده تا $d+2$ عدد نهایی و بدون تغییر بعدی است (سازگار با فرض بند ۵-۲ سند مسئله).
4. آیا سیاست جریمه عدم‌دریافت وجود دارد یا در بازه داده تغییر کرده؟ **خیر، در نظر گرفته نشود.** ریسک Concept Drift ناشی از تغییر سیاست جریمه (بند ۵-۳ سند مسئله) از فهرست ریسک‌های فعال حذف/کم‌اولویت می‌شود؛ اگر شکست رژیم در فاز ۴-۳-۵ کشف شود، منبع آن سیاست جریمه نیست و باید توضیح دیگری جست‌وجو شود.
5. ظرفیت اسمی هر سلف چقدر است؟ **محدودیت مهمی نیست** — سلف توانایی پاسخ‌گویی به سقف نیاز دانشکده‌های تحت پوشش را دارد؛ فیچر/قید ظرفیت از فضای کاوش مهندسی ویژگی حذف می‌شود (سازگار با پاسخ بند ۱-۳ بالا).

### 1.5 مرور ادبیات هدفمند
- کلیدواژه‌ها: `food waste forecasting university canteen`, `newsvendor demand forecasting`, `no-show prediction`, `quantile regression demand`, `hierarchical time series reconciliation`, `data-driven newsvendor machine learning`, `censored newsvendor`, `patient no-show classification imbalance`, `individual reservation history feature importance`.
- **لنگرهای شروع (برای پرکردن سریع‌تر جدول ۱۰-۱۵ منبع):**
  - Ban & Rudin (۲۰۱۹, *Big Data Newsvendor*) — معادل‌سازی newsvendor با کمینه‌سازی ریسک تجربی/رگرسیون کوانتایل تک‌مرحله‌ای؛ مبنای مستقیم فرمول‌بندی بند ۲-۲ سند مسئله.
  - Huber و همکاران (۲۰۱۹) — درخت‌های تصمیم افزایشی با رگرسیون کوانتایل برای سفارش مستقیم؛ الگوی خانواده ۲ (بند ۷-۲).
  - Posch و همکاران (۲۰۲۱) — GAM بیزی با توزیع دوجمله‌ای منفی برای فروش سلف/رستوران، با فصلی‌بودن چندگانه و پرت؛ الگوی مستقیم خانواده ۵ (بند ۷-۵).
  - Zhai و همکاران (۲۰۲۳) — بیش‌رزروی هتل مبتنی بر طبقه‌بندی احتمال no-show در سطح مشتری منفرد؛ الگوی مستقیم مدل B (بند ۱-۶، ۷-۱۱).
  - ادبیات no-show بیمارستانی/کلینیکی (چند منبع مستقل) — تاریخچه رفتاری فرد (٪no-show قبلی، تعداد نوبت‌های ازدست‌رفته قبلی) به‌طور مکرر از قوی‌ترین فیچرها گزارش شده؛ توجیه اولویت بالای بند ۵-۱۶-۱.
  - مطالعات چندگانه پیش‌بینی تقاضای سلف دانشجویی با XGBoost/LSTM/Prophet و GAM بیزی — گزارش کاهش هدررفت در بازه‌ی تقریبی ۶ تا ۵۰ درصد بسته به محیط؛ محک واقع‌بینانه برای بند ۱۰-۴.
- **خروجی:** جدول ۱۰-۱۵ منبع با ستون‌های: مسئله، داده، روش، معیار، نتیجه، تفاوت با ما — تکمیل‌شده در `doc/Literature_Review.md` (فصل کامل، ۱۵ منبع، بند ۲-۴ آن سند شامل جدول مقایسه‌ای).
- **راهنمای روش کار:** `doc/other/Literature_Review_Structure.md` — روند ۶ گامی جستجو/غربالگری/استخراج/ماتریس مقایسه‌ای/نگارش ترکیبی/توجیه انتخاب، به‌همراه نمونه ماتریس با همان ستون‌بندی بالا.

### 1.6 تثبیت مسیر دوگانه مدل‌سازی: مدل A (تجمیعی) و مدل B (سطح فرد)
با تأیید دسترسی به داده خام سطح رزرو فردی (`PersonId`)، سند تعریف مسئله (نسخه به‌روزشده، بند ۴-۱-الف) دو مسیر موازی برای رسیدن به همان خروجی عملیاتی $(d,m,r,f)$ تعریف کرده است:
- **مدل A:** رگرسیون کوانتایل روی نرخ تجمیعی $\rho_{d,m,r,f}$ — مسیر اصلی نسخه ۱ این سند، بدون تغییر.
- **مدل B:** طبقه‌بندی احتمال عدم‌دریافت در سطح هر رزرو منفرد $P(ReserveStatus_{p,d,m,r,f})$، با تاریخچه رفتاری فرد و فیچرهای جمعیتی (دانشکده/رشته/مقطع/خوابگاه)، سپس تجمیع آماری به سطح $(d,m,r,f)$.
- **این WBS از این بند به بعد، هر فاز مرتبط را برای هر دو مسیر بسط داده است** — بندهای اختصاصی مدل B با «مدل B» علامت‌گذاری شده‌اند.
- **معیار داوری نهایی بین A و B:** هر دو با معیار یکسان Pinball Loss در سطح $(d,m,r,f)$ (بند ۲-۲ سند مسئله) و با آزمون معناداری آماری (بند ۶-۶) مقایسه می‌شوند؛ A و B رقیب نیستند بلکه مسیرهای مکمل‌اند و می‌توانند ترکیب (Ensemble) شوند (بند ۷-۹، ۸-۱۰).
- **خروجی:** بند تثبیت‌شده در `docs/decision_log.md`.

---

# فاز ۲ — جمع‌آوری و غنی‌سازی داده

این فاز جایی است که پروژه شما از یک تمرین کلاسی جدا می‌شود. **داده خام سامانه تنها ۱ منبع از ۶ منبع ممکن است.**

## 2.1 داده اصلی (سامانه تغذیه)

### 2.1.1 دریافت، تأیید یکپارچگی و قفل نسخه
- بررسی تعداد ردیف، بازه تاریخ، کدگذاری فایل (UTF-8 vs Windows-1256)، جداکننده.
- ثبت هش فایل. کپی فقط‌خواندنی در `data/raw/`.

### 2.1.2 درخواست رسمی داده تکمیلی (اقدام فوری — زمان‌بر است)
این فهرست را در یک نامه واحد به معاونت دانشجویی بفرستید:

| قلم درخواستی | چرا حیاتی است | اولویت |
|---|---|---|
| بازه زمانی بیشتر (ترم‌های قبل، مهر-آبان) | تنها راه رفع محدودیت «عدم پوشش فصلی» که خودتان به‌عنوان ریسک بالا ثبت کرده‌اید | 🔴 بحرانی |
| **زمان ثبت رزرو** (timestamp) | رزروکنندگان دقیقه‌آخری احتمالاً رفتار عدم‌دریافت متفاوتی دارند — یکی از قوی‌ترین فیچرهای ممکن | 🔴 بحرانی |
| داده **لغو رزرو** (تعداد و زمان) | سیگنال مستقیم و بدون نشت تا لحظه برش | 🔴 بحرانی |
| ظرفیت اسمی هر سلف | نرمال‌سازی و تشخیص اشباع | 🟡 مهم |
| تعداد کل دانشجویان واجد شرایط / ساکن خوابگاه | مخرج نرخ نفوذ | 🟡 مهم |
| منوی کامل هر وعده (حتی غذاهای سرو نشده) | ساخت فیچر «رقیب» | 🟡 مهم |
| قیمت هر غذا در هر تاریخ | اثر قیمت بر رفتار | 🟢 مفید |
| ~~داده در سطح دانشجوی ناشناس‌شده (hashed ID)~~ | ✅ **دریافت شد** — فایل رزرو سطح فردی با `PersonId` در دسترس است.| ✅ انجام‌شده |

- **⚠️ توجه:** دریافت‌شدن ردیف بالا سایر ردیف‌های جدول را منتفی نمی‌کند — به‌ویژه **زمان ثبت رزرو** و **داده لغو رزرو** در فایل سطح فردی هم وجود ندارند و همچنان با اولویت 🔴 در همان نامه درخواست می‌شوند (بند ۲-۱-۴).
- **معیار پایان:** نامه ارسال و پاسخ (مثبت یا منفی) برای اقلام باقی‌مانده دریافت شده و در `docs/data_requests.md` ثبت شده. **در صورت رد، ادعای تعمیم‌پذیری در گزارش محدود می‌شود.**

### 2.1.3 دیکشنری داده
- برای هر ستون: نام، نوع، واحد، دامنه مجاز، معنی تجاری، درصد گمشده، مثال، منبع.
- **خروجی:** `docs/data_dictionary.md`

### 2.1.4 مستندسازی و بازرسی فایل سطح فردی (`PersonId`)
با دریافت فایل رزرو سطح فردی، پیش از هرگونه استفاده، هر ستون باید بازرسی و مستند شود:

| ستون خام | اقدام لازم |
|---|---|
| `PersonId`, `Reserveid` | بدون ناشناس‌سازی؛ بررسی یکتایی `Reserveid` (کلید اصلی سطح رزرو) |
| `Name` | ⚠️ نام‌گذاری گمراه‌کننده — مقدار واقعی، وعده غذایی است (نمونه: «شام»)؛ تغییرنام به `Meal` در فاز پاکسازی |
| `ReserveStatus` | نگاشت به همان مقیاس دودویی معادلِ `DontReceive` در سند اصلی؛ بررسی مقادیر یکتا (آیا فقط «دریافت‌شده/دریافت‌نشده» است یا حالت‌های میانی مثل «لغوشده» هم دارد؟) |
| `CollegeCode/Name`, `FieldCode/Name`, `DegreeCode/Name` | بررسی سازگاری کد↔نام (آیا هر کد همیشه به یک نام یکتا نگاشت می‌شود؟ نگاشت‌های ۱-به-چند = خطای داده) |
| `GroupName` (خوابگاه) | یکسان‌سازی نام مشابه بند ۳-۳ (تطبیق فازی + جدول نگاشت مستقل `dorm_mapping.csv`) |
| `EducationSession`, `PersonType` | شمارش مقادیر یکتا — آیا `PersonType` غیر از «دانشجو» هم دارد (کارمند/استاد)؟ اگر بله، تصمیم صریح: در محدوده مدل بماند یا فیلتر شود |
| `Comment` | در نمونه اولیه همیشه «بدون نظر» — بررسی روی کل داده؛ اگر واقعاً بدون تنوع است، در فاز ۳ حذف شود |
| `Reception` | معنا و مقداردهی نامشخص — **باید در همان نامه بند ۲-۱-۲ از معاونت دانشجویی استعلام شود**؛ تا آن زمان از فیچرسازی کنار گذاشته می‌شود |
| `Price` | بررسی: آیا در طول بازه یا بین غذاها/گروه‌های آموزشی تغییر می‌کند؟ اگر ثابت است بی‌فایده؛ اگر متغیر است ممکن است سیگنال یارانه/نوع دانشجو باشد |

- **⭐ تست تقاطعی حیاتی (افزوده به بند ۳-۶ به‌عنوان T6):** جمع `Count` به تفکیک $(d,m,r,f)$ در فایل فردی باید با `Reservation` در فایل تجمیعی مطابقت داشته باشد. **عدم تطابق نشانه ناهمگامی دو منبع یا تعریف متفاوت غذا/سلف بین دو فایل است و باید پیش از هر مدل‌سازی حل شود.**
- **خروجی:** `docs/data_dictionary_individual.md` + گزارش تست تقاطعی در `docs/leakage_audit.md` (بخش مقدماتی).

## 2.2 داده تقویمی (بالاترین نسبت ارزش به هزینه)

### 2.2.1 تقویم پایه
- تبدیل شمسی↔میلادی (`jdatetime` / `khayyam` / `persiantools`).
- روز هفته (شنبه=۰ … جمعه=۶)، شماره هفته سال، شماره ماه، روز ماه.

### 2.2.2 تعطیلات
- **منابع:** تقویم رسمی ایران، پکیج `holidays` (پشتیبانی Iran)، `time.ir` (استخراج دستی یا اسکرپ).
- انواعی که باید تفکیک شوند:
  - تعطیل رسمی ملی (۲۲ بهمن، ۱۲ فروردین…)
  - تعطیل مذهبی متحرک (تقویم قمری — شهادت‌ها، اعیاد)
  - جمعه (تعطیل ثابت هفتگی)
  - تعطیلات نوروز (بازه پیوسته)
  - **تعطیلی اضطراری** (آلودگی هوا، برف، بحران سوخت) — در بازه آذر ۱۴۰۲ تا خرداد ۱۴۰۳ تهران چندین بار تعطیلی آلودگی داشت. این‌ها را باید از آرشیو خبری استخراج کرد.

### 2.2.3 ویژگی‌های مشتق تقویمی (فضای کاوش)
- `days_to_next_holiday`, `days_since_last_holiday`
- `is_bridge_day` (روز کاری منفرد بین دو تعطیلی — احتمال عدم‌دریافت بسیار بالا)
- `is_day_before_holiday`, `is_day_after_holiday`
- `holiday_block_length` (طول بلوک تعطیلی پیشِ‌رو)

### 2.2.4 تقویم آموزشی دانشگاه (این را دست‌کم نگیرید)
- تاریخ شروع/پایان هر ترم، حذف و اضافه، **بازه امتحانات پایان‌ترم** (دی ۱۴۰۲ و خرداد ۱۴۰۳ هر دو در داده شما هستند).
- `week_of_semester` (هفته چندم ترم — الگوی حضور دانشجو در هفته ۱ با هفته ۱۲ کاملاً متفاوت است).
- `is_exam_period`, `days_to_exam_start`
- **منبع:** سایت آموزش دانشگاه تهران، تقویم آموزشی مصوب.

## 2.3 داده آب‌وهوا و کیفیت هوا

### 2.3.1 اقلام
| متغیر | فرضیه اثر |
|---|---|
| دمای بیشینه/کمینه/میانگین | سرمای شدید → کاهش تردد → افزایش عدم‌دریافت |
| بارش (باران/برف) و شدت آن | برف سنگین → غیبت گسترده |
| AQI / PM2.5 / PM10 | آلودگی بحرانی → تعطیلی یا کاهش حضور |
| رطوبت، سرعت باد | اثر ثانویه (احتمالاً بی‌معنی، ولی تست کنید) |
| «دمای محسوس» (feels-like) | ممکن است از دمای خام قوی‌تر باشد |

### 2.3.2 منابع
- **Open-Meteo Historical Weather API** — رایگان، بدون کلید، دقت ساعتی، پوشش تهران. مناسب‌ترین گزینه.
- **NASA POWER** — رایگان، روزانه.
- سازمان هواشناسی کشور (`irimo.ir`) — رسمی ولی دسترسی API دشوار.
- شرکت کنترل کیفیت هوای تهران (`airnow.tehran.ir`) — برای AQI ایستگاه‌های تهران؛ ایستگاه نزدیک به هر سلف را انتخاب کنید.

### 2.3.3 ⚠️ تله نشت داده در متغیرهای جوی (این را حتماً حل کنید)
سند شما لحظه تصمیم را «پایان روز $d-1$» تعریف کرده است. **دمای واقعی و AQI واقعی روز $d$ در آن لحظه در دسترس نیست.** استفاده از مقدار واقعی روز هدف = نشت داده.

سه راه مجاز:
1. **پیش‌بینی هواشناسی** ثبت‌شده در شب $d-1$ برای روز $d$ (واقع‌گرایانه‌ترین، ولی آرشیو تاریخی پیش‌بینی سخت به دست می‌آید).
2. **مقادیر روز $d-1$** به‌عنوان جانشین (lag-1) — ساده، بی‌خطر، معمولاً همبستگی بالایی با روز $d$ دارد.
3. **مقادیر واقعی روز $d$ فقط برای تحلیل اکتشافی** (فاز ۴)، و **حذف صریح از فیچرست مدل**.

- **معیار پایان:** تصمیم انتخاب‌شده در `decision_log.md` ثبت و در ممیزی نشت (5.13) بررسی شده.

## 2.4 داده رویدادی و بافتاری
- آرشیو خبری برای: تعطیلی‌های اضطراری دانشگاه، اعتصاب/اعتراض، رویدادهای دانشگاهی (جشن فارغ‌التحصیلی، همایش).
- رویدادهای ملی: انتخابات مجلس (اسفند ۱۴۰۲)، بازی‌های مهم ملی، جام ملت‌های آسیا (دی-بهمن ۱۴۰۲ — بازی‌های ایران در ساعات ناهار/شام).
- **روش:** یک فایل `data/external/events.csv` دستی با ستون‌های `date, event_type, description, expected_effect, source_url`.
- **هشدار روش‌شناختی:** با ۱۴۲ روز داده، هر رویداد یکتا فقط ۱ مشاهده دارد. اینها **نباید** به‌عنوان فیچر یادگیری وارد شوند (Overfit قطعی)؛ نقششان **توضیح داده‌های پرت** در فاز ۴ است.

---

# فاز ۳ — پاکسازی و یکپارچه‌سازی

قاعده: **هیچ پاکسازی دستی در Excel.** هر تغییر باید یک خط کد قابل‌بازاجرا باشد.

### 3.1 بازرسی اولیه
- ابعاد، نوع داده هر ستون، مصرف حافظه، نمونه‌گیری تصادفی ۲۰ ردیف و **خواندن چشمی**.
- شمارش مقادیر یکتا هر ستون دسته‌ای (اینجاست که ناهماهنگی نام سلف‌ها خودش را نشان می‌دهد).

### 3.2 نرمال‌سازی متن فارسی (پیش‌نیاز هر کار دیگر)
- یکسان‌سازی `ي`↔`ی` و `ك`↔`ک` (عربی→فارسی)
- حذف/یکسان‌سازی نیم‌فاصله (`\u200c`)
- تبدیل ارقام فارسی/عربی → لاتین
- حذف فاصله‌های اضافی، `strip`، یکسان‌سازی حروف بزرگ/کوچک لاتین
- حذف اعراب و کاراکترهای نامرئی (ZWNJ اضافی، RLM/LRM)
- **ابزار:** `hazm` یا `parsivar` یا یک تابع regex اختصاصی.

### 3.3 یکسان‌سازی نام سلف‌ها
- **روش:** ابتدا نرمال‌سازی متن (3.2) → سپس تطبیق فازی (`rapidfuzz`, نسبت Levenshtein) → **سپس بازبینی دستی کامل جدول نگاشت.**
- **قاعده طلایی:** جدول نگاشت را به‌صورت یک فایل صریح `data/external/restaurant_mapping.csv` ذخیره کنید (`raw_name → canonical_name`). هرگز نگاشت را داخل کد hardcode نکنید.
- **دام:** آیا دو نام مشابه واقعاً یک سلف‌اند یا دو سلف مجزا با نام‌های شبیه؟ نیاز به تأیید از دانشگاه.

### 3.4 تجزیه نام غذا از مخلفات
- الگوی رایج: `چلوخورشت قیمه + سالاد + دوغ` یا `کباب کوبیده (با نوشابه)`.
- **روش:** جداسازی با جداکننده‌ها (`+`, `،`, `-`, پرانتز) → استخراج «غذای اصلی» → ساخت ستون جداگانه `has_extras`, `extras_list`.
- **فرضیه قابل‌آزمون:** وجود مخلفات ممکن است خودش بر نرخ دریافت اثر بگذارد → این را دور نریزید، به فیچر تبدیل کنید.
- خروجی: جدول نگاشت `food_mapping.csv` مشابه سلف‌ها.

### 3.5 مدیریت زمان
- تبدیل `DateReserve` شمسی → `datetime` میلادی + نگه‌داشتن نسخه شمسی برای گزارش.
- ساخت کلید مرتب‌شده زمانی و تأیید عدم وجود تاریخ نامعتبر (۳۱ اسفند، ۳۰ ماه‌های ۳۱ روزه…).

### 3.6 بررسی سازگاری حسابداری (تست‌های صحت)
تست‌های اجباری روی هر ردیف:
```
T1: Reservation == ReceiveWithCard + ReceiveWithCode + DontReceive
T2: تمام ستون‌های شمارشی ≥ 0
T3: DontReceive ≤ Reservation
T4: Reservation > 0  (اگر ۰ است، ρ تعریف‌نشده)
T5: عدم وجود کلید تکراری در سطح (d, m, r, f, g)
T6: Σ Count در فایل فردی به ازای هر (d,m,r,f) == Reservation در فایل تجمیعی برای همان (d,m,r,f)
T7: هر Reserveid دقیقاً یک‌بار در فایل فردی ظاهر می‌شود (کلید یکتا)
```
- **برای هر نقض:** شمارش، بررسی الگو (آیا در سلف/تاریخ خاصی متمرکز است؟)، تصمیم مستند (حذف / اصلاح / نگه‌داشتن با فلگ).
- **ابزار:** `pandera` یا `great_expectations` یا مجموعه `assert` های ساده در `src/validate.py`.

### 3.7 داده گمشده
- **نقشه گمشدگی:** heatmap با `missingno` — الگو در چه سلف/تاریخی متمرکز است؟
- **طبقه‌بندی مکانیزم:** MCAR / MAR / MNAR — و این تشخیص را مستند کنید.
- **⚠️ تفکیک حیاتی:** «رکورد وجود ندارد چون سلف آن روز سرو نداشته (تعطیل)» ≠ «رکورد گم شده». اولی باید به‌عنوان **عدم سرو** ثبت شود، دومی باید پر یا حذف شود. اشتباه گرفتن این دو، فصلی‌بودن کاذب ایجاد می‌کند.
- **فضای کاوش استراتژی:** حذف ردیف / حذف ستون / پرکردن با میانه گروهی / درون‌یابی زمانی / MICE / پرکردن با شاخص جداگانه (`is_imputed` flag).

### 3.8 داده تکراری
- تکرار کامل (همه ستون‌ها) → حذف.
- تکرار کلید با مقادیر متفاوت → **بررسی دستی** (احتمالاً دو رکورد باید جمع شوند، یا یکی نسخه اصلاح‌شده دیگری است).

### 3.9 تجمیع روی جنسیت
```
Res_{d,m,r,f}     = Σ_g Reservation
Recv_{d,m,r,f}    = Σ_g (ReceiveWithCard + ReceiveWithCode)
NoRecv_{d,m,r,f}  = Res - Recv
gender_ratio      = Σ_g[g=F] Reservation / Res     ← فیچر کمکی، حفظ می‌شود
card_ratio        = Σ_g ReceiveWithCard / Recv     ← فیچر تاریخی (فقط با lag!)
```

### 3.10 ساخت شبکه کامل و تشخیص عدم سرو
- ساخت حاصل‌ضرب دکارتی `تاریخ‌های سرو × سلف × وعده × غذا` و مقایسه با داده واقعی.
- شناسایی ترکیب‌های غایب و طبقه‌بندی آنها: تعطیل رسمی / سلف بسته / غذا در منو نبوده / داده گم‌شده.
- **خروجی:** ستون `is_served` و یک گزارش پوشش.

### 3.11 قفل کردن snapshot پردازش‌شده
- ثبت هش + تعداد ردیف نهایی + بازه تاریخ در `data_manifest.md`.
- **معیار پایان M1:** یک اسکریپت واحد `make_dataset.py` که از `data/raw/` به `data/processed/dataset_v1.csv` می‌رسد و تمام تست‌های 3.6 را پاس می‌کند.

### 3.12 پاکسازی اختصاصی داده سطح فردی
- اعمال بند ۲-۱-۴ (تغییرنام `Name→Meal`، تصمیم درباره `Comment`/`Reception`، یکسان‌سازی `GroupName`).
- نرمال‌سازی متن فارسی (بند ۳-۲) روی تمام ستون‌های جدید — نمونه اولیه بررسی‌شده نشان می‌دهد کاراکترهای newline اضافه در `Gender` و `EducationSession` وجود دارد (مثلاً «مرد\n»، «روزانه\n»).
- **تفکیک fact/dimension:** ساخت دو جدول جدا به‌جای یک جدول عریض:
  - `person_dim.csv` — یک ردیف به ازای هر `PersonId`، با ویژگی‌های نسبتاً ثابت (دانشکده، رشته، مقطع، خوابگاه، جنسیت).
  - `person_reservation_fact.csv` — یک ردیف به ازای هر `Reserveid` (رویداد).
  این تفکیک مانع تکرار غیرضروری داده و ناسازگاری احتمالی می‌شود (مثلاً اگر خوابگاه فردی در طول ترم عوض شود).
- **معیار پایان:** `make_dataset_individual.py` که از خام به `data/processed/person_reservation_fact_v1.csv` و `person_dim_v1.csv` می‌رسد و تست‌های T1–T7 (بند ۳-۶) را پاس می‌کند.

---

# فاز ۴ — کاوش اکتشافی داده (EDA)

> این طولانی‌ترین فاز تحلیلی است. خروجی آن یک نوت‌بوک زیبا نیست — خروجی آن **«دفتر حقایق داده»** است: فهرستی از گزاره‌های آماری تأییدشده که هر کدام مستقیماً یک ویژگی در فاز ۵ یا یک تصمیم طراحی در فاز ۶ تولید می‌کند.

## 4.1 تحلیل تک‌متغیره

### 4.1.1 متغیر هدف $\rho$
- هیستوگرام + KDE + ECDF + Box/Violin plot.
- آماره‌ها: میانگین، میانه، مد، انحراف معیار، IQR، **چولگی (skewness)**، **کشیدگی (kurtosis)**، صدک‌های ۱، ۵، ۱۰، ۲۵، ۵۰، ۷۵، ۹۰، ۹۵، ۹۹.
- **پرسش‌های کلیدی:**
  - چند درصد رکوردها $\rho = 0$ دارند؟ (تورم صفر — Zero-inflation)
  - آیا $\rho$ دو-مُدی است؟ (نشانه وجود دو رژیم رفتاری متفاوت)
  - دُم راست چقدر سنگین است؟
- **آزمون‌ها:** Shapiro-Wilk (n<5000)، Anderson-Darling، Kolmogorov-Smirnov در برابر توزیع‌های کاندید: Normal، **Beta**، Gamma، Log-normal.
- **انتظار:** $\rho$ توزیع Beta-مانند و راست‌چوله با تورم صفر خواهد داشت → این مستقیماً به انتخاب Beta regression یا Binomial GLM در فاز ۷ راه می‌برد.

### 4.1.2 متغیرهای شمارشی
- توزیع $Res$, $Recv$, $NoRecv$ — احتمالاً راست‌چوله شدید.
- بررسی نیاز به تبدیل: $\log(1+x)$, Box-Cox, Yeo-Johnson.
- توزیع اندازه سلف‌ها (چند سلف بزرگ + دنباله بلندی از سلف‌های کوچک؟).

### 4.1.3 متغیرهای دسته‌ای
- فراوانی سلف، وعده، نوع غذا، جنسیت، روز هفته.
- **تشخیص عدم توازن:** آیا برخی سلف‌ها فقط ۵ رکورد دارند؟ آیا نسبت ناهار به شام متعادل است؟
- نمودار Pareto: چند سلف چند درصد کل رزروها را می‌سازند؟

## 4.2 تحلیل گروهی و آزمون فرضیه

### 4.2.1 جدول‌های محوری (Pivot)
$\rho$ میانگین و میانه به تفکیک هر یک از: `سلف`, `وعده`, `روز هفته`, `نوع غذا`, `ماه`, `جنسیت`, و **ترکیب‌های دوتایی** آنها (heatmap).

### 4.2.2 انتخاب آزمون آماری صحیح
| مقایسه | آزمون پارامتری | آزمون ناپارامتری (احتمالاً لازم) |
|---|---|---|
| دو گروه مستقل (ناهار vs شام) | t-test | **Mann-Whitney U** |
| چند گروه (سلف‌ها، روزهای هفته) | ANOVA | **Kruskal-Wallis** |
| مقایسه زوجی پس از رد $H_0$ | Tukey HSD | **Dunn's test** |
| ارتباط دو متغیر دسته‌ای | — | **Chi-square** / Fisher exact |
| برابری واریانس‌ها | Bartlett | **Levene** |

> چون $\rho$ نرمال نیست، **ناپارامتری مسیر پیش‌فرض است.** پارامتری فقط پس از تأیید مفروضات.

### 4.2.3 ⚠️ سه اشتباه رایج که باید از آنها پرهیز کنید
1. **گزارش فقط p-value:** با هزاران مشاهده، هر تفاوت ناچیزی معنادار می‌شود. **همیشه اندازه اثر** را گزارش کنید: $\eta^2$ (ANOVA)، Cliff's delta یا rank-biserial (ناپارامتری)، Cohen's d، **و فاصله اطمینان bootstrap برای تفاوت میانه‌ها**.
2. **مسئله آزمون‌های چندگانه:** شما ده‌ها آزمون خواهید زد. تصحیح **Benjamini-Hochberg (FDR)** یا Bonferroni را اعمال و در گزارش ذکر کنید.
3. **نقض استقلال:** مشاهدات همان سلف در روزهای متوالی مستقل نیستند (خودهمبستگی). آزمون‌های کلاسیک این را نادیده می‌گیرند → p-value خوش‌بینانه است. راه‌حل: آزمون روی داده تجمیع‌شده در سطح سلف، یا مدل اثرات مختلط، یا bootstrap بلوکی.

### 4.2.4 دفترچه فرضیه‌ها
هر فرضیه را از پیش بنویسید، سپس آزمون کنید (نه برعکس — این HARKing است):

| # | فرضیه | آزمون | نتیجه | اندازه اثر | فیچر حاصل |
|---|---|---|---|---|---|
| H1 | نرخ عدم‌دریافت شام > ناهار | Mann-Whitney | ? | ? | `meal` |
| H2 | چهارشنبه/پنجشنبه نرخ بالاتری دارند | Kruskal-Wallis + Dunn | ? | ? | `dow` |
| H3 | نرخ در روز قبل از تعطیلی به‌شدت بالاتر است | Mann-Whitney | ? | ? | `is_pre_holiday` |
| H4 | غذاهای خورشتی نرخ عدم‌دریافت بالاتری دارند | Mann-Whitney | ? | ? | `food_type` |
| H5 | تکرار غذا در هفته نرخ را بالا می‌برد | Spearman | ? | ? | `days_since_same_food` |
| H6 | سلف‌ها تفاوت پایدار و معنادار دارند | Kruskal-Wallis | ? | ? | `restaurant` |
| H7 | نرخ در بازه امتحانات متفاوت است | Mann-Whitney | ? | ? | `is_exam_period` |
| H8 | AQI بالا با نرخ عدم‌دریافت همبستگی مثبت دارد | Spearman + partial | ? | ? | `aqi_lag1` |
| H9 | دمای پایین نرخ را بالا می‌برد | Spearman | ? | ? | `temp_lag1` |
| H10 | نسبت جنسیتی رزرو با نرخ مرتبط است | Spearman | ? | ? | `gender_ratio` |
| H11 | رکوردهای با $Res$ کوچک واریانس $\rho$ بالاتری دارند | Levene بین چارک‌های Res | ? | ? | تصمیم وزن‌دهی |
| H12 | نرخ در طول ترم روند دارد | Mann-Kendall | ? | ? | `week_of_semester` |

## 4.3 تحلیل سری زمانی

### 4.3.1 ترسیم
- سری زمانی $\rho$ برای هر ترکیب سلف×وعده (نمودار شبکه‌ای / small multiples).
- سری تجمیع‌شده کل دانشگاه.
- نمودار انباشته $Res$ vs $Recv$ در طول زمان.

### 4.3.2 تجزیه
- **STL Decomposition** (روند / فصلی هفتگی / باقیمانده) — با `period=7`.
- بررسی قدرت فصلی‌بودن (`seasonal strength` معیار Wang-Smith-Hyndman).
- **محدودیت صریح:** با ۱۴۲ روز، فصلی‌بودن **هفتگی** قابل‌تخمین است؛ فصلی‌بودن **سالانه/ترمی قابل‌تخمین نیست** — این را در گزارش تکرار کنید.

### 4.3.3 خودهمبستگی
- **ACF / PACF** تا lag 30 — انتظار قله در lag 7، 14، 21.
- **Ljung-Box test** برای تأیید وجود خودهمبستگی معنادار.
- این نمودارها **مستقیماً** به شما می‌گویند کدام lag ها را در فاز ۵ بسازید.

### 4.3.4 ایستایی
- **ADF** ($H_0$: ریشه واحد) و **KPSS** ($H_0$: ایستا) — هر دو را بزنید، چون مکمل‌اند.
- اگر ناایستا: بررسی تفاضل‌گیری (`d`) و تفاضل فصلی (`D`) — عمدتاً برای مدل‌های SARIMA در فاز ۷.

### 4.3.5 نقاط شکست و تغییر رژیم (Change Point Detection)
- روش‌ها: **PELT / Binary Segmentation** (کتابخانه `ruptures`)، **CUSUM**، Chow test.
- **چرا مهم است:** اگر در بهمن سیاست جریمه عوض شده باشد، داده قبل و بعد از دو توزیع متفاوت می‌آید. کشف این نکته می‌تواند **مهم‌ترین یافته کل پروژه** باشد و تصمیم بگیرید داده قدیمی را کنار بگذارید یا فیچر رژیم بسازید.

## 4.4 همبستگی و وابستگی

### 4.4.1 ماتریس‌ها
- **Pearson** (خطی)، **Spearman** (یکنوا — احتمالاً معتبرتر)، **Kendall tau** (مقاوم به داده کم).
- **Mutual Information** — تنها معیاری که رابطه غیرخطی غیریکنوا را می‌بیند (مثلاً اثر U-شکل دما).
- **همبستگی فاصله‌ای (Distance Correlation)** — تشخیص هر نوع وابستگی.

### 4.4.2 هم‌خطی
- **VIF** برای همه فیچرهای عددی. آستانه هشدار: VIF > 5 یا > 10.
- خوشه‌بندی فیچرهای همبسته (dendrogram روی $1 - |\text{corr}|$) و انتخاب یک نماینده از هر خوشه.

### 4.4.3 همبستگی متقابل با تأخیر (Cross-Correlation)
- CCF بین $\rho$ و متغیرهای خارجی (دما، AQI، بارش) در تأخیرهای مختلف — آیا اثر همان‌روزه است یا با تأخیر؟
- **همبستگی جزئی (Partial Correlation):** آیا اثر AQI پس از کنترل دما و روز هفته باقی می‌ماند؟ (AQI و دما در زمستان تهران به‌شدت همبسته‌اند — خطر همبستگی کاذب بالاست.)

## 4.5 داده پرت (Outliers)

### 4.5.1 روش‌های شناسایی (چندروشی — نه فقط یکی)
| روش | نوع | مناسب برای |
|---|---|---|
| IQR (۱.۵×) | تک‌متغیره | غربالگری سریع |
| Z-score | تک‌متغیره | داده تقریباً نرمال |
| **Modified Z (MAD)** | تک‌متغیره | مقاوم، توصیه می‌شود |
| **Isolation Forest** | چندمتغیره | پرت ترکیبی |
| **LOF** | چندمتغیره | پرت محلی (نسبت به همسایه‌ها) |
| DBSCAN (نقاط نویز) | چندمتغیره | همزمان با خوشه‌بندی |
| باقیمانده STL | سری زمانی | پرت زمانی |

### 4.5.2 ⚠️ تحلیل ریشه‌ای — مهم‌ترین بخش این بند
**هرگز پرت را کورکورانه حذف نکنید.** برای هر پرت شناسایی‌شده، یک ردیف در جدول زیر پر کنید:

| تاریخ | سلف | وعده | $\rho$ | طبقه‌بندی | شواهد | تصمیم |
|---|---|---|---|---|---|---|
| … | … | … | 0.87 | رویداد واقعی (برف سنگین) | آرشیو هواشناسی | نگه‌داشتن + فیچر `heavy_snow` |
| … | … | … | 0.02 | خطای ثبت احتمالی | نقض تست T1 | حذف |
| … | … | … | 0.95 | تعطیلی اضطراری آلودگی | خبرگزاری | حذف از آموزش، گزارش جداگانه |

- **این جدول یکی از ارزشمندترین خروجی‌های EDA است** و مستقیماً موجه‌سازی می‌کند که چرا داده‌های خارجی فاز ۲ لازم بودند.
- **سیاست‌های ممکن:** حذف / نگه‌داشتن با فلگ باینری / Winsorize (بریدن در صدک ۱ و ۹۹) / وزن‌دهی کمتر.
- **قاعده:** پرت را فقط از **مجموعه آموزش** حذف کنید، هرگز از مجموعه آزمون (چون در واقعیت هم اتفاق می‌افتد).

## 4.6 خوشه‌بندی و کشف الگو

### 4.6.1 خوشه‌بندی سلف‌ها بر اساس پروفایل رفتاری
- **بردار ویژگی هر سلف:** [میانگین $\rho$، انحراف معیار $\rho$، پروفایل ۷ روز هفته (نرمال‌شده)، نسبت ناهار/شام، میانگین حجم رزرو، روند زمانی].
- **الگوریتم‌ها:** K-Means، **خوشه‌بندی سلسله‌مراتبی (Ward)** با dendrogram، **DTW-based clustering** (`tslearn`) اگر می‌خواهید شکل سری زمانی را مقایسه کنید نه فقط آماره‌ها.
- **تعیین تعداد خوشه:** Elbow، **Silhouette score**، Gap statistic، Davies-Bouldin.
- **⚠️ نکته:** تعداد سلف‌ها احتمالاً کم است (شاید ۱۰-۲۰). با این تعداد، خوشه‌بندی الگوریتمی ممکن است بی‌ثبات باشد — **dendrogram + تفسیر دستی معمولاً قابل‌دفاع‌تر است.**

### 4.6.2 خوشه‌بندی غذاها
- بر اساس [میانگین $\rho$، تعداد دفعات سرو، میانگین حجم رزرو نسبی].
- **نتیجه عملی:** یک «شاخص محبوبیت غذا» که فیچر قدرتمندی در فاز ۵ می‌شود (با احتیاط نشت — فقط از تاریخچه محاسبه شود).

### 4.6.3 کاهش بعد برای بصری‌سازی
- **PCA** (خطی، قابل‌تفسیر، واریانس توضیح‌داده‌شده را گزارش کنید)
- **t-SNE** / **UMAP** (غیرخطی، فقط برای بصری‌سازی — هرگز فاصله‌ها را در آنها کمّی تفسیر نکنید)

### 4.6.4 تصمیم استراتژیک حاصل از این بند
> **آیا یک مدل سراسری (Global) بسازیم، یا یک مدل به‌ازای هر خوشه، یا یک مدل به‌ازای هر سلف؟**
> اگر خوشه‌ها به‌وضوح جدا باشند → مدل per-cluster.
> اگر همگن باشند → مدل global با فیچر `restaurant_id`.
> این تصمیم را اینجا بگیرید و در `decision_log.md` ثبت کنید.

## 4.7 توازن، پوشش و کفایت داده
- **ماتریس پوشش:** heatmap تعداد مشاهدات به ازای سلف × هفته — کجا حفره داریم؟
- توزیع تعداد مشاهدات هر سری زمانی $(m,r,f)$ — کدام سری‌ها کمتر از ۳۰ نقطه دارند؟ (این‌ها به مدل global نیاز دارند)
- توزیع $Res$ — چند درصد رکوردها $Res < 10$ دارند؟ (نرخ در این‌ها به‌شدت نویزی است)
- **تعادل کلاسی معادل در رگرسیون:** بررسی اینکه آیا دُم بالای $\rho$ نمونه کافی برای یادگیری دارد.

## 4.8 ناهم‌واریانسی (Heteroscedasticity)
- نمودار پراکنش $\text{Var}(\rho)$ در برابر $Res$ (bin-شده) — انتظار: کاهش واریانس با افزایش $Res$، دقیقاً مطابق $\text{Var} \approx p(1-p)/Res$ در مدل دوجمله‌ای.
- **آزمون‌ها:** Breusch-Pagan، White test، Levene بین چارک‌های $Res$.
- **نتیجه عملی (سه گزینه):**
  1. **وزن‌دهی نمونه** با $w_i = Res_i$ در تابع زیان.
  2. **مدل دوجمله‌ای/Beta-Binomial** — راه‌حل اصولی.
  3. **کوچک‌سازی بیزی تجربی (Empirical Bayes shrinkage):** $\tilde{\rho} = \frac{NoRecv + \alpha}{Res + \alpha + \beta}$ که $\alpha, \beta$ از توزیع Beta برازش‌شده روی کل داده می‌آید. این نرخ‌های نویزی رزروهای کوچک را به سمت میانگین کل می‌کشد.
- **این بند مستقیماً ریسک شماره ۳ سند شما را حل می‌کند.**

## 4.9 تحلیل تعامل (Interaction)
- Heatmap دوبعدی $\rho$ برای: سلف × روز هفته، وعده × نوع غذا، ماه × وعده، دما(bin) × وعده.
- **آزمون رسمی:** ANOVA دوطرفه با جمله تعامل، یا مقایسه AIC مدل با/بدون تعامل.
- **خروجی:** فهرست تعامل‌های معنادار → فیچرهای ضربی صریح در فاز ۵ (برای مدل‌های خطی؛ مدل‌های درختی خودشان کشف می‌کنند).

## 4.10 تحلیل اختصاصی متغیرهای خارجی
- **دما:** آیا رابطه خطی است یا U-شکل (هم سرمای شدید هم گرمای شدید بد است)؟ نمودار binscatter + برازش GAM.
- **AQI:** آیا اثر آستانه‌ای دارد (فقط بالای ۱۵۰ اهمیت پیدا می‌کند)؟
- **بارش:** آیا نوع (باران vs برف) مهم‌تر از مقدار است؟
- **تعطیلات:** پروفایل $\rho$ در روزهای `d-3, d-2, d-1` قبل از بلوک تعطیلی و `d+1, d+2` بعد از آن.
- **رمضان/نوروز:** بررسی وجود داده و تصمیم درباره کنارگذاری.
- **⚠️ در تمام این تحلیل‌ها، اثر را پس از کنترل روز هفته و سلف بسنجید** — وگرنه همبستگی کاذب می‌گیرید.

## 4.11 دفتر حقایق داده (Data Facts Register) — خروجی اصلی فاز ۴
یک سند واحد که هر ردیف آن یک گزاره تأییدشده است:

| # | حقیقت | شاهد آماری | اندازه اثر | اثر بر پروژه |
|---|---|---|---|---|
| F01 | میانگین کل $\rho$ برابر X٪ با CI [a,b] | bootstrap | — | تنظیم baseline |
| F02 | $\rho$ توزیع Beta راست‌چوله با تورم صفر Y٪ دارد | KS test | — | انتخاب Beta/Binomial GLM |
| F03 | تفاوت بین سلف‌ها معنادار و بزرگ است | KW, p<.001 | $\eta^2$=… | نیاز به فیچر سلف |
| F04 | قله ACF در lag 7 | ACF | r=… | ساخت lag_7 |
| … | … | … | … | … |

- **قاعده:** هیچ فیچری در فاز ۵ ساخته نشود که به یک ردیف این جدول یا به دانش دامنه صریح متصل نباشد.

## 4.12 گزارش تصویری EDA
- انتخاب ۸-۱۲ نمودار **کلیدی** (نه ۵۰ نمودار) برای گزارش نهایی.
- استاندارد: عنوان معنادار (نه «Distribution of rho» بلکه «نرخ عدم‌دریافت در شام‌ها به‌طور معناداری بالاتر است»)، برچسب محور فارسی، منبع داده، پشتیبانی از فونت فارسی در matplotlib.

## 4.13 کاوش اکتشافی سطح فرد (ورودی مستقیم برای مدل B)

### 4.13.1 توزیع تاریخچه فردی
- هیستوگرام «تعداد رزرو قبلی هر دانشجو» — چند درصد دانشجویان کمتر از ۵ رزرو تاریخی دارند؟ (این جمعیت = چالش cold-start، بند ۵-۱۶-۴)
- توزیع طول سابقه (روز) هر `PersonId` در بازه داده.

### 4.13.2 نرخ عدم‌دریافت به تفکیک جمعیتی
- Heatmap نرخ به تفکیک: دانشکده، مقطع تحصیلی، خوابگاه (`GroupName`)، جنسیت.
- **آزمون‌های افزوده به دفترچه فرضیه‌ها (بند ۴-۲-۴):**

| # | فرضیه | آزمون | فیچر حاصل |
|---|---|---|---|
| H13 | خوابگاه‌های دورتر از یک سلف نرخ عدم‌دریافت بالاتری دارند | Kruskal-Wallis روی `GroupName` (پس از کنترل سلف) | `dorm_restaurant_distance` (در صورت دسترسی به داده مکانی بند ۲-۵) |
| H14 | مقطع تحصیلات تکمیلی نرخ متفاوتی از کارشناسی دارد | Mann-Whitney روی `DegreeName` | `degree_level` |
| H15 | تاریخچه شخصی، پس از کنترل دانشکده/خوابگاه، هنوز قدرت پیش‌بینی افزوده دارد | مقایسه AUC مدل با/بدون فیچر تاریخی | تصمیم شمول فیچر تاریخی در FS_selected |
| H16 | جمعیت کم‌سابقه (cold-start) رفتاری نزدیک به میانگین کل دارد، نه رفتار خوشه خاص | مقایسه توزیع نرخ | استراتژی fallback بند ۵-۱۶-۴ |

### 4.13.3 کمّی‌سازی عدم‌توازن کلاس
- نرخ کلی `ReserveStatus=دریافت‌نشده` در سطح رزرو منفرد (احتمالاً نزدیک به همان ~۸٪ سند، اما توزیع بین افراد را هم بررسی کنید).
- **نمودار Lorenz/Pareto:** چند درصد از کل موارد عدم‌دریافت توسط چند درصد از دانشجویان ایجاد می‌شود؟
- این عدد مستقیماً استراتژی وزن‌دهی کلاس در بند ۷-۱۱-۱ را تعیین می‌کند.

### 4.13.4 سازگاری بین دو منبع داده
- مقایسه بصری سری زمانی $Res_{d,m,r}$ محاسبه‌شده از جمع فایل فردی در برابر مقدار فایل تجمیعی — باید منطبق باشد (طبق تست T6، بند ۳-۶).

---

# فاز ۵ — مهندسی ویژگی

> **نسخه ۲.۱ — بازنویسی‌شده پس از فاز ۴ (۲۰۲۶-۰۸-۱۴).** نسخه‌ی ۲.۰ پیش از دیدن داده نوشته شده بود؛ فاز ۴ چند فیچر پیشنهادی آن را رد کرد و سه ساختار مهم کشف کرد که در نسخه‌ی قبلی اصلاً جایی نداشتند. تغییرات در بند ۵.۰ فهرست شده‌اند.

قاعده حاکم: **هر فیچر ابتدا در برابر قاعده برش اطلاعاتی بند ۴-۲ سند مسئله سنجیده می‌شود، سپس ساخته می‌شود.** مرجع الزام‌آور: `doc/data_facts_register.md` — هیچ فیچری بدون اتصال به یک ردیف آن ساخته نمی‌شود.

## 5.0 تغییرات این نسخه نسبت به ۲.۰ (مبتنی بر شواهد فاز ۴)

| تغییر | شاهد | بند |
|---|---|---|
| ❌ **حذف** `days_since_same_food_served` و `food_popularity_score` شخصی | مصنوع تقویمی؛ پس از کنترل تقویم p=۰.۳۲ (F27, F66) | 5.6 |
| ❌ **حذف** `has_extras`, `n_extras`, `Count`, `Price` | ستون‌های منحط: `has_extras` ۱۰۰٪ True، `Count` ۱۰۰٪ برابر ۱ (F66) | 5.6 |
| ❌ **حذف** `aqi`, `aqi_above_150`, PM | همبستگی کاذب بین‌شهری؛ داخل تهران p=۰.۶۵ (F25) | 5.8 |
| 🔄 **تغییر** بارش از پیوسته به **دسته‌ای** | «نوع» معنادار (p=۲.۶e−۶)، «مقدار» نه (p=۰.۷۰) (F24) | 5.8 |
| 🔄 **تغییر** lag از یکسان به **آگاه‌به‌وعده** | ناهار lag۱۴/۲۸، شام lag۱؛ lag۷ ناهار مصنوع ترکیب سلف بود (F33, F34) | 5.2 |
| 🔄 **تغییر** منطق `Res` غیرعادی: علامت **برعکس** فرضیه‌ی ۲.۰ | Spearman(حجم روزانه، نرخ) = **−۰.۳۵۲** (F54) | 5.5 |
| 🔄 **تنزل** پنجره‌ی متحرک کوتاه برای فیچر فردی | انبساطی AUC=۰.۷۲۰ در برابر rolling(۳)=۰.۶۲۷ (F55, F56) | 5.3, 5.16 |
| ➕ **افزوده** بند ۵.۱۷ — فیچرهای **عامل روز** (سلسله‌مراتبی) | ۸۳٪ واریانس سلول شوک مشترک روزانه است؛ R²خارج‌نمونه=۰.۶۰۲ (F59, F60, F61) | 5.17 |
| ➕ **افزوده** بند ۵.۱۸ — فیچرهای **برهم‌کنش ضرب‌شونده** | اثر فرد×روز ضرب‌شونده است، نسبت ۳.۳۶× (F62) | 5.18 |
| ➕ **افزوده** بند ۵.۱۹ — **پل A↔B** (تجمیع ترکیب رزروکنندگان) | برای شام r=+۰.۵۲، برای ناهار صفر (F63) | 5.19 |
| ⬆️ **گسترش** بند ۵.۱۶ (مدل B) از ۵ زیربند به ۹ | تاریخچه‌ی فردی قوی‌ترین فیچر کل پروژه است (AUC=۰.۷۲، ۸ برابر جمعیت‌شناسی) | 5.16 |

## 5.1 ویژگی‌های تقویمی (بدون ریسک نشت)
`dow`, `month`, `day_of_month`, `week_of_semester`, `is_holiday_any`, `days_to_next_holiday`, `days_since_last_holiday`, `is_bridge_day`, `holiday_block_length`, `is_exam_period`, `is_final_exam_period`, `days_to_exam_start`, `is_ramadan`

- **اولویت اول: `is_day_before_holiday`** — بزرگ‌ترین اثر تقویمی، +۳.۱ واحد درصد با CI [+۲.۱, +۴.۲] (F19).
- **برهم‌کنش با طول بلوک:** `is_day_before_holiday × holiday_block_length` — بلوک ۱۵روزه +۰.۰۴۴ در برابر بلوک ۳روزه +۰.۰۳۴ (F19).
- `is_ramadan` فقط **فلگ گزارشی** است، نه فیچر یادگیری‌شونده (۱۴ روز، F22).
- ❌ فیچر اختصاصی جمعه ساخته نشود — جمعه رفتار متفاوتی ندارد و فقط ۸۶ رکورد دارد (F31).

## 5.2 ویژگی‌های تأخیری (Lag) — **آگاه به وعده**

⚠️ **مهم‌ترین تغییر این نسخه.** نسخه‌ی ۲.۰ یک مجموعه lag یکسان برای همه پیشنهاد می‌داد. فاز ۴ نشان داد ناهار و شام **دو دینامیک زمانی کاملاً متفاوت** دارند (F33):

| وعده | تأخیرهای معنادار | تأخیرهای بی‌اثر |
|---|---|---|
| **ناهار** | `lag_14` (+۰.۳۹)، `lag_28` (+۰.۳۹) | `lag_7` (پس از حذف اثر سلف از ۰.۲۵۵ به ۰.۱۲۸ می‌افتد) |
| **شام** | `lag_1` (+۰.۷۶)، `lag_2` (+۰.۴۹)، `lag_7` (+۰.۵۱) | `lag_14` (−۰.۰۳) |

- **سطح تعریف lag:** فقط `(m, r)` — یعنی «همان وعده در همان سلف». سطح `(m,r,f)` کنار گذاشته می‌شود چون سری‌هایش میانه‌ی ۴ نقطه دارند (F39) و lag اغلب `NaN` می‌شود.
- **⚠️ قاعده‌ی برش به تفکیک وعده (تله‌ی اصلی پروژه):** برای هدفِ **ناهار روز $d$**، آخرین outcome در دسترس **ناهار روز $d-1$** است — شام روز $d-1$ هنوز سرو نشده. برای هدفِ **شام روز $d$**، شام روز $d-1$ در دسترس است. پس `lag_1` باید در **توالی هم‌وعده** شمرده شود، نه در توالی خام رزروها.
- **⚠️ عبور از شکاف رمضان ممنوع:** ۲۹ روز بدون سرو ناهار (F38). پنجره باید شکاف را تشخیص دهد و `NaN` بدهد، نه اینکه از رویش بپرد.
- **مدیریت `NaN`:** فلگ `lag_X_is_missing` + پرکردن با میانگین انبساطی همان سری (نه صفر، نه میانگین کل).

## 5.3 آماره‌های پنجره متحرک (Rolling)
- در سطح **سلول** (سلف×وعده): `rolling_mean_7`, `rolling_std_7`, `rolling_mean_14`, `rolling_mean_28` — همچنان کاندیدند.
- **قاعده حیاتی نشت:** همیشه `.shift(1)` **قبل از** `.rolling()`.
```python
df['roll7'] = df.groupby(key)['rho'].transform(lambda s: s.shift(1).rolling(7, min_periods=3).mean())
```
- ⚠️ **برای فیچر سطح فرد، پنجره‌ی کوتاه ساخته نشود** — بند ۵.۱۶.۱ (انبساطی به‌مراتب بهتر است).

## 5.4 ویژگی‌های تجمعی گسترشی (Expanding)
- `expanding_mean` نرخ تاریخی هر سلف / هر سلف×وعده / هر سلف×وعده×روزهفته — فقط از داده‌ی پیش از لحظه‌ی برش.
- **این‌ها در عمل قوی‌ترین فیچرهای سطح سلول‌اند** و هم‌زمان معادل باینشده‌ی baseline بند ۲.۳ هستند.
- برای سلف‌های کم‌حجم، **کوچک‌سازی بیزی** با α=۰.۹۷۶ و β=۱۱.۱۵ (برآورد شده در F8.3).

## 5.5 ویژگی‌های مقیاس رزرو
- `Res` (خام), `log1p(Res)`
- `Res / expanding_mean(Res)` — نسبت به عادت همان سری
- `Res_zscore` نسبت به همان سلف×وعده×روزهفته تاریخی
- **➕ `log(daily_total_Res)`** — حجم کل رزرو دانشگاه در آن روز-وعده. در لحظه‌ی برش کاملاً معلوم است و به‌تنهایی R² پیش‌بینی شوک روزانه را از ۰.۳۹۶ به ۰.۴۲۳ می‌برد (F61).
- 🔄 **منطق تصحیح‌شده:** نسخه‌ی ۲.۰ فرض کرده بود «رزرو غیرعادی بالا ⇒ رزروکنندگان کم‌تعهدتر ⇒ نرخ بالاتر». داده **عکس** آن را نشان داد: Spearman(حجم روزانه، نرخ روزانه) = **−۰.۳۵۲** (F54). روزهای کم‌حجم روزهای «تعطیلی عملی»اند و نرخشان بالاست.

## 5.6 ویژگی‌های منو و غذا — **به‌شدت کوتاه‌شده**
- ✅ `food_id` / `food_canonical` — اثر **عمومی** غذا واقعی است: دامنه‌ی باقیمانده ۰.۰۵۰، Kruskal H=۱۲۸۱.۸ (F67).
- ✅ `food_norecv_rate_expanding` — نرخ تاریخی همان غذا، **فقط از گذشته** (target encoding زمانی-امن، بند ۵.۱۰).
- ✅ `competitor_food` — غذای دیگرِ همان وعده (انتخاب بین دو گزینه).
- ✅ `is_new_food` (اولین سرو).
- ❌ `days_since_same_food_served` — **رد شد** (F27): همبستگی خام +۰.۱۷۶ کاملاً مصنوع تقویمی است؛ پس از کنترل تقویم p=۰.۳۲.
- ❌ `food_popularity_score` در سطح **شخص** — **رد شد** (F66): پایداری جفت (فرد،غذا) ۰.۲۳۹ < پایداری خود فرد ۰.۴۳۱.
- ❌ `has_extras`, `n_extras` — **ستون منحط**، ۱۰۰٪ رکوردها True (F66).
- ❌ `meal × food_type` — ΔAIC=−۱.۲، p=۰.۳۸ (F28).

## 5.7 ویژگی‌های ترکیبی رزرو
- `gender_ratio` — ⚠️ اولویت پایین: علامتش پس از کنترل سلف×وعده **برمی‌گردد** (خام +۰.۱۵۵ → کنترل‌شده −۰.۰۹۶، F32). عملاً نماینده‌ی هویت سلف است.
- ❌ `card_ratio` خام **ممنوع** (از خروجی همان وعده مشتق می‌شود). فقط `card_ratio_lag` با رعایت قاعده‌ی برش.

## 5.8 ویژگی‌های خارجی — **به‌شدت کوتاه‌شده**
- ✅ **`precip_type` دسته‌ای** ∈ {برف، باران، بدون بارش} — «نوع» معنادار (Kruskal p=۲.۶e−۶)، «مقدار» بی‌معنا (Spearman −۰.۰۰۹، p=۰.۷۰) (F23, F24).
- ⚠️ `temp_min` تنها نماینده‌ی خانواده‌ی دما (VIF `temp_mean`=۱۸۲.۹)، و **فقط اگر** در فاز ۷ بهبود معنادار نشان دهد — اثر کنترل‌شده r=−۰.۰۶۱، نه خطی نه U-شکل (F26).
- ❌ `aqi`, `aqi_above_150`, `pm2_5`, `pm10` — **حذف کامل** (F25).
- **⚠️ قاعده‌ی الحاق:** کلید `(city, date_gregorian)`، نه `date` تنها (ردیف ۲۱ decision_log).
- **⚠️ قاعده‌ی برش:** مقدار **واقعی** روز $d$ در ساعت ۱۵ روز $d-1$ معلوم نیست. فقط **پیش‌بینی** هواشناسی یا lag مجاز است. (برف با ۱۱ روز مشاهده، پایه‌ی نمونه‌ای ضعیفی دارد — با عدم‌قطعیت گزارش شود.)

## 5.9 ویژگی‌های فوریه‌ای
$\sin(2\pi k t/7)$, $\cos(2\pi k t/7)$ برای $k=1,2,3$. مفید برای مدل‌های خطی؛ برای مدل‌های درختی `dow` کافی است.

## 5.10 رمزگذاری متغیرهای دسته‌ای
| روش | کِی مناسب است | ریسک |
|---|---|---|
| One-Hot | کاردینالیتی پایین (وعده، نوع غذا) | انفجار ابعاد |
| Ordinal/Label | مدل‌های درختی | ترتیب کاذب در مدل خطی |
| **Target Encoding انبساطی** | سلف، نام غذا، دانشکده | 🔴 نشت شدید اگر زمانی-امن نباشد |
| Frequency Encoding | `FieldName` (۶۹۹ سطح) | امن، ساده |

**⚠️ قاعده Target Encoding:** مقدار انکودشده برای روز $d$ باید **فقط** از داده تا لحظه‌ی برش محاسبه شود. `sklearn`'s TargetEncoder با CV معمولی **کافی نیست**. پیاده‌سازی expanding-mean دستی، به‌همراه کوچک‌سازی بیزی برای سطوح کم‌نمونه.

## 5.11 تبدیل متغیر هدف
خام $\rho$ · Logit · Arcsine-sqrt · بدون تبدیل + Beta/Binomial. هر چهار در فاز ۷ مقایسه شوند؛ **معیار مقایسه در فضای اصلی $\rho$**.

## 5.12 انتخاب ویژگی و فیچرست‌ها
روش‌ها: Filter (MI، همبستگی) · Embedded (L1، اهمیت درختی) · **Permutation Importance** · **Boruta** · حذف بازگشتی مبتنی بر SHAP.

**قاعده:** انتخاب ویژگی **داخل هر fold**، نه یک‌بار روی کل داده.

**فیچرست‌های تعریف‌شده (محور آزمایش فاز ۷):**

| نام | محتوا |
|---|---|
| `FS_baseline` | فقط `expanding_mean` سلف×وعده×روزهفته (معادل baseline بند ۲.۳) |
| `FS_calendar` | + تقویمی (۵.۱) |
| `FS_lag` | + lag آگاه‌به‌وعده (۵.۲) + rolling (۵.۳) |
| `FS_day` | + **عامل روز** (۵.۱۷) ← فیچرست کلیدی این نسخه |
| `FS_full_A` | همه‌ی فیچرهای سطح تجمیعی |
| `FS_B_person` | فیچرهای سطح فرد (۵.۱۶) |
| `FS_bridge` | `FS_full_A` + خروجی تجمیع‌شده‌ی مدل B (۵.۱۹) |
| `FS_selected` | نتیجه‌ی انتخاب ویژگی |

## 5.13 ⭐ ممیزی نشت داده — دروازه اجباری

```
□ آیا این مقدار در لحظه‌ی برشِ همان وعده واقعاً محاسبه‌شدنی است؟
  (ناهار d ← ۱۵:۰۰ روز d-1 · شام d ← ۲۳:۰۰ روز d-1)
□ آیا هیچ تجمیعی روی داده آینده انجام نشده؟ (mean کل دیتاست ممنوع)
□ اگر rolling است، آیا shift(1) قبل از rolling اعمال شده؟
□ اگر target encoding است، آیا فقط از گذشته محاسبه شده؟
□ اگر scaler/imputer است، آیا فقط روی train fold برازش شده؟
□ آیا متغیر جوی، مقدار واقعی روز d است یا پیش‌بینی/lag؟
□ آیا فیچر با هدف همبستگی مشکوکاً بالا (>0.95) دارد؟
□ ➕ آیا پنجره از شکاف ساختاری رمضان عبور کرده؟ (F38)
□ ➕ آیا فیچر تاریخی فرد از رزروهای بعد از d نشتی گرفته؟
□ ➕ آیا فرد در یک روز بیش از یک رزرو دارد که در تاریخچه دوبار شمرده شود؟
□ ➕ آیا از «نتیجه‌ی ناهار همان روز» برای پیش‌بینی شام همان روز استفاده شده؟
```

> **➕ تله‌ی جدید کشف‌شده در فاز ۴ (F57):** نتیجه‌ی ناهار روز $d$ با نتیجه‌ی شام روز $d$ به‌شدت گره خورده است (نسبت خطر ۳.۱۸). این وسوسه‌انگیز است که از آن فیچر بسازیم — **ولی ممنوع است**: لحظه‌ی برشِ شام روز $d$ ساعت ۲۳ روز $d-1$ است، یعنی ناهار روز $d$ هنوز سرو نشده. این یافته فقط ارزش **تفسیری** دارد (متغیر پنهان = غیبت از دانشگاه)، نه فیچری.

- **تست نهایی:** اگر $R^2$ اعتبارسنجی >۰.۹ شد، تقریباً قطعاً نشتی دارید. با توجه به F59 (۸۳٪ واریانس شوک روزانه) و F09 (بیش‌پراکندگی باقیمانده ۷.۶ برابر)، **سقف واقع‌بینانه‌ی $R^2$ در این مسئله حدود ۰.۴–۰.۵ است** — هر عدد بالاتر باید مشکوک تلقی شود.
- **خروجی:** `doc/leakage_audit.md` با یک ردیف به ازای هر فیچر.

## 5.14 خط لوله (Pipeline)
کل تبدیل‌ها در `sklearn.Pipeline` / `ColumnTransformer` تا `fit` فقط روی train انجام شود. مقیاس‌بندی: `RobustScaler` (به‌دلیل پرت‌های تأییدشده) برای مدل‌های خطی؛ بدون مقیاس برای درختی.

## 5.15 رجیستری ویژگی
**خروجی:** `doc/feature_registry.md` — برای هر فیچر: نام، فرمول، منبع داده، وابستگی زمانی، ردیف پشتیبان دفتر حقایق، وضعیت ممیزی نشت، فیچرست‌های شامل آن.

---

## 5.16 ویژگی‌های سطح فرد (مدل B) — **گسترش‌یافته**

> **چرا این بند بزرگ‌تر از نسخه‌ی ۲.۰ شد.** فاز ۴ نشان داد `person_expanding_norecv_rate` با **AUC=۰.۷۲۰۰** قوی‌ترین تک‌فیچر کل پروژه است و تاریخچه‌ی فردی ~۸ برابر بیشتر از کل جمعیت‌شناسی توضیح می‌دهد (F56, F58). اما هم‌زمان نشان داد جمع‌کردن ساده‌ی پیش‌بینی‌های فردی به نرخ تجمیعی **کار نمی‌کند** (F59) مگر با ساختار ضرب‌شونده (F62). پس مدل B هم مهم‌تر و هم ظریف‌تر از تصور اولیه است.

### 5.16.1 تاریخچه رفتاری فرد — **ترتیب اولویت تجربی‌شده**

| فیچر | AUC تک‌متغیره | اولویت |
|---|---|---|
| `person_expanding_norecv_rate` | **۰.۷۲۰۰** | ⭐ اصلی |
| `person_ewm_norecv_rate` (نیمه‌عمر ۵) | ۰.۷۱۲۸ | مکمل |
| `person_rolling_norecv_rate_10` | ۰.۶۸۴۵ | اختیاری |
| `person_rolling_norecv_rate_3` | ۰.۶۲۶۷ | ❌ ساخته نشود |
| `person_last_outcome` | ۰.۵۷۱۸ | ❌ ساخته نشود |

- **تصمیم:** فیچر اصلی **انبساطی** است. افزودن جمله‌ی تازگی فقط ΔAUC=+۰.۰۰۵۲ می‌دهد (F55, F56) — پس حداکثر **یک** جمله‌ی EWM اضافه شود، نه یک خانواده‌ی پنجره.
- ✅ `person_n_prior_reservations` — **قوی‌ترین همبسته‌ی سطح فرد پس از خود نرخ**: Spearman=−۰.۲۶۲؛ چارک کم‌مصرف ۰.۱۳۴ در برابر پرمصرف ۰.۰۵۹ (F64).
- ✅ `person_n_prior_norecv` (شمارش خام، مکمل نرخ).
- ✅ `person_tenure_rank` — شماره‌ی ترتیبی رزرو جاری در سابقه‌ی فرد. **اثر ماه‌عسل**: باقیمانده رزرو ۱-۵ برابر −۰.۰۱۸ و رزرو ۱۰۱-۱۵۰ برابر +۰.۰۰۹ (F65).
- ⚠️ `person_days_since_last_norecv` و `person_streak_recv` — **اولویت پایین**: جزء «حالت» فقط ≤۳ روز عمر دارد و همبستگی درون‌فردی +۰.۰۴۹ است (F55).
- **کوچک‌سازی بیزی (الزامی):** $\tilde{p}_{person}=\frac{n\_norecv+\alpha}{n\_total+\alpha+\beta}$ با α=۰.۹۷۶، β=۱۱.۱۵. بدون این، نرخ خام فرد کم‌رزرو بی‌معناست (F49: میانه‌ی نرخ گروه ۱-۴ رزرو دقیقاً صفر است با انحراف معیار ۰.۳۷).

### 5.16.2 فیچرهای جمعیتی ثابت — **اولویت پایین‌تر از نسخه‌ی ۲.۰**
- `is_dorm_resident` ⭐ — تنها فیچر جمعیتی با اثر بزرگ و مقاوم: ضریب −۰.۰۳۴۶ حتی پس از کنترل جنسیت/مقطع/شهر/دانشکده؛ در ۲۲ از ۲۴ دانشکده برقرار (F16).
- `education_session` — شبانه/نوبت دوم ۰.۱۴۰ در برابر روزانه ۰.۱۰۲ (F50).
- `college` (۲۶ سطح، frequency یا target encoding انبساطی)، `field` (۶۹۹ سطح، **فقط** frequency encoding)، `degree_level`، `gender`.
- ⚠️ **هشدار کالیبراسیون انتظار:** کل خانواده‌ی جمعیتی R²adj≈۰.۰۶ می‌دهد در برابر ۰.۴۴ برای تاریخچه‌ی فردی (F58). ارزش واقعی‌شان **فقط برای cold-start** است.

### 5.16.3 مدیریت کاردینالیتی بالای شناسه فرد
- **قاعده:** `PersonId` هرگز فیچر گسسته‌ی مستقیم نمی‌شود. فقط آمار خلاصه‌شده‌ی ۵.۱۶.۱.
- در مدل اثرات مختلط (بند ۷.۵) می‌تواند **اثر تصادفی** باشد.

### 5.16.4 استراتژی کاربر جدید (Cold-Start)
- آستانه: **$k=10$** رزرو تاریخی (نه ۵ نسخه‌ی قبلی) — چون فیچر انبساطی زیر ۱۰ رزرو ناپایدار است.
- جمعیت هدف: ۱۲.۹٪ افراد ولی فقط **۰.۷٪ حجم رزرو** (F48) — اولویت پایین.
- **پرکردن:** میانگین زیرگروه جمعیتی (`is_dorm_resident × education_session × college`) + فلگ `is_cold_start`.
- **توجیه تجربی:** توزیع نرخ cold-start از باسابقه‌ها **تفکیک‌پذیر نیست** (Mann-Whitney p=۰.۱۱۲، δ=+۰.۰۱۷) — پس fallback به میانگین جمعیتی موجه است (F49).
- **ارزیابی جداگانه** در بند ۸.۳.

### 5.16.5 ➕ فیچرهای زمینه‌ی فرد در آن وعده
- `person_meal_share` — سهم تاریخی این وعده از رزروهای فرد (آیا این فرد معمولاً ناهارخور است یا شام‌خور؟).
- `person_restaurant_share` — سهم تاریخی این سلف از رزروهای فرد.
- `person_dow_share` — سهم تاریخی این روزِ هفته.
- **منطق:** رزرو در «وعده/سلف/روز غیرمعمولِ» فرد، سیگنال عدم‌قطعیت است. همه انبساطی و leakage-safe.

### 5.16.6 ➕ ⚠️ فیچرهای **ممنوع** سطح فرد (تله‌های کشف‌شده)
- ❌ نتیجه‌ی ناهار همان روز برای پیش‌بینی شام همان روز (F57 — تله‌ی اصلی، بند ۵.۱۳).
- ❌ ترجیح غذایی شخصی `person × food` (F66 — پایداری کمتر از خود فرد).
- ❌ فیچر مبتنی بر هم‌خوابگاهی‌ها به‌عنوان واحد سرایت (F66 — ICC پس از کنترل ۰.۰۰۳).
- ❌ `Price`, `Count`, `has_extras` (F66 — منحط یا بی‌اثر).

### 5.16.7 ➕ هدف و وزن‌دهی مدل B
- هدف: `dont_receive` باینری در سطح رزرو.
- نرخ کلاس مثبت ۸.۱٪ — **نامتوازن ولی نه شدید**. ۱۰٪ افراد ۳۸٪ کل موارد را می‌سازند (F47).
- گزینه‌ها برای فاز ۷: `class_weight='balanced'` · `scale_pos_weight` · بدون وزن‌دهی + معیار کالیبراسیون‌محور.
- ⚠️ چون خروجی نهایی یک **کوانتایل تجمیعی** است نه یک برچسب، **کالیبراسیون احتمال مهم‌تر از AUC است**. معیار: Brier score و منحنی کالیبراسیون، نه فقط AUC.

### 5.16.8 ➕ ارزیابی اختصاصی مدل B
- AUC و Brier در سطح رزرو منفرد.
- **مهم‌تر:** خطای تجمیع‌شده در سطح $(d,m,r)$ پس از جمع احتمالات — این همان چیزی است که با مدل A مقایسه می‌شود.
- برش‌های اجباری گزارش: cold-start در برابر باسابقه · خوابگاهی در برابر غیرخوابگاهی · ناهار در برابر شام.

### 5.16.9 ➕ ⚠️ سقف واقع‌بینانه‌ی مدل B
فاز ۴ نشان داد صفت‌های فردی در **تجمیع** یکدیگر را خنثی می‌کنند: ترکیب رزروکنندگان فقط ۹.۸٪ واریانس نرخ سلول را توضیح می‌دهد در برابر ۸۳.۲٪ برای شوک روزانه (F59). پس:
- مدل B **خودکار** از مدل A بهتر نیست.
- برتری‌اش فقط از دو مسیر می‌آید: (الف) **برهم‌کنش ضرب‌شونده** (بند ۵.۱۸)، (ب) **وعده‌ی شام** که ترکیبش واقعاً متغیر است، r=+۰.۵۲ در برابر صفر برای ناهار (F63).
- این انتظار باید **پیش از** فاز ۷ ثبت شود تا نتیجه‌ی منفی، شکست تلقی نشود.

---

## 5.17 ➕ ویژگی‌های **عامل روز** (ساختار سلسله‌مراتبی) — بند جدید

> **چرا این بند وجود دارد.** بزرگ‌ترین کشف فاز ۴: **۸۳.۲٪ واریانس نرخ سلول، شوک مشترک روزانه است** (F59)، این شوک **سراسری دانشگاه** است (همبستگی جفت‌سلف +۰.۴۲، PC1=۴۰.۵٪، F60)، و **قابل‌پیش‌بینی** است (R²خارج‌نمونه=۰.۶۰۲، F61). نسخه‌ی ۲.۰ WBS هیچ فیچری برای این ساختار نداشت.

### 5.17.1 تعریف عامل روز
$$\text{shock}_{d,m} = \frac{1}{|R|}\sum_{r \in R}\left(\rho_{d,m,r} - \overline{\rho}_{m,r}\right)$$
یعنی میانگین انحراف سلف‌ها از عادت خودشان در آن روز-وعده. با ≥۳ سلف محاسبه می‌شود.

### 5.17.2 فیچرهای مشتق (همه leakage-safe)
- ⭐ **`day_shock_lag1`** — شوک آخرین وعده‌ی **هم‌نوع** در دسترس. بزرگ‌ترین تک‌بهبود: R²خارج‌نمونه از ۰.۴۲۳ به **۰.۶۰۲** و MAE −۴۲.۴٪ (F61).
- `day_shock_lag2`, `day_shock_lag7` — برای شام معنادارند (lag۷=+۰.۵۰۵)، برای ناهار کمتر (F61).
- `day_shock_rolling_mean_7` — سطح اخیر عامل روز.
- ⚠️ **قاعده‌ی برش:** شوک روز $d-1$ فقط اگر آن وعده تا لحظه‌ی برش سرو شده باشد. برای هدفِ ناهار روز $d$، شوکِ **شام** روز $d-1$ در دسترس **نیست**.

### 5.17.3 پیامد معماری
مدل باید **دو مرحله‌ای** باشد، نه تخت:
1. پیش‌بینی عامل روزِ دانشگاه (فیچرها: تقویم + `log(daily_total_Res)` + `day_shock_lag`) — R²خارج‌نمونه ۰.۶۰ اثبات‌شده.
2. پیش‌بینی انحراف هر سلف از عامل روز.

این تصمیم باید در `decision_log` ثبت و در فاز ۷ در برابر مدل تخت محک بخورد.

## 5.18 ➕ ویژگی‌های **برهم‌کنش ضرب‌شونده** — بند جدید

> **چرا.** اثر فرد و روز **ضرب‌شونده** است نه جمع‌شونده: در روزهای بد، بی‌ثبات‌ها +۱۸.۵ واحد درصد می‌ریزند و وفادارها +۵.۵ (نسبت **۳.۳۶×**، ضریب تعامل +۳.۸۸، p≈۰) (F62). این دقیقاً توضیح می‌دهد چرا میانگین ساده‌ی تاریخچه‌ها در سطح سلول شکست خورد.

- `composition_mean × day_shock_lag1` — فیچر ضربی صریح.
- `composition_std` و `composition_p90` — **پراکندگی** ترکیب، نه فقط میانگینش. اگر اثر ضربی است، دُم توزیع تاریخچه‌ها مهم‌تر از میانگینش است.
- برای مدل‌های خطی/کوانتایل خطی **الزامی**؛ برای مدل‌های درختی به‌عنوان کمک (باید در فاز ۷ آزمون شود که آیا درخت خودش کشفش می‌کند).
- چهار برهم‌کنش تأییدشده‌ی سطح تجمیعی از F42: `dow×restaurant_type` (ΔAIC=۵۶.۲) · `meal×restaurant_type` (۵۲.۷) · `dow×city` (۲۰.۷) · `city×meal` (۱۹.۴).

## 5.19 ➕ **پل A↔B**: تجمیع خروجی مدل B به فیچر مدل A — بند جدید

- `composition_expected_rate` = میانگین `person_expanding_norecv_rate` رزروکنندگان آن سلول (leakage-safe؛ رزرو ۷۲ ساعت زودتر بسته می‌شود پس هویت رزروکنندگان در لحظه‌ی برش معلوم است).
- ⚠️ **فقط برای شام مفید است:** درون سلف×وعده، شام r=+۰.۵۱۹ (p<۱e−۴) · ناهار r=−۰.۰۲۰ (p=۰.۳۷) (F63). دلیل: شام خوابگاهی جمعیت کوچک و متغیر دارد؛ ناهار دانشکده جمعیت بزرگ و همگن.
- **پس فیچر باید با برهم‌کنش وعده وارد شود** یا فقط در زیرمدل شام.
- `composition_coverage` — سهم رزروکنندگانی که سابقه‌ی کافی دارند (کیفیت فیچر بالا).

---

**معیار پایان فاز ۵:** `doc/feature_registry.md` و `doc/leakage_audit.md` کامل · همه‌ی فیچرست‌های بند ۵.۱۲ ساخته و ذخیره شده · هیچ فیچر رد‌شده‌ی فاز ۴ در فیچرست نهایی نباشد · تست خودکار عدم‌نشت پاس شود.

---

# فاز ۶ — طراحی پروتکل اعتبارسنجی

> **نسخه ۲.۱ — بازنویسی‌شده پس از فازهای ۴ و ۵ (۲۰۲۶-۰۸-۱۴).** تغییرات در بند ۶.۰.
> این فاز کوتاه است ولی **اعتبار کل پروژه به آن وابسته است.** آن را قبل از هر مدل‌سازی نهایی کنید و دیگر تغییرش ندهید.

## 6.0 تغییرات این نسخه نسبت به ۲.۰ (مبتنی بر شواهد فاز ۴ و ۵)

| تغییر | شاهد | بند |
|---|---|---|
| 🔄 **walk-forward به‌جای تقسیم منفرد، معیار اصلی می‌شود**؛ پنجره‌ی انتهایی به «آزمون فشار» تنزل می‌کند | ۲۵٪ انتهایی داده غیرعادی‌ترین بخش آن است (پس از رمضان + سوگواری ملی)؛ در ممیزی فاز ۵ همین باعث شد $R^2$ فیچرست‌ها گمراه‌کننده شود | 6.1 |
| ➕ **بوت‌استرپ بلوکی دوبعدی** (روز **و** سلف)، نه تک‌بعدی | ICC(روز)=۰.۲۲۵ و ICC(سلف)=۰.۲۰۵ تقریباً برابرند (F10) | 6.6 |
| ➕ **اندازه‌ی نمونه‌ی مؤثر** صریحاً گزارش شود | ۸۳٪ واریانس سلول شوک مشترک روز است (F59) ⇒ n مؤثر به‌مراتب کمتر از ۷٬۵۷۹ | 6.6 |
| 🔄 **B2 با کوچک‌سازی بیزی** به‌جای میانه‌ی خام (B3 کوانتایل خام می‌ماند — کوچک‌سازی برای میانگین تعریف شده، نه کوانتایل) | نرخ گروه‌های کم‌حجم به‌شدت نویزی است (F11، F8.3) | 6.5 |
| ➕ **B6: خط پایه‌ی عامل روز** افزوده شد | شوک روزانه با اطلاعات برش R²out=۰.۶۰ دارد (F61) — رقیب جدی‌تر از B3 | 6.5 |
| ➕ بند ۶.۱۰ — **تحلیل حساسیت τ** (تعهد سند تعریف مسئله، سررسیدش همین فاز) | بند ۱.۲ WBS و بند ۲-۲ سند مسئله | 6.10 |
| ➕ بند ۶.۱۱ — **سیاست پرت در CV** | F05 (۱۲ رکورد ρ=۱ همگی رویداد واقعی)، بند ۴.۵.۲ | 6.11 |
| ⚠️ **هشدار حیاتی مدل B:** جمع ساده‌ی احتمالات فردی عدم‌قطعیت را ~۳.۸ برابر کم‌برآورد می‌کند | بیش‌پراکندگی ۱۴.۴ برابر در سطح فرد (F08) | 6.9 |
| ➕ سه روز پایانی داده (سوگواری ملی) صریحاً پرچم‌گذاری شد | ردیف ۲۰ decision_log آن را برای همین فاز نگه داشته بود | 6.1 |

## 6.1 تقسیم زمانی

- **قاعده اول:** تقسیم بر اساس **تاریخ**، نه ردیف. تمام رکوردهای یک روز در یک بخش. (چون فیچرها بین‌سلفی‌اند و ICC(روز)=۰.۲۲۵.)
- **قاعده دوم:** پنجره‌ی انتهایی فقط **یک بار** استفاده می‌شود.

### 🔄 تغییر نسبت به ۲.۰: چرا تقسیم منفرد ۶۰/۲۰/۲۰ کافی نیست

نسخه‌ی ۲.۰ یک تقسیم `Train(60٪) → Val(20٪) → Test(20٪)` پیشنهاد می‌داد. فاز ۵ نشان داد این طراحی در **این** داده گمراه‌کننده است: ۲۵٪ انتهایی بازه (۱۴۰۳-۰۱-۲۸ تا ۱۴۰۳-۰۳-۰۱) شامل بازگشت پس از رمضان و سه روز سوگواری ملی است — یعنی همه‌ی رژیم‌های غیرعادی دقیقاً در مجموعه‌ی آزمون جمع می‌شوند و هیچ نمونه‌ی مشابهی در آموزش نیست.

**طراحی جایگزین (دو لایه):**

| لایه | نقش | کاربرد |
|---|---|---|
| **الف) Walk-forward با مبدأ متحرک** (۵ fold، پنجره‌ی گسترشی) | 🔴 **معیار اصلی انتخاب مدل** | همه‌ی تصمیم‌های فاز ۷ |
| **ب) پنجره‌ی انتهایی قفل‌شده** (~۲۵ روز آخر) | 🟡 **آزمون فشار**، نه معیار انتخاب | فقط یک‌بار در فاز ۸، با ذکر صریح غیرعادی‌بودنش |

> **چرا این تنزل درست است و «تقلب» نیست:** مجموعه‌ی آزمون باید نماینده‌ی شرایط استقرار باشد. یک پنجره‌ی ۲۵ روزه که تماماً پس از یک شکاف ساختاری ۲۹ روزه و در ایام سوگواری ملی می‌افتد، نماینده‌ی «یک روز عادی سلف» نیست. گزارش هر دو لایه — و صریح‌گفتن اینکه لایه‌ی (ب) بدترین حالت است — صادقانه‌تر از تکیه بر یکی است.

### سه روز پایانی
۱۴۰۳-۰۲-۳۱ تا ۱۴۰۳-۰۳-۰۱ تحت تأثیر سوگواری ملی‌اند (ردیف ۲۰ decision_log). این‌ها **حذف نمی‌شوند** (در واقعیت هم رخ می‌دهند) ولی در گزارش فاز ۸ به‌عنوان یک برش جداگانه می‌آیند.

## 6.2 اعتبارسنجی متقاطع زمانی (Walk-Forward)

- ✅ **گزینه A — پنجره گسترشی (Expanding):** آموزش روی [۰..t]، آزمون روی [t+1..t+k]. **انتخاب‌شده** — با ۱۴۲ روز، دور‌ریختن داده‌ی قدیمی قابل‌توجیه نیست.
- گزینه B (لغزان) فقط اگر فاز ۸ شکست رژیمی نشان دهد.
- **تعداد fold: ۵**، هر کدام ~۲۰ روز آزمون، با حداقل ۶۰ روز آموزش اولیه.

### ⚠️ آیا purge gap لازم است؟
**خیر — و دلیلش ساختاری است، نه تجربی.** همه‌ی فیچرهای تاریخی از `src/features/cutoff.py` عبور می‌کنند که مرزش `meal_seq ≤ target − 2` است، یعنی هر فیچر فقط از گذشته‌ی خودِ آن ردیف ساخته می‌شود. پس هیچ فیچری در fold آزمون از داده‌ی fold آزمون تغذیه نمی‌شود.

⚠️ **ولی یک استثنا هست:** فیچرهای انبساطی (`cell_expanding_rate`, `person_expanding_norecv_rate`) روی **کل بازه** یک‌بار محاسبه شده‌اند. چون قاعده‌ی برش رعایت شده، برای هر ردیف فقط گذشته‌ی خودش استفاده شده — پس نشتی از test به train نیست. ولی **مقدار فیچر برای ردیف‌های آموزش، مستقل از اینکه fold کجا تمام می‌شود، ثابت است** که همان رفتار درست است. این باید با تست واحد تأیید شود (بند ۶.۸).

## 6.3 افق پیش‌بینی
- $h = 1$ وعده (تصمیم در لحظه‌ی برشِ همان وعده). **تثبیت‌شده.**
- **کاوش اختیاری:** $h = 2, 3$ روز — چون رزرو ۷۲ ساعت زودتر بسته می‌شود، پیش‌بینی زودتر ممکن است و برای تدارک مواد اولیه ارزشمندتر. عملکرد در برابر $h$ رسم شود.
- ⚠️ در $h>1$، فیچر `day_shock_lag1` ضعیف‌تر می‌شود (باید به lag دورتر تکیه کند) — این افت باید کمّی شود.

## 6.4 مجموعه معیارها

| معیار | نقش | نکته |
|---|---|---|
| **Pinball Loss @ τ** | 🔴 اصلی | $L_\tau(y,\hat y) = \max(\tau(y-\hat y), (\tau-1)(y-\hat y))$ |
| **نرخ کمبود** | 🔴 اصلی-عملیاتی | ٪ مواردی که $\widehat{CookQty} < Recv$ |
| **٪ کاهش هدررفت** | 🔴 اصلی-عملیاتی | نسبت به B0 |
| **صرفه‌جویی ریالی** | 🔴 کسب‌وکاری | با CI بوت‌استرپ **بلوکی دوبعدی** (۶.۶) |
| **Coverage / Calibration** | 🔴 کیفیت کوانتایل | آیا $\tau$ واقعاً همان نرخ کمبود را می‌دهد؟ **با بیش‌پراکندگی ۷.۶ برابر (F09) این تضمین‌شده نیست** |
| RMSE / MAE | 🟡 فنی | برای مقایسه با ادبیات |
| $R^2$ | 🟡 گزارشی | ⚠️ سقف واقع‌بینانه ۰.۴–۰.۵ (F59، F09) — عدد بالاتر مشکوک است |
| MASE | 🟡 فنی | مقیاس‌ناپذیر |
| **sMAPE** | ❌ | با $\rho$ نزدیک صفر منفجر می‌شود — **استفاده نشود** |

- **⚠️ حیاتی:** هر معیار هم در فضای **نرخ ($\rho$)** و هم **تعداد پرس** ($\times Res$) گزارش شود. مدیریت سلف به دومی اهمیت می‌دهد و وزن‌دهی این دو کاملاً متفاوت است.

## 6.5 خط پایه‌های چندگانه

| کد | تعریف | نقش |
|---|---|---|
| **B0** | $\widehat{CookQty} = Res$ (وضع موجود) | سقف هدررفت |
| **B1** | $\hat\rho$ = میانگین سراسری تاریخی | ساده‌ترین |
| **B2** | $\hat\rho$ = نرخ **کوچک‌شده‌ی** سلف×وعده×روزهفته | baseline رسمی سند |
| **B3** | $\hat\rho$ = کوانتایل تجربی $\tau$ همان گروه | ✅ **رقیب سرسخت** |
| **B4** | $\hat\rho_d = \rho_{d-7}$ (ساده فصلی) | baseline سری زمانی |
| **B5** | میانگین متحرک ۷ روزه | baseline هموارساز |
| **➕ B6** | نرخ تاریخی سلف×وعده **+ شوک روز پیش‌بینی‌شده** | ✅ **رقیب جدی‌تر از B3** |

🔄 **تغییر B2:** به‌جای میانه‌ی خام، از نرخ کوچک‌شده‌ی بیزی $(k+\alpha)/(n+\alpha+\beta)$ با α=۰.۹۷۶ و β=۱۱.۱۵ استفاده می‌شود — چون F11 نشان داد دُم توزیع عمدتاً از گروه‌های کم‌حجم می‌آید.

⚠️ **B3 کوانتایل خام باقی می‌ماند و این یک محدودیت آگاهانه است.** کوچک‌سازی بیزی برای *میانگین* یک نسبت تعریف شده و تعمیم مستقیمی به *کوانتایل* ندارد. اندازه‌ی گروه‌ها این را قابل‌قبول می‌کند ولی نه بی‌اشکال: میانه‌ی ۱۸ مشاهده در هر گروه (۲۱۳ گروه)، ولی **۱۱٪ گروه‌ها کمتر از ۱۰ مشاهده دارند**. در $\tau=0.20$ برآورد حدوداً «چهارمین مقدار کوچک» است که پذیرفتنی است؛ در $\tau=0.05$ عملاً «کمینه» می‌شود و شکننده است. **این یکی از جاهایی است که مدل فاز ۷ می‌تواند واقعاً برتری بگیرد** — رگرسیون کوانتایل قدرت را بین گروه‌ها قرض می‌گیرد.

➕ **چرا B6 اضافه شد:** فاز ۴ نشان داد شوک روزانه با تقویم + حجم رزرو + lag، $R^2$ خارج‌نمونه ۰.۶۰ دارد (F61). یک خط پایه‌ی ساده که همین را به نرخ تاریخی سلف اضافه کند، رقیب واقعی‌تری از B3 است — و اگر مدل‌های فاز ۷ آن را نبرند، یافته‌ی مهمی است.

> **B3 و B6 را دست‌کم نگیرید.** اگر مدل شما آن‌ها را نبرد، این یک یافته‌ی صادقانه و ارزشمند برای گزارش است، نه شکست.

## 6.6 آزمون معناداری تفاوت مدل‌ها

- **Diebold-Mariano** برای مقایسه‌ی دو مدل روی سری زمانی.
- ➕ **بوت‌استرپ بلوکی دوبعدی:** نمونه‌گیری هم‌زمان روی **روز** و **سلف**. تک‌بعدی کافی نیست چون ICC هر دو بُعد تقریباً برابر است (F10: ۰.۲۲۵ و ۰.۲۰۵).
- ➕ **گزارش اندازه‌ی نمونه‌ی مؤثر:** با ICC(روز)=۰.۲۲۵ و میانگین ~۵۳ رکورد در روز، $n_{\text{eff}} \approx n / (1 + (\bar m - 1)\rho_{ICC})$ به‌مراتب کمتر از تعداد خام است. **هر CI که این را نادیده بگیرد باریک‌تر از واقعیت است.**
- **قاعده:** «مدل A بهتر از B است» فقط با CI عدم‌پوشش صفر گفته شود.

## 6.7 تصمیم درباره تطبیق سلسله‌مراتبی
- مدل در سطح $f$ + جمع، یا مستقیم در سطح $(d,m,r)$، یا هر دو + آشتی (MinT).
- ➕ **گزینه‌ی چهارم از فاز ۵:** مدل **دومرحله‌ای** (عامل روز دانشگاه → انحراف سلف)، طبق بند ۵.۱۷.۳.
- **معیار پایان:** هر چهار رویکرد در فاز ۷ به‌عنوان یک محور آزمایش مقایسه شوند.

## 6.8 پیاده‌سازی و آزمون خود پروتکل
- `src/cv.py` با کلاس splitter و **تست واحد**:
  - هیچ تاریخی هم‌زمان در train و test یک fold نباشد
  - ترتیب زمانی fold‌ها صعودی باشد
  - ➕ تأیید اینکه مقدار فیچرهای انبساطی مستقل از مرز fold است (بند ۶.۲)
  - ➕ تأیید اینکه مرزهای تاریخی مدل A و B **یکسان**‌اند
- **معیار پایان M3:** پروتکل پیاده و تست‌شده + هر ۷ baseline اجرا و ثبت‌شده.

## 6.9 اعتبارسنجی و معیار اختصاصی مدل B

- **همان مرزهای تاریخی بند ۶.۱** (نه تقسیم مستقل) تا A و B قابل‌مقایسه بمانند.
- **معیارهای سطح فرد:** Log-loss، Brier، **PR-AUC** (نه ROC-AUC؛ کلاس ~۸٪ نامتوازن)، نمودار کالیبراسیون.
- **معیار نهایی:** تجمیع به سطح $(d,m,r,f)$ و همان Pinball Loss.

### ⚠️ هشدار حیاتی تجمیع (F08)
فرض استقلال افراد **قاطعانه رد شده**: واریانس گروهی ۱۴.۴ برابر دوجمله‌ای است. پس:

$$\text{Var}\Big(\sum_p \hat p_p\Big) \ne \sum_p \hat p_p(1-\hat p_p)$$

جمع ساده‌ی احتمالات، **عدم‌قطعیت را ~۳.۸ برابر کم‌برآورد می‌کند** ⇒ کوانتایل خوش‌بینانه ⇒ **ریسک کمبود واقعی بیشتر از ادعای مدل**. روش تجمیع در بند ۷.۱۱.۳ باید این را جبران کند (مثلاً بوت‌استرپ خوشه‌ای روی روز، یا مدل Beta-Binomial با پارامتر بیش‌پراکندگی برآوردشده).

## 6.10 ➕ تحلیل حساسیت τ — تعهد سند تعریف مسئله

بند ۲-۲ سند مسئله و بند ۱.۲ WBS: پس از EDA و **پیش از قفل پروتکل**، هر سناریوی $\tau \in \{0.02, 0.05, 0.10, 0.15, 0.20\}$ روی داده‌ی واقعی محک بخورد و در `reports/tau_sensitivity.md` با دقیقاً دو عدد گزارش شود:

- **(الف)** ٪ صرفه‌جویی در غذای مازاد نسبت به B0
- **(ب)** ٪ رخداد کمبود ($\widehat{CookQty} < Recv$)

**رابطه‌ی نظری:** چون $\widehat{CookQty} = \lceil Res(1-\hat\rho_\tau) \rceil$ و کمبود یعنی $\hat\rho_\tau > \rho$، در حالت کالیبره‌ی کامل **$\tau$ خودش نرخ کمبود است**. انحراف مشاهده‌شده از این تساوی، سنجه‌ی کالیبراسیون است.

**خروجی:** توصیه‌ی نهایی با اولویت **نادر ماندن کمبود**، برای تأیید مدیریت سلف.

## 6.11 ➕ سیاست داده‌ی پرت در CV

طبق بند ۴.۵.۲ و F05 (هر ۱۲ رکورد $\rho=1$ به رویداد واقعی نگاشت شدند — آلودگی، برف، شروع رمضان):

| سیاست | مجموعه آموزش | مجموعه آزمون |
|---|---|---|
| پرت‌های **رویداد واقعی** | نگه‌داشتن + فلگ | **همیشه نگه‌داشتن** |
| پرت‌های **خطای ثبت** | حذف | حذف |

- **قاعده‌ی مطلق:** هیچ پرتی از مجموعه‌ی **آزمون** حذف نمی‌شود — در واقعیت هم رخ می‌دهد و مدلی که نمی‌تواند با آن کنار بیاید، در استقرار شکست می‌خورد.
- در این داده هیچ پرت «خطای ثبت» تأییدشده‌ای پیدا نشد (F05)، پس عملاً **هیچ ردیفی حذف نمی‌شود** — این تصمیم صریحاً ثبت می‌شود تا بعداً بازآفرینی‌پذیر باشد.

---

# فاز ۷ — مدل‌سازی و تنظیم

راهبرد: **از ساده به پیچیده، با ثبت هر آزمایش.** هرگز مستقیم به سراغ شبکه عصبی نروید.

## 7.1 خانواده ۱ — خطی و تعمیم‌یافته
- Linear Regression, **Ridge**, **Lasso**, ElasticNet
- **Quantile Regression** (خطی، مستقیماً pinball loss را کمینه می‌کند)
- **GLM با خانواده Binomial** (logit link, با وزن $Res$) ← از نظر آماری اصولی‌ترین برای این هدف
- **Beta Regression** (`statsmodels` / `betareg`)
- **GAM** (`pyGAM`) — روابط غیرخطی صاف با قابلیت تفسیر بالا
- **مزیت این خانواده:** قابل تفسیر، پایدار روی داده کم، مناسب دفاع پایان‌نامه.

## 7.2 خانواده ۲ — درختی و بوستینگ (احتمالاً برنده)
- Decision Tree (به‌عنوان مرجع تفسیری)
- **Random Forest**, Extra Trees
- **Quantile Regression Forest** (`skgarden` / `quantile-forest`) — کوانتایل بومی
- **LightGBM** با `objective='quantile', alpha=τ` ✅ نقطه شروع قوی
- **XGBoost** با `reg:quantileerror`
- **CatBoost** با `loss_function='Quantile:alpha=τ'` — مدیریت بومی متغیر دسته‌ای، مناسب نام سلف/غذا
- **نکته:** برای هدف نرخ، `objective='binary'` با `weight=Res` هم گزینه هوشمندانه‌ای است (معادل دوجمله‌ای).

## 7.3 خانواده ۳ — سری زمانی کلاسیک
- **SARIMAX** با متغیرهای برون‌زا (فصلی هفتگی $s=7$)
- **ETS / Holt-Winters**
- **Prophet** (مدیریت خوب تعطیلات، ولی برای ۱۴۲ روز و ساختار پنلی ضعیف‌تر است)
- **TBATS** (فصلی‌های چندگانه)
- **⚠️ ارزیابی واقع‌بینانه:** با ۱۴۲ روز و ساختار پنلی (چند سری موازی)، این خانواده احتمالاً **از درختی ضعیف‌تر** خواهد بود — چون قدرت اصلی مسئله شما در $Res$ (کوواریت آینده معلوم) و فیچرهای مقطعی است، نه در دینامیک زمانی سری. اما **اجرا و گزارش آن برای کامل بودن مقایسه لازم است.**

## 7.4 خانواده ۴ — شبکه عصبی
- MLP ساده
- **LSTM / GRU / TCN**
- **N-BEATS / NHITS** (`darts` / `neuralforecast`)
- **TFT (Temporal Fusion Transformer)** — از نظر مفهومی **دقیقاً** برای مسئله شما طراحی شده: پشتیبانی بومی از known-future covariates ($Res$)، متغیرهای ایستا (سلف)، و **خروجی کوانتایل**.
- **⚠️ هشدار صادقانه:** با ~۱۶ هزار ردیف و ۱۴۲ روز، شبکه‌های عصبی به‌شدت مستعد Overfit هستند و احتمالاً از LightGBM بهتر نخواهند بود. **اجرا کنید، گزارش کنید، ولی انتظار برد نداشته باشید** — و همین «انتظار نداشتن مستدل» خودش یک بحث علمی خوب در گزارش است.

## 7.5 خانواده ۵ — سلسله‌مراتبی و بیزی
- **Mixed Effects Model** — سلف به‌عنوان اثر تصادفی (`statsmodels MixedLM`)
- **Hierarchical Bayesian** (`PyMC` / `NumPyro`) با ساختار Beta-Binomial — به‌طور طبیعی کوچک‌سازی برای سلف‌های کم‌داده انجام می‌دهد و **توزیع پسین کامل** می‌دهد که مستقیماً کوانتایل قابل استخراج است.
- **این خانواده اگر وقت دارید بسیار ارزش دارد** — از نظر روش‌شناختی زیبا و دقیقاً منطبق با ساختار مسئله.

## 7.6 رویکردهای عدم‌قطعیت و کوانتایل
- **Quantile Regression** مستقیم (LightGBM/QRF/Linear) — یک مدل به ازای هر $\tau$
- **Conformal Quantile Regression (CQR)** — تضمین پوشش توزیع‌آزاد ✅ بسیار قدرتمند و مدرن
- **Bootstrap / Ensemble prediction intervals**
- **Bayesian posterior quantiles**
- **کار:** کالیبراسیون هر روش را بسنجید (بند 8.8) — مدلی که «کوانتایل ۹۰٪» می‌گوید ولی ۷۵٪ پوشش می‌دهد بی‌فایده است.

## 7.7 محورهای آزمایش (Experiment Axes)
به‌جای آزمایش تصادفی، فضا را به‌صورت شبکه‌ای بپیمایید:

| محور | گزینه‌ها |
|---|---|
| خانواده مدل | Linear / GLM / RF / LGBM / NN / Bayesian |
| فیچرست | FS_minimal / FS_calendar / FS_full / FS_selected |
| هدف | ρ / logit(ρ) / Binomial / count |
| دامنه مدل | Global / per-cluster / per-restaurant |
| سطح تجمیع | per-food / aggregated / reconciled |
| وزن‌دهی | بدون وزن / $w=Res$ |
| $\tau$ | 0.05 / 0.10 / 0.15 / 0.20 |

- **راهبرد عملی:** ابتدا محورهای مدل و فیچرست را با $\tau$ ثابت بهینه کنید، سپس روی مدل برنده تحلیل حساسیت $\tau$ بزنید. پیمایش کامل شبکه غیرضروری و پرهزینه است.

## 7.8 تنظیم فراپارامتر
- **روش‌ها:** Grid Search / Random Search / **Bayesian با Optuna** ✅ / Hyperband
- **قاعده:** تنظیم فقط با CV زمانی (بند 6.2) و **فقط روی Train+Validation**. مجموعه Test هرگز.
- ثبت فضای جستجو، تعداد trial، و بهترین پارامترها برای هر مدل.
- **مراقب باشید:** با ۱۴۲ روز، جستجوی خیلی گسترده = Overfit روی مجموعه اعتبارسنجی. ۵۰-۱۰۰ trial کافی است.

## 7.9 ترکیب مدل‌ها (Ensemble)
- میانگین ساده / میانگین وزنی (وزن معکوس خطا)
- **Stacking** با متامدل خطی (با CV زمانی برای تولید out-of-fold predictions)
- Blending
- **نکته:** اگر ensemble فقط ۱٪ بهبود می‌دهد ولی تفسیرپذیری را نابود می‌کند، **در گزارش پایان‌نامه مدل ساده‌تر را توصیه کنید** و این trade-off را صریح بنویسید.

## 7.10 جدول مقایسه مدل‌ها
- خروجی نهایی فاز: جدولی با ستون‌های `مدل | فیچرست | Pinball@τ | RMSE | MAE | نرخ کمبود | ٪کاهش هدررفت | زمان آموزش | تفسیرپذیری` — مرتب‌شده بر اساس معیار اصلی.
- **همراه با CI** برای معیار اصلی.

## 7.11 خانواده ۶ — طبقه‌بندی سطح فرد (مدل B)

### 7.11.1 الگوریتم‌های طبقه‌بندی
- **Logistic Regression** با وزن‌دهی کلاس (`class_weight='balanced'`) — پایه تفسیرپذیر، دفاع‌پذیر برای پایان‌نامه.
- **LightGBM/XGBoost Classifier** با `scale_pos_weight` یا `class_weight` تنظیم‌شده روی نسبت واقعی کلاس‌ها (بند ۴-۱۳-۳).
- **Focal Loss** (پیاده‌سازی سفارشی objective) — برای عدم‌توازن شدید، وزن بیشتر به نمونه‌های سخت می‌دهد؛ فقط اگر وزن‌دهی ساده کافی نبود امتحان شود.
- **مدل اثرات مختلط با اثر تصادفی فرد** (ادامه بند ۵-۱۶-۳ و ۷-۵) — از نظر آماری زیباترین گزینه، `PersonId` به‌عنوان random intercept، طبیعتاً کوچک‌سازی برای افراد کم‌سابقه انجام می‌دهد.

### 7.11.2 کالیبراسیون احتمال
- خروجی خام درخت‌های تقویت‌شده اغلب ناکالیبره است. **Platt Scaling** یا **Isotonic Regression** روی مجموعه اعتبارسنجی اعمال شود (`CalibratedClassifierCV` با CV زمانی بند ۶-۲، نه تصادفی).
- کالیبراسیون **پیش از** تجمیع حیاتی است — احتمالات ناکالیبره، جمع‌شان را هم سیستماتیک اریب می‌کند.

### 7.11.3 ⭐ تجمیع از سطح فرد به سطح $(d,m,r,f)$ — نکته فنی محوری
جمع ساده احتمالات پیش‌بینی‌شده فقط **انتظار** ($E[NoRecv]$) را می‌دهد، نه یک **کوانتایل**. برای رسیدن به $\hat\rho_{\tau,d,m,r,f}$ قابل‌مقایسه مستقیم با مدل A، سه گزینه:
1. **توزیع پواسون-دوجمله‌ای (Poisson–Binomial):** مجموع $n$ متغیر برنولی مستقل با احتمالات ناهمسان دقیقاً این توزیع را دارد. با کتابخانه `poibin` می‌توان تابع توزیع تجمعی و از آن هر کوانتایل دلخواه را مستقیماً و بسته‌فرم محاسبه کرد — **دقیق‌ترین و توصیه‌شده‌ترین روش برای این پروژه.**
2. **شبیه‌سازی مونت‌کارلو:** نمونه‌گیری برنولی از هر احتمال پیش‌بینی‌شده، هزاران تکرار، کوانتایل تجربی از توزیع شبیه‌سازی‌شده مجموع — جایگزین ساده اگر `poibin` در دسترس نبود.
3. **تقریب نرمال (De Moivre–Laplace)** با میانگین $\sum p_i$ و واریانس $\sum p_i(1-p_i)$ — فقط برای $n$ بزرگ و احتمالات میانی معتبر؛ برای رزروهای کوچک (بند ۴-۷) نامعتبر است.
- **⚠️ فرض استقلال:** روش‌های بالا فرض می‌کنند تصمیم دریافت/عدم‌دریافت افراد از هم مستقل است. این فرض را در فاز ۴-۱۳ به‌طور اکتشافی محک بزنید (مثلاً آیا هم‌خوابگاهی‌ها رفتار همبسته دارند؟) — همبستگی مثبت بین افراد، واریانس واقعی مجموع را بزرگ‌تر از پواسون-دوجمله‌ای می‌کند و کوانتایل محاسبه‌شده را خوش‌بینانه می‌کند.
- **خروجی:** تابع `aggregate_person_to_group(probs, tau) -> CookQty` در `src/`, با تست واحد در برابر یک مثال دستی محاسبه‌شده.

---

# فاز ۸ — ارزیابی و تحلیل خطا

### 8.1 ارزیابی نهایی روی Test قفل‌شده
- **فقط یک بار**، فقط با مدل‌های نهایی منتخب (حداکثر ۳ مدل + baseline ها).
- گزارش تمام معیارهای بند 6.4.

### 8.2 تحلیل باقیمانده
- توزیع باقیمانده‌ها: هیستوگرام، Q-Q plot
- باقیمانده در برابر: مقدار پیش‌بینی‌شده، $Res$، تاریخ، سلف، روز هفته
- **الگو در باقیمانده = فیچر جامانده.** اگر باقیمانده‌ها با تاریخ روند دارند، مدل روند را نگرفته است.
- ACF باقیمانده‌ها + Ljung-Box (باید سفید باشند)

### 8.3 تجزیه خطا (Error Decomposition)
جدول عملکرد به تفکیک هر بُعد — این جایی است که می‌فهمید مدل کجا شکست می‌خورد:

| بُعد | برش‌ها |
|---|---|
| سلف | هر سلف جداگانه |
| وعده | ناهار / شام |
| روز هفته | ۶ روز کاری |
| حجم رزرو | چارک‌های $Res$ |
| نوع غذا | خورشتی / برنجی |
| نوع روز | عادی / قبل تعطیل / بازه امتحان |
| زمان | ماه به ماه (آیا عملکرد در ماه‌های آخر افت می‌کند؟ → drift) |
| دانشکده/خوابگاه (فقط مدل B) | تفکیک عملکرد جمعیتی؛ آیا خطا در گروه خاصی متمرکز است؟ |
| کاربر جدید در برابر باسابقه (فقط مدل B) | ارزیابی مجزای جمعیت cold-start (بند ۵-۱۶-۴) |

### 8.4 تحلیل بدترین موارد
- ۲۰ پیش‌بینی با بیشترین خطا: چه اتفاقی افتاده؟ الگوی مشترک؟
- **تفکیک بحرانی:** بدترین **کمبودها** جداگانه تحلیل شوند (چون هزینه‌شان بالاتر است).

### 8.5 شبیه‌سازی عملیاتی (Backtest کسب‌وکاری)
- سناریو: «اگر این مدل در کل بازه Test اجرا می‌شد، چه اتفاقی می‌افتاد؟»
- خروجی: تعداد پرس صرفه‌جویی‌شده، ریال صرفه‌جویی‌شده، تعداد روز-سلف-وعده با کمبود، بیشترین کمبود.
- **نمودار کلیدی برای مدیریت:** منحنی trade-off — محور X: نرخ کمبود، محور Y: صرفه‌جویی ریالی، هر نقطه یک $\tau$. این تک‌نمودار احتمالاً مهم‌ترین خروجی بصری پروژه شماست.

### 8.6 پایداری و حساسیت
- **پایداری روی fold ها:** انحراف معیار معیار اصلی بین fold ها — مدل با میانگین کمی بهتر ولی واریانس بسیار بالاتر، بدتر است.
- **حساسیت به seed:** آموزش با ۵ seed مختلف.
- **آزمون تنش:** عملکرد اگر یک فیچر مهم در دسترس نباشد (شبیه‌سازی خرابی داده هوا).
- **حساسیت به $\tau$:** جدول کامل.

### 8.7 مقایسه آماری با خط پایه
- Diebold-Mariano و bootstrap CI (بند 6.6) در برابر B2 و B3.
- **گزاره نهایی باید این شکل باشد:** «مدل X با اطمینان ۹۵٪، pinball loss را نسبت به B3 به میزان $\Delta$ [CI: a, b] کاهش می‌دهد.»

### 8.8 کالیبراسیون کوانتایل
- **نمودار قابلیت اطمینان:** برای $\tau \in \{0.05, ..., 0.95\}$، نسبت واقعی مشاهدات زیر پیش‌بینی را رسم کنید. خط ایده‌آل = قطر.
- اگر ناکالیبره است: بازکالیبراسیون با conformal prediction.

### 8.9 بررسی Overfit / Underfit
- منحنی یادگیری (خطای train vs validation در برابر اندازه داده)
- شکاف train-test: اگر بزرگ است → Overfit → منظم‌سازی بیشتر / فیچر کمتر
- اگر هر دو بد است → Underfit → مدل پیچیده‌تر / فیچر بهتر

### 8.10 مقایسه نهایی مدل A در برابر مدل B
- هر دو مدل روی همان مجموعه Test (بند ۸-۱) و با همان معیار Pinball در سطح $(d,m,r,f)$ ارزیابی می‌شوند (تجمیع مدل B طبق بند ۷-۱۱-۳).
- **آزمون معناداری Diebold-Mariano** (بند ۶-۶) بین A و B.
- **سه نتیجه ممکن و اقدام متناظر:**
  1. B به‌طور معنادار بهتر است → B به‌عنوان مدل نهایی معرفی می‌شود؛ دلیل احتمالی برتری (سیگنال فردی) در گزارش تحلیل شود.
  2. تفاوت معنادار نیست → مدل A به‌دلیل سادگی و عدم نیاز به داده سطح فردی ترجیح داده می‌شود؛ این خودش یک یافته صادقانه و ارزشمند است.
  3. A و B خطاهای مکمل دارند (همبستگی خطای پایین بین دو مدل) → **Ensemble A+B** (بند ۷-۹) را امتحان کنید.
- **خروجی:** یک بند صریح در گزارش نهایی که یکی از این سه حالت را با عدد و CI مستند می‌کند.

---

# فاز ۹ — تفسیرپذیری

### 9.1 اهمیت ویژگی
- اهمیت درختی (gain, split count) — **با احتیاط، سوگیری به سمت کاردینالیتی بالا دارد**
- **Permutation Importance** روی مجموعه اعتبارسنجی ✅ قابل‌اعتمادتر
- مقایسه رتبه‌بندی دو روش

### 9.2 SHAP
- **Global:** summary plot (beeswarm), bar plot میانگین |SHAP|
- **Dependence plots:** برای ۵ فیچر برتر — شکل رابطه و تعامل
- **Local:** waterfall برای موارد جالب (یک روز عادی، یک روز قبل تعطیلی، یک پرت)
- **Interaction values** برای تأیید تعامل‌های کشف‌شده در بند 4.9

### 9.3 اثرات نهایی
- **PDP** (Partial Dependence) — با احتیاط در حضور همبستگی
- **ICE** (Individual Conditional Expectation) — کشف ناهمگنی
- **ALE** (Accumulated Local Effects) ✅ مقاوم به همبستگی، بهتر از PDP

### 9.4 اعتبارسنجی دامنه‌ای
- ✅ **مهم‌ترین بند این فاز:** هر یافته مدل را در برابر منطق دامنه بسنجید.
- مثال بررسی: آیا مدل می‌گوید «نرخ عدم‌دریافت با افزایش دما زیاد می‌شود»؟ آیا این معقول است یا هم‌آیندی کاذب با تابستان است؟
- **اگر مدل رابطه‌ای می‌یابد که هیچ توجیه دامنه‌ای ندارد، آن را به‌عنوان یافته گزارش نکنید — به‌عنوان هشدار گزارش کنید.**

### 9.5 مدل جانشین قابل‌فهم
- برازش یک درخت تصمیم کم‌عمق (عمق ۳-۴) روی پیش‌بینی‌های مدل پیچیده.
- **خروجی:** چند قاعده ساده که به مدیر سلف قابل توضیح است، مثل: «در روزهای قبل از تعطیلی، حدود ۱۵٪ کمتر از رزرو بپزید».
- **این احتمالاً چیزی است که واقعاً در سلف استفاده خواهد شد.**

### 9.6 تفسیرپذیری مدل B
- SHAP برای طبقه‌بند سطح فرد: آیا تاریخچه شخصی یا فیچرهای جمعیتی (دانشکده/خوابگاه) اهمیت بیشتری دارند؟
- **اعتبارسنجی دامنه‌ای اختصاصی:** آیا الگوی کشف‌شده (مثلاً «خوابگاه X نرخ بالاتر دارد») با فرضیه فاصله فیزیکی (H13) همخوانی دارد، یا صرفاً هم‌آیندی با یک متغیر پنهان دیگر (مثلاً ترکیب رشته‌های ساکن آن خوابگاه) است؟

---

# فاز ۱۰ — لایه تصمیم و تحلیل کسب‌وکاری

### 10.1 تبدیل پیش‌بینی به عدد پخت
$$\widehat{CookQty}_{d,m,r,f} = \left\lceil Res_{d,m,r,f} \times (1-\hat\rho_{\tau}) \right\rceil$$
- اعمال قیود عملیاتی: کف/سقف، گرد کردن به مضرب بسته پخت، ظرفیت آشپزخانه.
- **تجمیع روی $f$** با روش تصمیم‌گیری‌شده در بند 6.7.

### 10.2 بهینه‌سازی Newsvendor
$$\tau^{*} = \frac{C_u}{C_u + C_o}$$
- جدول $\tau^*$ برای مقادیر مختلف نسبت $C_u/C_o$.
- تحلیل نقطه سربه‌سر: در چه نسبتی مدل دیگر صرفه ندارد؟

### 10.3 سناریوهای سیاستی
| سناریو | $\tau$ | نرخ کمبود انتظاری | صرفه‌جویی سالانه انتظاری |
|---|---|---|---|
| محافظه‌کارانه | 0.02 | … | … |
| متعادل | 0.10 | … | … |
| تهاجمی | 0.20 | … | … |

### 10.4 برآورد صرفه‌جویی با عدم‌قطعیت
- تعمیم از ۵ ماه به سالانه با **ذکر صریح فرضیات** (فرض تکرار الگو در ماه‌های پوشش‌داده‌نشده — که خودتان به‌عنوان محدودیت ثبت کرده‌اید).
- **فاصله اطمینان بوت‌استرپ** — نه یک عدد قطعی.
- **صداقت روش‌شناختی:** ننویسید «۳۰ میلیارد تومان صرفه‌جویی». بنویسید: «بر اساس عملکرد بازه آزمون و با فرض تداوم الگوی هفتگی، صرفه‌جویی سالانه در بازه X تا Y میلیارد تومان برآورد می‌شود؛ این برآورد به ماه‌های پوشش‌داده‌نشده تعمیم‌پذیر نیست.»

### 10.5 پروتکل پیشنهادی پیاده‌سازی آزمایشی
- طرح یک پایلوت: ۲ سلف، ۴ هفته، با اعلام دستی عدد مدل و ثبت نتیجه واقعی.
- **گروه کنترل:** ۲ سلف مشابه با روش فعلی — این تنها راه اثبات علّی اثر است.

### 10.6 بسط اختیاری: Newsvendor چندکالایی با ظرفیت مشترک — پیشنهاد آینده
- اگر ظرفیت آشپزخانه/بودجه پخت بین دو نوع غذای یک وعده مشترک باشد (نه مستقل)، مسئله به یک **newsvendor چندکالایی با قید ظرفیت** تبدیل می‌شود که با تجمیع ساده کوانتایل‌های مستقل هر غذا حل نمی‌شود.
- ادبیات مرتبط: رگرسیون کوانتایل مبتنی بر یادگیری ماشین برای سفارش هم‌زمان چند کالا با قید ظرفیت مشترک (بند ۱-۵).
- **خارج از محدوده نسخه فعلی**، ولی به‌عنوان جهت پژوهشی آینده در فصل «کار آینده» گزارش (بند ۱۱-۱) ذکر شود — به‌ویژه اگر پرسش‌نامه ذی‌نفعان (بند ۱-۴) نشان دهد ظرفیت واقعاً مشترک است.

---

# فاز ۱۱ — مستندسازی و ارائه (موازی با همه فازها)

### 11.1 گزارش فنی
ساختار پیشنهادی: مقدمه و بیان مسئله → مرور ادبیات → داده و روش جمع‌آوری → EDA و یافته‌ها → مهندسی ویژگی → روش‌شناسی و اعتبارسنجی → نتایج → تفسیر → تحلیل کسب‌وکاری → محدودیت‌ها → کار آینده

### 11.2 کارت مدل (Model Card)
کاربرد مورد نظر، داده آموزش، معیارها، عملکرد به تفکیک زیرگروه، **محدودیت‌ها**، شرایطی که مدل نباید استفاده شود (مثلاً تابستان، ترم جدید بدون بازآموزی). 

### 11.3 مجموعه نمودارهای کلیدی
حداکثر ۱۲ نمودار نهایی، با فونت فارسی صحیح و عنوان‌های گزاره‌ای.

### 11.4 مخزن کد قابل بازتولید
`README.md` با: نصب، ساختار، دستور بازتولید کامل (`make all`), جدول نتایج مرجع.

### 11.5 ارائه به ذی‌نفعان
- نسخه فنی (استاد/داور): روش‌شناسی و اعتبارسنجی محور.
- نسخه مدیریتی (معاونت دانشجویی): منحنی trade-off بند 8.5، قواعد ساده بند 9.5، عدد صرفه‌جویی با CI.

### 11.6 فصل محدودیت‌ها (این را جدی بنویسید)
پوشش زمانی ۵ ماهه، عدم پوشش فصلی/ترمی، عدم تأیید تجربی در محیط واقعی، حساسیت به تغییر سیاست، عدم قطعیت $C_u$. **محدودیت‌های مرتبط با داده سطح فردی:** فرض استقلال بین افراد در تجمیع پواسون-دوجمله‌ای مدل B (بند ۷-۱۱-۳) تأییدنشده و صرفاً به‌طور اکتشافی محک خورده است؛ عملکرد ضعیف‌تر و گزارش‌شده جداگانه برای جمعیت cold-start (بند ۵-۱۶-۴)؛ عدم دسترسی به زمان ثبت رزرو و داده لغو حتی در فایل سطح فردی (بند ۲-۱-۲).

---

# فاز ۱۲ — استقرار و پایش (خارج از محدوده — فقط طراحی مفهومی)

`12.1` API (FastAPI) · `12.2` زمان‌بند شبانه · `12.3` داشبورد · `12.4` پایش drift (PSI/KS روی توزیع فیچرها) · `12.5` بازآموزی دوره‌ای · `12.6` حلقه بازخورد

---

# ماتریس وابستگی و مسیر بحرانی

```
0 ──▶ 1 ──▶ 2.1.2 (نامه درخواست داده) ─────────────┐  ← زودترین شروع ممکن
      │                                             │
      └──▶ 2.2–2.6 (داده خارجی) ──▶ 3 ──▶ 4 ──▶ 5 ──┼──▶ 7 ──▶ 8 ──▶ 9 ──▶ 10
                                          │         │
                                          └──▶ 6 ───┘
                                                    
11 ═══════════ موازی با همه فازها ═══════════════════
```

**مسیر بحرانی:** `3 → 4 → 5 → 7 → 8`
**اقدام با بیشترین زمان انتظار (فوراً شروع کنید):** `2.1.2` — درخواست رسمی داده تکمیلی.
**نقطه بی‌بازگشت:** پس از باز کردن مجموعه Test در `8.1`، دیگر نمی‌توانید مدل را تغییر دهید.

---

# ریسک‌های افزوده بر ماتریس ریسک سند اصلی

| ریسک | احتمال | اثر | راهبرد |
|---|---|---|---|
| نشت داده از طریق متغیرهای جوی روز هدف | **بالا** | بحرانی (نتایج بی‌اعتبار) | بند 2.3.3 + ممیزی 5.13 |
| نشت از طریق Target Encoding | **بالا** | بحرانی | پیاده‌سازی expanding-mean زمانی-امن |
| جمع کوانتایل‌ها به‌جای کوانتایل جمع | **متوسط** | زیاد (محافظه‌کاری بیش از حد) | بند 1.3 + 6.7 |
| مدل پیچیده از baseline B3 بهتر نشود | **متوسط** | متوسط (ولی یافته معتبر) | گزارش صادقانه + تمرکز بر تحلیل چرایی |
| Overfit به‌دلیل جستجوی گسترده فراپارامتر روی داده کم | **متوسط** | زیاد | محدود کردن trial، تأکید بر پایداری بین fold |
| ماه رمضان/نوروز رژیم متفاوت ایجاد کند | **بالا** | متوسط | شناسایی در 4.10، تصمیم صریح کنارگذاری یا فیچر رژیم |
| عدم دریافت پاسخ درخواست داده تکمیلی | **بالا** | متوسط | برنامه B: پیش‌رفتن با داده موجود + تصریح محدودیت |
| نرخ بی‌ثبات در $Res$ کوچک | متوسط | متوسط | بند 4.8 — Empirical Bayes یا مدل دوجمله‌ای |
| عدم‌توازن شدید کلاس در مدل B | متوسط | زیاد (مدل ساده‌انگارانه می‌شود) | class_weight/focal loss + PR-AUC، بند ۷-۱۱-۱ و ۶-۹ |
| نقض فرض استقلال در تجمیع پواسون-دوجمله‌ای (مدل B) | متوسط | متوسط (دست‌کم‌گیری واریانس واقعی) | آزمون اکتشافی همبستگی گروهی، بند ۷-۱۱-۳ |
| عدم تطابق آماری بین فایل تجمیعی و فایل فردی (T6) | پایین | بحرانی اگر رخ دهد | تست تقاطعی بند ۳-۶/۳-۱۲ پیش از هرگونه مدل‌سازی |
| ابهام معنایی ستون‌های Name/Reception در فایل فردی | پایین-متوسط | کم (تا رفع ابهام) | استعلام از معاونت دانشجویی، بند ۲-۱-۴ |

---

# چک‌لیست دروازه‌های کیفیت (Quality Gates)

قبل از عبور از هر فاز، این‌ها باید ✅ باشند:

**دروازه M1 (پایان فاز ۳):** اسکریپت واحد raw→processed اجرا می‌شود · همه تست‌های 3.6 (شامل T6/T7) پاس · دیکشنری داده کامل (شامل فایل فردی، بند ۲-۱-۴) · هش snapshot ثبت شده

**دروازه M2 (پایان فاز ۴):** دفتر حقایق داده حداقل ۱۵ ردیف دارد · هر پرت طبقه‌بندی شده · تصمیم global/per-cluster گرفته شده · اندازه اثر برای هر آزمون گزارش شده · **یافته‌های EDA سطح فردی بند ۴-۱۳ (H13–H16) آزمون شده‌اند**

**دروازه M3 (پایان فاز ۶):** splitter تست واحد دارد · هر ۶ baseline اجرا شده · ممیزی نشت هر فیچر کامل است (شامل فیچرهای سطح فرد، بند ۵-۱۶-۵) · Test هنوز باز نشده · مرزهای تاریخی CV مدل A و B یکسان بودن‌شان تأیید شده (بند ۶-۹)

**دروازه M4 (پایان فاز ۷):** هر آزمایش در رجیستری ثبت شده · مدل نهایی با CI از B3 بهتر است (یا صادقانه گزارش شده که نیست) · **در صورت ساخت مدل B: تابع تجمیع پواسون-دوجمله‌ای (بند ۷-۱۱-۳) با تست واحد صحت‌سنجی شده**

**دروازه M5 (پایان فاز ۸-۱۰):** Test فقط یک بار باز شده · منحنی trade-off رسم شده · محدودیت‌ها با همان دقت نتایج نوشته شده‌اند · **نتیجه مقایسه مدل A/B (بند ۸-۱۰) با CI مستند شده**
