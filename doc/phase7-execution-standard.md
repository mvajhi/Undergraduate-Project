# استاندارد اجرای فاز ۷ — رویه‌ی الزام‌آور هر عامل

> **این سند «چه کاری» را نمی‌گوید — آن در `doc/WBS-phase7-modeling.md` است.**
> این سند **«چگونه»** را می‌گوید: قرارداد کد، رویه‌ی اجرا، و تعهدات مستندسازی که هر عامل
> (انسان یا هوش مصنوعی) هنگام کار روی فاز ۷ باید رعایت کند.
>
> نسخه ۱.۰ · ۲۰۲۶-۰۸-۱۴

## چرا این سند وجود دارد

WBS نُه محور آزمایش و یک منشور انصاف هفت‌قاعده‌ای تعریف کرده. ولی سندِ تنها هیچ چیزی را
اجبار نمی‌کند. تجربه‌ی اسپرینت اول این را ثابت کرد: اولین اجرای S0 خانواده‌ی خطی **فقط یک
نقطه** از فضای نُه‌محوره را زد (L1 · سراسری · FS_day · تخت · ρ خام · بدون وزن · τ اشتباه)
و در گزارش چنان دیده می‌شد که انگار «خانواده‌ی خطی آزموده شد».

سه شکست مشخص که این استاندارد برای جلوگیری از تکرارشان نوشته شده:

| چه شد | چرا شد | چه چیزی جلویش را می‌گیرد |
|---|---|---|
| τ تنظیم ۰.۱۰ بود در حالی که تصمیم پروژه ۰.۲۰ است | عدد در سند و کد پراکنده بود، از `decision_log` جدا افتاد | ثابت واحد `axes.TUNING_TAU` + بند «⚠️ چرا τ برابر ۰.۲۰ است» در 7.3.1 |
| هشت محور از نُه محور بی‌صدا روی پیش‌فرض ماندند | هیچ‌کجا لازم نبود موضع اجرا اعلام شود | `RunConfig` اجباری + `coverage.py` |
| S0 هیچ معیار کیفیتی حساب نمی‌کرد، دو باگ پنهان ماند | «امکان‌سنجی» را به‌معنای «فقط زمان اجرا» گرفتیم | قاعده‌ی «هر اجرا معیار عملیاتی + مرجع خط پایه» |
| `python -m src.models.families.f01_linear` با S1 موازی روی `BrokenProcessPool`/«model_id تکراری» کرش کرد | فایل خانواده هم زیر نام `"__main__"` (از `-m`) هم — توسط `_init_worker` هر spawn — زیر نام دات‌دار واقعی‌اش import شد؛ دو import مستقل یعنی حلقه‌ی `register()` دوبار اجرا شد | `src/models/run_family.py`: نقطه‌ورود مشترک که ماژول خانواده را **فقط** با نام دات‌دار import می‌کند — بند ۱.۵ |
| 🔄 **اجرای کامل خ۱ (۱۵ مدل) ۸.۷۶ ساعت-هسته گرفت؛ ۸۶٪ آن اتلاف قابل‌حذف بود** — بودجه‌ی trial فقط تابع تعداد هایپرپارامتر بود، نه کاردینالیتی فضا (یک مدل با فضای ۲-حالته ۲۵ trial زد، ۷.۳ ساعت). علاوه‌براین هر ۳۱۶ run روی یک نقطه از فضای هفت‌محوره نشستند — محور «کدام مدل خطی» پیمایش شد، نه محور پرثمر (F59: شوک روز ۸۳٪ واریانس) | بودجه‌ی S2 فقط از روی تعداد هایپرپارامتر تعیین می‌شد؛ ترتیب پیمایش «خانواده اول» بود | بند 7.3.2/7.6.2 بازنویسی شد (نصف‌سازی متوالی + کِران کاردینالیتی + سقف زمانی سخت)؛ بند 7.9.1 معکوس شد (محور اول). تفصیل: `doc/decisions/37-phase7-rescope.md` — بند ۲.۱ زیر همین سند هم به‌روز شد |

---

## ۱. قرارداد کد (غیرقابل‌مذاکره)

### ۱.۱ امضای یکسان هر مدل

```python
def fit_predict_<model>(train: pd.DataFrame, test: pd.DataFrame, tau: float,
                        **hyperparams) -> np.ndarray:
    """برمی‌گرداند: ρ̂_τ به‌ازای هر ردیف test، در بازه‌ی [0,1]."""
```

دقیقاً همان امضای `src/baselines.py` (بند ۶.۵). دلیلش این است که هارنس‌های S0/S1/S2 و
خط پایه‌های فاز ۶ باید بدون هیچ استثنایی یکسان صدا زده شوند.

⚠️ `train`/`test` **زیرمجموعه‌ی سطرهای یک fold از دیتافریم کامل‌اند**، نه ماتریس فیچرِ از
پیش برش‌خورده. انتخاب فیچر، پیش‌پردازش و مقیاس‌بندی **داخل** خودِ تابع و **فقط روی train**
انجام می‌شود (قاعده‌ی انصاف A3).

### ۱.۲ ساختار فایل‌ها

| مسیر | مسئولیت |
|---|---|
| `src/models/registry.py` | فراداده‌ی خانواده‌ها و مدل‌ها (بدون منطق آموزش) |
| `src/models/axes.py` | نُه محور آزمایش + `RunConfig` + `TUNING_TAU` |
| `src/models/naming.py` | ساخت/تجزیه‌ی `run_name` طبق بند 7.7.1 |
| `src/models/tracking.py` | باز کردن MLflow run با فیلدهای اجباری بند 7.7.2 |
| `src/models/spaces.py` | رجیستری فضای هایپرپارامتر + جدول بودجه‌ی 7.6.2 |
| `src/models/cards.py` | کارت مدل ۱۴ گامی |
| `src/models/coverage.py` | گزارش «کدام نقاط زده نشده‌اند» |
| `src/models/s0_runner.py` · `s1_runner.py` · … | هارنس هر مرحله — **عمومی، نه مخصوص یک خانواده** |
| `src/models/run_family.py` | نقطه‌ورود مشترک CLI — تنها راه مجاز اجرای `main()`/`run_s1()` هر خانواده (بند ۱.۵) |
| `src/models/families/common.py` | پیش‌پردازش و مسیرهای کوانتایل مشترک |
| `src/models/families/f0X_*.py` | یک ماژول به‌ازای هر خانواده: توابع مدل + فضاها + `main()` |

**قاعده:** منطق مشترک هرگز در ماژول خانواده کپی نمی‌شود. اگر خانواده‌ی دوم به چیزی نیاز
داشت که خانواده‌ی اول ساخته، آن چیز به `common.py` منتقل می‌شود.

### ۱.۳ هیچ اجرایی بدون `RunConfig`

```python
cfg = RunConfig(family="F02", model_id="lightgbm", stage="S2", seed=42,
                level="L1", feature_set="FS_lgbm", tau=TUNING_TAU,
                scope="per_cluster", weighting="res")     # ← محورها صریح
with start_model_run(cfg, data_snapshot_hash=..., cv_folds_hash=...):
    ...
```

`start_model_run` فقط `RunConfig` می‌پذیرد. یعنی **اعلام‌نکردن موضع روی هر نُه محور از نظر
فنی ناممکن است** — نه یک توصیه‌ی نانوشته که فراموش شود.

### ۱.۴ دیتاست، نوع مدل، و مرجع کد — هرکدام در جای اختصاصی خودشان در MLflow

هش/شناسه به‌تنهایی کافی نیست — باید بدون باز کردن param جداگانه از تب‌های اصلی MLflow
UI دیده شود که این run روی **کدام دیتاست** (تجمیعی L1-L4 یا فردی L5، با کدام فیچرست)،
**چه نوع الگوریتمی**، و از **کدام فایل/خط کد** train شده:

- `start_model_run(cfg, ..., train=..., test=..., dataset_source=...)` هر دو زیرمجموعه
  را با `mlflow.log_input` در تب **Dataset** ثبت می‌کند؛ نام دیتاست خودگویاست:
  `{تجمیعی|فردی}-{feature_set}-{train|test}`.
- tag اختصاصی **`model_type`** (نه `model_id`) کلاس/کتابخانه‌ی واقعی را نگه می‌دارد
  (مثلاً `statsmodels.GLM(Gamma)`) — از فیلد اجباری `algorithm` در `ModelSpec` هر مدل
  خوانده می‌شود، پس هر مدلی که ثبت می‌شود باید این فیلد را پر کند.
- tag اختصاصی **`code_ref`** (`مسیر/فایل.py:خط#تابع`) با `start_model_run(..., source_fn=fit_predict_x)`
  خودکار از روی خودِ تابع با `inspect` ساخته می‌شود — دستی نوشته نمی‌شود که با جابه‌جایی
  کد قدیمی بماند. کنار tag `git_commit` (که همیشه ثبت می‌شود)، بازتولید یک run یعنی
  `git checkout {git_commit}` و باز کردن دقیقاً همان آدرس — نه گشتن در کل مخزن.
- برای مدل‌های **سنگین** (شبکه‌ی عصبی GPU، بیزی NUTS سنگین)، `start_model_run` پارامتر
  اختیاری `log_model_fn` می‌گیرد تا خودِ مدل fitشده هم با فلیور مناسب
  (`mlflow.pytorch`/`mlflow.pyfunc`/…) در Model Registry ثبت شود. برای مدل‌های سبکِ
  خانواده‌های خطی/درختی که هر trial را در چند صدم ثانیه دوباره fit می‌کنند لازم نیست —
  طبق بند 7.29.1 artifact کامل مدل برای **قهرمانان S3** رزرو شده، نه هر trial.

### ۱.۵ ⚠️ هرگز ماژول خانواده را مستقیم `-m` اجرا نکنید

فایل خانواده (`f0X_*.py`) هیچ بلوک `if __name__ == "__main__":` ندارد — عمداً. دلیلش
یک باگ واقعی است که در همین اسپرینت رخ داد: `python -m src.models.families.f01_linear`
فایل را زیر نام `"__main__"` import می‌کند (کدهای سطح ماژول، از جمله حلقه‌ی `register()`
هر مدل، همان‌جا اجرا می‌شوند). با `multiprocessing` روی `spawn` (لازم برای S1/S2 —
بند ۱.۴)، هر worker تازه هنگام bootstrap **دوباره** همان اسکریپت را زیر نام `"__main__"`
بارگذاری می‌کند؛ اگر کد داخلی هم صریحاً همان فایل را زیر نام دات‌دار واقعی‌اش import کند
(لازم برای `_init_worker`)، آن فایل **دوبار** در یک process اجرا می‌شود ⇒ هر `register()`
دوبار ⇒ `ValueError: model_id تکراری` / `BrokenProcessPool`.

**همیشه از طریق نقطه‌ورود مشترک اجرا کنید** — این ماژول خانواده را فقط با نام دات‌دار
import می‌کند، هرگز `__main__` نمی‌شود:

```bash
python -m src.models.run_family src.models.families.f01_linear --stage s0
python -m src.models.run_family src.models.families.f01_linear --stage s1 --jobs 8
```

---

## ۲. رویه‌ی الزام‌آور هر مدل

### ۲.۱ ترتیب پیمایش (بند 7.9.1 — معکوس‌شده ۲۰۲۶-۰۸-۱۶، ردیف ۳۷ decision_log)

> ⚠️ **این بخش برای خانواده‌های بعد از خ۱ (F02 به بعد) اعمال می‌شود.** خ۱ با ترتیب
> قدیمی («خانواده اول») اجرا شد و همان اجرا ۸.۷۶ ساعت-هسته گرفت — ۸۶٪ آن اتلاف
> قابل‌حذف (تفصیل: `doc/decisions/37-phase7-rescope.md`). خ۱ دوباره اجرا نمی‌شود؛
> فقط قاعده برای بقیه عوض شده.

```
۰. کاوشگر ارزان روی هر محور  ← دو مدل ثابت (یک خطی + LightGBM پیش‌فرض)، آزمون زوجی
                                در برابر لنگر (flat/L1/global/…)، ~۱ ساعت جمعاً
۱. قفل محورهای بی‌اثر         ← اختلاف < δ ⇒ ساده‌ترین مقدار + ثبت در axis_coverage.md
۲. خانواده × سطح داده          ← R0–R2 (نصف‌سازی متوالی بند 7.3.2)، فقط روی محورهای
                                 باقی‌مانده‌ی مؤثر
۳. فیچرست اختصاصی + هایپرپارامتر ← R2/R3 (بند 7.5/7.6)، برای فهرست کوتاه هر خانواده
۴. τ و کالیبراسیون             ← اسپرینت زودهنگام (بند 7.22)، نه منتظر همه‌ی خانواده‌ها
```

**هرگز از وسط شروع نکنید.** بهینه‌سازی هایپرپارامتر روی محوری که هنوز اندازه‌گیری
نشده، بودجه را هدر می‌دهد — دقیقاً همان اشتباهی که خ۱ کرد.

### ۲.۲ چهار مرحله (بازنویسی — R0–R3 به‌جای S0–S2 بی‌قید)

| مرحله | بودجه | fold | τ | معیار عبور | سقف زمانی سخت |
|---|---|---|---|---|---|
| **R0** (=S0) | ۱ برازش پیش‌فرض | fold ۰ | `TUNING_TAU` | بدون خطا | **۶۰ ثانیه** |
| **R1** (=S1) | ۱۰ trial تصادفی | ۳ | `TUNING_TAU` | بالای میانه یا برنده‌ی یک برش | **۶۰۰ ثانیه** |
| **R2** (=S2) | جدول 7.6.2 بازنویسی‌شده (کِران به کاردینالیتی $\lvert\Theta\rvert$) | ۵ | `TUNING_TAU` | حذف با آزمون زوجی بند ۶.۶ در برابر رهبر؛ داخل $\delta$ = هم‌ارز | جدول 7.6.2 |
| **R3** (=S3، فهرست کوتاه) | گسترده | ۵ | کل شبکه | ورودی فاز ۸ | — |

⚠️ عبور از سقف زمانی ⇒ نمونه‌ی طبقه‌ای ۲۵٪ (بلوک روزانه) یا تگ `budget-capped` —
هرگز توقف بی‌صدا و هرگز ادامه‌ی بی‌قید.

### ۲.۳ چیزهایی که هر اجرا **باید** تولید کند

- ✅ یک MLflow run با `RunConfig` کامل، `cv_folds_hash`، `data_snapshot_hash`
- ✅ معیارهای عملیاتی کامل (`operational_metrics`) — **نه فقط زمان اجرا**
- ✅ مقایسه با مرجع خط پایه‌ی همان fold (B3، بند ۶.۵)

> **درس اسپرینت اول:** نسخه‌ی اول هارنس S0 فقط `fit_seconds` ثبت می‌کرد. نتیجه: یک باگ
> علامت که مدل را وادار می‌کرد کوانتایل ۱−τ را حل کند، و یک انفجار برون‌یابی اسپلاین، هر
> دو **کاملاً پنهان ماندند** تا وقتی pinball اضافه شد. محاسبه‌ی معیار هزینه‌اش صفر است —
> پیش‌بینی و مقدار واقعی هر دو در دست‌اند.

---

## ۳. تعهدات مستندسازی

| چه چیزی | کجا | کِی |
|---|---|---|
| وضعیت بسته‌های کاری | `doc/progress/07-*.md` | با هر گام تمام‌شده |
| تحلیل هر مدل (۱۴ گام) | `reports/models/{model_id}.md` | ورود به S2 |
| تصمیم روش‌شناختی | `doc/decision_log.md` | همان لحظه، نه آخر فاز |
| مدل کنارگذاشته‌شده + جایگزین | `reports/phase7/incompatible_models.md` | همان لحظه |
| پوشش محورها | `reports/phase7/axis_coverage.md` | خودکار — `python -m src.models.coverage` |

### ۳.۱ قاعده‌ی طلایی مستندسازی

> **نزدن یک نقطه اشکالی ندارد؛ نگفتنِ اینکه نزده‌ای اشکال دارد.**

اگر یک محور را نپیمودید (مثلاً `scope="per_cluster"` را اصلاً امتحان نکردید)، این کاملاً
مجاز است — به شرطی که:
1. در `axis_coverage.md` به‌عنوان «زده‌نشده» دیده شود (خودکار)، **و**
2. در کارت مدل یا `incompatible_models.md` دلیلش نوشته شده باشد.

گزارش نهایی هرگز نباید بگذارد خواننده فکر کند چیزی آزموده شده که نشده.

### ۳.۲ نتیجه‌ی منفی هم داده است

بند 7.2 قاعده‌ی هشتم: انتظار پیش از اجرای هر خانواده **از پیش** در سند نوشته شده. اگر
نتیجه خلاف انتظار شد، این **یافته** است و باید در `decision_log` ثبت شود. «اجرا نکردن
به‌دلیل انتظار شکست» در هیچ حالتی مجاز نیست.

---

## ۴. ثابت‌های قفل‌شده — تغییرشان رویه‌ی خاص دارد

| ثابت | مقدار | برای تغییر چه باید کرد |
|---|---|---|
| `cv_folds.json` + هشش | `bd08d6f7…` | فایل منجمد است. بازتولید عمدی ⇒ هش جدید در `data_manifest.md` **و** حذف همه‌ی runهای قبلی از جدول مقایسه (بند 7.7.3) |
| `axes.TUNING_TAU` | ۰.۲۰ | ردیف جدید در `decision_log` + بند 7.3.1 سند + همین ثابت، **باهم** |
| پروتکل CV فاز ۶ | قفل | بند 7.29.3 قاعده‌ی ۳: هیچ تغییری مجاز نیست |
| پنجره‌ی انتهایی (~۲۵ روز) | دست‌نخورده | در کل فاز ۷ لمس نمی‌شود. فقط یک‌بار در فاز ۸ |

---

## ۵. چک‌لیست عملی — پیش از اعلام «این خانواده تمام شد»

```
□ هر عضو جدول «اعضا»ی بند خانواده یک تابع fit_predict دارد
□ هر عضو S0 را پاس کرده یا در incompatible_models.md دلیلش آمده
□ فضای هایپرپارامتر هر عضو در spaces.py ثبت و نسخه‌بندی شده
□ S1 روی ۳ fold اجرا شده — بدون حذف هیچ مدلی
□ هر مدل S2 کارت ۱۴ گامی دارد (گام‌های ۴، ۹، ۱۰، ۱۳ بدون استثنا)
□ آزمون‌های پیش‌پرواز بند 7.9.2 اجرا و نتایج منفی هم ثبت شده‌اند
□ axis_coverage.md بازتولید شده و نقاط زده‌نشده توضیح دارند
□ سه seed برای مدل‌های تصادفی (قاعده‌ی A7)
□ doc/progress/07-*.md تیک خورده با شاهد
□ یافته‌های خودِ خانواده به جدول یافته‌های progress اضافه شده
```

---

## ۶. اگر عامل جدیدی هستید — از کجا شروع کنید

1. `AGENTS.md` (خودکار لود می‌شود) → قاعده‌ی برش اطلاعاتی را بخوانید. **مهم‌ترین قانون پروژه.**
2. همین سند → قرارداد کد و رویه.
3. `doc/WBS-phase7-modeling.md` بند خانواده‌ای که رویش کار می‌کنید.
4. `doc/progress/07-*.md` → چه چیزی تمام شده و یافته‌های تاکنون.
5. `reports/phase7/axis_coverage.md` → کجا هنوز زده نشده.
6. `src/models/families/f01_linear.py` → **الگوی مرجع** یک ماژول خانواده.
