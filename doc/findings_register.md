# ثبت یافته‌ها (findings register)

> فضای شناسه‌ی **واحد** برای هر یافته/کشف پروژه که خودش شناسه‌ی دیگری ندارد.
>
> ⚠️ **این سند با `doc/data_facts_register.md` فرق دارد و نباید با آن اشتباه گرفته شود:**
> - `data_facts_register.md` → **حقایق** توصیفی EDA فاز ۴، با پیشوند **`F##`** (F01–F86).
> - این سند → **یافته‌ها** (کشف‌های تفسیری/تصمیم‌ساز از فازهای ۰، ۷، ۸، ۹، ۱۰)، با **عدد ساده
>   بدون پیشوند حرفی** — تا با شناسه‌ی حقایق اشتباه گرفته نشود.
>
> شناسه‌های ۰ تا ۳۵ از فاز ۷ می‌آیند و **قبلاً در `decision_log.md` (ردیف‌های ۳۷–۵۱) به همین
> اعداد ارجاع داده شده‌اند** — این شماره‌ها هرگز عوض نشده‌اند. شناسه‌های ۳۶ به بعد در همین
> ادغام (۲۰۲۶-۰۸-۲۷) تازه صادر شدند.
>
> ستون‌ها: **شناسه** · **یافته** (یک‌خط، تفصیل در فایل شاهد) · **فاز** · **شاهد** (مسیر فایل) ·
> **وضعیت** (تأییدشده/رد‌شده/رفع‌شده/باز) · **ارجاع متقابل**.

---

## فاز ۷ — یافته‌های مدل‌سازی (شناسه‌ی ۰ تا ۳۵)

> منتقل‌شده‌ی عیناً از جدول «🔑 یافته‌های خودِ فاز ۷» در
> `doc/progress/07-مدل‌سازی-و-تنظیم.md` (اکنون در آن فایل فقط یک خط اشاره‌گر به اینجا باقی
> مانده). ⚠️ **دو شناسه‌ی تکراری واقعی در منبع اصلی وجود داشت** — شناسه‌ی ۲۸ و ۲۹ هرکدام
> دوبار با محتوای کاملاً متفاوت ظاهر شده بودند. طبق تصمیم ادغام، **شماره‌ها عوض نشدند**
> (چون در `decision_log.md` ردیف‌های ۳۷–۵۱ به همین اعداد ارجاع داده شده)؛ هر دو نسخه با
> پسوند `-الف`/`-ب` (به ترتیب ظهور در منبع اصلی) نگه داشته شدند.

| شناسه | یافته (یک خط) | فاز | شاهد (مسیر فایل) | وضعیت | ارجاع متقابل |
|---|---|---|---|---|---|
| 0 | τ تنظیم فاز ۷ اشتباه بود (۰.۱۰ به‌جای ۰.۲۰ نقطه‌ی عملیاتی پروژه) — رتبه‌بندی مدل‌ها به τ وابسته است | ۷ | `reports/phase7/S0_feasibility.md` | رفع‌شده | ردیف ۳۴ decision_log |
| 1 | `week_of_semester` بین train/test یک fold می‌تواند کاملاً مجزا باشد — مدل‌های غیرخطی/اسپلاین نیاز به مهار برون‌یابی دارند | ۷ | `reports/phase7/S0_feasibility.md` | رفع‌شده | — |
| 2 | مسیر کوانتایل Q3 (میانگین+آفست) به بی‌اریبی خارج‌نمونه‌ی مدل میانگین حساس است — کوانتایل باید مستقیم هدف گرفته شود | ۷ | `reports/phase7/S0_feasibility.md` | تأییدشده | یافته‌ی ۱ جدول فاز ۶ (`doc/progress/06-*.md`) |
| 3 | با هایپرپارامتر پیش‌فرض، Lasso/ElasticNet/Adaptive-Lasso/Group-Lasso هر چهار به عدد یکسان می‌رسند (فروپاشی به فقط-عرض‌ازمبدأ) | ۷ | `reports/phase7/S0_feasibility.md` | تأییدشده | — |
| 4 | باگ علامت در LP رگرسیون کوانتایل ترکیبی — مدل بی‌صدا کوانتایل ۱−τ را حل می‌کرد | ۷ | `reports/phase7/S0_feasibility.md` | رفع‌شده | — |
| 5 | واگرایی MLE در GLM Gamma/Beta روی fold با شبه‌جدایی دسته‌ای | ۷ | `doc/progress/07-مدل‌سازی-و-تنظیم.md` (بخش خ۱، فایل گزارش جداگانه ندارد) | رفع‌شده | — |
| 6 | پیوند `inverse` در GLM Gamma ساختاری ناپایدار است (محدودیت خودِ statsmodels) | ۷ | `doc/progress/07-مدل‌سازی-و-تنظیم.md` (همان بخش) | رفع‌شده | بند 7.27 WBS |
| 7 | چهار مدل خطی خط پایه‌ی B3 را در S1 (۳ fold) بردند — اولین‌بار در کل پروژه؛ هنوز قطعی نیست | ۷ | `reports/phase7/S1_screening_F01.md` | رد‌شده (در S2 تأیید نشد) | یافته‌ی ۱۱، ۱۳ |
| 8 | GAM با `lam` پایین به‌شدت بیش‌برازش می‌کند — رفتار سالم فضای جستجو، نه باگ | ۷ | `reports/phase7/S1_screening_F01.md` | تأییدشده | — |
| 9 | هرس دستی حریصانه‌ی VIF روی ماتریس یک‌هات‌شده گمراه‌کننده است — حتی `log_res` را حذف می‌کند | ۷ | `doc/progress/07-مدل‌سازی-و-تنظیم.md` (بند فیچرست S2 خ۱) | تأییدشده | بند 7.5.4 WBS |
| 10 | پوشش تجربی@τ=۰.۲۰ در اکثر مدل‌های S2-تنظیم‌شده‌ی خ۱ نظام‌مند کمتر از اسمی است | ۷ | `src/models/calibration.py`، گام ۱۳ هر کارت `reports/models/*.md` | باز (بعداً حل‌شده) | یافته‌ی ۲۲ |
| 11 | از چهار «برنده»ی S1، فقط سه‌تا در S2 هم برنده ماندند و margin هرسه ناچیز/ناپایدار است | ۷ | `reports/phase7/S2_tuning_F01.md`، گام ۱۴ هر کارت | رد‌شده (نیازمند تأیید DM) | یافته‌ی ۱۳ |
| 12 | اجرای کامل خ۱ ۸.۷۶ ساعت-هسته گرفت که ۸۶٪ آن اتلاف قابل‌حذف بود | ۷ | `doc/decisions/37-phase7-rescope.md` | تأییدشده (منجر به بازطراحی) | ردیف ۳۷ decision_log |
| 13 | آزمون Diebold-Mariano ادعای «سه مدل خطی B3 را بردند» را تأیید نکرد؛ B6 از B3 ضعیف‌تر بود | ۷ | `reports/phase7/dm_test_F01.md` | رد‌شده | تأیید نهایی یافته‌ی ۱۱ |
| 14 | پیمایش محور-اول سه محور از چهار را قفل کرد؛ محور «دامنه‌ی مدل» برهم‌کنش قوی با نوع مدل نشان داد | ۷ | `reports/phase7/axis_screening.md/.json` | تأییدشده | یافته‌ی ۱۵، ۲۹-ب |
| 15 | خ۲ (F02) اولین خانواده‌ای است که DM-test برد واقعی و معنادار روی B3 تأیید می‌کند | ۷ | `reports/phase7/S2_tuning_F02.md`، `dm_test_F02.md/.json` | تأییدشده | یافته‌ی ۱۴ |
| 16 | آزمون هم‌ارزی نظری ERM-Newsvendor: هدف واقعی Res-وزن‌دار است، نه pinball خام | ۷ | `src/models/families/f11_decision.py` | تأییدشده | — |
| 17 | دروازه‌ی ARCH-LM خ۵ قویاً منفی — ۱۲ مدل GARCH/ARCH اجرا نمی‌شوند | ۷ | `reports/phase7/arch_lm_gate_F05.md` | تأییدشده (خانواده کنار گذاشته شد) | ردیف ۳۸ decision_log |
| 18 | خ۹ (رگرسیون توزیعی دومرحله‌ای) به‌طور آماری معنادار بدتر از B3 است | ۷ | `reports/models/lightgbm_lss_beta.md`، `dm_test_F09.md` | رد‌شده | بند ۷ سند تصمیم ۳۷ |
| 19 | خ۱۰ (kNN-Quantile) دومین خانواده‌ی برنده‌ی تأییدشده — با مارجین کوچک‌تر از خ۲ | ۷ | `reports/models/knn_quantile.md`، `dm_test_F10.md` | تأییدشده (بعداً تعدیل‌شده) | یافته‌ی ۲۵ |
| 20 | CQR/Mondrian ساده کمک نمی‌کند — در هر ۴ قهرمان تأییدشده شکاف پوشش را بدتر کرد | ۷ | `src/models/conformal.py`، `reports/phase7/cqr_champions.md` | رد‌شده | یافته‌ی ۲۲ |
| 21 | فیچرهای کوهورت (پل L5→L1، اسپرینت B) کمک نکردند — سیگنال فردی افزونه بود، نه تازه | ۷ | `src/features/cohort_features.py`، `reports/phase7/cohort_features_sprint_b.md` | رد‌شده | یافته‌ی ۳۱ |
| 22 | ACI دقیقاً همان‌جایی موفق شد که CQR شکست خورد — کالیبراسیون فاز ۷ عملاً حل شد | ۷ | `src/models/conformal.py::aci_predict`، `reports/phase7/cqr_champions.md` | تأییدشده (راه‌حل نهایی) | یافته‌ی ۲۰، ۱۰ |
| 23 | ترکیب قهرمانان دقت را نبرد ولی کالیبراسیون را برد — معاوضه، نه شکست | ۷ | `src/models/run_ensemble_champions.py`، `reports/phase7/ensemble_champions.md` | تأییدشده | — |
| 24 | هایپرپارامتر تنظیم‌شده روی τ=۰.۲۰ به کل شبکه‌ی τ تعمیم یافت | ۷ | `src/models/tau_sensitivity.py`، `reports/phase7/tau_sensitivity_champion.md` | تأییدشده | سند تصمیم ۳۷ |
| 25 | جدول مقایسه‌ی نهایی با بوت‌استرپ بلوکی دوبعدی فقط ۳ برد پایدار پیدا کرد | ۷ | `reports/phase7/model_comparison.md`، `mandatory_cuts.md`، `three_explicit_questions.md` | تأییدشده (استاندارد نهایی) | ردیف ۴۰ decision_log |
| 26 | خ۳ (سری‌زمانی L3) پیش‌بینی WBS را تأیید کرد — SARIMAX-با-رگرسور فقط برای شام رقابتی، Theta هیچ‌جا | ۷ | `src/features/l3_series.py`، `reports/phase7/F03_feasibility.md` | تأییدشده | ردیف ۴۱ decision_log؛ بعداً تعدیل‌شده با یافته‌ی ۲۸-ب |
| 27 | خ۴ (DFM سطح L4) عامل مشترک را در پاییز/زمستان باارزش نشان داد، در بهار/رمضان نه | ۷ | `src/features/l4_series.py`، `reports/phase7/F04_feasibility.md` | باز (وابسته به فصل) | ردیف ۴۲ decision_log |
| 28-الف | خ۷ روی L1: انتظار منفی بند 7.16 تأیید شد — هر سه معماری شبکه B3 را بردند ولی هر سه از LightGBM خ۲ معنادار باختند | ۷ | `reports/gpu/F07a_neural_L1.md`، `reports/models/mlp_entity_embedding.md` | رد‌شده (کاندید S3 نیست) | یافته‌ی ۱۵ |
| 28-ب | AR/MA ساده‌ی بدون هیچ رگرسوری برنده‌ی واقعی خ۳ بودند — نه SARIMAX-تقویمی که تنها نماینده تصور می‌شد | ۷ | `src/models/families/f03_timeseries.py`، `reports/phase7/F03_feasibility.md` | تأییدشده (یافته‌ی غیرمنتظره) | ردیف ۴۳ decision_log؛ متفاوت از یافته‌ی ۲۶ |
| 29-الف | سر چند-کوانتایلی شبکه از همان ابتدا تقریباً کالیبره است — برخلاف قهرمانان درختی که به ACI نیاز داشتند | ۷ | `reports/models/mlp_entity_embedding.md` (گام ۱۳) | تأییدشده | یافته‌ی ۲۲ |
| 29-ب | خوشه‌بندی هوشمند شهر (تهران در برابر سراسری) روی بهترین مدل‌های خ۳/خ۴ کمکی نکرد | ۷ | `src/models/run_f03_city_cluster.py`، `reports/phase7/{F03_city_cluster,F04_city_cluster}.md` | رد‌شده (نتیجه‌ی خنثی) | ردیف ۴۴ decision_log؛ یافته‌ی ۱۴ |
| 30 | آشتی L3→L1 با قوی‌ترین مدل خ۳ (`MA(2)`) به‌طور آماری‌معنادار از B3 بدتر بود | ۷ | `src/models/families/f03_reconciled.py`، `reports/phase7/F03_reconciliation.md` | رد‌شده (معماری رد شد) | ردیف ۴۵ decision_log؛ بازتست یافته‌ی ۱۴ |
| 31 | خ۷ روی L5: فرضیه‌ی یافته‌ی ۲۱ در قوی‌ترین شکل رد شد — embedding مستقیم PersonId هم کمک نکرد | ۷ | `reports/models/mlp_embedding_l5.md`، `reports/gpu/F07b_neural_L5.md` | رد‌شده (مدل L5 توجیه ندارد) | یافته‌ی ۲۱ |
| 32 | خ۸ (بیزی سلسله‌مراتبی، اثر متقاطع + φ متغیر) چهارمین خانواده‌ی برنده‌ی تأییدشده — عملاً هم‌ارز قهرمان اصلی | ۷ | `reports/models/bhm_varying_dispersion.md`، `reports/gpu/F08_bayesian_L1.md` | تأییدشده (با بدهی همگرایی) | ردیف ۴۶ decision_log |
| 33 | خ۶ (GP روی L1): از B3 معنادار باخت، ولی بهترین کالیبراسیون کل فاز ۷ را داد (پس از ACI) | ۷ | `reports/models/gp_quantile.md`، `reports/gpu/F06_gp_L1.md` | باز (کاندید S3 نیست) | یافته‌ی ۲۲ |
| 34 | باگ سیم‌کشی: S2 خ۱ هرگز فیچرست اختصاصی‌اش را استفاده نکرد — با فیچرست درست، `l1_quantile_regression` حالا B3 را معنادار می‌برد | ۷ | `src/models/run_f01_s2_feature_bug_check.py`، `reports/phase7/f01_s2_feature_bug_check.md/.json` | باز (خ۱ منجمد می‌ماند) | یافته‌ی ۱۳ (مغایرت مستقیم)؛ ردیف ۴۰، ۴۷ decision_log |
| 35 | آزمون ضدتقاطع کوانتایل: دو از چهار عضو مسیر Q1 خ۱ از آستانه‌ی ۱٪ عبور کردند؛ isotonic نقض را به صفر رساند | ۷ | `src/models/quantile_crossing.py`، `reports/phase7/quantile_crossing_F01.md/.json` | رفع‌شده | یافته‌ی ۲۴ |

## فاز ۸/۹/۱۰ — یافته‌های ارزیابی، تفسیر، تصمیم (شناسه‌ی ۳۶ تا ۴۸)

| شناسه | یافته (یک خط) | فاز | شاهد (مسیر فایل) | وضعیت | ارجاع متقابل |
|---|---|---|---|---|---|
| 36 | روی پنجره‌ی Test قفل‌شده (رژیم غیرعادی رمضان+سوگواری)، `bhm_varying_dispersion` تنها مدل با برد آماری‌معنادار در برابر B3 است — برخلاف رتبه‌بندی رسمی فاز ۷ که `lightgbm_quantile` را قهرمان می‌داند؛ قهرمان رسمی طبق پروتکل عوض نمی‌شود | ۸ | `reports/phase8/8.1_final_holdout_evaluation.md`, `8.1_holdout_significance.csv` | تأییدشده | ردیف ۴۹ decision_log؛ یافته‌ی ۴۶ فاز ۷ |
| 37 | Q4 (چارک حجم بالا) هم‌زمان بهترین pinball میانگین **و** بیشترین نرخ کمبود (۳۲.۷٪) را دارد — pinball خام هزینه‌ی واقعی کمبود در سلف‌های پرحجم را دست‌کم نشان می‌دهد | ۸ | `reports/phase8/8.3_error_decomposition.md`, `8.3_by_res_quartile.csv` | تأییدشده | یافته‌ی ۳۹ |
| 38 | صفر همپوشانی بین ۲۰ بدترین pinball و ۲۰ بدترین کمبود مطلق روی Test — پایش صرفاً با pinball میانگین بزرگ‌ترین ریسک کسب‌وکاری را نشان نمی‌دهد | ۸ | `reports/phase8/8.4_worst_cases.md` (بخش «همپوشانی: ۰ از ۲۰») | تأییدشده | یافته‌ی ۳۷ |
| 39 | ۲۰ بدترین pinball روی چهارشنبه‌ها متمرکزند (۴۵٪ در برابر پایه‌ی ۱۸٪) — سازگار با الگوی روز هفته‌ی بند ۸.۳ | ۸ | `reports/phase8/8.4_worst_cases.md` (بخش «الگوی مشترک») | تأییدشده | یافته‌ی ۳۷ |
| 40 | حساسیت seed روی مرز holdout: seed=2024 آشکارا بدتر (نرخ کمبود ۲۷.۲٪ در برابر ~۱۹-۲۰٪ بقیه)، سازگار با n_eff پایین همین پنجره | ۸ | `reports/phase8/8.6_stability_sensitivity.md`, `8.6_seed_sensitivity.csv` | تأییدشده | یافته‌ی ۳۶ |
| 41 | ACI روی همین پنجره‌ی Test غیرعادی هم شکاف پوشش (+۱۳.۶٪→+۰.۱٪) **و** pinball (۰.۰۱۰۰۸→۰.۰۰۹۷۵) را هم‌زمان بهبود داد — تعمیم موفق یافته‌ی ۲۲ فاز ۷ به یک رژیم کاملاً متفاوت | ۸ | `reports/phase8/8.8_calibration.md` | تأییدشده | یافته‌ی ۲۲ |
| 42 | فیچرهای خام `city`، `Meal`، `RestaurantType` اهمیت SHAP **دقیقاً صفر** دارند — `RestaurantName` و ستون‌های تعاملی مهندسی‌شده‌ی فاز ۵ همان اطلاعات را واضح‌تر می‌دهند | ۹ | `reports/phase9/9.2_shap_analysis.md` (جدول تعامل‌ها) | تأییدشده | تصمیم فاز ۵ (ستون‌های تعاملی صریح) |
| 43 | رتبه‌بندی ΔAIC فاز ۴ با اهمیت واقعی در مدل نهایی همخوان نیست: `dow_x_type` بالاترین ΔAIC داشت ولی اهمیت SHAP آن یک‌دهم `dow_x_city` است — معناداری آماری در مدل خطی، سودمندی پیش‌بینی در مدل درختی را پیش‌بینی نمی‌کند | ۹ | `reports/phase9/9.2_shap_analysis.md` (جدول «ستون مهندسی‌شده») | تأییدشده | — |
| 44 | تناقض ظاهری جهت‌اثر `log_res`: ALE/PDP صعودی نشان می‌دهد (کوانتایل τ=۰.۲۰) در حالی که F01/F11/F54 (میانگین) خلافش می‌گویند — علت تفاوت آماره است، نه خطای مدل | ۹ | `reports/phase9/9.4_domain_validation.md`, `9.4_log_res_quantile_vs_mean.csv` | تأییدشده | ردیف ۵۱ decision_log؛ F01, F11, F54 |
| 45 | درخت جانشین (بند ۹.۵) هرگز روی `RestaurantName`/`dow_x_city` — دو فیچر برتر قهرمان — split نمی‌زند؛ نرخ تاریخی سطح سلول همان هویت را از قبل حمل می‌کند | ۹ | `reports/phase9/9.5_surrogate.md`, `9.5_surrogate_tree.txt` | تأییدشده | — |
| 46 | در سطح فرد (L5)، فیچرهای تاریخچه‌ی شخصی (۸۷.۰٪ از کل \|SHAP\|) به‌مراتب مهم‌تر از فیچرهای جمعیتی (۷.۳٪) هستند — توضیح مستقیم اینکه چرا L5 در بند ۸.۱۰ به L1/L2 نباخت | ۹ | `reports/phase9/9.6_l5_interpretability.md` (جدول «پاسخ صریح») | تأییدشده | یافته‌ی ۲۱، ۳۱؛ ردیف ۵۰ decision_log |
| 47 | فرضیه‌ی H13 (خوابگاه‌های دورتر نرخ عدم‌دریافت بالاتر) همچنان آزموده‌نشده باقی می‌ماند — داده فقط پرچم دوتایی دارد، نه فاصله‌ی واقعی | ۹ | `reports/phase9/9.6_l5_interpretability.md` (بخش «اعتبارسنجی دامنه‌ای H13») | باز | بند ۹.۴ WBS (صریحاً هشدار نه یافته)؛ بند ۲.۵ WBS |
| 48 | در نقطه‌ی عملیاتی τ=۰.۲۰، سود خالص قهرمان نسبت به B3 عملاً صفر است (−۰.۰۰۳ میلیارد تومان روی پنجره‌ی Test) — تکرار عددیِ کسب‌وکاری همان یافته‌ی بند ۸.۱، نه تناقض تازه | ۱۰ | `reports/phase10/10.2_newsvendor.md` (جدول نقطه‌ی سربه‌سر، τ=۰.۲۰) | تأییدشده | یافته‌ی ۳۶؛ ردیف ۴۹ decision_log |

## فاز ۰-۳ — یافته‌های زیرساخت و داده‌ی خام (شناسه‌ی ۴۹ تا ۵۱)

| شناسه | یافته (یک خط) | فاز | شاهد (مسیر فایل) | وضعیت | ارجاع متقابل |
|---|---|---|---|---|---|
| 49 | اسکیمای ۷ فایل خام رزرو فردی یکسان نیست — ستون `ReceptionType` فقط در ۴ از ۷ فایل ماهانه وجود دارد (نه خالی، اصلاً غایب) | ۲/۳ | `doc/data_dictionary_individual.md` (بخش «⚠️ ناسازگاری اسکیمای بین فایل‌ها») | باز — هر concat باید صریح مدیریت کند | — |
| 50 | ستون `Comment` سطح فردی حاوی سیگنال رضایت ضمنی است (۵.۶٪ رکوردها غیر «بدون نظر»)، برخلاف فرض اولیه‌ی WBS که آن را ثابت فرض کرده بود | ۲/۳ | `doc/data_dictionary_individual.md` (ردیف جدول `Comment`) | باز — کاندید فیچر، هنوز استفاده‌نشده | — |
| 51 | ستون `Price` سطح فردی ثابت نیست (۲۰ مقدار متمایز، شامل صفر) — برخلاف فرض اولیه‌ی WBS | ۲/۳ | `doc/data_dictionary_individual.md` (ردیف جدول `Price`) | باز — نیازمند بررسی ارتباط با گروه/نوع دانشجو | `doc/data_requests.md` (قلم ۸) |

## فاز ۴/۵ — بدون یافته‌ی بی‌شناسه

هیچ یافته‌ای در فاز ۴ (EDA) یا فاز ۵ (مهندسی ویژگی) بدون شناسه پیدا نشد — هر کشف این دو فاز
یا در `doc/data_facts_register.md` (F01–F67) یا در `doc/decision_log.md` (ردیف‌های ۲۰–۳۱) یا
مستقیماً در بند مربوطه‌ی `doc/WBS-food-demand-forecasting.md` شناسه دارد.

---

## یادداشت روش‌شناسی ادغام

این سند از ۴ قطعه‌ی موازی (`doc/_fragments/frag-{00-03,04-05,06-07,08-10}.md`) در
۲۰۲۶-۰۸-۲۷ ادغام شد. تغییرات نسبت به خروجی خام قطعه‌ها:

- شناسه‌های فاز ۸-۱۰ از `F36`–`F48` به `36`–`48` تغییر کرد (حذف پیشوند حرفی) — تا با
  فضای شناسه‌ی حقایق (`data_facts_register.md`, F01–F67) تداخل نکند.
- شناسه‌های فاز ۰-۳ از `NEW-P0-1/2/3` به `49/50/51` تغییر کرد — ادامه‌ی پیوسته‌ی شماره‌گذاری.
- شناسه‌های ۰ تا ۳۵ فاز ۷ **دست‌نخورده** ماندند (شامل دو شناسه‌ی تکراری ۲۸/۲۹ با پسوند
  `-الف`/`-ب`) — این‌ها در `decision_log.md` از قبل ارجاع داده شده بودند.
